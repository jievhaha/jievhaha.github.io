<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>IoC 之自定义类型转换器</title>
    <link href="/2021/02/24/IoC%20%E4%B9%8B%E8%87%AA%E5%AE%9A%E4%B9%89%E7%B1%BB%E5%9E%8B%E8%BD%AC%E6%8D%A2%E5%99%A8/"/>
    <url>/2021/02/24/IoC%20%E4%B9%8B%E8%87%AA%E5%AE%9A%E4%B9%89%E7%B1%BB%E5%9E%8B%E8%BD%AC%E6%8D%A2%E5%99%A8/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">IoC 之自定义类型转换器</font></div><p>ConversionService 是 Spring 类型转换器体系中的核心接口，它定义了是否可以完成转换（<code>canConvert(...)</code>） 与类型转换（<code>convert(...)</code>） 两类接口方法。</p><p>ConversionService 有三个子类，每个子类针对不同的类型转换：</p><ul><li>Converter&lt;S,T&gt;: 将 S 类型对象转为 T 类型对象。</li><li>GenericConverter: 会根据源类对象及目标类对象所在的宿主类中的上下文信息进行类型转换。</li><li>ConverterFactory: 将相同系列多个 “同质” Converter 封装在一起。如果希望将一种类型的对象转换为另一种类型及其子类的对象(例如将 String 转换为 Number 及 Number 子类(Integer、Long、Double 等)对象)可使用该转换器工厂类。</li></ul><h4 id="ConversionServiceFactoryBean"><a href="#ConversionServiceFactoryBean" class="headerlink" title="ConversionServiceFactoryBean"></a><code>ConversionServiceFactoryBean</code></h4><p>如何自定义类型转换器：</p><ol><li>实现 Converter / GenericConverter / ConverterFactory 接口</li><li>将该类注册到 ConversionServiceFactoryBean 中。</li></ol><p>ConversionServiceFactoryBean 实现了 InitializingBean 接口实现 <code>afterPropertiesSet()</code> 方法，之前分析了在 Bean 实例化 Bean 阶段，Spring 容器会检查当前 Bean 是否实现了 InitializingBean 接口，如果是则执行相应的初始化方法：</p><pre><code class="hljs java"><span class="hljs-comment">// ConversionServiceFactoryBean.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">afterPropertiesSet</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-comment">// 创建 DefaultConversionService 对象</span>    <span class="hljs-keyword">this</span>.conversionService = createConversionService();    <span class="hljs-comment">// 注册到 ConversionServiceFactory 中</span>    ConversionServiceFactory.registerConverters(<span class="hljs-keyword">this</span>.converters, <span class="hljs-keyword">this</span>.conversionService);&#125;</code></pre><p>首先调用 <code>createConversionService()</code> 方法，初始化 <code>conversionService</code>：</p><pre><code class="hljs java"><span class="hljs-comment">// ConversionServiceFactoryBean.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> GenericConversionService <span class="hljs-title">createConversionService</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> DefaultConversionService();&#125;</code></pre><p>然后调用 <code>ConversionServiceFactory.registerConverters(Set converters, ConverterRegistry registry)</code> 方法，将定义的 <code>converters</code> 注入到类型转换体系中：</p><pre><code class="hljs java"><span class="hljs-comment">// ConverterRegistry.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerConverters</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Set&lt;?&gt; converters, ConverterRegistry registry)</span> </span>&#123;<span class="hljs-keyword">if</span> (converters != <span class="hljs-keyword">null</span>) &#123;    <span class="hljs-comment">// 遍历 converters 数组，逐个注解</span><span class="hljs-keyword">for</span> (Object converter : converters) &#123;<span class="hljs-keyword">if</span> (converter <span class="hljs-keyword">instanceof</span> GenericConverter) &#123;registry.addConverter((GenericConverter) converter);&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (converter <span class="hljs-keyword">instanceof</span> Converter&lt;?, ?&gt;) &#123;registry.addConverter((Converter&lt;?, ?&gt;) converter);&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (converter <span class="hljs-keyword">instanceof</span> ConverterFactory&lt;?, ?&gt;) &#123;registry.addConverterFactory((ConverterFactory&lt;?, ?&gt;) converter);&#125; <span class="hljs-keyword">else</span> &#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(<span class="hljs-string">&quot;Each converter object must implement one of the &quot;</span> +<span class="hljs-string">&quot;Converter, ConverterFactory, or GenericConverter interfaces&quot;</span>);&#125;&#125;&#125;&#125;</code></pre><ul><li>ConverterRegistry 是一个 Converter 注册器，他定义了一系列注册方法。</li><li>通过调用 ConverterRegistry 的 <code>addConverter(...)</code> 方法将转换器注册到容器中。所以在我们使用 Spring 容器的时候，Spring 将会自动识别出 IOC 容器中注册的 ConversionService 并且在 Bean 属性注入阶段使用自定义的转换器完成属性的转换了。</li></ul>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>IoC 之深入分析 Bean 的类型转换体系</title>
    <link href="/2021/02/24/IoC%20%E4%B9%8B%E6%B7%B1%E5%85%A5%E5%88%86%E6%9E%90%20Bean%20%E7%9A%84%E7%B1%BB%E5%9E%8B%E8%BD%AC%E6%8D%A2%E4%BD%93%E7%B3%BB/"/>
    <url>/2021/02/24/IoC%20%E4%B9%8B%E6%B7%B1%E5%85%A5%E5%88%86%E6%9E%90%20Bean%20%E7%9A%84%E7%B1%BB%E5%9E%8B%E8%BD%AC%E6%8D%A2%E4%BD%93%E7%B3%BB/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">IoC 之深入分析 Bean 的类型转换体系</font></div><p>不管 Bean 对象里面的属性是什么类型，它们都是通过 XML 、Properties 或者其他方式来配置这些<strong>属性</strong>对象类型的。在 Spring 容器加载过程中，这些属性都是以 String 类型加载进容器的，但是最终都需要将这些 String 类型的属性转换为 Bean 对象属性所对应真正的类型，要想完成这种由字符串到具体对象的转换，就需要这种转换规则相关的信息，而这些信息以及转换过程由 <strong>Spring 类型转换体系</strong>来完成。</p><p>以 XML 为例，在 Spring 容器加载阶段，容器将 xml 文件中定义的 <code>&lt;bean&gt;</code> 解析为 BeanDefinition，BeanDefinition 中存储着我们定义一个 bean 需要的所有信息，包括属性，这些属性是以 String 类型的存储的。当用户触发 Bean 实例化阶段时，Spring 容器会将这些属性转换为这些属性真正对应的类型。我们知道在 Bean 实例化阶段，属性的注入是在实例化 Bean 阶段的属性注入阶段，即 AbstractAutowireCapableBeanFactory 的 <code>populateBean(String beanName, RootBeanDefinition mbd, BeanWrapper bw)</code> 方法。</p><p>在 <code>populateBean(String beanName, RootBeanDefinition mbd, BeanWrapper bw)</code> 方法中，会将 BeanDefinition 中定义的属性值翻译为 PropertyValue ，然后调用 <code>applyPropertyValues(String beanName, BeanDefinition mbd, BeanWrapper bw, PropertyValues pvs)</code> 方法，进行属性应用。其中 PropertyValue 用于保存单个 bean 属性的信息和值的对象。</p><h4 id="convertForProperty-方法"><a href="#convertForProperty-方法" class="headerlink" title="convertForProperty()方法"></a><code>convertForProperty()</code>方法</h4><p>在 <code>applyPropertyValues(String beanName, BeanDefinition mbd, BeanWrapper bw, PropertyValues pvs)</code> 方法中，会调用 <code>convertForProperty(Object value, String propertyName, BeanWrapper bw, TypeConverter converter)</code> 进行属性转换：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory。java</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">private</span> Object <span class="hljs-title">convertForProperty</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">        <span class="hljs-meta">@Nullable</span> Object value, String propertyName, BeanWrapper bw, TypeConverter converter)</span> </span>&#123;    <span class="hljs-comment">// 若 TypeConverter 为 BeanWrapperImpl 类型，则使用 BeanWrapperImpl 来进行类型转换</span>    <span class="hljs-comment">// 这里主要是因为 BeanWrapperImpl 实现了 PropertyEditorRegistry 接口</span>    <span class="hljs-keyword">if</span> (converter <span class="hljs-keyword">instanceof</span> BeanWrapperImpl) &#123;        <span class="hljs-keyword">return</span> ((BeanWrapperImpl) converter).convertForProperty(value, propertyName);    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 获得属性对应的 PropertyDescriptor 对象</span>        PropertyDescriptor pd = bw.getPropertyDescriptor(propertyName);        <span class="hljs-comment">// 获得属性对应的 setting MethodParameter 对象</span>        MethodParameter methodParam = BeanUtils.getWriteMethodParameter(pd);        <span class="hljs-comment">// 执行转换</span>        <span class="hljs-keyword">return</span> converter.convertIfNecessary(value, pd.getPropertyType(), methodParam);    &#125;&#125;</code></pre><ul><li>若 TypeConverter 为 BeanWrapperImpl 类型，则使用 BeanWrapperImpl 来进行类型转换，这里主要是因为 BeanWrapperImpl 实现了 PropertyEditorRegistry 接口。</li><li>否则，调用 TypeConverter 的 <code>convertIfNecessary(Object value, Class requiredType, MethodParameter methodParam)</code> 方法，进行类型转换。TypeConverter 是定义类型转换方法的接口，通常情况下与 PropertyEditorRegistry 配合使用实现类型转换。</li></ul><h5 id="convertIfNecessary-方法"><a href="#convertIfNecessary-方法" class="headerlink" title="convertIfNecessary()方法"></a><code>convertIfNecessary()</code>方法</h5><p><code>convertIfNecessary(Object value, Class requiredType, MethodParameter methodParam)</code> 方法的实现者有两个：DataBinder 和 TypeConverterSupport 类。</p><ul><li>DataBinder 主要用于参数绑定（熟悉 Spring MVC 的都应该知道这个类）</li><li>TypeConverterSupport 则是 TypeConverter 的基本实现，使用的是 <code>typeConverterDelegate</code> </li></ul><p>这里只需要关注 TypeConverterSupport 的 <code>convertIfNecessary(Object value, Class requiredType, MethodParameter methodParam)</code> 方法：</p><pre><code class="hljs java"><span class="hljs-comment">// TypeConverterSupport.java</span><span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-keyword">public</span> &lt;T&gt; <span class="hljs-function">T <span class="hljs-title">convertIfNecessary</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Object value, <span class="hljs-meta">@Nullable</span> Class&lt;T&gt; requiredType,</span></span><span class="hljs-function"><span class="hljs-params"><span class="hljs-meta">@Nullable</span> MethodParameter methodParam)</span> <span class="hljs-keyword">throws</span> TypeMismatchException </span>&#123;    <span class="hljs-keyword">return</span> convertIfNecessary(value, requiredType,(methodParam != <span class="hljs-keyword">null</span> ? <span class="hljs-keyword">new</span>      TypeDescriptor(methodParam) : TypeDescriptor.valueOf(requiredType)));&#125;<span class="hljs-meta">@Nullable</span><span class="hljs-meta">@Override</span><span class="hljs-keyword">public</span> &lt;T&gt; <span class="hljs-function">T <span class="hljs-title">convertIfNecessary</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Object value, <span class="hljs-meta">@Nullable</span> Class&lt;T&gt; requiredType,</span></span><span class="hljs-function"><span class="hljs-params"><span class="hljs-meta">@Nullable</span> TypeDescriptor typeDescriptor)</span> <span class="hljs-keyword">throws</span> TypeMismatchException </span>&#123;    Assert.state(<span class="hljs-keyword">this</span>.typeConverterDelegate != <span class="hljs-keyword">null</span>, <span class="hljs-string">&quot;No TypeConverterDelegate&quot;</span>);    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.typeConverterDelegate.convertIfNecessary(<span class="hljs-keyword">null</span>, <span class="hljs-keyword">null</span>, value, requiredType, typeDescriptor);    &#125;    <span class="hljs-keyword">catch</span> (ConverterNotFoundException | IllegalStateException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ConversionNotSupportedException(value, requiredType, ex);    &#125;    <span class="hljs-keyword">catch</span> (ConversionException | IllegalArgumentException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> TypeMismatchException(value, requiredType, ex);    &#125;&#125;</code></pre><p>最后执行TypeConverterDelegate 的 <code>convertIfNecessary(Object newValue, @Nullable Class requiredType, ...)</code> 方法：</p><pre><code class="hljs java"><span class="hljs-comment">// TypeConverterDelegate.java</span><span class="hljs-meta">@Nullable</span><span class="hljs-keyword">public</span> &lt;T&gt; <span class="hljs-function">T <span class="hljs-title">convertIfNecessary</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String propertyName, <span class="hljs-meta">@Nullable</span> Object oldValue, <span class="hljs-meta">@Nullable</span> Object newValue,<span class="hljs-meta">@Nullable</span> Class&lt;T&gt; requiredType, <span class="hljs-meta">@Nullable</span> TypeDescriptor typeDescriptor)</span> <span class="hljs-keyword">throws</span> IllegalArgumentException </span>&#123;        <span class="hljs-comment">// ... 省略暂时非关键的代码</span>    <span class="hljs-comment">//如果没有自定义的编辑器则使用 ConversionService</span>        ConversionService conversionService = <span class="hljs-keyword">this</span>.propertyEditorRegistry.getConversionService();        <span class="hljs-keyword">if</span> (editor == <span class="hljs-keyword">null</span> &amp;&amp; conversionService != <span class="hljs-keyword">null</span> &amp;&amp; newValue != <span class="hljs-keyword">null</span> &amp;&amp; typeDescriptor != <span class="hljs-keyword">null</span>) &#123;            TypeDescriptor sourceTypeDesc = TypeDescriptor.forObject(newValue);            <span class="hljs-keyword">if</span> (conversionService.canConvert(sourceTypeDesc, typeDescriptor)) &#123;                <span class="hljs-keyword">try</span> &#123;                    <span class="hljs-keyword">return</span> (T) conversionService.convert(newValue, sourceTypeDesc, typeDescriptor);                &#125; <span class="hljs-keyword">catch</span> (ConversionFailedException ex) &#123;                    conversionAttemptEx = ex;                &#125;            &#125;        &#125;        <span class="hljs-comment">// ... 省略暂时非关键的代码</span>&#125;</code></pre><h6 id="ConversionService"><a href="#ConversionService" class="headerlink" title="ConversionService"></a>ConversionService</h6><pre><code class="hljs java"><span class="hljs-comment">// ConversionService.java</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">ConversionService</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">canConvert</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Class&lt;?&gt; sourceType, Class&lt;?&gt; targetType)</span></span>;    <span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">canConvert</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> TypeDescriptor sourceType, TypeDescriptor targetType)</span></span>;    <span class="hljs-meta">@Nullable</span>    &lt;T&gt; <span class="hljs-function">T <span class="hljs-title">convert</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Object source, Class&lt;T&gt; targetType)</span></span>;    <span class="hljs-meta">@Nullable</span>    <span class="hljs-function">Object <span class="hljs-title">convert</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Object source, <span class="hljs-meta">@Nullable</span> TypeDescriptor sourceType, TypeDescriptor targetType)</span></span>;&#125;</code></pre><ul><li><strong>ConfigurableConversionService</strong>：ConversionService 的配置接口，继承 ConversionService 和 ConverterRegistry 两个接口，用于合并他们两者的操作，以便于通过 add 和 remove 的方式添加和删除转换器。</li><li><strong>GenericConversionService</strong>：ConversionService 接口的基础实现，适用于大部分条件下的转换工作，通过 ConfigurableConversionService 接口间接地将 ConverterRegistry 实现为注册 API 。</li><li><strong>DefaultConversionService</strong>：ConversionService 接口的默认实现，适用于大部分条件下的转换工作。</li></ul><p>回到 TypeConverterDelegate 的 <code>convertIfNecessary(String propertyName, Object oldValue, @Nullable Object newValue, Class requiredType, TypeDescriptor typeDescriptor)</code> 方法，在该方法中，如果没有自定义的属性编辑器，则调用 ConversionService 接口的 <code>convert(...)</code>：</p><pre><code class="hljs java"><span class="hljs-comment">// ConversionService.java</span><span class="hljs-function">Object <span class="hljs-title">convert</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Object source, <span class="hljs-meta">@Nullable</span> TypeDescriptor sourceType, TypeDescriptor targetType)</span></span>;</code></pre><ul><li><code>source</code> ：要转换的源对象，可以为 <code>null</code> 。</li><li><code>sourceType</code>：<code>source</code> 的类型的上下文，如果 <code>source</code> 为 <code>null</code> ，则可以为 <code>null</code> 。</li><li><code>targetType</code> ：<code>source</code> 要转换的类型的上下文。</li></ul><p><code>convert(...)</code> 方法，将给定的源对象 <code>source</code> 转换为指定的 <code>targetType</code> 。TypeDescriptors 提供有关发生转换的源位置和目标位置的附加上下文，通常是对象字段或属性位置。该方法由子类 GenericConversionService 实现：</p><pre><code class="hljs java"><span class="hljs-comment">// GenericConversionService.java</span><span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">convert</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Object source, <span class="hljs-meta">@Nullable</span> TypeDescriptor sourceType, TypeDescriptor targetType)</span> </span>&#123;    Assert.notNull(targetType, <span class="hljs-string">&quot;Target type to convert to cannot be null&quot;</span>);    <span class="hljs-comment">// &lt;1&gt; 如果 sourceType 为空，则直接处理结果</span>    <span class="hljs-keyword">if</span> (sourceType == <span class="hljs-keyword">null</span>) &#123;        Assert.isTrue(source == <span class="hljs-keyword">null</span>, <span class="hljs-string">&quot;Source must be [null] if source type == [null]&quot;</span>);        <span class="hljs-keyword">return</span> handleResult(<span class="hljs-keyword">null</span>, targetType, convertNullSource(<span class="hljs-keyword">null</span>, targetType));    &#125;    <span class="hljs-comment">// &lt;2&gt; 如果类型不对，抛出 IllegalArgumentException 异常</span>    <span class="hljs-keyword">if</span> (source != <span class="hljs-keyword">null</span> &amp;&amp; !sourceType.getObjectType().isInstance(source)) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(<span class="hljs-string">&quot;Source to convert from must be an instance of [&quot;</span> +                sourceType + <span class="hljs-string">&quot;]; instead it was a [&quot;</span> + source.getClass().getName() + <span class="hljs-string">&quot;]&quot;</span>);    &#125;    <span class="hljs-comment">// &lt;3&gt; 获得对应的 GenericConverter 对象</span>    GenericConverter converter = getConverter(sourceType, targetType);    <span class="hljs-comment">// &lt;4&gt; 如果 converter 非空，则进行转换，然后再处理结果</span>    <span class="hljs-keyword">if</span> (converter != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// &lt;4.1&gt; 执行转换</span>        Object result = ConversionUtils.invokeConverter(converter, source, sourceType, targetType);        <span class="hljs-comment">// &lt;4.2&gt; 处理器结果</span>        <span class="hljs-keyword">return</span> handleResult(sourceType, targetType, result);    &#125;    <span class="hljs-comment">// &lt;5&gt; 处理 converter 为空的情况</span>    <span class="hljs-keyword">return</span> handleConverterNotFound(source, sourceType, targetType);&#125;</code></pre><ul><li><p><code>&lt;1&gt;</code> 处，如果 <code>sourceType</code> 为空，则直接处理结果。</p></li><li><p><code>&lt;2&gt;</code> 处，如果类型不对，抛出 IllegalArgumentException 异常。</p></li><li><p><code>&lt;3&gt;</code> 处，调用 <code>getConverter(TypeDescriptor sourceType, TypeDescriptor targetType)</code> 方法，获取 GenericConverter 对象 <code>converter</code> 。</p></li><li><p><code>&lt;4&gt;</code> 处，如果 <code>converter</code> 非空，则进行转换，然后再处理结果。</p><p><code>&lt;4.1&gt;</code> 处，调用 <code>ConversionUtils#invokeConverter(GenericConverter converter, Object source, TypeDescriptor sourceType, TypeDescriptor targetType)</code> 方法，执行转换：</p></li></ul><pre><code class="hljs java"><span class="hljs-comment">// ConversionUtils.java</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Object <span class="hljs-title">invokeConverter</span><span class="hljs-params">(GenericConverter converter, <span class="hljs-meta">@Nullable</span> Object source,</span></span><span class="hljs-function"><span class="hljs-params">        TypeDescriptor sourceType, TypeDescriptor targetType)</span> </span>&#123;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// 执行转换</span>        <span class="hljs-keyword">return</span> converter.convert(source, sourceType, targetType);    &#125; <span class="hljs-keyword">catch</span> (ConversionFailedException ex) &#123;        <span class="hljs-keyword">throw</span> ex;    &#125; <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ConversionFailedException(sourceType, targetType, source, ex);    &#125;&#125;</code></pre><p><code>&lt;4.2&gt;</code> 处，调用 <code>handleResult(TypeDescriptor sourceType, TypeDescriptor targetType, Object result)</code> 方法，处理结果，实际上就是<strong>校验</strong>结果。。</p><pre><code class="hljs java"><span class="hljs-comment">// GenericConversionService.java</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">private</span> Object <span class="hljs-title">handleResult</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> TypeDescriptor sourceType, TypeDescriptor targetType, <span class="hljs-meta">@Nullable</span> Object result)</span> </span>&#123;<span class="hljs-keyword">if</span> (result == <span class="hljs-keyword">null</span>) &#123;assertNotPrimitiveTargetType(sourceType, targetType);&#125;<span class="hljs-keyword">return</span> result;&#125;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">assertNotPrimitiveTargetType</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> TypeDescriptor sourceType, TypeDescriptor targetType)</span> </span>&#123;<span class="hljs-keyword">if</span> (targetType.isPrimitive()) &#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ConversionFailedException(sourceType, targetType, <span class="hljs-keyword">null</span>,<span class="hljs-keyword">new</span> IllegalArgumentException(<span class="hljs-string">&quot;A null value cannot be assigned to a primitive type&quot;</span>));&#125;&#125;</code></pre><ul><li><code>&lt;5&gt;</code> 处，调用 <code>handleConverterNotFound(Object source, TypeDescriptor sourceType, TypeDescriptor targetType)</code> 方法，处理 <code>converter</code> 为空的情况：</li></ul><pre><code class="hljs java"><span class="hljs-comment">// GenericConversionService.java</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">private</span> Object <span class="hljs-title">handleConverterNotFound</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">        <span class="hljs-meta">@Nullable</span> Object source, <span class="hljs-meta">@Nullable</span> TypeDescriptor sourceType, TypeDescriptor targetType)</span> </span>&#123;    <span class="hljs-comment">// 情况一，如果 source 为空，则返回空</span>    <span class="hljs-keyword">if</span> (source == <span class="hljs-keyword">null</span>) &#123;        assertNotPrimitiveTargetType(sourceType, targetType);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;    <span class="hljs-comment">// 情况二，如果 sourceType 为空，或者 targetType 是 sourceType 的子类，则返回 source</span>    <span class="hljs-keyword">if</span> ((sourceType == <span class="hljs-keyword">null</span> || sourceType.isAssignableTo(targetType)) &amp;&amp;            targetType.getObjectType().isInstance(source)) &#123;        <span class="hljs-keyword">return</span> source;    &#125;    <span class="hljs-comment">// 抛出 ConverterNotFoundException 异常</span>    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ConverterNotFoundException(sourceType, targetType);&#125;</code></pre><h4 id="getConverter-方法"><a href="#getConverter-方法" class="headerlink" title="getConverter()方法"></a><code>getConverter()</code>方法</h4><p>先从 <code>getConverter(TypeDescriptor sourceType, TypeDescriptor targetType)</code> 方法，获取 GenericConverter 对象 <code>converter</code> 开始，即上边**&lt;3&gt;**。</p><pre><code class="hljs java"><span class="hljs-comment">// GenericConversionService.java</span><span class="hljs-comment">// 所有 Converter 集合的封装对象</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Converters converters = <span class="hljs-keyword">new</span> Converters();<span class="hljs-comment">// GenericConverter 缓存</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;ConverterCacheKey, GenericConverter&gt; converterCache = <span class="hljs-keyword">new</span> ConcurrentReferenceHashMap&lt;&gt;(<span class="hljs-number">64</span>);<span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">protected</span> GenericConverter <span class="hljs-title">getConverter</span><span class="hljs-params">(TypeDescriptor sourceType, TypeDescriptor targetType)</span> </span>&#123;    <span class="hljs-comment">// 创建 ConverterCacheKey 对象</span>    ConverterCacheKey key = <span class="hljs-keyword">new</span> ConverterCacheKey(sourceType, targetType);    <span class="hljs-comment">// 从 converterCache 缓存中，获得 GenericConverter 对象 converter</span>    GenericConverter converter = <span class="hljs-keyword">this</span>.converterCache.get(key);    <span class="hljs-comment">// 如果获得到，则返回 converter</span>    <span class="hljs-keyword">if</span> (converter != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> (converter != NO_MATCH ? converter : <span class="hljs-keyword">null</span>);    &#125;    <span class="hljs-comment">// 如果获取不到，则从 converters 中查找</span>    converter = <span class="hljs-keyword">this</span>.converters.find(sourceType, targetType);    <span class="hljs-comment">// 如果查找不到，则获得默认的 Converter 对象</span>    <span class="hljs-keyword">if</span> (converter == <span class="hljs-keyword">null</span>) &#123;        converter = getDefaultConverter(sourceType, targetType);    &#125;    <span class="hljs-comment">// 如果找到 converter ，则添加 converter 到 converterCache 中，并返回 converter</span>    <span class="hljs-keyword">if</span> (converter != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">this</span>.converterCache.put(key, converter);        <span class="hljs-keyword">return</span> converter;    &#125;    <span class="hljs-comment">// 如果找不到 converter ，则添加 NO_MATCH 占位符到 converterCache 中，并返回 null</span>    <span class="hljs-keyword">this</span>.converterCache.put(key, NO_MATCH);    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre><p>逻辑比较简单：从 <code>converterCache</code> 缓存中获取，如果存在返回，否则从 <code>converters</code> 中获取，然后加入到 <code>converterCache</code> 缓存中。</p><p>Converters 是 GenericConversionService 内部类，用于管理所有注册的转换器，其内部维护一个 Set 和 Map 的数据结构用于管理转换器：</p><pre><code class="hljs java"><span class="hljs-comment">// GenericConversionService.java#Converters</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Set&lt;GenericConverter&gt; globalConverters = <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;();<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;ConvertiblePair, ConvertersForPair&gt; converters = <span class="hljs-keyword">new</span> LinkedHashMap&lt;&gt;(<span class="hljs-number">36</span>);</code></pre><p>在 <code>getConverter(TypeDescriptor sourceType, TypeDescriptor targetType)</code> 方法中，如果缓存 <code>converterCache</code> 中不存在，则调用 Converters 对象的 <code>find(TypeDescriptor sourceType, TypeDescriptor targetType)</code> 方法，查找相应的 GenericConverter：</p><pre><code class="hljs java"><span class="hljs-comment">// GenericConversionService.java#Converters</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> GenericConverter <span class="hljs-title">find</span><span class="hljs-params">(TypeDescriptor sourceType, TypeDescriptor targetType)</span> </span>&#123;    <span class="hljs-comment">// Search the full type hierarchy</span>    List&lt;Class&lt;?&gt;&gt; sourceCandidates = getClassHierarchy(sourceType.getType());    List&lt;Class&lt;?&gt;&gt; targetCandidates = getClassHierarchy(targetType.getType());    <span class="hljs-comment">// 遍历 sourceCandidates 数组</span>    <span class="hljs-keyword">for</span> (Class&lt;?&gt; sourceCandidate : sourceCandidates) &#123;        <span class="hljs-comment">// 遍历 targetCandidates 数组</span>        <span class="hljs-keyword">for</span> (Class&lt;?&gt; targetCandidate : targetCandidates) &#123;            <span class="hljs-comment">// 创建 ConvertiblePair 对象</span>            ConvertiblePair convertiblePair = <span class="hljs-keyword">new</span> ConvertiblePair(sourceCandidate, targetCandidate);            <span class="hljs-comment">// 获得 GenericConverter 对象</span>            GenericConverter converter = getRegisteredConverter(sourceType, targetType, convertiblePair);            <span class="hljs-keyword">if</span> (converter != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">return</span> converter;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;<span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">private</span> GenericConverter <span class="hljs-title">getRegisteredConverter</span><span class="hljs-params">(TypeDescriptor sourceType,</span></span><span class="hljs-function"><span class="hljs-params">        TypeDescriptor targetType, ConvertiblePair convertiblePair)</span> </span>&#123;    <span class="hljs-comment">// Check specifically registered converters</span>    <span class="hljs-comment">// 从 converters 中，获得 converter</span>    ConvertersForPair convertersForPair = <span class="hljs-keyword">this</span>.converters.get(convertiblePair);    <span class="hljs-keyword">if</span> (convertersForPair != <span class="hljs-keyword">null</span>) &#123;        GenericConverter converter = convertersForPair.getConverter(sourceType, targetType);        <span class="hljs-keyword">if</span> (converter != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> converter;        &#125;    &#125;    <span class="hljs-comment">// Check ConditionalConverters for a dynamic match</span>    <span class="hljs-comment">// 从 globalConverters 中，获得 globalConverter</span>    <span class="hljs-keyword">for</span> (GenericConverter globalConverter : <span class="hljs-keyword">this</span>.globalConverters) &#123;        <span class="hljs-keyword">if</span> (((ConditionalConverter) globalConverter).matches(sourceType, targetType)) &#123;            <span class="hljs-keyword">return</span> globalConverter;        &#125;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre><p>在 <code>find(TypeDescriptor sourceType, TypeDescriptor targetT)</code> 方法中，会根据 <code>sourceType</code> 和 <code>targetType</code> 去查询 Converters 中维护的 Map 中是否包括支持的注册类型。如果存在返回 GenericConverter ，如果没有存在返回 <code>null</code> 。</p><h4 id="convert-方法"><a href="#convert-方法" class="headerlink" title="convert()方法"></a><code>convert()</code>方法</h4><p>当得到 GenericConverter 后，则调用其 <code>convert(Object source, TypeDescriptor sourceType, TypeDescriptor targetType)</code> 方法，进行类型转换，即**&lt;4.1&gt;**处。</p><pre><code class="hljs java"><span class="hljs-comment">// &lt;4.1&gt; 执行转换</span>Object result = ConversionUtils.invokeConverter(converter, source, sourceType, targetType);</code></pre><p>该方法内部调用的就是<code>GenericConverter.convert()</code>，这里就可以得到 Bean 属性定义的真正类型了。</p><h4 id="GenericConverter接口"><a href="#GenericConverter接口" class="headerlink" title="GenericConverter接口"></a><code>GenericConverter</code>接口</h4><p>GenericConverter 是一个转换接口，一个用于在两种或更多种类型之间转换的通用型转换器接口。它是 Converter SPI 体系中最灵活的，也是最复杂的接口，灵活性在于 GenericConverter 可以支持在多个源/目标类型对之间进行转换，同时也可以在类型转换过程中访问源/目标字段上下文。由于该接口足够复杂，所有当更简单的 Converter 或 ConverterFactory 接口足够使用时，通常不应使用此接口：</p><pre><code class="hljs java"><span class="hljs-comment">// GenericConverter.java</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">GenericConverter</span> </span>&#123;    <span class="hljs-meta">@Nullable</span>    <span class="hljs-function">Set&lt;ConvertiblePair&gt; <span class="hljs-title">getConvertibleTypes</span><span class="hljs-params">()</span></span>;    <span class="hljs-meta">@Nullable</span>    <span class="hljs-function">Object <span class="hljs-title">convert</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Object source, TypeDescriptor sourceType, TypeDescriptor targetType)</span></span>;&#125;</code></pre><p>在类型转换体系中，Spring 提供了非常多的类型转换器，除了上面的 GenericConverter，还有 Converter、ConditionalConverter、ConverterFactory。</p><h4 id="Converter接口"><a href="#Converter接口" class="headerlink" title="Converter接口"></a><code>Converter</code>接口</h4><p>Converter 是一个将 <code>类型的源对象转换为</code> 类型的目标对象的转换器。该接口是线程安全的，所以可以共享：</p><pre><code class="hljs java"><span class="hljs-comment">// Converter.java</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Converter</span>&lt;<span class="hljs-title">S</span>, <span class="hljs-title">T</span>&gt; </span>&#123;    <span class="hljs-meta">@Nullable</span>    <span class="hljs-function">T <span class="hljs-title">convert</span><span class="hljs-params">(S source)</span></span>;&#125;</code></pre><h4 id="ConditionalConverter接口"><a href="#ConditionalConverter接口" class="headerlink" title="ConditionalConverter接口"></a><code>ConditionalConverter</code>接口</h4><p>ConditionalConverter 接口用于表示有条件的类型转换，通过转入的<code>sourceType</code> 与 <code>targetType</code> 判断转换能否匹配，只有可匹配的转换才会调用convert 方法进行转换。</p><pre><code class="hljs java"><span class="hljs-comment">// ConditionalConverter.java</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">ConditionalConverter</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">matches</span><span class="hljs-params">(TypeDescriptor sourceType, TypeDescriptor targetType)</span></span>;&#125;</code></pre><h4 id="ConverterFactory接口"><a href="#ConverterFactory接口" class="headerlink" title="ConverterFactory接口"></a><code>ConverterFactory</code>接口</h4><p>一个用于“远程”转换的转换工厂，可以将对象从 <code>转换为</code> 的子类型：</p><pre><code class="hljs java"><span class="hljs-comment">// ConverterFactory.java</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">ConverterFactory</span>&lt;<span class="hljs-title">S</span>, <span class="hljs-title">R</span>&gt; </span>&#123;    &lt;T extends R&gt; <span class="hljs-function">Converter&lt;S, T&gt; <span class="hljs-title">getConverter</span><span class="hljs-params">(Class&lt;T&gt; targetType)</span></span>;&#125;</code></pre><p>四种不同的转换器承载着不同的转换过程：</p><ul><li>Converter：用于 <code>1:1</code> 的 <code>source -&gt; target</code> 类型转换。</li><li>ConverterFactory：用于 <code>1:N</code> 的 <code>source -&gt; target</code> 类型转换。</li><li>GenericConverter用于 <code>N:N</code> 的 <code>source -&gt; target</code> 类型转换。</li><li>ConditionalConverter：有条件的 <code>source -&gt; target</code> 类型转换。</li></ul><h4 id="GenericConversionService-接口"><a href="#GenericConversionService-接口" class="headerlink" title="GenericConversionService()接口"></a><code>GenericConversionService()</code>接口</h4><p>ConversionService 接口中定义了两类方法：</p><ul><li><code>canConvert(sourceType, targetType)</code> 方法，用于判 <code>sourceType</code> 能否转成 <code>targetType</code> 。</li><li><code>convert(source, targetType)</code> 方法，用于将 <code>source</code> 转成转入的 TargetType 类型实例。</li></ul><p>这两类方法都是在 GenericConversionService 中实现。<br>类 GenericConversionService 实现 ConfigurableConversionService 接口，而 ConfigurableConversionService 接口继承 ConversionService 和 ConverterRegistry。<br>ConverterRegistry 提供了类型转换器的管理功能，他提供了四个 add 和一个 remove 方法，支持注册/删除相应的类型转换器。</p><p>GenericConversionService 作为一个基础实现类，它即支持了不同类型之间的转换，也对各类型转换器进行管理，主要是通过一个 Map 类型的 <code>converterCache</code> 和一个内部类 Converters 。在上面已经分析了 GenericConversionService 执行类型转换的过程 <code>cover(...)</code> 方法。下面我们就一个 <code>addConverter(Converter converter)</code> 方法，来看看它是如何完成转换器的注入的工作的。</p><pre><code class="hljs java"><span class="hljs-comment">// GenericConversionService.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">addConverter</span><span class="hljs-params">(Converter&lt;?, ?&gt; converter)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 获取 ResolvableType 对象，基于 converter.getClass() 类</span>    ResolvableType[] typeInfo = getRequiredTypeInfo(converter.getClass(), Converter.class);    <span class="hljs-comment">// &lt;1&gt; 如果获取不到，并且 converter 是 DecoratingProxy 类型，则基于 ((DecoratingProxy) converter).getDecoratedClass() 类</span>    <span class="hljs-keyword">if</span> (typeInfo == <span class="hljs-keyword">null</span> &amp;&amp; converter <span class="hljs-keyword">instanceof</span> DecoratingProxy) &#123;        typeInfo = getRequiredTypeInfo(((DecoratingProxy) converter).getDecoratedClass(), Converter.class);    &#125;    <span class="hljs-comment">// 如果获取不到，抛出 IllegalArgumentException 异常</span>    <span class="hljs-keyword">if</span> (typeInfo == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(<span class="hljs-string">&quot;Unable to determine source type &lt;S&gt; and target type &lt;T&gt; for your Converter [&quot;</span> + converter.getClass().getName() + <span class="hljs-string">&quot;]; does the class parameterize those types?&quot;</span>);    &#125;    <span class="hljs-comment">// &lt;2&gt; 封装成 ConverterAdapter 对象，添加到 converters 中</span>    addConverter(<span class="hljs-keyword">new</span> ConverterAdapter(converter, typeInfo[<span class="hljs-number">0</span>], typeInfo[<span class="hljs-number">1</span>]));&#125;</code></pre><ul><li><code>&lt;1&gt;</code> 首先，根据 <code>converter</code> 获取 ResolvableType 数组。</li><li><code>&lt;2&gt;</code> 然后，将其与 <code>converter</code> 封装成一个 ConverterAdapter 实例。</li><li><code>&lt;2&gt;</code> 最后，调用 <code>addConverter(GenericConverter converter)</code> 方法，添加到 <code>converters</code> 中。</li><li>ResolvableType 用于封装 Java 的 Type类型。</li><li>ConverterAdapter 则是 Converter 的一个适配器， 它实现了 GenericConverter 和 ConditionalConverter 两个类型转换器。</li></ul><p><code>addConverter(GenericConverter converter)</code>方法：</p><pre><code class="hljs java"><span class="hljs-comment">// GenericConversionService.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">addConverter</span><span class="hljs-params">(GenericConverter converter)</span> </span>&#123;    <span class="hljs-comment">// 添加到 converters 中</span>    <span class="hljs-keyword">this</span>.converters.add(converter);    <span class="hljs-comment">// 过期缓存</span>    invalidateCache();&#125;</code></pre><p>直接调用内部类 Converters 的 <code>add(GenericConverter converter)</code> 方法：</p><pre><code class="hljs java"><span class="hljs-comment">// GenericConversionService.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">add</span><span class="hljs-params">(GenericConverter converter)</span> </span>&#123;    <span class="hljs-comment">// 获得 ConvertiblePair 集合</span>    Set&lt;ConvertiblePair&gt; convertibleTypes = converter.getConvertibleTypes();    <span class="hljs-comment">// 如果为空，并且 converter 是 ConditionalConverter 类型，则添加到 【globalConverters】 中</span>    <span class="hljs-keyword">if</span> (convertibleTypes == <span class="hljs-keyword">null</span>) &#123;        Assert.state(converter <span class="hljs-keyword">instanceof</span> ConditionalConverter,                     <span class="hljs-string">&quot;Only conditional converters may return null convertible types&quot;</span>);        <span class="hljs-keyword">this</span>.globalConverters.add(converter);    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 通过迭代的方式依次添加【converters】中</span>        <span class="hljs-keyword">for</span> (ConvertiblePair convertiblePair : convertibleTypes) &#123;            <span class="hljs-comment">// 从 converters 中，获得 ConvertersForPair 对象，添加 converter 到 ConvertersForPair 中</span>            getMatchableConverters(convertiblePair).add(converter);        &#125;    &#125;&#125;</code></pre><ul><li>首先调用 GenericConverter 的 <code>getConvertibleTypes()</code> 方法，获取 ConvertiblePair 集合。如果为空，则加入到 <code>globalConverters</code> 集合中，否则通过迭代的方式依次添加 <code>converters</code> 中。</li><li>ConvertiblePair 为 source-to-target 的持有者，它持有 <code>source</code> 和 <code>target</code> 的 class 类型，</li></ul><pre><code class="hljs java"><span class="hljs-comment">// GenericConverter.java#ConvertiblePair</span><span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConvertiblePair</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Class&lt;?&gt; sourceType;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Class&lt;?&gt; targetType;    <span class="hljs-comment">// 省略其他代码</span>&#125;</code></pre><p>在迭代过程中会根据 ConvertiblePair 获取相应的 ConvertersForPair 对象，然后添加 <code>converter</code> 转换器加入其中。ConvertiblePair 用于管理使用特定GenericConverter.ConvertiblePair 注册的转换器，也是个内部类。</p><pre><code class="hljs java"><span class="hljs-comment">// GenericConversionService.java#ConvertersForPair</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConvertersForPair</span> </span>&#123;   <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> LinkedList&lt;GenericConverter&gt; converters = <span class="hljs-keyword">new</span> LinkedList&lt;&gt;();    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">add</span><span class="hljs-params">(GenericConverter converter)</span> </span>&#123;        <span class="hljs-keyword">this</span>.converters.addFirst(converter);    &#125;    <span class="hljs-meta">@Nullable</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> GenericConverter <span class="hljs-title">getConverter</span><span class="hljs-params">(TypeDescriptor sourceType, TypeDescriptor targetType)</span> </span>&#123;        <span class="hljs-keyword">for</span> (GenericConverter converter : <span class="hljs-keyword">this</span>.converters) &#123;            <span class="hljs-keyword">if</span> (!(converter <span class="hljs-keyword">instanceof</span> ConditionalGenericConverter) ||                    ((ConditionalGenericConverter) converter).matches(sourceType, targetType)) &#123;                <span class="hljs-keyword">return</span> converter;            &#125;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;&#125;</code></pre><p>其实内部就是维护一个 LinkedList 集合。他内部有两个方法：<code>add(GenericConverter converter)</code> 和 <code>getConverter(TypeDescriptor sourceType, TypeDescriptor targetType)</code>，实现较为简单。</p><h4 id="DefaultConversionService"><a href="#DefaultConversionService" class="headerlink" title="DefaultConversionService"></a><code>DefaultConversionService</code></h4><p>DefaultConversionService 是 ConversionService 的默认实现，它继承 GenericConversionService，GenericConversionService 主要用于转换器的注册和调用，DefaultConversionService 则是为 ConversionService 体系提供一些默认的转换器。在 DefaultConversionService 构造方法中就会添加默认的 Converter ：</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultConversionService.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">DefaultConversionService</span><span class="hljs-params">()</span> </span>&#123;    addDefaultConverters(<span class="hljs-keyword">this</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">addDefaultConverters</span><span class="hljs-params">(ConverterRegistry converterRegistry)</span> </span>&#123;    addScalarConverters(converterRegistry);    addCollectionConverters(converterRegistry);    converterRegistry.addConverter(<span class="hljs-keyword">new</span> ByteBufferConverter((ConversionService) converterRegistry));    converterRegistry.addConverter(<span class="hljs-keyword">new</span> StringToTimeZoneConverter());    converterRegistry.addConverter(<span class="hljs-keyword">new</span> ZoneIdToTimeZoneConverter());    converterRegistry.addConverter(<span class="hljs-keyword">new</span> ZonedDateTimeToCalendarConverter());    converterRegistry.addConverter(<span class="hljs-keyword">new</span> ObjectToObjectConverter());    converterRegistry.addConverter(<span class="hljs-keyword">new</span> IdToEntityConverter((ConversionService) converterRegistry));    converterRegistry.addConverter(<span class="hljs-keyword">new</span> FallbackObjectToStringConverter());    converterRegistry.addConverter(<span class="hljs-keyword">new</span> ObjectToOptionalConverter((ConversionService) converterRegistry));&#125;</code></pre><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>Bean的属性转换大致调用流程：</p><p>TypeConverterSupport =&gt; ConversionService =&gt; Converter</p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>IOC之深入分析 PropertyOverrideConfigurer</title>
    <link href="/2021/02/24/IOC%E4%B9%8B%E6%B7%B1%E5%85%A5%E5%88%86%E6%9E%90%20PropertyOverrideConfigurer/"/>
    <url>/2021/02/24/IOC%E4%B9%8B%E6%B7%B1%E5%85%A5%E5%88%86%E6%9E%90%20PropertyOverrideConfigurer/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">IOC之深入分析 PropertyOverrideConfigurer</font></div><p>BeanFactoryPostProcessor 作用于 BeanDefinition 完成加载之后与 Bean 实例化之前，是 Spring 提供的一种强大的扩展机制。它有两个重要的子类，一个是 PropertyPlaceholderConfigurer，另一个是 PropertyOverrideConfigurer ，其中 PropertyPlaceholderConfigurer 允许我们通过配置 Properties 的方式来取代 Bean 中定义的占位符，PropertyOverrideConfigurer 允许我们对 Spring 容器中配置的任何我们想处理的 bean 定义的 property 信息进行覆盖替换。</p><h4 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h4><p>UML图：</p><p><img src="/images/spring_IOC/propertyOverrideConfigurer%E7%BB%93%E6%9E%84%E5%9B%BE.png" alt="propertyOverrideConfigurer结构图"></p><p>与 PropertyPlaceholderConfigurer 一样，也是继承 PropertyResourceConfigurer，我们知道 PropertyResourceConfigurer 对 BeanFactoryPostProcessor 的 <code>postProcessBeanFactory(ConfigurableListableBeanFactory beanFactory)</code> 方法提供了实现，在该实现中它会去读取指定配置文件中的内容，然后调用 <code>processProperties(ConfigurableListableBeanFactory beanFactoryToProcess, Properties props)</code> 方法。该方法是一个抽象方法，具体的实现由子类来实现，所以这里我们只需要看 PropertyOverrideConfigurer 中 <code>processProperties(ConfigurableListableBeanFactory beanFactoryToProcess, Properties props)</code> 方法的具体实现：</p><pre><code class="hljs java"><span class="hljs-comment">// PropertyOverrideConfigurer.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">processProperties</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactory, Properties props)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-comment">// 迭代配置文件中的内容</span>    <span class="hljs-keyword">for</span> (Enumeration&lt;?&gt; names = props.propertyNames(); names.hasMoreElements();) &#123;        String key = (String) names.nextElement();        <span class="hljs-keyword">try</span> &#123;            processKey(beanFactory, key, props.getProperty(key));        &#125;        <span class="hljs-keyword">catch</span> (BeansException ex) &#123;            String msg = <span class="hljs-string">&quot;Could not process key &#x27;&quot;</span> + key + <span class="hljs-string">&quot;&#x27; in PropertyOverrideConfigurer&quot;</span>;            <span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.ignoreInvalidKeys) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInitializationException(msg, ex);            &#125;            <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                logger.debug(msg, ex);            &#125;        &#125;    &#125;&#125;</code></pre><p>迭代 <code>props</code> 数组，依次调用 <code>processKey(ConfigurableListableBeanFactory factory, String key, String value)</code> 方法：</p><pre><code class="hljs java"><span class="hljs-comment">// PropertyOverrideConfigurer.java</span><span class="hljs-comment">/**</span><span class="hljs-comment">* Bean 名字的分隔符</span><span class="hljs-comment">*/</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String DEFAULT_BEAN_NAME_SEPARATOR = <span class="hljs-string">&quot;.&quot;</span>;<span class="hljs-keyword">private</span> String beanNameSeparator = DEFAULT_BEAN_NAME_SEPARATOR;<span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">processKey</span><span class="hljs-params">(ConfigurableListableBeanFactory factory, String key, String value)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-comment">// 判断是否存在 &quot;.&quot;，即获取其索引位置</span>    <span class="hljs-keyword">int</span> separatorIndex = key.indexOf(<span class="hljs-keyword">this</span>.beanNameSeparator);    <span class="hljs-keyword">if</span> (separatorIndex == -<span class="hljs-number">1</span>) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInitializationException(<span class="hljs-string">&quot;Invalid key &#x27;&quot;</span> + key +                                              <span class="hljs-string">&quot;&#x27;: expected &#x27;beanName&quot;</span> + <span class="hljs-keyword">this</span>.beanNameSeparator + <span class="hljs-string">&quot;property&#x27;&quot;</span>);    &#125;    <span class="hljs-comment">// 得到 beanName</span>    String beanName = key.substring(<span class="hljs-number">0</span>, separatorIndex);    <span class="hljs-comment">// 得到属性名</span>    String beanProperty = key.substring(separatorIndex + <span class="hljs-number">1</span>);    <span class="hljs-keyword">this</span>.beanNames.add(beanName);    <span class="hljs-comment">// 替换</span>    applyPropertyValue(factory, beanName, beanProperty, value);    <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;        logger.debug(<span class="hljs-string">&quot;Property &#x27;&quot;</span> + key + <span class="hljs-string">&quot;&#x27; set to value [&quot;</span> + value + <span class="hljs-string">&quot;]&quot;</span>);    &#125;&#125;</code></pre><p>获取分割符 <code>“.”</code> 的索引位置，得到 <code>beanName</code> 以及相应的属性，然后调用 <code>applyPropertyValue(ConfigurableListableBeanFactory factory, String beanName, String property, String value)</code> 方法：</p><pre><code class="hljs java"><span class="hljs-comment">// PropertyOverrideConfigurer.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">applyPropertyValue</span><span class="hljs-params">(ConfigurableListableBeanFactory factory, String beanName, String property, String value)</span> </span>&#123;    <span class="hljs-comment">// 获得 BeanDefinition 对象</span>    BeanDefinition bd = factory.getBeanDefinition(beanName);    BeanDefinition bdToUse = bd;    <span class="hljs-keyword">while</span> (bd != <span class="hljs-keyword">null</span>) &#123;        bdToUse = bd;        bd = bd.getOriginatingBeanDefinition();    &#125;    <span class="hljs-comment">// 设置 PropertyValue 到 BeanDefinition 中</span>    PropertyValue pv = <span class="hljs-keyword">new</span> PropertyValue(property, value);    pv.setOptional(<span class="hljs-keyword">this</span>.ignoreInvalidKeys);    bdToUse.getPropertyValues().addPropertyValue(pv);&#125;</code></pre><p>从容器中获取 BeanDefinition ，然后根据属性 <code>property</code> 和其值 <code>value</code> 构造成一个 PropertyValue 对象，最后调用 <code>addPropertyValue(PropertyValue pv )</code> 方法。PropertyValue 是用于保存一组bean属性的信息和值的对像：</p><pre><code class="hljs java"><span class="hljs-comment">// MutablePropertyValues.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> MutablePropertyValues <span class="hljs-title">addPropertyValue</span><span class="hljs-params">(PropertyValue pv)</span> </span>&#123;    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-keyword">this</span>.propertyValueList.size(); i++) &#123;        PropertyValue currentPv = <span class="hljs-keyword">this</span>.propertyValueList.get(i);        <span class="hljs-comment">// 匹配</span>        <span class="hljs-keyword">if</span> (currentPv.getName().equals(pv.getName())) &#123;            <span class="hljs-comment">// 合并属性</span>            pv = mergeIfRequired(pv, currentPv);            <span class="hljs-comment">// 覆盖属性</span>            setPropertyValueAt(pv, i);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>;        &#125;    &#125;    <span class="hljs-comment">// 未匹配到，添加到 propertyValueList 中</span>    <span class="hljs-keyword">this</span>.propertyValueList.add(pv);    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>;&#125;</code></pre><p>添加 PropertyValue 对象，替换或者合并相同的属性值。</p><h4 id="propertyOverride和PropertyPlaceholder"><a href="#propertyOverride和PropertyPlaceholder" class="headerlink" title="propertyOverride和PropertyPlaceholder"></a>propertyOverride和PropertyPlaceholder</h4><p><img src="/images/spring_IOC/propertyOverride%E5%92%8CPropertyPlaceholder.png" alt="propertyOverride和PropertyPlaceholder"></p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>IoC 之深入分析 PropertyPlaceholderConfigurer</title>
    <link href="/2021/02/24/IoC%20%E4%B9%8B%E6%B7%B1%E5%85%A5%E5%88%86%E6%9E%90%20PropertyPlaceholderConfigurer/"/>
    <url>/2021/02/24/IoC%20%E4%B9%8B%E6%B7%B1%E5%85%A5%E5%88%86%E6%9E%90%20PropertyPlaceholderConfigurer/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">IoC 之深入分析 PropertyPlaceholderConfigurer</font></div><p>BeanFactoryPostProcessor 作用：在容器启动阶段，可以对解析好的 BeanDefinition 进行定制化处理，而其中 <strong>PropertyPlaceholderConfigurer</strong> 是其一个非常重要的应用，也是其<strong>子类</strong>。</p><p><code>PropertyPlaceholderConfigurer</code> 允许我们用 Properties 文件中的属性，来定义应用上下文（配置文件或者注解），即：我们可以在 XML 配置文件（或者其他方式，如注解方式）中使用<strong>占位符</strong>的方式来定义一些资源，并将这些占位符所代表的资源配置到 Properties 中，这样只需要对 Properties 文件进行修改即可。</p><h4 id="PropertyPlaceholderConfigurer"><a href="#PropertyPlaceholderConfigurer" class="headerlink" title="PropertyPlaceholderConfigurer"></a><code>PropertyPlaceholderConfigurer</code></h4><p>该类间接实现了 Aware 和 BeanFactoryPostProcessor 两大扩展接口，之前介绍BeanFactoryPostProcessor，知道了BeanFactoryPostProcessor 提供了 <code>postProcessBeanFactory(ConfigurableListableBeanFactory beanFactory)</code> 接口方法，在这个体系中该方法的是在 <strong>PropertyResourceConfigurer</strong> 中实现，该类为属性资源的配置类，它实现了 BeanFactoryPostProcessor 接口：</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">postProcessBeanFactory</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactory)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// &lt;1&gt; 返回合并的 Properties 实例</span>        Properties mergedProps = mergeProperties();        <span class="hljs-comment">// Convert the merged properties, if necessary.</span>        <span class="hljs-comment">// &lt;2&gt; 转换合并属性</span>        convertProperties(mergedProps);        <span class="hljs-comment">// Let the subclass process the properties.</span>        <span class="hljs-comment">// &lt;3&gt; 让子类处理属性</span>        processProperties(beanFactory, mergedProps);    &#125;    <span class="hljs-keyword">catch</span> (IOException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInitializationException(<span class="hljs-string">&quot;Could not load properties&quot;</span>, ex);    &#125;&#125;</code></pre><ul><li><code>&lt;1&gt;</code> 处，调用 <code>mergeProperties()</code> 方法，返回合并的 Properties 实例。Properties 实例维护着<strong>一组</strong> <code>key-value</code> ，其实就是 Properties 配置文件中的内容。</li><li><code>&lt;2&gt;</code> 处，调用 <code>convertProperties(Properties props)</code> 方法，转换合并的值，其实就是将原始值替换为真正的值。</li><li><code>&lt;3&gt;</code> 处，调用 <code>processProperties(ConfigurableListableBeanFactory beanFactory, Properties props)</code> 方法，前面两个步骤已经将配置文件中的值进行了处理，那么该方法就是真正的替换过程，该方法<strong>由子类实现</strong>。</li></ul><pre><code class="hljs java"><span class="hljs-comment">// PropertyResourceConfigurer.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">processProperties</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactory, Properties props)</span> <span class="hljs-keyword">throws</span> BeansException</span>;</code></pre><p><strong>注：</strong><code>PropertyPlaceholderConfigurer</code>继承了<code>PlaceholderConfigurerSupport</code>，<code>PlaceholderConfigurerSupport</code>继承了<code>PropertyResourceConfigurer</code>。</p><h4 id="PropertyPlaceholderConfigurer-1"><a href="#PropertyPlaceholderConfigurer-1" class="headerlink" title="PropertyPlaceholderConfigurer"></a><code>PropertyPlaceholderConfigurer</code></h4><p>在 PropertyPlaceholderConfigurer 中，重写 <code>processProperties(ConfigurableListableBeanFactory beanFactory, Properties props)</code> 方法：</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">processProperties</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactoryToProcess, Properties props)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 创建 StringValueResolver 对象，PlaceholderResolvingStringValueResolver是个内部类</span>    StringValueResolver valueResolver = <span class="hljs-keyword">new</span> PlaceholderResolvingStringValueResolver(props);    <span class="hljs-comment">// &lt;2&gt; 处理</span>    doProcessProperties(beanFactoryToProcess, valueResolver);&#125;</code></pre><h5 id="PlaceholderResolvingStringValueResolver类"><a href="#PlaceholderResolvingStringValueResolver类" class="headerlink" title="PlaceholderResolvingStringValueResolver类"></a><code>PlaceholderResolvingStringValueResolver</code>类</h5><p>首先，构造一个 PlaceholderResolvingStringValueResolver 类型的 StringValueResolver 实例，该类是个内部类。StringValueResolver 为一个解析 String 类型值的策略接口，该接口提供了 <code>resolveStringValue(String strVal)</code> 方法，用于解析 String 值。PlaceholderResolvingStringValueResolver 为其一个解析策略：</p><pre><code class="hljs java"><span class="hljs-comment">// PropertyPlaceholderConfigurer.java</span>    <span class="hljs-keyword">private</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PlaceholderResolvingStringValueResolver</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">StringValueResolver</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> PropertyPlaceholderHelper helper;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> PlaceholderResolver resolver;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">PlaceholderResolvingStringValueResolver</span><span class="hljs-params">(Properties props)</span> </span>&#123;<span class="hljs-keyword">this</span>.helper = <span class="hljs-keyword">new</span> PropertyPlaceholderHelper(placeholderPrefix, placeholderSuffix, valueSeparator, ignoreUnresolvablePlaceholders);<span class="hljs-keyword">this</span>.resolver = <span class="hljs-keyword">new</span> PropertyPlaceholderConfigurerResolver(props);&#125;<span class="hljs-comment">// ... 省略 resolveStringValue 方法</span>&#125;</code></pre><ul><li>在构造 String 值解析器 StringValueResolver 时，将已经解析的 Properties 实例对象封装在 PlaceholderResolver 实例 <code>resolver</code> 中。PlaceholderResolver 是一个用于解析字符串中包含占位符的替换值的策略接口，该接口有一个 <code>resolvePlaceholder(String strVa)</code> 方法，用于返回占位符的替换值。</li><li>还有一个 PropertyPlaceholderHelper 工具 <code>helper</code> ，从名字上面看应该是进行替换的工具类。</li></ul><h5 id="doProcessProperties-方法"><a href="#doProcessProperties-方法" class="headerlink" title="doProcessProperties()方法"></a><code>doProcessProperties()</code>方法</h5><p>得到 String 解析器的实例 <code>valueResolver</code> 后，则会调用 <code>doProcessProperties(ConfigurableListableBeanFactory beanFactoryToProcess, StringValueResolver valueResolver)</code> 方法，来进行真值的替换操作。该方法在父类 PlaceholderConfigurerSupport 中实现：</p><pre><code class="hljs java"><span class="hljs-comment">// PlaceholderConfigurerSupport.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">doProcessProperties</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactoryToProcess,</span></span><span class="hljs-function"><span class="hljs-params">                                   StringValueResolver valueResolver)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 创建 BeanDefinitionVisitor 对象</span>    BeanDefinitionVisitor visitor = <span class="hljs-keyword">new</span> BeanDefinitionVisitor(valueResolver);    <span class="hljs-comment">// &lt;2&gt; 得到该容器的所有 BeanName</span>    String[] beanNames = beanFactoryToProcess.getBeanDefinitionNames();    <span class="hljs-keyword">for</span> (String curName : beanNames) &#123;        <span class="hljs-comment">// 校验</span>        <span class="hljs-comment">//检查我们是否没有解析我们自己的bean定义，以避免在属性文件位置中无法解析的占位符失败。</span>        <span class="hljs-keyword">if</span> (!(curName.equals(<span class="hljs-keyword">this</span>.beanName)<span class="hljs-comment">// 1. 当前实例 PlaceholderConfigurerSupport 不在解析范围内</span>              &amp;&amp; beanFactoryToProcess.equals(<span class="hljs-keyword">this</span>.beanFactory))) &#123;<span class="hljs-comment">// 2. 同一个 Spring 容器</span>            BeanDefinition bd = beanFactoryToProcess.getBeanDefinition(curName);            <span class="hljs-keyword">try</span> &#123;                visitor.visitBeanDefinition(bd);            &#125;            <span class="hljs-keyword">catch</span> (Exception ex) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(bd.getResourceDescription(), curName, ex.getMessage(), ex);            &#125;        &#125;    &#125;    <span class="hljs-comment">// &lt;3&gt; Spring 2.5的新功能：解析别名目标名称和别名中的占位符。</span>    beanFactoryToProcess.resolveAliases(valueResolver);    <span class="hljs-comment">// &lt;4&gt; Spring 3.0的新功能：解决嵌入值（例如注释属性）中的占位符。</span>    beanFactoryToProcess.addEmbeddedValueResolver(valueResolver);&#125;</code></pre><ul><li>&lt;1&gt; 处，根据 String 值解析策略 <code>valueResolver</code> 得到 BeanDefinitionVisitor 实例。BeanDefinitionVisitor 是 BeanDefinition 的访问者，我们通过它可以实现对 BeanDefinition 内容的进行访问，内容很多，例如 Scope、PropertyValues、FactoryMethodName 等等。</li><li>&lt;2&gt; 处，得到该容器的所有 BeanName，然后对其进行访问（ <code>visitBeanDefinition(BeanDefinition beanDefinition)</code> 方法）。</li><li>&lt;3&gt; 处，解析别名的占位符。</li><li>&lt;4&gt; 处，解析嵌入值的占位符，例如注释属性。</li></ul><h6 id="visitBeanDefinition-方法"><a href="#visitBeanDefinition-方法" class="headerlink" title="visitBeanDefinition()方法"></a><code>visitBeanDefinition()</code>方法</h6><p><code>doProcessProperties()</code>的<strong>核心</strong>在于 <code>visitBeanDefinition(BeanDefinition beanDefinition)</code> 方法的调用：</p><pre><code class="hljs java"><span class="hljs-comment">//BeanDefinitionVisitor.java</span><span class="hljs-comment">/**</span><span class="hljs-comment"> *遍历给定的BeanDefinition对象以及其中包含的MutablePropertyValues和ConstructorArgumentValues。</span><span class="hljs-comment"> */</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">visitBeanDefinition</span><span class="hljs-params">(BeanDefinition beanDefinition)</span> </span>&#123;    visitParentName(beanDefinition);    visitBeanClassName(beanDefinition);    visitFactoryBeanName(beanDefinition);    visitFactoryMethodName(beanDefinition);    visitScope(beanDefinition);    <span class="hljs-keyword">if</span> (beanDefinition.hasPropertyValues()) &#123;        <span class="hljs-comment">//本篇重点</span>        visitPropertyValues(beanDefinition.getPropertyValues());    &#125;    <span class="hljs-keyword">if</span> (beanDefinition.hasConstructorArgumentValues()) &#123;        ConstructorArgumentValues cas = beanDefinition.getConstructorArgumentValues();        visitIndexedArgumentValues(cas.getIndexedArgumentValues());        visitGenericArgumentValues(cas.getGenericArgumentValues());    &#125;&#125;</code></pre><p>该方法基本访问了 BeanDefinition 中所有比较有用的东西了，包括 parent 、class 、factory-bean 、factory-method 、scope 、property 、constructor-arg 。</p><h6 id="visitPropertyValues-方法"><a href="#visitPropertyValues-方法" class="headerlink" title="visitPropertyValues()方法"></a><code>visitPropertyValues()</code>方法</h6><p>因为该篇主要内容是<code>property</code>，所以重点只看<code>visitPropertyValues()</code>方法。</p><p>过程就是对属性数组进行遍历，调用 <code>resolveValue(Object value)</code>方法，对属性进行解析获取最新值，如果新值和旧值不等，则用新值替换旧值。：</p><pre><code class="hljs java"><span class="hljs-comment">//BeanDefinitionVisitor.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">visitPropertyValues</span><span class="hljs-params">(MutablePropertyValues pvs)</span> </span>&#123;    PropertyValue[] pvArray = pvs.getPropertyValues();    <span class="hljs-comment">// 遍历 PropertyValue 数组</span>    <span class="hljs-keyword">for</span> (PropertyValue pv : pvArray) &#123;        <span class="hljs-comment">// 解析真值</span>        Object newVal = resolveValue(pv.getValue());        <span class="hljs-keyword">if</span> (!ObjectUtils.nullSafeEquals(newVal, pv.getValue())) &#123;            <span class="hljs-comment">// 设置到 PropertyValue 中</span>            pvs.add(pv.getName(), newVal);        &#125;    &#125;&#125;</code></pre><p><code>resolveValue()</code>方法：</p><pre><code class="hljs java"><span class="hljs-comment">//BeanDefinitionVisitor.java</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">resolveValue</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Object value)</span> </span>&#123;    <span class="hljs-keyword">if</span> (value <span class="hljs-keyword">instanceof</span> BeanDefinition) &#123;        visitBeanDefinition((BeanDefinition) value);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value <span class="hljs-keyword">instanceof</span> BeanDefinitionHolder) &#123;        visitBeanDefinition(((BeanDefinitionHolder) value).getBeanDefinition());    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value <span class="hljs-keyword">instanceof</span> RuntimeBeanReference) &#123;        RuntimeBeanReference ref = (RuntimeBeanReference) value;        String newBeanName = resolveStringValue(ref.getBeanName());        <span class="hljs-keyword">if</span> (newBeanName == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;        <span class="hljs-keyword">if</span> (!newBeanName.equals(ref.getBeanName())) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> RuntimeBeanReference(newBeanName);        &#125;    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value <span class="hljs-keyword">instanceof</span> RuntimeBeanNameReference) &#123;        RuntimeBeanNameReference ref = (RuntimeBeanNameReference) value;        String newBeanName = resolveStringValue(ref.getBeanName());        <span class="hljs-keyword">if</span> (newBeanName == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;        <span class="hljs-keyword">if</span> (!newBeanName.equals(ref.getBeanName())) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> RuntimeBeanNameReference(newBeanName);        &#125;    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value <span class="hljs-keyword">instanceof</span> Object[]) &#123;        visitArray((Object[]) value);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value <span class="hljs-keyword">instanceof</span> List) &#123;        visitList((List) value);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value <span class="hljs-keyword">instanceof</span> Set) &#123;        visitSet((Set) value);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value <span class="hljs-keyword">instanceof</span> Map) &#123;        visitMap((Map) value);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value <span class="hljs-keyword">instanceof</span> TypedStringValue) &#123;        TypedStringValue typedStringValue = (TypedStringValue) value;        String stringValue = typedStringValue.getValue();        <span class="hljs-keyword">if</span> (stringValue != <span class="hljs-keyword">null</span>) &#123;            String visitedString = resolveStringValue(stringValue);            typedStringValue.setValue(visitedString);        &#125;    &#125;    <span class="hljs-comment">// 由于 Properties 中的是 String，所以重点在此处</span>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value <span class="hljs-keyword">instanceof</span> String) &#123;        <span class="hljs-keyword">return</span> resolveStringValue((String) value);    &#125;    <span class="hljs-keyword">return</span> value;&#125;</code></pre><p><code>resolveStringValue()</code>方法：</p><pre><code class="hljs java"><span class="hljs-comment">/**</span><span class="hljs-comment">* 解决给定的String值，例如解析占位符。</span><span class="hljs-comment">*/</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">protected</span> String <span class="hljs-title">resolveStringValue</span><span class="hljs-params">(String strVal)</span> </span>&#123;    <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.valueResolver == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">&quot;No StringValueResolver specified - pass a resolver object into the constructor or override the &#x27;resolveStringValue&#x27; method&quot;</span>);    &#125;    <span class="hljs-comment">// 解析真值</span>    String resolvedValue = <span class="hljs-keyword">this</span>.valueResolver.resolveStringValue(strVal);    <span class="hljs-comment">// Return original String if not modified.</span>    <span class="hljs-keyword">return</span> (strVal.equals(resolvedValue) ? strVal : resolvedValue);&#125;</code></pre><p><code>valueResolver</code> 是我们在构造 BeanDefinitionVisitor 实例时传入的 String 类型解析器 PlaceholderResolvingStringValueResolver，调用其 <code>resolveStringValue(String strVal)</code> 方法：</p><pre><code class="hljs java"><span class="hljs-comment">// PropertyPlaceholderConfigurer.java</span><span class="hljs-comment">// 内部类 PlaceholderResolvingStringValueResolver.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">PlaceholderResolvingStringValueResolver</span><span class="hljs-params">(Properties props)</span> </span>&#123;    <span class="hljs-keyword">this</span>.helper = <span class="hljs-keyword">new</span> PropertyPlaceholderHelper(placeholderPrefix, placeholderSuffix, valueSeparator, ignoreUnresolvablePlaceholders);    <span class="hljs-keyword">this</span>.resolver = <span class="hljs-keyword">new</span> PropertyPlaceholderConfigurerResolver(props);&#125;<span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">resolveStringValue</span><span class="hljs-params">(String strVal)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-comment">// 解析真值</span>    String resolved = <span class="hljs-keyword">this</span>.helper.replacePlaceholders(strVal, <span class="hljs-keyword">this</span>.resolver);    <span class="hljs-keyword">if</span> (trimValues) &#123;        resolved = resolved.trim();    &#125;    <span class="hljs-comment">// 返回真值</span>    <span class="hljs-keyword">return</span> (resolved.equals(nullValue) ? <span class="hljs-keyword">null</span> : resolved);&#125;</code></pre><p><code>helper</code> 为 PropertyPlaceholderHelper 实例对象，而 PropertyPlaceholderHelper 则是处理应用程序中包含占位符的字符串工具类。在构造 <code>helper</code> 实例对象时需要传入了几个参数：<code>placeholderPrefix</code>、<code>placeholderSuffix</code>、<code>valueSeparator</code>，这些值在 PlaceholderConfigurerSupport 中定义如下：</p><pre><code class="hljs java"><span class="hljs-comment">// PlaceholderConfigurerSupport.java</span><span class="hljs-comment">/** Default placeholder prefix: &#123;<span class="hljs-doctag">@value</span>&#125;. */</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String DEFAULT_PLACEHOLDER_PREFIX = <span class="hljs-string">&quot;$&#123;&quot;</span>;<span class="hljs-comment">/** Default placeholder suffix: &#123;<span class="hljs-doctag">@value</span>&#125;. */</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String DEFAULT_PLACEHOLDER_SUFFIX = <span class="hljs-string">&quot;&#125;&quot;</span>;<span class="hljs-comment">/** Default value separator: &#123;<span class="hljs-doctag">@value</span>&#125;. */</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String DEFAULT_VALUE_SEPARATOR = <span class="hljs-string">&quot;:&quot;</span>;<span class="hljs-comment">/** Defaults to &#123;<span class="hljs-doctag">@value</span> #DEFAULT_PLACEHOLDER_PREFIX&#125;. */</span><span class="hljs-keyword">protected</span> String placeholderPrefix = DEFAULT_PLACEHOLDER_PREFIX;<span class="hljs-comment">/** Defaults to &#123;<span class="hljs-doctag">@value</span> #DEFAULT_PLACEHOLDER_SUFFIX&#125;. */</span><span class="hljs-keyword">protected</span> String placeholderSuffix = DEFAULT_PLACEHOLDER_SUFFIX;<span class="hljs-comment">/** Defaults to &#123;<span class="hljs-doctag">@value</span> #DEFAULT_VALUE_SEPARATOR&#125;. */</span><span class="hljs-meta">@Nullable</span><span class="hljs-keyword">protected</span> String valueSeparator = DEFAULT_VALUE_SEPARATOR;</code></pre><p><code>replacePlaceholders()</code>方法</p><p>调用 PropertyPlaceholderHelper 的 <code>replacePlaceholders(String value, PlaceholderResolver placeholderResolver)</code> 方法，进行占位符替换：</p><pre><code class="hljs java"><span class="hljs-comment">// PropertyPlaceholderHelper.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">replacePlaceholders</span><span class="hljs-params">(String value, PlaceholderResolver placeholderResolver)</span> </span>&#123;    Assert.notNull(value, <span class="hljs-string">&quot;&#x27;value&#x27; must not be null&quot;</span>);    <span class="hljs-keyword">return</span> parseStringValue(value, placeholderResolver, <span class="hljs-keyword">null</span>);&#125;</code></pre><p>调用 <code>parseStringValue(String value, PlaceholderResolver placeholderResolver, Set visitedPlaceholders)</code> 方法，<strong>该方法是本篇最核心的地方</strong>，<code>$&#123;&#125;</code> 占位符的替换：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> String <span class="hljs-title">parseStringValue</span><span class="hljs-params">(String value, PlaceholderResolver placeholderResolver, <span class="hljs-meta">@Nullable</span> Set&lt;String&gt; visitedPlaceholders)</span> </span>&#123;    <span class="hljs-comment">// 获取前缀 &quot;$&#123;&quot; 的索引位置</span>    <span class="hljs-keyword">int</span> startIndex = value.indexOf(<span class="hljs-keyword">this</span>.placeholderPrefix);    <span class="hljs-keyword">if</span> (startIndex == -<span class="hljs-number">1</span>) &#123;        <span class="hljs-keyword">return</span> value;    &#125;    StringBuilder result = <span class="hljs-keyword">new</span> StringBuilder(value);    <span class="hljs-keyword">while</span> (startIndex != -<span class="hljs-number">1</span>) &#123;        <span class="hljs-comment">// 获取 后缀 &quot;&#125;&quot; 的索引位置</span>        <span class="hljs-keyword">int</span> endIndex = findPlaceholderEndIndex(result, startIndex);        <span class="hljs-keyword">if</span> (endIndex != -<span class="hljs-number">1</span>) &#123;            <span class="hljs-comment">// 截取 &quot;$&#123;&quot; 和 &quot;&#125;&quot; 中间的内容，这也就是我们在配置文件中对应的值</span>            String placeholder = result.substring(startIndex + <span class="hljs-keyword">this</span>.placeholderPrefix.length(), endIndex);            String originalPlaceholder = placeholder;            <span class="hljs-keyword">if</span> (visitedPlaceholders == <span class="hljs-keyword">null</span>) &#123;                visitedPlaceholders = <span class="hljs-keyword">new</span> HashSet&lt;&gt;(<span class="hljs-number">4</span>);            &#125;            <span class="hljs-keyword">if</span> (!visitedPlaceholders.add(originalPlaceholder)) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(                    <span class="hljs-string">&quot;Circular placeholder reference &#x27;&quot;</span> + originalPlaceholder + <span class="hljs-string">&quot;&#x27; in property definitions&quot;</span>);            &#125;            <span class="hljs-comment">// 解析占位符键中包含的占位符，真正的值</span>            placeholder = parseStringValue(placeholder, placeholderResolver, visitedPlaceholders);            <span class="hljs-comment">// 从 Properties 中获取 placeHolder 对应的值 propVal</span>            String propVal = placeholderResolver.resolvePlaceholder(placeholder);            <span class="hljs-comment">// 如果不存在</span>            <span class="hljs-keyword">if</span> (propVal == <span class="hljs-keyword">null</span> &amp;&amp; <span class="hljs-keyword">this</span>.valueSeparator != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">// 查询 : 的位置</span>                <span class="hljs-keyword">int</span> separatorIndex = placeholder.indexOf(<span class="hljs-keyword">this</span>.valueSeparator);                <span class="hljs-comment">// 如果存在 :</span>                <span class="hljs-keyword">if</span> (separatorIndex != -<span class="hljs-number">1</span>) &#123;                    <span class="hljs-comment">// 获取 : 前面部分 actualPlaceholder</span>                    String actualPlaceholder = placeholder.substring(<span class="hljs-number">0</span>, separatorIndex);                    <span class="hljs-comment">// 获取 : 后面部分 defaultValue</span>                    String defaultValue = placeholder.substring(separatorIndex + <span class="hljs-keyword">this</span>.valueSeparator.length());                    <span class="hljs-comment">// 从 Properties 中获取 actualPlaceholder 对应的值</span>                    propVal = placeholderResolver.resolvePlaceholder(actualPlaceholder);                    <span class="hljs-comment">// 如果不存在 则返回 defaultValue</span>                    <span class="hljs-keyword">if</span> (propVal == <span class="hljs-keyword">null</span>) &#123;                        propVal = defaultValue;                    &#125;                &#125;            &#125;            <span class="hljs-keyword">if</span> (propVal != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">// 递归调用，解析先前解析的占位符值中包含的占位符。</span>                propVal = parseStringValue(propVal, placeholderResolver, visitedPlaceholders);                result.replace(startIndex, endIndex + <span class="hljs-keyword">this</span>.placeholderSuffix.length(), propVal);                <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                    logger.trace(<span class="hljs-string">&quot;Resolved placeholder &#x27;&quot;</span> + placeholder + <span class="hljs-string">&quot;&#x27;&quot;</span>);                &#125;                startIndex = result.indexOf(<span class="hljs-keyword">this</span>.placeholderPrefix, startIndex + propVal.length());            &#125;            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.ignoreUnresolvablePlaceholders) &#123;                <span class="hljs-comment">// 忽略值</span>                startIndex = result.indexOf(<span class="hljs-keyword">this</span>.placeholderPrefix, endIndex + <span class="hljs-keyword">this</span>.placeholderSuffix.length());            &#125;            <span class="hljs-keyword">else</span> &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(<span class="hljs-string">&quot;Could not resolve placeholder &#x27;&quot;</span> +                                                   placeholder + <span class="hljs-string">&quot;&#x27;&quot;</span> + <span class="hljs-string">&quot; in value \&quot;&quot;</span> + value + <span class="hljs-string">&quot;\&quot;&quot;</span>);            &#125;            visitedPlaceholders.remove(originalPlaceholder);        &#125;        <span class="hljs-keyword">else</span> &#123;            startIndex = -<span class="hljs-number">1</span>;        &#125;    &#125;    <span class="hljs-comment">// 返回propVal，就是替换之后的值</span>    <span class="hljs-keyword">return</span> result.toString();&#125;</code></pre><ol><li>获取占位符前缀 <code>&quot;$&#123;&quot;</code> 的索引位置 <code>startIndex</code> 。</li><li>如果前缀 <code>&quot;$&#123;&quot;</code> 存在，则从 <code>“&#123;”</code> 后面开始获取占位符后缀 “}” 的索引位置 <code>endIndex</code> 。</li><li>如果前缀 <code>“$&#123;”</code> 和后缀 <code>&quot;&#125;&quot;</code> 都存在，则截取中间部分 <code>placeholder</code> 。</li><li>从 Properties 中获取 <code>placeHolder</code> 对应的值 <code>propVal</code> 。</li><li>如果 <code>propVal</code> 为空，则判断占位符中是否存在 <code>&quot;:&quot;</code>，如果存在则对占位符进行分割处理，前面部分为 <code>actualPlaceholder</code>，后面部分 <code>defaultValue</code>，尝试从 Properties 中获取 <code>actualPlaceholder</code> 对应的值 <code>propVal</code>，如果不存在，则将 <code>defaultValue</code> 的值赋值给 <code>propVal</code></li><li>返回 <code>propVal</code>，也就是 Properties 中对应的值。</li></ol>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>IOC之BeanFactoryPostProcessor</title>
    <link href="/2021/02/24/IOC%E4%B9%8BBeanFactoryPostProcessor/"/>
    <url>/2021/02/24/IOC%E4%B9%8BBeanFactoryPostProcessor/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">IOC之BeanFactoryPostProcessor</font></div><p>BeanPostProcessor 是 Spring 提供一种扩展机制，该机制允许我们在 Bean <strong>实例化之后初始化之际</strong>对 Bean 进行增强处理（前、后置处理）。</p><p>同样在 Spring 容器<strong>启动</strong>阶段，Spring 也提供了一种容器扩展机制：<strong>BeanFactoryPostProcessor</strong>，该机制作用于容器启动阶段，允许我们在容器实例化 Bean 之前对注册到该容器的 BeanDefinition 做出修改。</p><h4 id="BeanFactoryPostProcessor"><a href="#BeanFactoryPostProcessor" class="headerlink" title="BeanFactoryPostProcessor"></a><code>BeanFactoryPostProcessor</code></h4><p>BeanFactoryPostProcessor 的机制，就相当于给了我们在 Bean 实例化之前最后一次修改 BeanDefinition 的机会，我们可以利用这个机会对 BeanDefinition 来进行一些额外的操作，比如更改某些 bean 的一些属性，给某些 Bean 增加一些其他的信息等等操作。</p><pre><code class="hljs java"><span class="hljs-meta">@FunctionalInterface</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">BeanFactoryPostProcessor</span> </span>&#123;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">postProcessBeanFactory</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactory)</span> <span class="hljs-keyword">throws</span> BeansException</span>;&#125;</code></pre><p><code>BeanFactoryPostProcessor</code>是个函数式接口，仅有一个 <code>postProcessBeanFactory(...)</code> 方法，该方法接收一个 ConfigurableListableBeanFactory 类型的 <code>beanFactory</code> 参数。</p><p><code>postProcessBeanFactory(...)</code> 方法，工作于 BeanDefinition 加载完成之后，Bean 实例化之前，其主要作用是对加载 BeanDefinition 进行修改。有一点需要需要<strong>注意</strong>的是在 <code>postProcessBeanFactory(...)</code> 方法中，千万不能进行 Bean 的实例化工作，因为这样会导致 Bean 过早实例化，会产生严重后果，<strong>我们始终需要注意的是 BeanFactoryPostProcessor 是与 BeanDefinition 打交道的，如果想要与 Bean 打交道，请使用 BeanPostProcessor</strong> 。</p><p>与 BeanPostProcessor 一样，BeanFactoryPostProcessor 同样支持<strong>排序</strong>，一个容器可以同时拥有多个 BeanFactoryPostProcessor ，这个时候如果我们比较在乎他们的顺序的话，可以实现 Ordered 接口。</p><p>如果要自定义 BeanFactoryPostProcessor ，直接实现该接口即可。</p><h4 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h4><p>直接提<code>git</code>了。</p><h4 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h4><p>对于 ApplicationContext 来说，使用 BeanFactoryPostProcessor 非常方便，因为他会自动识别配置文件中的 BeanFactoryPostProcessor 并且完成注册和调用，我们只需要简单的配置声明即可。而对于 BeanFactory 容器来说则不行，他和 BeanPostProcessor 一样需要容器主动去进行注册调用：</p><pre><code class="hljs java"><span class="hljs-comment">//T_BeanFactoryPostProcessor实现了BeanFactoryPostProcessor</span>T_BeanFactoryPostProcessor beanFactoryPostProcessor = <span class="hljs-keyword">new</span> T_BeanFactoryPostProcessor();beanFactoryPostProcessor.postProcessBeanFactory(factory);</code></pre><p>一般情况下我们是不会主动去自定义 BeanFactoryPostProcessor ，其实 Spring 为我们提供了几个常用的 BeanFactoryPostProcessor，他们是<code>PropertyPlaceholderConfigurer</code> 和 <code>PropertyOverrideConfigurer</code> ，其中 <code>PropertyPlaceholderConfigurer</code> 允许我们在 XML 配置文件中使用占位符并将这些占位符所代表的资源单独配置到简单的 properties 文件中来加载，<code>PropertyOverrideConfigurer</code> 则允许我们使用占位符来明确表明bean 定义中的 property 与 properties 文件中的各配置项之间的对应关系，这两个类在我们大型项目中有非常重要的作用。</p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>IOC之Bean的生命周期</title>
    <link href="/2021/02/24/IOC%E4%B9%8BBean%E7%9A%84%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F/"/>
    <url>/2021/02/24/IOC%E4%B9%8BBean%E7%9A%84%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">IOC之Bean的生命周期</font></div><p>Spring 并不是一启动容器就开启 bean 的实例化进程，只有当客户端通过显示或者隐式的方式调用 BeanFactory 的 <code>getBean(...)</code> 方法来请求某个实例对象的时候，它才会触发相应 bean 的实例化进程。当然，也可以选择直接使用 ApplicationContext 容器，因为该容器启动的时候会立刻调用注册到该容器所有 bean 定义的实例化方法。当然，对于 BeanFactory 容器而言，并不是所有的 <code>getBean(...)</code> 方法都会触发实例化进程，比如 singleton 类型的 bean，该类型的 bean 只会在第一次调用 <code>getBean()</code> 的时候才会触发，而后续的调用则会直接返回容器缓存中的实例对象。</p><p><code>getBean(...)</code> 方法，只是 bean 实例化进程的入口，真正的实现逻辑其实是在 AbstractAutowireCapableBeanFactory 的 <code>doCreateBean(...)</code> 中实现，实例化过程如下图：</p><p><img src="/images/spring_IOC/bean%E5%AE%9E%E4%BE%8B%E5%8C%96%E8%BF%87%E7%A8%8B.png" alt="bean实例化过程"></p><p>原来我们采用 new 的方式创建一个对象，用完该对象在其脱离作用域后就会被回收，对于后续操作我们无权也没法干涉，但是采用 Spring 容器后，我们完全摆脱了这种命运，Spring 容器将会对其所有管理的 Bean 对象全部给予一个<strong>统一的生命周期管理</strong>，同时在这个阶段我们也可以对其进行干涉。</p><h4 id="Bean实例化"><a href="#Bean实例化" class="headerlink" title="Bean实例化"></a>Bean实例化</h4><p>在 <code>doCreateBean(...)</code> 方法中，首先进行 bean 实例化工作，主要由 <code>createBeanInstance(...)</code> 方法实现，该方法返回一个 BeanWrapper 对象。BeanWrapper 对象是 Spring 的一个低级 Bean 基础结构的核心接口，为什么说是<strong>低级</strong>呢？因为这个时候的 Bean 还不能够被我们使用，连最基本的属性都没有设置。而且在我们实际开发过程中，一般都不会直接使用该类，而是通过 BeanFactory 隐式使用。</p><p>BeanWrapper 接口有一个默认实现类 BeanWrapperImpl，其主要作用是对 Bean 进行“包裹”，然后对这个包裹的 bean 进行操作，比如后续注入 bean 属性。</p><p>在实例化 bean 过程中，Spring 采用“<code>策略模式</code>”来决定采用哪种方式来实例化 bean，一般有<code>反射</code>和 <code>CGLIB</code> 动态字节码两种方式。</p><p>InstantiationStrategy 定义了 Bean 实例化策略的抽象接口，其子类 SimpleInstantiationStrategy 提供了基于反射来实例化对象的功能，但是不支持方法注入方式的对象实例化。CglibSubclassingInstantiationStrategy 继承 SimpleInstantiationStrategy，他除了拥有父类以反射实例化对象的功能外，还提供了通过 CGLIB 的动态字节码的功能进而支持方法注入所需的对象实例化需求。默认情况下，Spring 采用 CglibSubclassingInstantiationStrategy。</p><p>查看<code>20-25</code>。</p><h4 id="激活Aware"><a href="#激活Aware" class="headerlink" title="激活Aware"></a>激活Aware</h4><p>当 Spring 完成 bean 对象实例化并且设置完相关属性和依赖后，则会开始 bean 的初始化进程（ <code>initializeBean()</code> ），初始化第一个阶段是检查当前 bean 对象是否实现了一系列以 Aware 结尾的的接口。</p><p>Aware 接口为 Spring 容器的核心接口，是一个具有标识作用的超级接口，实现了该接口的 bean 是具有被 Spring 容器通知的能力，通知的方式是采用回调的方式。</p><p>在初始化阶段主要是感知 BeanNameAware、BeanClassLoaderAware、BeanFactoryAware 。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">invokeAwareMethods</span><span class="hljs-params">(<span class="hljs-keyword">final</span> String beanName, <span class="hljs-keyword">final</span> Object bean)</span> </span>&#123;<span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> Aware) &#123;    <span class="hljs-comment">// BeanNameAware</span><span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> BeanNameAware) &#123;((BeanNameAware) bean).setBeanName(beanName);&#125;<span class="hljs-comment">// BeanClassLoaderAware</span><span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> BeanClassLoaderAware) &#123;ClassLoader bcl = getBeanClassLoader();<span class="hljs-keyword">if</span> (bcl != <span class="hljs-keyword">null</span>) &#123;((BeanClassLoaderAware) bean).setBeanClassLoader(bcl);&#125;&#125;<span class="hljs-comment">// BeanFactoryAware</span><span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> BeanFactoryAware) &#123;((BeanFactoryAware) bean).setBeanFactory(AbstractAutowireCapableBeanFactory.<span class="hljs-keyword">this</span>);&#125;&#125;&#125;</code></pre><ul><li>BeanNameAware：对该 bean 对象定义的 beanName 设置到当前对象实例中</li><li>BeanClassLoaderAware：将当前 bean 对象相应的 ClassLoader 注入到当前对象实例中</li><li>BeanFactoryAware：BeanFactory 容器会将自身注入到当前对象实例中，这样当前对象就会拥有一个 BeanFactory 容器的引用。</li></ul><p>当然，Spring 不仅仅只是提供了上面三个 Aware 接口，而是一系列：</p><ul><li>LoadTimeWeaverAware：加载Spring Bean时织入第三方模块，如AspectJ</li><li>BootstrapContextAware：资源适配器BootstrapContext，如JCA,CCI</li><li>ResourceLoaderAware：底层访问资源的加载器</li><li>PortletConfigAware：PortletConfig</li><li>PortletContextAware：PortletContext</li><li>ServletConfigAware：ServletConfig</li><li>ServletContextAware：ServletContext</li><li>MessageSourceAware：国际化</li><li>ApplicationEventPublisherAware：应用事件</li><li>NotificationPublisherAware：JMX通知</li></ul><h4 id="BeanPostProcessor"><a href="#BeanPostProcessor" class="headerlink" title="BeanPostProcessor"></a><code>BeanPostProcessor</code></h4><p>初始化第二个阶段则是 BeanPostProcessor 增强处理，在该阶段 BeanPostProcessor 会处理当前容器内所有符合条件的实例化后的 bean 对象。它主要是对 Spring 容器提供��� bean 实例对象进行有效的扩展，允许 Spring 在初始化 bean 阶段对其进行定制化修改，如处理标记接口或者为其提供代理实现。</p><p>BeanPostProcessor 接口提供了两个方法，在不同的时机执行，分别对应上图的前置处理和后置处理。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">BeanPostProcessor</span> </span>&#123;<span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">default</span> Object <span class="hljs-title">postProcessBeforeInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;<span class="hljs-keyword">return</span> bean;&#125;<span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">default</span> Object <span class="hljs-title">postProcessAfterInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;<span class="hljs-keyword">return</span> bean;&#125;&#125;</code></pre><h4 id="InitializingBean-和-init-method"><a href="#InitializingBean-和-init-method" class="headerlink" title="InitializingBean 和 init-method"></a><code>InitializingBean</code> 和 <code>init-method</code></h4><p>InitializingBean 是一个接口，它为 Spring Bean 的初始化提供了一种方式，它有一个 <code>afterPropertiesSet()</code> 方法，在 bean 的初始化进程中会判断当前 bean 是否实现了 InitializingBean，如果实现了则调用 <code>afterPropertiesSet()</code> 方法，进行初始化工作。然后再检查是否也指定了 <code>init-method</code> ，如果指定了则通过反射机制调用指定的 <code>init-method</code> 方法。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">invokeInitMethods</span><span class="hljs-params">(String beanName, <span class="hljs-keyword">final</span> Object bean, <span class="hljs-meta">@Nullable</span> RootBeanDefinition mbd)</span></span><span class="hljs-function"><span class="hljs-keyword">throws</span> Throwable </span>&#123;    <span class="hljs-comment">// 首先会检查是否是 InitializingBean ，如果是的话需要调用 afterPropertiesSet()</span><span class="hljs-keyword">boolean</span> isInitializingBean = (bean <span class="hljs-keyword">instanceof</span> InitializingBean);<span class="hljs-keyword">if</span> (isInitializingBean &amp;&amp; (mbd == <span class="hljs-keyword">null</span> || !mbd.isExternallyManagedInitMethod(<span class="hljs-string">&quot;afterPropertiesSet&quot;</span>))) &#123;<span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;logger.trace(<span class="hljs-string">&quot;Invoking afterPropertiesSet() on bean with name &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);&#125;<span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123; <span class="hljs-comment">// 安全模式</span><span class="hljs-keyword">try</span> &#123;AccessController.doPrivileged((PrivilegedExceptionAction&lt;Object&gt;) () -&gt; &#123;                    <span class="hljs-comment">// 属性初始化的处理</span>((InitializingBean) bean).afterPropertiesSet();<span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;, getAccessControlContext());&#125; <span class="hljs-keyword">catch</span> (PrivilegedActionException pae) &#123;<span class="hljs-keyword">throw</span> pae.getException();&#125;&#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// 属性初始化的处理</span>((InitializingBean) bean).afterPropertiesSet();&#125;&#125;<span class="hljs-keyword">if</span> (mbd != <span class="hljs-keyword">null</span> &amp;&amp; bean.getClass() != NullBean.class) &#123;        <span class="hljs-comment">// 判断是否指定了 init-method()，</span>        <span class="hljs-comment">// 如果指定了 init-method()，则再调用制定的init-method</span>String initMethodName = mbd.getInitMethodName();<span class="hljs-keyword">if</span> (StringUtils.hasLength(initMethodName) &amp;&amp;!(isInitializingBean &amp;&amp; <span class="hljs-string">&quot;afterPropertiesSet&quot;</span>.equals(initMethodName)) &amp;&amp;!mbd.isExternallyManagedInitMethod(initMethodName)) &#123;            <span class="hljs-comment">// 激活用户自定义的初始化方法</span>            <span class="hljs-comment">// 利用反射机制执行</span>invokeCustomInitMethod(beanName, bean, mbd);&#125;&#125;&#125;</code></pre><p>对于 Spring 而言，虽然上面两种方式都可以实现初始化定制化，但是更加推崇 <code>init-method</code> 方式，因为对于 InitializingBean 接口而言，他需要 bean 去实现接口，这样就会污染我们的应用程序，显得 Spring 具有一定的侵入性。但是由于 <code>init-method</code> 是采用反射的方式，所以执行效率上相对于 InitializingBean 接口回调的方式可能会低一些。</p><h4 id="DisposableBean-和-destroy-method"><a href="#DisposableBean-和-destroy-method" class="headerlink" title="DisposableBean 和 destroy-method"></a><code>DisposableBean</code> 和 <code>destroy-method</code></h4><p>与 InitializingBean 和 <code>init-method</code> 用于对象的自定义初始化工作相似，DisposableBean和 <code>destroy-method</code> 则用于对象的自定义销毁工作。</p><p>当一个 bean 对象经历了实例化、设置属性、初始化阶段，那么该 bean 对象就可以供容器使用了（调用的过程）。当完成调用后，如果是 singleton 类型的 bean ，则会看当前 bean 是否应实现了 DisposableBean 接口或者配置了 <code>destroy-method</code> 属性，如果是的话，则会为该实例注册一个用于对象销毁的回调方法，便于在这些 singleton 类型的 bean 对象销毁之前执行销毁逻辑。</p><p>但是，并不是对象完成调用后就会立刻执行销毁方法，因为这个时候 Spring 容器还处于运行阶段，只有当 Spring 容器关闭的时候才会去调用。但是， Spring 容器不会这么聪明会自动去调用这些销毁方法，而是需要我们主动去告知 Spring 容器。</p><ul><li>对于 BeanFactory 容器而言，我们需要主动调用 <code>destroySingletons()</code> 方法，通知 BeanFactory 容器去执行相应的销毁方法。</li><li>对于 ApplicationContext 容器而言，调用 <code>registerShutdownHook()</code> 方法。</li></ul><h4 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h4><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">LifeCycleBean</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">BeanNameAware</span>, <span class="hljs-title">BeanFactoryAware</span>, <span class="hljs-title">BeanClassLoaderAware</span>, <span class="hljs-title">BeanPostProcessor</span>, <span class="hljs-title">InitializingBean</span>, <span class="hljs-title">DisposableBean</span> </span>&#123;<span class="hljs-keyword">private</span> String test;<span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getTest</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> test;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setTest</span><span class="hljs-params">(String test)</span> </span>&#123;System.out.println(<span class="hljs-string">&quot;属性注入....&quot;</span>);<span class="hljs-keyword">this</span>.test = test;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">LifeCycleBean</span><span class="hljs-params">(String test)</span> </span>&#123;System.out.println(<span class="hljs-string">&quot;有参构造函数调用...&quot;</span>);<span class="hljs-keyword">this</span>.test = test;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">LifeCycleBean</span><span class="hljs-params">()</span> </span>&#123;System.out.println(<span class="hljs-string">&quot;无参构造函数调用...&quot;</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">display</span><span class="hljs-params">()</span></span>&#123;System.out.println(<span class="hljs-string">&quot;方法调用...&quot;</span>);&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setBeanClassLoader</span><span class="hljs-params">(ClassLoader classLoader)</span> </span>&#123;System.out.println(<span class="hljs-string">&quot;BeanClassLoaderAware 被调用...&quot;</span>);&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setBeanFactory</span><span class="hljs-params">(BeanFactory beanFactory)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;System.out.println(<span class="hljs-string">&quot;BeanFactoryAware 被调用...&quot;</span>);&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setBeanName</span><span class="hljs-params">(String name)</span> </span>&#123;System.out.println(<span class="hljs-string">&quot;BeanNameAware 被调用...&quot;</span>);&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">destroy</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;System.out.println(<span class="hljs-string">&quot;DisposableBean destroy 被调动...&quot;</span>);&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">afterPropertiesSet</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;System.out.println(<span class="hljs-string">&quot;InitializingBean afterPropertiesSet 被调动...&quot;</span>);&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">postProcessBeforeInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;System.out.println(<span class="hljs-string">&quot;BeanPostProcessor postProcessBeforeInitialization 被调用...&quot;</span>);<span class="hljs-keyword">return</span> bean;&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">postProcessAfterInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;System.out.println(<span class="hljs-string">&quot;BeanPostProcessor postProcessAfterInitialization 被调用...&quot;</span>);<span class="hljs-keyword">return</span> bean;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">initMethod</span><span class="hljs-params">()</span></span>&#123;System.out.println(<span class="hljs-string">&quot;init-method 被调用...&quot;</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">destroyMethod</span><span class="hljs-params">()</span></span>&#123;System.out.println(<span class="hljs-string">&quot;destroy-method 被调用...&quot;</span>);&#125;&#125;</code></pre><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">beans</span> <span class="hljs-attr">xmlns</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans&quot;</span></span><span class="hljs-tag">   <span class="hljs-attr">xmlns:xsi</span>=<span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span><span class="hljs-tag">   <span class="hljs-attr">xsi:schemaLocation</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">bean</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;lifeCycle&quot;</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;com.jievhaha.LifeCycleBean&quot;</span> <span class="hljs-attr">init-method</span>=<span class="hljs-string">&quot;initMethod&quot;</span> <span class="hljs-attr">destroy-method</span>=<span class="hljs-string">&quot;destroyMethod&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;test&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;test&quot;</span>/&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">bean</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">beans</span>&gt;</span></code></pre><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MyApplication</span> </span>&#123;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;ClassPathResource resource = <span class="hljs-keyword">new</span> ClassPathResource(<span class="hljs-string">&quot;applicationContext.xml&quot;</span>);DefaultListableBeanFactory factory = <span class="hljs-keyword">new</span> DefaultListableBeanFactory();XmlBeanDefinitionReader reader = <span class="hljs-keyword">new</span> XmlBeanDefinitionReader(factory);LifeCycleBean lifeCycleBean = <span class="hljs-keyword">new</span> LifeCycleBean();        <span class="hljs-comment">// BeanFactory 容器一定要调用该方法进行 BeanPostProcessor 注册</span>factory.addBeanPostProcessor(lifeCycleBean);reader.loadBeanDefinitions(resource);LifeCycleBean lifeCycleBean1 = (LifeCycleBean) factory.getBean(<span class="hljs-string">&quot;lifeCycle&quot;</span>);lifeCycleBean1.display();System.out.println(<span class="hljs-string">&quot;方法调用完成，容器开始关闭....&quot;</span>);        <span class="hljs-comment">// 关闭容器</span>factory.destroySingletons();    &#125;&#125;</code></pre><p>运行结果：</p><pre><code class="hljs xml">无参构造函数调用...//要注入BeanPostProcessor无参构造函数调用...//自己getBean属性注入....BeanNameAware 被调用...BeanClassLoaderAware 被调用...BeanFactoryAware 被调用...BeanPostProcessor postProcessBeforeInitialization 被调用...InitializingBean afterPropertiesSet 被调动...init-method 被调用...BeanPostProcessor postProcessAfterInitialization 被调用...方法调用...方法调用完成，容器开始关闭....DisposableBean destroy 被调动...destroy-method 被调用...</code></pre><p>总结，Spring Bean 的声明周期过程如下：</p><ol><li>Spring 容器根据实例化策略对 Bean 进行实例化。</li><li>实例化完成后，如果该 bean 设置了一些属性的话，则利用 set 方法设置一些属性。</li><li>如果该 Bean 实现了 BeanNameAware 接口，则调用 <code>setBeanName(String beanName)</code> 方法。</li><li>如果该 bean 实现了 BeanClassLoaderAware 接口，则调用 <code>setBeanClassLoader(ClassLoader classLoader)</code> 方法。</li><li>如果该 bean 实现了 BeanFactoryAware接口，则调用 <code>setBeanFactory(BeanFactory beanFactory)</code> 方法。</li><li>如果该容器注册了 BeanPostProcessor，则会调用<code>postProcessBeforeInitialization(Object bean, String beanName)</code> 方法,完成 bean 前置处理</li><li>如果该 bean 实现了 InitializingBean 接口，则调用<code>afterPropertiesSet()</code> 方法。</li><li>如果该 bean 配置了 <code>init-method</code> 方法，则调用其指定的方法。</li><li>初始化完成后，如果该容器注册了 BeanPostProcessor 则会调用 <code>postProcessAfterInitialization(Object bean, String beanName)</code> 方法,完成 bean 的后置处理。</li><li>对象完成初始化，开始方法调用。</li><li>在容器进行关闭之前，如果该 bean 实现了 DisposableBean 接口，则调用 <code>destroy()</code> 方法。</li><li>在容器进行关闭之前，如果该 bean 配置了 <code>destroy-method</code> ，则调用其指定的方法。</li><li>到这里一个 bean 也就完成了它的一生。</li></ol>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>IOC之InitializingBean 和 init-method</title>
    <link href="/2021/02/24/IOC%E4%B9%8BInitializingBean%20%E5%92%8C%20init-method/"/>
    <url>/2021/02/24/IOC%E4%B9%8BInitializingBean%20%E5%92%8C%20init-method/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">IOC之InitializingBean 和 init-method</font></div><p>Spring在初始化时进行三个检测扩展：之前说到的<code>Aware</code>和<code>BeanPostProcessor</code>，还有本篇<code>InitializingBean</code>接口和<code>init-method</code>。</p><p>因为<code>DisposableBean</code>接口 和 <code>destroy-method</code>与初始化的时候逻辑一样，故只分析<code>InitializingBean</code>接口和<code>init-method</code>。</p><h4 id="InitializingBean接口"><a href="#InitializingBean接口" class="headerlink" title="InitializingBean接口"></a><code>InitializingBean</code>接口</h4><p><code>InitializingBean</code>接口只提供了一个方法<code>afterPropertiesSet()</code>，用来为bean提供定义初始化方法。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">InitializingBean</span> </span>&#123;<span class="hljs-comment">//该方法在 BeanFactory 设置完了所有属性之后被调用</span><span class="hljs-comment">//  该方法允许 bean 实例设置了所有 bean 属性时执行初始化工作，如果该过程出现了错误则需要抛出异常</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">afterPropertiesSet</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception</span>;&#125;</code></pre><p>Spring 在完成实例化后，设置完所有属性，进行 <code>Aware</code>接口和 <code>BeanPostProcessor</code>前置处理之后，会接着检测当前 bean 对象是否实现了 <code>InitializingBean</code> 接口。如果是，则会调用其 <code>afterPropertiesSet()</code> 方法，进一步调整 bean 实例对象的状态，最后会执行<code>BeanPostProcessor</code>后置处理。</p><h5 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h5><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TestInitializingBean</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">InitializingBean</span> </span>&#123;<span class="hljs-keyword">private</span> String name;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">afterPropertiesSet</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;System.out.println(<span class="hljs-string">&quot;InitializingBean init...&quot;</span>);<span class="hljs-keyword">this</span>.name = <span class="hljs-string">&quot;jievhahaTest&quot;</span>;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getName</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> name;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setName</span><span class="hljs-params">(String name)</span> </span>&#123;<span class="hljs-keyword">this</span>.name = name;&#125;&#125;</code></pre><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MyApplication</span> </span>&#123;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;ApplicationContext applicationContext = <span class="hljs-keyword">new</span> ClassPathXmlApplicationContext(<span class="hljs-string">&quot;applicationContext.xml&quot;</span>);TestInitializingBean testInitializingBean = (TestInitializingBean) applicationContext.getBean(<span class="hljs-string">&quot;testInitializingBean&quot;</span>);System.out.println(testInitializingBean.getName());&#125;&#125;</code></pre><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">beans</span> <span class="hljs-attr">xmlns</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans&quot;</span></span><span class="hljs-tag">   <span class="hljs-attr">xmlns:xsi</span>=<span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span><span class="hljs-tag">   <span class="hljs-attr">xsi:schemaLocation</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">bean</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;testInitializingBean&quot;</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;com.jievhaha.TestInitializingBean&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;name&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;test&quot;</span>/&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">bean</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">beans</span>&gt;</span></code></pre><p>最后执行结果为：</p><pre><code class="hljs xml">Bean[testInitializingBean]开始初始化InitializingBean init...Bean[testInitializingBean]完成初始化jievhahaTest  //假如在afterPropertiesSet中没有设置，最后结果为test</code></pre><h5 id="invokeInitMethods"><a href="#invokeInitMethods" class="headerlink" title="invokeInitMethods"></a><code>invokeInitMethods</code></h5><p>在<code>Bean</code>初始化阶段， Spring 容器会主动检查当前 bean 是否已经实现了 InitializingBean 接口，如果实现了，则会掉用其 <code>afterPropertiesSet()</code> 方法。这个主动检查、调用的动作是由 <code>invokeInitMethods(String beanName, final Object bean, @Nullable RootBeanDefinition mbd)</code> 方法来完成的。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">invokeInitMethods</span><span class="hljs-params">(String beanName, <span class="hljs-keyword">final</span> Object bean, <span class="hljs-meta">@Nullable</span> RootBeanDefinition mbd)</span></span><span class="hljs-function">        <span class="hljs-keyword">throws</span> Throwable </span>&#123;    <span class="hljs-comment">// 首先会检查是否是 InitializingBean ，如果是的话需要调用 afterPropertiesSet()</span>    <span class="hljs-keyword">boolean</span> isInitializingBean = (bean <span class="hljs-keyword">instanceof</span> InitializingBean);    <span class="hljs-keyword">if</span> (isInitializingBean &amp;&amp; (mbd == <span class="hljs-keyword">null</span> || !mbd.isExternallyManagedInitMethod(<span class="hljs-string">&quot;afterPropertiesSet&quot;</span>))) &#123;        <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;            logger.trace(<span class="hljs-string">&quot;Invoking afterPropertiesSet() on bean with name &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);        &#125;        <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123; <span class="hljs-comment">// 安全模式</span>            <span class="hljs-keyword">try</span> &#123;                AccessController.doPrivileged((PrivilegedExceptionAction&lt;Object&gt;) () -&gt; &#123;                    <span class="hljs-comment">// 属性初始化的处理</span>                    ((InitializingBean) bean).afterPropertiesSet();                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;                &#125;, getAccessControlContext());            &#125; <span class="hljs-keyword">catch</span> (PrivilegedActionException pae) &#123;                <span class="hljs-keyword">throw</span> pae.getException();            &#125;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// 属性初始化的处理</span>            ((InitializingBean) bean).afterPropertiesSet();        &#125;    &#125;    <span class="hljs-keyword">if</span> (mbd != <span class="hljs-keyword">null</span> &amp;&amp; bean.getClass() != NullBean.class) &#123;        <span class="hljs-comment">// 判断是否指定了 init-method()，</span>        <span class="hljs-comment">// 如果指定了 init-method()，则再调用制定的init-method</span>        String initMethodName = mbd.getInitMethodName();        <span class="hljs-keyword">if</span> (StringUtils.hasLength(initMethodName) &amp;&amp;                !(isInitializingBean &amp;&amp; <span class="hljs-string">&quot;afterPropertiesSet&quot;</span>.equals(initMethodName)) &amp;&amp;                !mbd.isExternallyManagedInitMethod(initMethodName)) &#123;            <span class="hljs-comment">// 激活用户自定义的初始化方法</span>            <span class="hljs-comment">// 利用反射机制执行</span>            invokeCustomInitMethod(beanName, bean, mbd);        &#125;    &#125;&#125;</code></pre><ul><li>首先，检测当前 bean 是否实现了 InitializingBean 接口，如果实现了则调用其 <code>afterPropertiesSet()</code> 方法。</li><li>然后，再检查是否也指定了 <code>init-method</code>，如果指定了则通过反射机制调用指定的 <code>init-method</code> 方法。</li></ul><h4 id="init-method属性"><a href="#init-method属性" class="headerlink" title="init-method属性"></a><code>init-method</code>属性</h4><p>直接实现<code>InitializingBean</code>接口破坏了Spring的核心理念：无侵入性，故有另一种实现方式：<code>init-method</code>。</p><p>即spring配置文件注册bean时，给<code>&lt;bean&gt;</code>标签添加<code>init-method</code>属性，也可以直接使用<code>&lt;beans&gt;</code>标签的<code>default-init-method</code>属性来统一指定初始化方法，不用每个<code>&lt;bean&gt;</code>标签指定<code>init-method</code>属性。</p><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>从 <code>invokeInitMethods(...)</code> 方法中，我们知道 <code>init-method</code> 指定的方法会在 <code>afterPropertiesSet()</code> 方法之后执行，如果 <code>afterPropertiesSet()</code> 方法的执行的过程中出现了异常，则 <code>init-method</code> 是不会执行的，而且由于 <code>init-method</code> 采用的是反射执行的方式，所以 <code>afterPropertiesSet()</code> 方法的执行效率一般会高些，但是并不能排除我们要优先使用 <code>init-method</code>，主要是因为它消除了 bean 对 Spring 的依赖，Spring 没有侵入到我们业务代码，这样会更加符合 Spring 的理念。诚然，<code>init-method</code> 是基于 xml 配置文件的，就目前而言，我们的工程几乎都摒弃了配置，而采用注释的方式，那么 <code>@PreDestory</code> 可能比较适合。</p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>IOC之BeanPostProcessor</title>
    <link href="/2021/02/24/IOC%E4%B9%8BBeanPostProcessor/"/>
    <url>/2021/02/24/IOC%E4%B9%8BBeanPostProcessor/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">IOC之BeanPostProcessor</font></div><h4 id="BeanPostProcessor接口"><a href="#BeanPostProcessor接口" class="headerlink" title="BeanPostProcessor接口"></a><code>BeanPostProcessor</code>接口</h4><p>除了<code>Aware</code>这个扩展点，还有<code>BeanPostProcessor</code>，BeanPostProcessor 的作用：在 Bean 完成实例化后，如果我们需要对其进行一些配置、增加一些自己的处理逻辑，那么请使用 BeanPostProcessor。</p><h4 id="BeanPostProcessor实例"><a href="#BeanPostProcessor实例" class="headerlink" title="BeanPostProcessor实例"></a><code>BeanPostProcessor</code>实例</h4><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TestBeanPostProcessor</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">BeanPostProcessor</span> </span>&#123;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">postProcessBeforeInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;System.out.println(<span class="hljs-string">&quot;Bean[&quot;</span> + beanName + <span class="hljs-string">&quot;]开始初始化&quot;</span>);<span class="hljs-keyword">return</span> bean;&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">postProcessAfterInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;System.out.println(<span class="hljs-string">&quot;Bean[&quot;</span> + beanName + <span class="hljs-string">&quot;]完成初始化&quot;</span>);<span class="hljs-keyword">return</span> bean;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">display</span><span class="hljs-params">()</span></span>&#123;System.out.println(<span class="hljs-string">&quot;Hello BeanPostProcessor...&quot;</span>);&#125;&#125;</code></pre><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">beans</span> <span class="hljs-attr">xmlns</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans&quot;</span></span><span class="hljs-tag">   <span class="hljs-attr">xmlns:xsi</span>=<span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span><span class="hljs-tag">   <span class="hljs-attr">xsi:schemaLocation</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">bean</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;testBeanPostProcessor&quot;</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;com.jievhaha.TestBeanPostProcessor&quot;</span>/&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">beans</span>&gt;</span></code></pre><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MyApplication</span> </span>&#123;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;ClassPathResource resource = <span class="hljs-keyword">new</span> ClassPathResource(<span class="hljs-string">&quot;applicationContext.xml&quot;</span>);DefaultListableBeanFactory factory = <span class="hljs-keyword">new</span> DefaultListableBeanFactory();XmlBeanDefinitionReader reader = <span class="hljs-keyword">new</span> XmlBeanDefinitionReader(factory);reader.loadBeanDefinitions(resource);TestBeanPostProcessor testBeanPostProcessor = (TestBeanPostProcessor) factory.getBean(<span class="hljs-string">&quot;testBeanPostProcessor&quot;</span>);testBeanPostProcessor.display();    &#125;&#125;</code></pre><p>输出结果为：</p><pre><code class="hljs xml">Hello BeanPostProcessor...</code></pre><p>并没有执行前置、后置处理器。</p><p>在<code>AbstractAutowireCapableBeanFactory.initializeBean()</code>中：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">initializeBean</span><span class="hljs-params">(String beanName, Object bean, <span class="hljs-meta">@Nullable</span> RootBeanDefinition mbd)</span> </span>&#123;    <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123; <span class="hljs-comment">// 安全模式</span>        AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;            <span class="hljs-comment">// &lt;1&gt; 激活 Aware 方法，对特殊的 bean 处理：Aware、BeanClassLoaderAware、BeanFactoryAware</span>            invokeAwareMethods(beanName, bean);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;, getAccessControlContext());    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// &lt;1&gt; 激活 Aware 方法，对特殊的 bean 处理：Aware、BeanClassLoaderAware、BeanFactoryAware</span>        invokeAwareMethods(beanName, bean);    &#125;    <span class="hljs-comment">// &lt;2&gt; 后置处理器，before</span>    Object wrappedBean = bean;    <span class="hljs-keyword">if</span> (mbd == <span class="hljs-keyword">null</span> || !mbd.isSynthetic()) &#123;        wrappedBean = applyBeanPostProcessorsBeforeInitialization(wrappedBean, beanName);    &#125;    <span class="hljs-comment">// &lt;3&gt; 激活用户自定义的 init 方法</span>    <span class="hljs-keyword">try</span> &#123;        invokeInitMethods(beanName, wrappedBean, mbd);    &#125;    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(            (mbd != <span class="hljs-keyword">null</span> ? mbd.getResourceDescription() : <span class="hljs-keyword">null</span>),            beanName, <span class="hljs-string">&quot;Invocation of init method failed&quot;</span>, ex);    &#125;    <span class="hljs-comment">// &lt;2&gt; 后置处理器，after</span>    <span class="hljs-keyword">if</span> (mbd == <span class="hljs-keyword">null</span> || !mbd.isSynthetic()) &#123;        wrappedBean = applyBeanPostProcessorsAfterInitialization(wrappedBean, beanName);    &#125;    <span class="hljs-keyword">return</span> wrappedBean;&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">applyBeanPostProcessorsBeforeInitialization</span><span class="hljs-params">(Object existingBean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    Object result = existingBean;    <span class="hljs-comment">// 遍历 BeanPostProcessor 数组</span>    <span class="hljs-keyword">for</span> (BeanPostProcessor processor : getBeanPostProcessors()) &#123;        <span class="hljs-comment">// 处理</span>        Object current = processor.postProcessBeforeInitialization(result, beanName);        <span class="hljs-comment">// 返回空，则返回 result</span>        <span class="hljs-keyword">if</span> (current == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> result;        &#125;        <span class="hljs-comment">// 修改 result</span>        result = current;    &#125;    <span class="hljs-keyword">return</span> result;&#125;</code></pre><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;BeanPostProcessor&gt; <span class="hljs-title">getBeanPostProcessors</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.beanPostProcessors;&#125;<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> List&lt;BeanPostProcessor&gt; beanPostProcessors = <span class="hljs-keyword">new</span> BeanPostProcessorCacheAwareList();</code></pre><p>断点中可以查看到<code>getBeanPostProcessors()</code>的<code>size</code>为0。 </p><p>所以要想执行前置处理器就要保证该<code>List</code>不为空，那就要有<code>this.beanPostProcessors.add()</code>的过程：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">addBeanPostProcessor</span><span class="hljs-params">(BeanPostProcessor beanPostProcessor)</span> </span>&#123;    Assert.notNull(beanPostProcessor, <span class="hljs-string">&quot;BeanPostProcessor must not be null&quot;</span>);    <span class="hljs-keyword">this</span>.beanPostProcessors.remove(beanPostProcessor);    <span class="hljs-keyword">this</span>.beanPostProcessors.add(beanPostProcessor);&#125;</code></pre><p>该方法是由 AbstractBeanFactory 的父类 <code>org.springframework.beans.factory.config.ConfigurableBeanFactory</code> 接口定义，它的核心意思就是将指定 BeanPostProcessor 注册到该 BeanFactory 创建的 bean 中，同时它是<strong>按照插入的顺序进行注册的</strong>，完全忽略 Ordered 接口所表达任何排序语义（在 BeanPostProcessor 中我们提供一个 Ordered 顺序）。</p><p>所以要可以执行前后置处理器，只需增加如下步骤：</p><pre><code class="hljs java">TestBeanPostProcessor test = <span class="hljs-keyword">new</span> TestBeanPostProcessor();factory.addBeanPostProcesssor(test)。        ClassPathResource resource = <span class="hljs-keyword">new</span> ClassPathResource(<span class="hljs-string">&quot;applicationContext.xml&quot;</span>);DefaultListableBeanFactory factory = <span class="hljs-keyword">new</span> DefaultListableBeanFactory();XmlBeanDefinitionReader reader = <span class="hljs-keyword">new</span> XmlBeanDefinitionReader(factory);TestBeanPostProcessor beanPostProcessor = <span class="hljs-keyword">new</span> TestBeanPostProcessor();factory.addBeanPostProcessor(beanPostProcessor);reader.loadBeanDefinitions(resource);TestBeanPostProcessor testBeanPostProcessor = (TestBeanPostProcessor) factory.getBean(<span class="hljs-string">&quot;testBeanPostProcessor&quot;</span>);testBeanPostProcessor.display();</code></pre><p>这样，使用该factory创建任何Bean都会执行前后置处理器，ApplicationContext方法不需手动注册，会自动检测所有BeanPostProcessor然后进行自动注册（都已测试）。</p><p>总结：一般普通的 BeanFactory 是不支持自动注册 BeanPostProcessor 的，需要我们手动调用 <code>addBeanPostProcessor(BeanPostProcessor beanPostProcessor)</code> 方法进行注册。注册后的 BeanPostProcessor 适用于所有该 BeanFactory 创建的 bean，但是 <strong>ApplicationContext 可以在其 bean 定义中自动检测所有的 BeanPostProcessor 并自动完成注册，同时将他们应用到随后创建的任何 Bean 中</strong>。</p><h4 id="BeanPostProcessor基本原理"><a href="#BeanPostProcessor基本原理" class="headerlink" title="BeanPostProcessor基本原理"></a><code>BeanPostProcessor</code>基本原理</h4><p><code>BeanPostProcessor</code>接口：</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">BeanPostProcessor</span> </span>&#123;<span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">default</span> Object <span class="hljs-title">postProcessBeforeInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;<span class="hljs-keyword">return</span> bean;&#125;<span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">default</span> Object <span class="hljs-title">postProcessAfterInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;<span class="hljs-keyword">return</span> bean;&#125;&#125;</code></pre><p>BeanPostProcessor 可以理解为是 Spring 的一个工厂钩子（其实 Spring 提供一系列的钩子，如 Aware 、InitializingBean、DisposableBean），它是 Spring 提供的对象实例化阶段强有力的扩展点，允许 Spring 在实例化 bean 阶段对其进行定制化修改，比较常见的使用场景是处理标记接口实现类或者为当前对象提供代理实现（例如 AOP）。</p><p><code>postProcessBeforeInitialization(Object bean, String beanName)</code> 和 <code>postProcessAfterInitialization(Object bean, String beanName)</code> 两个方法，都接收一个 Object 类型的 <code>bean</code> ，一个 String 类型的 <code>beanName</code> ，其中 <code>bean</code> 是已经实例化了的 <code>instanceBean</code> ，能拿到这个你是不是可以对它为所欲为了？ 这两个方法是初始化 <code>bean</code> 的前后置处理器，他们应用 <code>invokeInitMethods(String beanName, final Object bean, RootBeanDefinition mbd)</code> 方法的前后。</p><p><img src="/images/spring_IOC/%E5%89%8D%E5%90%8E%E7%BD%AE%E5%A4%84%E7%90%86%E5%99%A8.png" alt="前后置处理器"></p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">initializeBean</span><span class="hljs-params">(String beanName, Object bean, <span class="hljs-meta">@Nullable</span> RootBeanDefinition mbd)</span> </span>&#123;    <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123; <span class="hljs-comment">// 安全模式</span>        AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;            <span class="hljs-comment">// &lt;1&gt; 激活 Aware 方法，对特殊的 bean 处理：Aware、BeanClassLoaderAware、BeanFactoryAware</span>            invokeAwareMethods(beanName, bean);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;, getAccessControlContext());    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// &lt;1&gt; 激活 Aware 方法，对特殊的 bean 处理：Aware、BeanClassLoaderAware、BeanFactoryAware</span>        invokeAwareMethods(beanName, bean);    &#125;    <span class="hljs-comment">// &lt;2&gt; 后置处理器，before</span>    Object wrappedBean = bean;    <span class="hljs-keyword">if</span> (mbd == <span class="hljs-keyword">null</span> || !mbd.isSynthetic()) &#123;        wrappedBean = applyBeanPostProcessorsBeforeInitialization(wrappedBean, beanName);    &#125;    <span class="hljs-comment">// &lt;3&gt; 激活用户自定义的 init 方法</span>    <span class="hljs-keyword">try</span> &#123;        invokeInitMethods(beanName, wrappedBean, mbd);    &#125;    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(            (mbd != <span class="hljs-keyword">null</span> ? mbd.getResourceDescription() : <span class="hljs-keyword">null</span>),            beanName, <span class="hljs-string">&quot;Invocation of init method failed&quot;</span>, ex);    &#125;    <span class="hljs-comment">// &lt;2&gt; 后置处理器，after</span>    <span class="hljs-keyword">if</span> (mbd == <span class="hljs-keyword">null</span> || !mbd.isSynthetic()) &#123;        wrappedBean = applyBeanPostProcessorsAfterInitialization(wrappedBean, beanName);    &#125;    <span class="hljs-keyword">return</span> wrappedBean;&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">applyBeanPostProcessorsBeforeInitialization</span><span class="hljs-params">(Object existingBean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    Object result = existingBean;    <span class="hljs-comment">// 遍历 BeanPostProcessor 数组</span>    <span class="hljs-keyword">for</span> (BeanPostProcessor processor : getBeanPostProcessors()) &#123;        <span class="hljs-comment">// 处理</span>        Object current = processor.postProcessBeforeInitialization(result, beanName);        <span class="hljs-comment">// 返回空，则返回 result</span>        <span class="hljs-keyword">if</span> (current == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> result;        &#125;        <span class="hljs-comment">// 修改 result</span>        result = current;    &#125;    <span class="hljs-keyword">return</span> result;&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">applyBeanPostProcessorsAfterInitialization</span><span class="hljs-params">(Object existingBean, String beanName)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> BeansException </span>&#123;    Object result = existingBean;    <span class="hljs-comment">// 遍历 BeanPostProcessor</span>    <span class="hljs-keyword">for</span> (BeanPostProcessor processor : getBeanPostProcessors()) &#123;        <span class="hljs-comment">// 处理</span>        Object current = processor.postProcessAfterInitialization(result, beanName);        <span class="hljs-comment">// 返回空，则返回 result</span>        <span class="hljs-keyword">if</span> (current == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> result;        &#125;        result = current;    &#125;    <span class="hljs-keyword">return</span> result;&#125;</code></pre><h5 id="自动检测并注册"><a href="#自动检测并注册" class="headerlink" title="自动检测并注册"></a>自动检测并注册</h5><p><code>getBeanPostProcessors()</code> 方法，返回的是 <code>beanPostProcessors</code> 集合，该集合里面存放就是我们自定义的 BeanPostProcessor ，如果该集合中存在元素则调用相应的方法，否则就直接返回 bean 了。这也是为什么使用 BeanFactory 容器是无法输出自定义 BeanPostProcessor 里面的内容，因为在 <code>BeanFactory.getBean(...)</code> 方法的过程中根本就没有将我们自定义的 BeanPostProcessor 注入进来，所以要想 BeanFactory 容器 的 BeanPostProcessor 生效我们必须手动调用 <code>addBeanPostProcessor(BeanPostProcessor beanPostProcessor)</code> 方法，将定义的 BeanPostProcessor 注册到相应的 BeanFactory 中。<strong>但是 ApplicationContext 不需要手动，因为 ApplicationContext 会自动检测并完成注册</strong>。</p><p>ApplicationContext 实现自动注册的原因，在于我们构造一个 ApplicationContext 实例对象的时候会调用 <code>AbstractApplicationContext.registerBeanPostProcessors(ConfigurableListableBeanFactory beanFactory)</code> 方法，将检测到的 BeanPostProcessor 注入到 ApplicationContext 容器中，同时应用到该容器创建的 bean 中。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractApplicationContext.java</span><span class="hljs-comment">/**</span><span class="hljs-comment"> * 实例化并调用已经注入的 BeanPostProcessor</span><span class="hljs-comment"> * 必须在应用中 bean 实例化之前调用</span><span class="hljs-comment"> */</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerBeanPostProcessors</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactory)</span> </span>&#123;    PostProcessorRegistrationDelegate.registerBeanPostProcessors(beanFactory, <span class="hljs-keyword">this</span>);&#125;<span class="hljs-comment">// PostProcessorRegistrationDelegate.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerBeanPostProcessors</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactory, AbstractApplicationContext applicationContext)</span> </span>&#123;    <span class="hljs-comment">// 获取所有的 BeanPostProcessor 的 beanName</span>    <span class="hljs-comment">// 这些 beanName 都已经全部加载到容器中去，但是没有实例化</span>    String[] postProcessorNames = beanFactory.getBeanNamesForType(BeanPostProcessor.class, <span class="hljs-keyword">true</span>, <span class="hljs-keyword">false</span>);    <span class="hljs-comment">// 记录所有的beanProcessor数量</span>    <span class="hljs-keyword">int</span> beanProcessorTargetCount = beanFactory.getBeanPostProcessorCount() + <span class="hljs-number">1</span> + postProcessorNames.length;    <span class="hljs-comment">// 注册 BeanPostProcessorChecker，它主要是用于在 BeanPostProcessor 实例化期间记录日志</span>    <span class="hljs-comment">// 当 Spring 中高配置的后置处理器还没有注册就已经开始了 bean 的实例化过程，这个时候便会打印 BeanPostProcessorChecker 中的内容</span>    beanFactory.addBeanPostProcessor(<span class="hljs-keyword">new</span> BeanPostProcessorChecker(beanFactory, beanProcessorTargetCount));    <span class="hljs-comment">// PriorityOrdered 保证顺序</span>    List&lt;BeanPostProcessor&gt; priorityOrderedPostProcessors = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();    List&lt;BeanPostProcessor&gt; internalPostProcessors = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();    <span class="hljs-comment">// 使用 Ordered 保证顺序</span>    List&lt;String&gt; orderedPostProcessorNames = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();    <span class="hljs-comment">// 没有顺序</span>    List&lt;String&gt; nonOrderedPostProcessorNames = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();    <span class="hljs-keyword">for</span> (String ppName : postProcessorNames) &#123;        <span class="hljs-keyword">if</span> (beanFactory.isTypeMatch(ppName, PriorityOrdered.class)) &#123;            <span class="hljs-comment">// 调用 getBean 获取 bean 实例对象</span>            BeanPostProcessor pp = beanFactory.getBean(ppName, BeanPostProcessor.class);            priorityOrderedPostProcessors.add(pp);            <span class="hljs-keyword">if</span> (pp <span class="hljs-keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;                internalPostProcessors.add(pp);            &#125;        &#125;        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (beanFactory.isTypeMatch(ppName, Ordered.class)) &#123;            <span class="hljs-comment">// 有序 Ordered</span>            orderedPostProcessorNames.add(ppName);        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// 无序</span>            nonOrderedPostProcessorNames.add(ppName);        &#125;    &#125;    <span class="hljs-comment">// 第一步，注册所有实现了 PriorityOrdered 的 BeanPostProcessor</span>    <span class="hljs-comment">// 先排序</span>    sortPostProcessors(priorityOrderedPostProcessors, beanFactory);    <span class="hljs-comment">// 后注册</span>    registerBeanPostProcessors(beanFactory, priorityOrderedPostProcessors);    <span class="hljs-comment">// 第二步，注册所有实现了 Ordered 的 BeanPostProcessor</span>    List&lt;BeanPostProcessor&gt; orderedPostProcessors = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;(orderedPostProcessorNames.size());    <span class="hljs-keyword">for</span> (String ppName : orderedPostProcessorNames) &#123;        BeanPostProcessor pp = beanFactory.getBean(ppName, BeanPostProcessor.class);        orderedPostProcessors.add(pp);        <span class="hljs-keyword">if</span> (pp <span class="hljs-keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;            internalPostProcessors.add(pp);        &#125;    &#125;    <span class="hljs-comment">// 先排序</span>    sortPostProcessors(orderedPostProcessors, beanFactory);    <span class="hljs-comment">// 后注册</span>    registerBeanPostProcessors(beanFactory, orderedPostProcessors);    <span class="hljs-comment">// 第三步注册所有无序的 BeanPostProcessor</span>    List&lt;BeanPostProcessor&gt; nonOrderedPostProcessors = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;(nonOrderedPostProcessorNames.size());    <span class="hljs-keyword">for</span> (String ppName : nonOrderedPostProcessorNames) &#123;        BeanPostProcessor pp = beanFactory.getBean(ppName, BeanPostProcessor.class);        nonOrderedPostProcessors.add(pp);        <span class="hljs-keyword">if</span> (pp <span class="hljs-keyword">instanceof</span> MergedBeanDefinitionPostProcessor) &#123;            internalPostProcessors.add(pp);        &#125;    &#125;    <span class="hljs-comment">// 注册，无需排序</span>    registerBeanPostProcessors(beanFactory, nonOrderedPostProcessors);    <span class="hljs-comment">// 最后，注册所有的 MergedBeanDefinitionPostProcessor 类型的 BeanPostProcessor</span>    sortPostProcessors(internalPostProcessors, beanFactory);    registerBeanPostProcessors(beanFactory, internalPostProcessors);    <span class="hljs-comment">// 加入ApplicationListenerDetector（探测器）</span>    <span class="hljs-comment">// 重新注册 BeanPostProcessor 以检测内部 bean，因为 ApplicationListeners 将其移动到处理器链的末尾</span>    beanFactory.addBeanPostProcessor(<span class="hljs-keyword">new</span> ApplicationListenerDetector(applicationContext));&#125;</code></pre><p>逻辑：</p><ul><li>首先 <code>beanFactory</code> 获取注册到该 BeanFactory 中所有 BeanPostProcessor 类型的 <code>beanName</code> 数组，其实就是找所有实现了 BeanPostProcessor 接口的 bean ，然后迭代这些 bean ，将其按照 PriorityOrdered、Ordered、无序的顺序，添加至相应的 List 集合中，最后依次调用 <code>sortPostProcessors(List postProcessors, ConfigurableListableBeanFactory beanFactory)</code> 方法来进行排序处理、 <code>registerBeanPostProcessors(ConfigurableListableBeanFactory beanFactory, List postProcessors)</code> 方法来完成注册。</li><li><strong>排序</strong>很简单，如果 <code>beanFactory</code> 为 DefaultListableBeanFactory ，则返回 BeanFactory 所依赖的比较器，否则反正默认的比较器(OrderComparator)，然后调用 <code>Listsort(Comparator c)</code> 方法即可。</li></ul><pre><code class="hljs java"><span class="hljs-comment">// PostProcessorRegistrationDelegate.java</span><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">sortPostProcessors</span><span class="hljs-params">(List&lt;?&gt; postProcessors, ConfigurableListableBeanFactory beanFactory)</span> </span>&#123;    <span class="hljs-comment">// Nothing to sort?</span>    <span class="hljs-keyword">if</span> (postProcessors.size() &lt;= <span class="hljs-number">1</span>) &#123;        <span class="hljs-keyword">return</span>;    &#125;    <span class="hljs-comment">// 获得 Comparator 对象</span>    Comparator&lt;Object&gt; comparatorToUse = <span class="hljs-keyword">null</span>;    <span class="hljs-keyword">if</span> (beanFactory <span class="hljs-keyword">instanceof</span> DefaultListableBeanFactory) &#123;<span class="hljs-comment">// 依赖的 Comparator 对象</span>        comparatorToUse = ((DefaultListableBeanFactory) beanFactory).getDependencyComparator();    &#125;    <span class="hljs-keyword">if</span> (comparatorToUse == <span class="hljs-keyword">null</span>) &#123;<span class="hljs-comment">// 默认 Comparator 对象</span>        comparatorToUse = OrderComparator.INSTANCE;    &#125;    <span class="hljs-comment">// 排序</span>    postProcessors.sort(comparatorToUse);&#125;</code></pre><ul><li><strong>注册</strong>同样是调用 <code>AbstractBeanFactory.addBeanPostProcessor(BeanPostProcessor beanPostProcessor)</code> 方法完成注册。</li></ul><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerBeanPostProcessors</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">    ConfigurableListableBeanFactory beanFactory, List&lt;BeanPostProcessor&gt; postProcessors)</span> </span>&#123;    <span class="hljs-comment">// 如果是AbstractBeanFactory，直接全部注册</span>    <span class="hljs-keyword">if</span> (beanFactory <span class="hljs-keyword">instanceof</span> AbstractBeanFactory) &#123;        <span class="hljs-comment">// Bulk addition is more efficient against our CopyOnWriteArrayList there</span>        ((AbstractBeanFactory) beanFactory).addBeanPostProcessors(postProcessors);    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 遍历 BeanPostProcessor 数组，注册</span>        <span class="hljs-keyword">for</span> (BeanPostProcessor postProcessor : postProcessors) &#123;            beanFactory.addBeanPostProcessor(postProcessor);        &#125;    &#125;&#125;</code></pre><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><ol><li>BeanPostProcessor 的作用域是容器级别的，它只和所在的容器相关 ，当 BeanPostProcessor 完成注册后，它会应用于所有跟它在同一个容器内的 bean 。</li><li>BeanFactory 和 ApplicationContext 对 BeanPostProcessor 的处理不同，ApplicationContext 会自动检测所有实现了 BeanPostProcessor 接口的 bean，并完成注册，但是使用 BeanFactory 容器时则需要手动调用 <code>AbstractBeanFactory.addBeanPostProcessor(BeanPostProcessor beanPostProcessor)</code> 方法来完成注册</li><li>ApplicationContext 的 BeanPostProcessor 支持 Ordered，而 BeanFactory 的 BeanPostProcessor 是不支持的，原因在于ApplicationContext 会对 BeanPostProcessor 进行 Ordered 检测并完成排序，而 BeanFactory 中的 BeanPostProcessor 只跟注册的顺序有关。</li></ol>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>IOC之Aware接口</title>
    <link href="/2021/02/24/IOC%E4%B9%8BAware%E6%8E%A5%E5%8F%A3/"/>
    <url>/2021/02/24/IOC%E4%B9%8BAware%E6%8E%A5%E5%8F%A3/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">IOC之Aware接口</font></div><p>AbstractAutowireCapableBeanFactory 的 <code>doCreateBean(final String beanName, final RootBeanDefinition mbd, final Object[] args)</code> 方法，主要干三件事情：</p><ol><li>实例化 bean 对象：<code>createBeanInstance(String beanName, RootBeanDefinition mbd, Object[] args)</code> 方法。</li><li>属性注入：<code>populateBean(String beanName, RootBeanDefinition mbd, BeanWrapper bw)</code> 方法。</li><li>初始化 bean 对象：<code>initializeBean(final String beanName, final Object bean, RootBeanDefinition mbd)</code> 方法。</li></ol><p>而初始化 bean 对象时，也是干了三件事情：</p><ol><li>激活 Aware 方法</li><li>后置处理器的应用</li><li>激活自定义的 init 方法</li></ol><h4 id="Aware接口"><a href="#Aware接口" class="headerlink" title="Aware接口"></a><code>Aware</code>接口</h4><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Aware</span> </span>&#123;    <span class="hljs-comment">// 空接口</span>&#125;</code></pre><p>Aware 接口为 Spring 容器的核心接口，是一个具有标识作用的超级接口，实现了该接口的 bean 是具有被 Spring 容器通知的能力，通知的方式是采用<strong>回调</strong>的方式。</p><p>Aware 接口为 Spring 容器的核心接口，是一个具有标识作用的超级接口，实现了该接口的 bean 是具有被 Spring 容器通知的能力，通知的方式是采用<strong>回调</strong>的方式。</p><p>Aware 的子接口需要提供一个 <code>setXxx</code> 方法，我们知道 set 是设置属性值的方法，即 Aware 类接口的 <code>setXxx</code> 方法其实就是设置 xxx 属性值的。 Aware 的含义是感知的、感应的，那么在 Spring 容器中是如何实现感知并设置属性值得呢？我们可以从初始化 bean 中的激活 Aware 的方法 <code>invokeAwareMethods(final String beanName, final Object bean)</code> 中看到一点点：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">invokeAwareMethods</span><span class="hljs-params">(<span class="hljs-keyword">final</span> String beanName, <span class="hljs-keyword">final</span> Object bean)</span> </span>&#123;<span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> Aware) &#123;    <span class="hljs-comment">// BeanNameAware</span><span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> BeanNameAware) &#123;((BeanNameAware) bean).setBeanName(beanName);&#125;<span class="hljs-comment">// BeanClassLoaderAware</span><span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> BeanClassLoaderAware) &#123;ClassLoader bcl = getBeanClassLoader();<span class="hljs-keyword">if</span> (bcl != <span class="hljs-keyword">null</span>) &#123;((BeanClassLoaderAware) bean).setBeanClassLoader(bcl);&#125;&#125;<span class="hljs-comment">// BeanFactoryAware</span><span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> BeanFactoryAware) &#123;((BeanFactoryAware) bean).setBeanFactory(AbstractAutowireCapableBeanFactory.<span class="hljs-keyword">this</span>);&#125;&#125;&#125;</code></pre><ul><li>首先，判断 bean 实例是否属于 Aware 接口的范畴，如果是的话，则调用实例的 <code>setXxx()</code> 方法给实例设置 xxx 属性值，在 <code>invokeAwareMethods(...)</code> 方法，主要是设置 beanName，beanClassLoader、BeanFactory 中三个属性值。</li></ul><h4 id="Aware子类"><a href="#Aware子类" class="headerlink" title="Aware子类"></a><code>Aware子类</code></h4><p><code>Aware</code>有诸多子类，比较熟悉的<code>BeanClassLoaderAware</code>、<code>BeanFactoryAware</code>、<code>BeanNameAware</code>：</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">BeanClassLoaderAware</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Aware</span> </span>&#123;    <span class="hljs-comment">/**</span><span class="hljs-comment">    * 将 BeanClassLoader 提供给 bean 实例回调</span><span class="hljs-comment">    * 在 bean 属性填充之后、初始化回调之前回调，</span><span class="hljs-comment">    * 例如InitializingBean的InitializingBean.afterPropertiesSet（）方法或自定义init方法</span><span class="hljs-comment">    */</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setBeanClassLoader</span><span class="hljs-params">(ClassLoader classLoader)</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">BeanFactoryAware</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Aware</span> </span>&#123;    <span class="hljs-comment">/**</span><span class="hljs-comment">    * 将 BeanFactory 提供给 bean 实例回调</span><span class="hljs-comment">    * 调用时机和 setBeanClassLoader 一样</span><span class="hljs-comment">    */</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setBeanFactory</span><span class="hljs-params">(BeanFactory beanFactory)</span> <span class="hljs-keyword">throws</span> BeansException</span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">BeanNameAware</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Aware</span> </span>&#123;    <span class="hljs-comment">/**</span><span class="hljs-comment">    * 在创建此 bean 的 bean工厂中设置 beanName</span><span class="hljs-comment">    */</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setBeanName</span><span class="hljs-params">(String name)</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">ApplicationContextAware</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Aware</span> </span>&#123;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * 设置此 bean 对象的 ApplicationContext，通常，该方法用于初始化对象</span><span class="hljs-comment">     */</span>    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setApplicationContext</span><span class="hljs-params">(ApplicationContext applicationContext)</span></span><span class="hljs-function">        <span class="hljs-keyword">throws</span> BeansException</span>;&#125;</code></pre><h5 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h5><p><code>JavaBean</code></p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TestAware</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">ApplicationContextAware</span>,<span class="hljs-title">BeanNameAware</span>, <span class="hljs-title">BeanClassLoaderAware</span>, <span class="hljs-title">BeanFactoryAware</span> </span>&#123;<span class="hljs-keyword">private</span> ClassLoader classLoader;<span class="hljs-keyword">private</span> String beanName;<span class="hljs-keyword">private</span> BeanFactory beanFactory;<span class="hljs-keyword">private</span> ApplicationContext applicationContext;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setBeanName</span><span class="hljs-params">(String name)</span> </span>&#123;System.out.println(<span class="hljs-string">&quot;调用了BeanNameAware的setBeanName...&quot;</span>);<span class="hljs-keyword">this</span>.beanName = name;&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setBeanClassLoader</span><span class="hljs-params">(ClassLoader classLoader)</span> </span>&#123;System.out.println(<span class="hljs-string">&quot;调用了BeanClassLoaderAware的setBeanClassLoader...&quot;</span>);<span class="hljs-keyword">this</span>.classLoader = classLoader;&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setBeanFactory</span><span class="hljs-params">(BeanFactory beanFactory)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;System.out.println(<span class="hljs-string">&quot;调用了BeanFactoryAware的setBeanFactory&quot;</span>);<span class="hljs-keyword">this</span>.beanFactory =beanFactory;&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setApplicationContext</span><span class="hljs-params">(ApplicationContext applicationContext)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;System.out.println(<span class="hljs-string">&quot;调用了ApplicationContextAware的setApplicationContext&quot;</span>);<span class="hljs-keyword">this</span>.applicationContext = applicationContext;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">display</span><span class="hljs-params">()</span></span>&#123;System.out.println(<span class="hljs-string">&quot;beanName:&quot;</span> + <span class="hljs-keyword">this</span>.beanName);System.out.println(<span class="hljs-string">&quot;是否为单例：&quot;</span> + <span class="hljs-keyword">this</span>.beanFactory.isPrototype(<span class="hljs-keyword">this</span>.beanName));<span class="hljs-comment">//System.out.println(&quot;系统环境为：&quot; + applicationContext.getEnvironment());</span>&#125;&#125;</code></pre><p><code>spring配置文件</code></p><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">beans</span> <span class="hljs-attr">xmlns</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans&quot;</span></span><span class="hljs-tag">   <span class="hljs-attr">xmlns:xsi</span>=<span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span><span class="hljs-tag">   <span class="hljs-attr">xsi:schemaLocation</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">bean</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;testAware&quot;</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;com.jievhaha.TestAware&quot;</span> <span class="hljs-attr">scope</span>=<span class="hljs-string">&quot;prototype&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">bean</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">beans</span>&gt;</span></code></pre><p>测试</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MyApplication</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        ClassPathResource classPathResource = <span class="hljs-keyword">new</span> ClassPathResource(<span class="hljs-string">&quot;applicationContext.xml&quot;</span>);        DefaultListableBeanFactory defaultListableBeanFactory = <span class="hljs-keyword">new</span> DefaultListableBeanFactory();        XmlBeanDefinitionReader xmlBeanDefinitionReader = <span class="hljs-keyword">new</span> XmlBeanDefinitionReader(defaultListableBeanFactory);        xmlBeanDefinitionReader.loadBeanDefinitions(classPathResource);        TestAware testAware = (TestAware) defaultListableBeanFactory.getBean(<span class="hljs-string">&quot;testAware&quot;</span>);        testAware.display();    &#125;&#125;</code></pre><p>结果：</p><pre><code class="hljs xml">调用了BeanNameAware的setBeanName...调用了BeanClassLoaderAware的setBeanClassLoader...调用了BeanFactoryAware的setBeanFactorybeanName:testAware是否为单例：true</code></pre><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MyApplication</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        ApplicationContext app = <span class="hljs-keyword">new</span> ClassPathXmlApplicationContext(<span class="hljs-string">&quot;applicationContext.xml&quot;</span>);TestAware testAware = (TestAware) app.getBean(<span class="hljs-string">&quot;testAware&quot;</span>);testAware.display();    &#125;&#125;</code></pre><p>结果，较上边的测试多调用了<code>setApplicationContext()</code>方法：</p><pre><code class="hljs xml">调用了BeanNameAware的setBeanName...调用了BeanClassLoaderAware的setBeanClassLoader...调用了BeanFactoryAware的setBeanFactory调用了ApplicationContextAware的setApplicationContextbeanName:testAware是否为单例：true</code></pre><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>常见<code>Aware</code>子接口</p><ul><li>LoadTimeWeaverAware：加载Spring Bean时织入第三方模块，如AspectJ</li><li>BeanClassLoaderAware：加载Spring Bean的类加载器</li><li>BootstrapContextAware：资源适配器BootstrapContext，如JCA,CCI</li><li>ResourceLoaderAware：底层访问资源的加载器</li><li>BeanFactoryAware：声明BeanFactory</li><li>PortletConfigAware：PortletConfig</li><li>PortletContextAware：PortletContext</li><li>ServletConfigAware：ServletConfig</li><li>ServletContextAware：ServletContext</li><li>MessageSourceAware：国际化</li><li>ApplicationEventPublisherAware：应用事件</li><li>NotificationPublisherAware：JMX通知</li><li>BeanNameAware：声明Spring Bean的名字</li></ul>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>加载Bean总结</title>
    <link href="/2021/02/24/%E5%8A%A0%E8%BD%BDBean%E6%80%BB%E7%BB%93/"/>
    <url>/2021/02/24/%E5%8A%A0%E8%BD%BDBean%E6%80%BB%E7%BB%93/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">加载Bean总结</font></div><p>开头十几篇分析了一个配置文件经历哪些过程转变成<code>BeanDefinition</code>对象，但这不是真正的bean，它仅仅包含了我们所需bean的信息。</p><p>之后十篇分析了bean的加载，此处做个总结。</p><p>bean的初始化起点，在我们第一次（显示或者隐式）调用<code>getBean()</code>来开启：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">// 内部调用 doGetBean(...) 方法。</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">getBean</span><span class="hljs-params">(String name)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-keyword">return</span> doGetBean(name, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">false</span>);&#125;<span class="hljs-keyword">protected</span> &lt;T&gt; <span class="hljs-function">T <span class="hljs-title">doGetBean</span><span class="hljs-params">(String name, <span class="hljs-meta">@Nullable</span> Class&lt;T&gt; requiredType, <span class="hljs-meta">@Nullable</span> Object[] args, <span class="hljs-keyword">boolean</span> typeCheckOnly)</span><span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-comment">/*</span><span class="hljs-comment"> * &lt;1&gt;</span><span class="hljs-comment"> * 通过 name 获取 beanName。这里不使用 name 直接作为 beanName 有两点原因：</span><span class="hljs-comment"> * 1. name 可能会以 &amp; 字符开头，表明调用者想获取 FactoryBean 本身，而非 FactoryBean</span><span class="hljs-comment"> * 实现类所创建的 bean。在 BeanFactory 中，FactoryBean 的实现类和其他的 bean 存储</span><span class="hljs-comment"> * 方式是一致的，即 &lt;beanName, bean&gt;，beanName 中是没有 &amp; 这个字符的。所以我们需要</span><span class="hljs-comment"> * 将 name 的首字符 &amp; 移除，这样才能从缓存里取到 FactoryBean 实例。</span><span class="hljs-comment"> * 2. 若 name 是一个别名，则应将别名转换为具体的实例名，也就是 beanName。</span><span class="hljs-comment"> */</span>    String beanName = transformedBeanName(name);    Object beanInstance;    <span class="hljs-comment">// 从缓存中或者实例工厂中获取 Bean 对象</span>    <span class="hljs-comment">// Eagerly check singleton cache for manually registered singletons.</span>    Object sharedInstance = getSingleton(beanName);    <span class="hljs-comment">/*</span><span class="hljs-comment"> * 如果容器中包含对应的bean则直接返回</span><span class="hljs-comment"> * 如果 sharedInstance = null，则说明缓存里没有对应的实例，表明这个实例还没创建。</span><span class="hljs-comment"> * BeanFactory 并不会在一开始就将所有的单例 bean 实例化好，而是在调用 getBean 获取</span><span class="hljs-comment"> * bean 时再实例化，也就是懒加载。</span><span class="hljs-comment"> * getBean 方法有很多重载，比如 getBean(String name, Object... args)，我们在首次获取</span><span class="hljs-comment"> * 某个 bean 时，可以传入用于初始化 bean 的参数数组（args），BeanFactory 会根据这些参数</span><span class="hljs-comment"> * 去匹配合适的构造方法构造 bean 实例。当然，如果单例 bean 早已创建好，这里的 args 就没有</span><span class="hljs-comment"> * 用了，BeanFactory 不会多次实例化单例 bean。</span><span class="hljs-comment"> */</span>    <span class="hljs-keyword">if</span> (sharedInstance != <span class="hljs-keyword">null</span> &amp;&amp; args == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;<span class="hljs-comment">//日志</span>            <span class="hljs-keyword">if</span> (isSingletonCurrentlyInCreation(beanName)) &#123;                logger.trace(<span class="hljs-string">&quot;Returning eagerly cached instance of singleton bean &#x27;&quot;</span> + beanName +                             <span class="hljs-string">&quot;&#x27; that is not fully initialized yet - a consequence of a circular reference&quot;</span>);            &#125;            <span class="hljs-keyword">else</span> &#123;                logger.trace(<span class="hljs-string">&quot;Returning cached instance of singleton bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);            &#125;        &#125;        <span class="hljs-comment">/*</span><span class="hljs-comment"> * &lt;2&gt;</span><span class="hljs-comment"> * 如果 sharedInstance 是普通的单例 bean，下面的方法会直接返回。但如果</span><span class="hljs-comment"> * sharedInstance 是 FactoryBean 类型的，则需调用 getObject 工厂方法获取真正的</span><span class="hljs-comment"> * bean 实例。如果用户想获取 FactoryBean 本身，这里也不会做特别的处理，直接返回</span><span class="hljs-comment"> * 即可。毕竟 FactoryBean 的实现类本身也是一种 bean，只不过具有一点特殊的功能而已。</span><span class="hljs-comment"> */</span>        beanInstance = getObjectForBeanInstance(sharedInstance, name, beanName, <span class="hljs-keyword">null</span>);    &#125;    <span class="hljs-comment">/*</span><span class="hljs-comment"> * 如果上面的条件不满足，则表明 sharedInstance 可能为空，此时 beanName 对应的 bean</span><span class="hljs-comment"> * 实例可能还未创建。这里还存在另一种可能，如果当前容器有父容器，beanName 对应的 bean 实例</span><span class="hljs-comment"> * 可能是在父容器中被创建了，所以在创建实例前，需要先去父容器里检查一下。</span><span class="hljs-comment"> */</span>    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// Fail if we&#x27;re already creating this bean instance:</span>        <span class="hljs-comment">// We&#x27;re assumably within a circular reference.</span>        <span class="hljs-comment">// 检查bean是否正在创建， BeanFactory 不缓存 Prototype 类型的 bean，无法处理该类型 bean 的循环依赖问题</span>        <span class="hljs-comment">// &lt;3&gt; 因为 Spring 只解决单例模式下得循环依赖，在原型模式下如果存在循环依赖则会抛出异常。</span>        <span class="hljs-keyword">if</span> (isPrototypeCurrentlyInCreation(beanName)) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCurrentlyInCreationException(beanName);        &#125;        <span class="hljs-comment">// Check if bean definition exists in this factory.</span>        <span class="hljs-comment">// &lt;4&gt; 如果 sharedInstance = null，则到父容器中查找 bean 实例</span>        BeanFactory parentBeanFactory = getParentBeanFactory();        <span class="hljs-keyword">if</span> (parentBeanFactory != <span class="hljs-keyword">null</span> &amp;&amp; !containsBeanDefinition(beanName)) &#123;            <span class="hljs-comment">// Not found -&gt; check parent.</span>            <span class="hljs-comment">// 获取 name 对应的 beanName，如果 name 是以 &amp; 字符开头，则返回 &amp; + beanName</span>            String nameToLookup = originalBeanName(name);            <span class="hljs-keyword">if</span> (parentBeanFactory <span class="hljs-keyword">instanceof</span> AbstractBeanFactory) &#123;                <span class="hljs-keyword">return</span> ((AbstractBeanFactory) parentBeanFactory).doGetBean(                    nameToLookup, requiredType, args, typeCheckOnly);            &#125;            <span class="hljs-comment">// 根据 args 是否为空，以决定调用父容器哪个方法获取 bean</span>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (args != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">// Delegation to parent with explicit args.</span>                <span class="hljs-keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup, args);            &#125;            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (requiredType != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">// No args -&gt; delegate to standard getBean method.</span>                <span class="hljs-keyword">return</span> parentBeanFactory.getBean(nameToLookup, requiredType);            &#125;            <span class="hljs-keyword">else</span> &#123;                <span class="hljs-keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup);            &#125;        &#125;        <span class="hljs-comment">//&lt;5&gt; 标记当前bean正在创建中，alreadyCreated中</span>        <span class="hljs-keyword">if</span> (!typeCheckOnly) &#123;            markBeanAsCreated(beanName);        &#125;        StartupStep beanCreation = <span class="hljs-keyword">this</span>.applicationStartup.start(<span class="hljs-string">&quot;spring.beans.instantiate&quot;</span>)            .tag(<span class="hljs-string">&quot;beanName&quot;</span>, name);        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-keyword">if</span> (requiredType != <span class="hljs-keyword">null</span>) &#123;                beanCreation.tag(<span class="hljs-string">&quot;beanType&quot;</span>, requiredType::toString);            &#125;            <span class="hljs-comment">// &lt;6&gt;</span>            <span class="hljs-comment">// 从容器中获取 beanName 相应的 GenericBeanDefinition 对象，并将其转换为 RootBeanDefinition 对象</span>            <span class="hljs-comment">// 因为从 XML 配置文件中读取到的 Bean 信息是存储在GenericBeanDefinition 中的。但是，所有的 Bean 后续处理都是针对于 RootBeanDefinition 的，所以这里需要进行一个转换。</span>            <span class="hljs-comment">// 转换的同时，如果父类 bean 不为空的话，则会一并合并父类的属性。</span>            RootBeanDefinition mbd = getMergedLocalBeanDefinition(beanName);            <span class="hljs-comment">//检查是否为抽象类</span>            checkMergedBeanDefinition(mbd, beanName, args);            <span class="hljs-comment">// Guarantee initialization of beans that the current bean depends on.</span>            <span class="hljs-comment">// &lt;7&gt; 处理所依赖的 bean，先创建需要在当前bean创建之前必须创建的bean，通过@DependsOn(&quot;eventListener&quot;)注解设置该信息， 检查是否有 dependsOn 依赖，如果有则先初始化所依赖的 bean</span>            String[] dependsOn = mbd.getDependsOn();            <span class="hljs-keyword">if</span> (dependsOn != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">for</span> (String dep : dependsOn) &#123;                    <span class="hljs-comment">/*</span><span class="hljs-comment"> * 检测是否存在 depends-on 循环依赖，若存在则抛异常。比如 A 依赖 B，</span><span class="hljs-comment"> * B 又依赖 A，他们的配置如下：</span><span class="hljs-comment"> *   &lt;bean id=&quot;beanA&quot; class=&quot;BeanA&quot; depends-on=&quot;beanB&quot;&gt;</span><span class="hljs-comment"> *   &lt;bean id=&quot;beanB&quot; class=&quot;BeanB&quot; depends-on=&quot;beanA&quot;&gt;</span><span class="hljs-comment"> * beanA 要求 beanB 在其之前被创建，但 beanB 又要求 beanA 先于它</span><span class="hljs-comment"> * 创建。这个时候形成了循环，对于 depends-on 循环，Spring 会直接</span><span class="hljs-comment"> * 抛出异常</span><span class="hljs-comment"> */</span>                    <span class="hljs-keyword">if</span> (isDependent(beanName, dep)) &#123;                        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                                        <span class="hljs-string">&quot;Circular depends-on relationship between &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; and &#x27;&quot;</span> + dep + <span class="hljs-string">&quot;&#x27;&quot;</span>);                    &#125;                    <span class="hljs-comment">// 缓存依赖调用，注册依赖记录，便于在销毁 Bean 之前对其进行销毁。</span>                    registerDependentBean(dep, beanName);                    <span class="hljs-keyword">try</span> &#123;                        <span class="hljs-comment">// 加载 depends-on 依赖</span>                        getBean(dep);                    &#125;                    <span class="hljs-keyword">catch</span> (NoSuchBeanDefinitionException ex) &#123;                        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                                        <span class="hljs-string">&quot;&#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; depends on missing bean &#x27;&quot;</span> + dep + <span class="hljs-string">&quot;&#x27;&quot;</span>, ex);                    &#125;                &#125;            &#125;            <span class="hljs-comment">// Create bean instance.</span>            <span class="hljs-comment">// &lt;8&gt; 创建 bean 实例</span>            <span class="hljs-keyword">if</span> (mbd.isSingleton()) &#123;<span class="hljs-comment">// 单例模式</span>                <span class="hljs-comment">/*</span><span class="hljs-comment"> * 这里并没有直接调用 createBean 方法创建 bean 实例，而是通过</span><span class="hljs-comment"> * getSingleton(String, ObjectFactory) 方法获取 bean 实例。</span><span class="hljs-comment"> * getSingleton(String, ObjectFactory) 方法会在内部调用</span><span class="hljs-comment"> * ObjectFactory 的 getObject() 方法创建 bean，并会在创建完成后，</span><span class="hljs-comment"> * 将 bean 放入缓存中。</span><span class="hljs-comment"> */</span>                sharedInstance = getSingleton(beanName, () -&gt; &#123;                    <span class="hljs-keyword">try</span> &#123;                        <span class="hljs-comment">// 创建 bean 实例</span>                        <span class="hljs-keyword">return</span> createBean(beanName, mbd, args);                    &#125;                    <span class="hljs-keyword">catch</span> (BeansException ex) &#123;                        <span class="hljs-comment">// Explicitly remove instance from singleton cache: It might have been put there</span>                        <span class="hljs-comment">// eagerly by the creation process, to allow for circular reference resolution.</span>                        <span class="hljs-comment">// Also remove any beans that received a temporary reference to the bean.</span>                        <span class="hljs-comment">// 显式从单例缓存中删除 Bean 实例</span>                        <span class="hljs-comment">// 因为单例模式下为了解决循环依赖，可能他已经存在了，所以销毁它。</span>                        destroySingleton(beanName);                        <span class="hljs-keyword">throw</span> ex;                    &#125;                &#125;);                <span class="hljs-comment">// 如果 bean 是 FactoryBean 类型，则调用工厂方法获取真正的 bean 实例。否则直接返回 bean 实例</span>                beanInstance = getObjectForBeanInstance(sharedInstance, name, beanName, mbd);            &#125;            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (mbd.isPrototype()) &#123;<span class="hljs-comment">// 原型模式</span>                <span class="hljs-comment">// It&#x27;s a prototype -&gt; create a new instance.</span>                Object prototypeInstance = <span class="hljs-keyword">null</span>;                <span class="hljs-keyword">try</span> &#123;                    <span class="hljs-comment">// &lt;1&gt; 加载前置处理</span>                    beforePrototypeCreation(beanName);                    <span class="hljs-comment">// &lt;2&gt; 创建 Bean 对象</span>                    prototypeInstance = createBean(beanName, mbd, args);                &#125;                <span class="hljs-keyword">finally</span> &#123;                    <span class="hljs-comment">// &lt;3&gt; 加载后置处理</span>                    afterPrototypeCreation(beanName);                &#125;                <span class="hljs-comment">// &lt;4&gt; 从 Bean 实例中获取对象</span>                beanInstance = getObjectForBeanInstance(prototypeInstance, name, beanName, mbd);            &#125;            <span class="hljs-keyword">else</span> &#123;<span class="hljs-comment">// 从指定的 scope 下创建 bean</span>                <span class="hljs-comment">// 获得 scopeName 对应的 Scope 对象</span>                String scopeName = mbd.getScope();                <span class="hljs-keyword">if</span> (!StringUtils.hasLength(scopeName)) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">&quot;No scope name defined for bean ´&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);                &#125;                Scope scope = <span class="hljs-keyword">this</span>.scopes.get(scopeName);                <span class="hljs-keyword">if</span> (scope == <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">&quot;No Scope registered for scope name &#x27;&quot;</span> + scopeName + <span class="hljs-string">&quot;&#x27;&quot;</span>);                &#125;                <span class="hljs-keyword">try</span> &#123;                    <span class="hljs-comment">// 从指定的 scope 下创建 bean</span>                    Object scopedInstance = scope.get(beanName, () -&gt; &#123;                        <span class="hljs-comment">// 加载前置处理</span>                        beforePrototypeCreation(beanName);                        <span class="hljs-keyword">try</span> &#123;                            <span class="hljs-comment">// 创建 Bean 对象</span>                            <span class="hljs-keyword">return</span> createBean(beanName, mbd, args);                        &#125;                        <span class="hljs-keyword">finally</span> &#123;                            <span class="hljs-comment">// 加载后缀处理</span>                            afterPrototypeCreation(beanName);                        &#125;                    &#125;);                    <span class="hljs-comment">// 从 Bean 实例中获取对象</span>                    beanInstance = getObjectForBeanInstance(scopedInstance, name, beanName, mbd);                &#125;                <span class="hljs-keyword">catch</span> (IllegalStateException ex) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ScopeNotActiveException(beanName, scopeName, ex);                &#125;            &#125;        &#125;        <span class="hljs-keyword">catch</span> (BeansException ex) &#123;            beanCreation.tag(<span class="hljs-string">&quot;exception&quot;</span>, ex.getClass().toString());            beanCreation.tag(<span class="hljs-string">&quot;message&quot;</span>, String.valueOf(ex.getMessage()));            cleanupAfterBeanCreationFailure(beanName);            <span class="hljs-keyword">throw</span> ex;        &#125;        <span class="hljs-keyword">finally</span> &#123;            beanCreation.end();        &#125;    &#125;    <span class="hljs-comment">// &lt;9&gt;</span>    <span class="hljs-keyword">return</span> adaptBeanInstance(name, beanInstance, requiredType);&#125;<span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span>&lt;T&gt; <span class="hljs-function">T <span class="hljs-title">adaptBeanInstance</span><span class="hljs-params">(String name, Object bean, <span class="hljs-meta">@Nullable</span> Class&lt;?&gt; requiredType)</span> </span>&#123;    <span class="hljs-comment">// Check if required type matches the type of the actual bean instance.</span>    <span class="hljs-comment">//&lt;9&gt; 检查需要的类型是否符合 bean 的实际类型，如果需要进行类型转换，则在此处进行转换。</span>    <span class="hljs-keyword">if</span> (requiredType != <span class="hljs-keyword">null</span> &amp;&amp; !requiredType.isInstance(bean)) &#123;        <span class="hljs-keyword">try</span> &#123;            Object convertedBean = getTypeConverter().convertIfNecessary(bean, requiredType);            <span class="hljs-keyword">if</span> (convertedBean == <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanNotOfRequiredTypeException(name, requiredType, bean.getClass());            &#125;            <span class="hljs-keyword">return</span> (T) convertedBean;        &#125;        <span class="hljs-keyword">catch</span> (TypeMismatchException ex) &#123;            <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                logger.trace(<span class="hljs-string">&quot;Failed to convert bean &#x27;&quot;</span> + name + <span class="hljs-string">&quot;&#x27; to required type &#x27;&quot;</span> +                             ClassUtils.getQualifiedName(requiredType) + <span class="hljs-string">&quot;&#x27;&quot;</span>, ex);            &#125;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanNotOfRequiredTypeException(name, requiredType, bean.getClass());        &#125;    &#125;    <span class="hljs-keyword">return</span> (T) bean;&#125;</code></pre><p><code>doGetBean()</code>方法的主要过程：</p><ol><li>**转换 beanName **。因为我们调用 <code>getBean(...)</code> 方法传入的 <code>name</code> 并不一定就是 beanName，可以传入 aliasName，FactoryBean，所以这里需要进行简单的转换过程。</li><li>尝试**从缓存中加载单例 bean **。</li><li><strong>bean 的实例化</strong>。</li><li><strong>原型模式的依赖检查</strong>。因为 Spring 只会解决单例模式的循环依赖，对于原型模式的循环依赖都是直接抛出 <code>BeanCurrentlyInCreationException</code> 异常。</li><li><strong>尝试从 parentBeanFactory 获取 bean 实例</strong>。如果 <code>parentBeanFactory != null &amp;&amp; !containsBeanDefinition(beanName)</code> 则尝试从 parentBeanFactory 中获取 bean 实例对象，因为 <code>!containsBeanDefinition(beanName)</code> 就意味着定义的 xml 文件中没有 beanName 相应的配置，这个时候就只能从 <code>parentBeanFactory</code> 中获取。</li><li><strong>获取 RootBeanDefinition，并对其进行合并检查</strong>。从缓存中获取已经解析的 RootBeanDefinition 。同时，如果父类不为 <code>null</code> 的话，则会合并父类的属性。</li><li><strong>依赖检查</strong>。某个 bean 依赖其他 bean ，则需要先加载依赖的 bean。</li><li><strong>对不同的 scope 进行处理</strong>。</li><li><strong>类型转换处理</strong>。如果传递的 <code>requiredType</code> 不为 <code>null</code>，则需要检测所得到 bean 的类型是否与该 <code>requiredType</code> 一致。如果不一致则尝试转换，当然也要能够转换成功，否则抛出 <code>BeanNotOfRequiredTypeException</code> 异常。</li></ol><p>更加简单的总结：</p><ol><li>从缓存中获取 bean。</li><li>创建 bean 实例对象。</li><li>从 bean 实例中获取对象。</li></ol><h4 id="从缓存中获取-bean"><a href="#从缓存中获取-bean" class="headerlink" title="从缓存中获取 bean"></a>从缓存中获取 bean</h4><p>Spring 中根据 scope 可以将 bean 分为以下几类：singleton、prototype 和 其他，这样分的原因在于 Spring 在对不同 scope 处理的时候是这么处理的：</p><ul><li>singleton ：在 Spring 的 IoC 容器中只存在一个对象实例，所有该对象的引用都共享这个实例。Spring 容器只会创建该 bean 定义的唯一实例，这个实例会被保存到缓存中，并且对该bean的所有后续请求和引用都将返回该缓存中的对象实例。</li><li>prototype ：每次对该bean的请求都会创建一个新的实例</li><li>其他：<ul><li>request：每次 http 请求将会有各自的 bean 实例。</li><li>session：在一个 http session 中，一个 bean 定义对应一个 bean 实例。</li><li>global session：在一个全局的 http session 中，一个 bean 定义对应一个 bean 实例。</li></ul></li></ul><p>所以，<strong>从缓存中获取的 bean 一定是 singleton bean</strong>，这也是 <strong>Spring 为何只解决 singleton bean 的循环依赖</strong>。</p><p>调用 <code>getSingleton(String beanName)</code> 方法，从缓存中获取 singleton bean。</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">getSingleton</span><span class="hljs-params">(String beanName)</span> </span>&#123;    <span class="hljs-keyword">return</span> getSingleton(beanName, <span class="hljs-keyword">true</span>);&#125;<span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">getSingleton</span><span class="hljs-params">(String beanName, <span class="hljs-keyword">boolean</span> allowEarlyReference)</span> </span>&#123;    <span class="hljs-comment">// 从单例缓冲中加载 bean</span>    Object singletonObject = <span class="hljs-keyword">this</span>.singletonObjects.get(beanName);    <span class="hljs-comment">// 缓存中的 bean 为空，且当前 bean 正在创建</span>    <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span> &amp;&amp; isSingletonCurrentlyInCreation(beanName)) &#123;        <span class="hljs-comment">// 加锁</span>        <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.singletonObjects) &#123;            <span class="hljs-comment">// 从 earlySingletonObjects 获取</span>            singletonObject = <span class="hljs-keyword">this</span>.earlySingletonObjects.get(beanName);            <span class="hljs-comment">// earlySingletonObjects 中没有，且允许提前创建</span>            <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span> &amp;&amp; allowEarlyReference) &#123;                <span class="hljs-comment">// 从 singletonFactories 中获取对应的 ObjectFactory</span>                ObjectFactory&lt;?&gt; singletonFactory = <span class="hljs-keyword">this</span>.singletonFactories.get(beanName);                <span class="hljs-keyword">if</span> (singletonFactory != <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-comment">// 获得 bean</span>                    singletonObject = singletonFactory.getObject();                    <span class="hljs-comment">// 添加 bean 到 earlySingletonObjects 中</span>                    <span class="hljs-keyword">this</span>.earlySingletonObjects.put(beanName, singletonObject);                    <span class="hljs-comment">// 从 singletonFactories 中移除对应的 ObjectFactory</span>                    <span class="hljs-keyword">this</span>.singletonFactories.remove(beanName);                &#125;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> singletonObject;&#125;</code></pre><p>该方法就是从 <code>singletonObjects</code>、<code>earlySingletonObjects</code>、 <code>singletonFactories</code> 三个缓存中获取，这里也是 Spring 解决 bean 循环依赖的关键之处。可看《从单例缓存中获取单例 bean》、《循环依赖处理》。</p><h4 id="创建Bean实例对象"><a href="#创建Bean实例对象" class="headerlink" title="创建Bean实例对象"></a>创建Bean实例对象</h4><p>如果缓存中没有，也没有 <code>parentBeanFactory</code> ，则会调用 <code>createBean(String beanName, RootBeanDefinition mbd, Object[] args)</code> 方法，创建 bean 实例。该方法主要是在处理不同 scope 的 bean 的时候进行调用。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">// bean 的名字</span><span class="hljs-comment">// 已经合并了父类属性的（如果有的话）BeanDefinition</span><span class="hljs-comment">// 用于构造函数或者工厂方法创建 bean 实例对象的参数 </span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">abstract</span> Object <span class="hljs-title">createBean</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> Object[] args)</span> <span class="hljs-keyword">throws</span> BeanCreationException</span>;</code></pre><p>该方法是定义在 AbstractBeanFactory 中的<strong>抽象</strong>方法，其含义是根据给定的 BeanDefinition 和 <code>args</code> 实例化一个 bean 对象。如果该 BeanDefinition 存在父类，则该 BeanDefinition 已经合并了父类的属性。所有 Bean 实例的创建都会委托给该方法实现。</p><p>该抽象方法的<strong>默认实现</strong>是在类 AbstractAutowireCapableBeanFactory 中实现，该方法其实只是做一些检查和验证工作，真正的初始化工作是由 <code>doCreateBean(final String beanName, final RootBeanDefinition mbd, final Object[] args)</code> 方法来实现。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">doCreateBean</span><span class="hljs-params">(<span class="hljs-keyword">final</span> String beanName, <span class="hljs-keyword">final</span> RootBeanDefinition mbd, <span class="hljs-keyword">final</span> <span class="hljs-meta">@Nullable</span> Object[] args)</span> <span class="hljs-keyword">throws</span> BeanCreationException </span>&#123;    <span class="hljs-comment">// BeanWrapper 是对 Bean 的包装，其接口中所定义的功能很简单包括设置获取被包装的对象，获取被包装 bean 的属性描述器</span>    BeanWrapper instanceWrapper = <span class="hljs-keyword">null</span>;    <span class="hljs-comment">// &lt;1&gt; 单例模型，则从未完成的 FactoryBean 缓存中删除</span>    <span class="hljs-keyword">if</span> (mbd.isSingleton()) &#123;        instanceWrapper = <span class="hljs-keyword">this</span>.factoryBeanInstanceCache.remove(beanName);    &#125;    <span class="hljs-comment">// &lt;2&gt; 使用合适的实例化策略来创建新的实例：工厂方法、构造函数自动注入、简单初始化</span>    <span class="hljs-keyword">if</span> (instanceWrapper == <span class="hljs-keyword">null</span>) &#123;        instanceWrapper = createBeanInstance(beanName, mbd, args);    &#125;    <span class="hljs-comment">// 包装的实例对象</span>    <span class="hljs-keyword">final</span> Object bean = instanceWrapper.getWrappedInstance();    <span class="hljs-comment">// 包装的实例对象的类型</span>    Class&lt;?&gt; beanType = instanceWrapper.getWrappedClass();    <span class="hljs-keyword">if</span> (beanType != NullBean.class) &#123;        mbd.resolvedTargetType = beanType;    &#125;    <span class="hljs-comment">// Allow post-processors to modify the merged bean definition.</span>    <span class="hljs-comment">// &lt;3&gt; 判断是否有后置处理</span>    <span class="hljs-comment">// 如果有后置处理，则允许后置处理修改 BeanDefinition</span>    <span class="hljs-keyword">synchronized</span> (mbd.postProcessingLock) &#123;        <span class="hljs-keyword">if</span> (!mbd.postProcessed) &#123;            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">// 后置处理修改 BeanDefinition</span>                applyMergedBeanDefinitionPostProcessors(mbd, beanType, beanName);            &#125; <span class="hljs-keyword">catch</span> (Throwable ex) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                        <span class="hljs-string">&quot;Post-processing of merged bean definition failed&quot;</span>, ex);            &#125;            mbd.postProcessed = <span class="hljs-keyword">true</span>;        &#125;    &#125;    <span class="hljs-comment">// &lt;4&gt; 解决单例模式的循环依赖</span>    <span class="hljs-keyword">boolean</span> earlySingletonExposure = (mbd.isSingleton() <span class="hljs-comment">// 单例模式</span>            &amp;&amp; <span class="hljs-keyword">this</span>.allowCircularReferences <span class="hljs-comment">// 运行循环依赖</span>            &amp;&amp; isSingletonCurrentlyInCreation(beanName)); <span class="hljs-comment">// 当前单例 bean 是否正在被创建</span>    <span class="hljs-keyword">if</span> (earlySingletonExposure) &#123;        <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;            logger.trace(<span class="hljs-string">&quot;Eagerly caching bean &#x27;&quot;</span> + beanName +                    <span class="hljs-string">&quot;&#x27; to allow for resolving potential circular references&quot;</span>);        &#125;        <span class="hljs-comment">// 提前将创建的 bean 实例加入到 singletonFactories 中</span>        <span class="hljs-comment">// 这里是为了后期避免循环依赖</span>        addSingletonFactory(beanName, () -&gt; getEarlyBeanReference(beanName, mbd, bean));    &#125;    <span class="hljs-comment">// 开始初始化 bean 实例对象</span>    Object exposedObject = bean;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// &lt;5&gt; 对 bean 进行填充，将各个属性值注入，其中，可能存在依赖于其他 bean 的属性</span>        <span class="hljs-comment">// 则会递归初始依赖 bean</span>        populateBean(beanName, mbd, instanceWrapper);        <span class="hljs-comment">// &lt;6&gt; 调用初始化方法</span>        exposedObject = initializeBean(beanName, exposedObject, mbd);    &#125; <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">if</span> (ex <span class="hljs-keyword">instanceof</span> BeanCreationException &amp;&amp; beanName.equals(((BeanCreationException) ex).getBeanName())) &#123;            <span class="hljs-keyword">throw</span> (BeanCreationException) ex;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(                    mbd.getResourceDescription(), beanName, <span class="hljs-string">&quot;Initialization of bean failed&quot;</span>, ex);        &#125;    &#125;    <span class="hljs-comment">// &lt;7&gt; 循环依赖处理</span>    <span class="hljs-keyword">if</span> (earlySingletonExposure) &#123;        <span class="hljs-comment">// 获取 earlySingletonReference</span>        Object earlySingletonReference = getSingleton(beanName, <span class="hljs-keyword">false</span>);        <span class="hljs-comment">// 只有在存在循环依赖的情况下，earlySingletonReference 才不会为空</span>        <span class="hljs-keyword">if</span> (earlySingletonReference != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">// 如果 exposedObject 没有在初始化方法中被改变，也就是没有被增强</span>            <span class="hljs-keyword">if</span> (exposedObject == bean) &#123;                exposedObject = earlySingletonReference;            <span class="hljs-comment">// 处理依赖</span>            &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.allowRawInjectionDespiteWrapping &amp;&amp; hasDependentBean(beanName)) &#123;                String[] dependentBeans = getDependentBeans(beanName);                Set&lt;String&gt; actualDependentBeans = <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;(dependentBeans.length);                <span class="hljs-keyword">for</span> (String dependentBean : dependentBeans) &#123;                    <span class="hljs-keyword">if</span> (!removeSingletonIfCreatedForTypeCheckOnly(dependentBean)) &#123;                        actualDependentBeans.add(dependentBean);                    &#125;                &#125;                <span class="hljs-keyword">if</span> (!actualDependentBeans.isEmpty()) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCurrentlyInCreationException(beanName,                            <span class="hljs-string">&quot;Bean with name &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; has been injected into other beans [&quot;</span> +                            StringUtils.collectionToCommaDelimitedString(actualDependentBeans) +                            <span class="hljs-string">&quot;] in its raw version as part of a circular reference, but has eventually been &quot;</span> +                            <span class="hljs-string">&quot;wrapped. This means that said other beans do not use the final version of the &quot;</span> +                            <span class="hljs-string">&quot;bean. This is often the result of over-eager type matching - consider using &quot;</span> +                            <span class="hljs-string">&quot;&#x27;getBeanNamesOfType&#x27; with the &#x27;allowEagerInit&#x27; flag turned off, for example.&quot;</span>);                &#125;            &#125;        &#125;    &#125;    <span class="hljs-comment">// &lt;8&gt; 注册 bean</span>    <span class="hljs-keyword">try</span> &#123;        registerDisposableBeanIfNecessary(beanName, bean, mbd);    &#125; <span class="hljs-keyword">catch</span> (BeanDefinitionValidationException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(                mbd.getResourceDescription(), beanName, <span class="hljs-string">&quot;Invalid destruction signature&quot;</span>, ex);    &#125;    <span class="hljs-keyword">return</span> exposedObject;&#125;</code></pre><p><code>doCreateBean(...)</code> 方法，是创建 bean 实例的核心方法，主要流程如下：</p><ul><li><code>&lt;1&gt;</code> 处，如果是单例模式，则清除 <code>factoryBeanInstanceCache 缓存</code>，同时返回 BeanWrapper 实例对象，当然如果存在。</li><li><code>&lt;2&gt;</code> 处，如果缓存中没有 BeanWrapper 或者不是单例模式，则调用 <code>createBeanInstance(...)</code> 方法，实例化 bean，主要是将 BeanDefinition 转换为 BeanWrapper 。</li><li><code>&lt;3&gt;</code> 处，MergedBeanDefinitionPostProcessor 的应用。</li><li><code>&lt;4&gt;</code> 处，单例模式的循环依赖处理。</li><li><code>&lt;5&gt;</code> 处，调用 <code>populateBean(String beanName, RootBeanDefinition mbd, BeanWrapper bw)</code> 方法，进行属性填充。将所有属性填充至 bean 的实例中。</li><li><code>&lt;6&gt;</code> 处，调用 <code>initializeBean(final String beanName, final Object bean, RootBeanDefinition mbd)</code> 方法，初始化 bean 。</li><li><code>&lt;7&gt;</code> 处，依赖检查。</li><li><code>&lt;8&gt;</code> 处，注册 DisposableBean 。</li></ul><h5 id="实例化Bean"><a href="#实例化Bean" class="headerlink" title="实例化Bean"></a>实例化Bean</h5><p>如果缓存中没有 BeanWrapper 实例对象或者该 bean 不是 singleton，则调用 <code>createBeanInstance(...)</code> 方法。创建 bean 实例。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> BeanWrapper <span class="hljs-title">createBeanInstance</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> Object[] args)</span> </span>&#123;    <span class="hljs-comment">// 解析 bean ，将 bean 类名解析为 class 引用。</span>    Class&lt;?&gt; beanClass = resolveBeanClass(mbd, beanName);    <span class="hljs-keyword">if</span> (beanClass != <span class="hljs-keyword">null</span> &amp;&amp; !Modifier.isPublic(beanClass.getModifiers()) &amp;&amp; !mbd.isNonPublicAccessAllowed()) &#123; <span class="hljs-comment">// 校验</span>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                <span class="hljs-string">&quot;Bean class isn&#x27;t public, and non-public access not allowed: &quot;</span> + beanClass.getName());    &#125;    <span class="hljs-comment">// &lt;1&gt; 如果存在 Supplier 回调，则使用给定的回调方法初始化策略</span>    Supplier&lt;?&gt; instanceSupplier = mbd.getInstanceSupplier();    <span class="hljs-keyword">if</span> (instanceSupplier != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> obtainFromSupplier(instanceSupplier, beanName);    &#125;    <span class="hljs-comment">// &lt;2&gt; 使用 FactoryBean 的 factory-method 来创建，支持静态工厂和实例工厂</span>    <span class="hljs-keyword">if</span> (mbd.getFactoryMethodName() != <span class="hljs-keyword">null</span>)  &#123;        <span class="hljs-keyword">return</span> instantiateUsingFactoryMethod(beanName, mbd, args);    &#125;    <span class="hljs-comment">// &lt;3&gt; Shortcut when re-creating the same bean...</span>    <span class="hljs-keyword">boolean</span> resolved = <span class="hljs-keyword">false</span>;    <span class="hljs-keyword">boolean</span> autowireNecessary = <span class="hljs-keyword">false</span>;    <span class="hljs-keyword">if</span> (args == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// constructorArgumentLock 构造函数的常用锁</span>        <span class="hljs-keyword">synchronized</span> (mbd.constructorArgumentLock) &#123;            <span class="hljs-comment">// 如果已缓存的解析的构造函数或者工厂方法不为空，则可以利用构造函数解析</span>            <span class="hljs-comment">// 因为需要根据参数确认到底使用哪个构造函数，该过程比较消耗性能，所有采用缓存机制</span>            <span class="hljs-keyword">if</span> (mbd.resolvedConstructorOrFactoryMethod != <span class="hljs-keyword">null</span>) &#123;                resolved = <span class="hljs-keyword">true</span>;                autowireNecessary = mbd.constructorArgumentsResolved;            &#125;        &#125;    &#125;    <span class="hljs-comment">// 已经解析好了，直接注入即可</span>    <span class="hljs-keyword">if</span> (resolved) &#123;        <span class="hljs-comment">// &lt;3.1&gt; autowire 自动注入，调用构造函数自动注入</span>        <span class="hljs-keyword">if</span> (autowireNecessary) &#123;            <span class="hljs-keyword">return</span> autowireConstructor(beanName, mbd, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">null</span>);        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// &lt;3.2&gt; 使用默认构造函数构造</span>            <span class="hljs-keyword">return</span> instantiateBean(beanName, mbd);        &#125;    &#125;    <span class="hljs-comment">// &lt;4&gt; 确定解析的构造函数</span>    <span class="hljs-comment">// 主要是检查已经注册的 SmartInstantiationAwareBeanPostProcessor</span>    Constructor&lt;?&gt;[] ctors = determineConstructorsFromBeanPostProcessors(beanClass, beanName);    <span class="hljs-comment">// &lt;4.1&gt; 有参数情况时，创建 Bean 。先利用参数个数，类型等，确定最精确匹配的构造方法。</span>    <span class="hljs-keyword">if</span> (ctors != <span class="hljs-keyword">null</span> || mbd.getResolvedAutowireMode() == AUTOWIRE_CONSTRUCTOR ||            mbd.hasConstructorArgumentValues() || !ObjectUtils.isEmpty(args))  &#123;        <span class="hljs-keyword">return</span> autowireConstructor(beanName, mbd, ctors, args);    &#125;    <span class="hljs-comment">// &lt;4.1&gt; 选择构造方法，创建 Bean 。</span>    ctors = mbd.getPreferredConstructors();    <span class="hljs-keyword">if</span> (ctors != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> autowireConstructor(beanName, mbd, ctors, <span class="hljs-keyword">null</span>); <span class="hljs-comment">// args = null</span>    &#125;    <span class="hljs-comment">// &lt;4.2&gt; 有参数时，又没获取到构造方法，则只能调用无参构造方法来创建实例了(兜底方法)</span>    <span class="hljs-keyword">return</span> instantiateBean(beanName, mbd);&#125;</code></pre><p>实例化 Bean 对象，主要过程：</p><ul><li><code>&lt;1&gt;</code> 处，如果存在 Supplier 回调，则调用 <code>obtainFromSupplier(Supplier instanceSupplier, String beanName)</code> 方法，进行初始化。</li><li><code>&lt;2&gt;</code> 处，如果存在工厂方法，则使用工厂方法进行初始化，并直接return。</li><li><code>&lt;3&gt;</code> 处，首先判断缓存，如果<strong>缓存中存在</strong>，即已经解析过了，则直接使用已经解析了的。根据 <code>constructorArgumentsResolved</code> 参数来判断：<ul><li><code>&lt;3.1&gt;</code> 处，是使用构造函数自动注入，即调用 <code>autowireConstructor(String beanName, RootBeanDefinition mbd, Constructor[] ctors, Object[] explicitArgs)</code> 方法。</li><li><code>&lt;3.2&gt;</code> 处，还是默认构造函数，即调用 <code>instantiateBean(final String beanName, final RootBeanDefinition mbd)</code> 方法。</li></ul></li></ul><ul><li><code>&lt;4&gt;</code> 处，如果<strong>缓存中没有</strong>，则需要先确定到底使用哪个构造函数来完成解析工作，因为一个类有多个构造函数，每个构造函数都有不同的构造参数，所以需要根据参数来锁定构造函数并完成初始化。<ul><li><code>&lt;4.1&gt;</code> 处，如果存在参数，则使用相应的带有参数的构造函数，即调用 <code>autowireConstructor(String beanName, RootBeanDefinition mbd, Constructor[] ctors, Object[] explicitArgs)</code> 方法。</li><li><code>&lt;4.2&gt;</code> 处，否则，使用默认构造函数，即调用 <code>instantiateBean(final String beanName, final RootBeanDefinition mbd)</code> 方法。</li></ul></li></ul><p>其实<strong>核心思想</strong>还是在于根据不同的情况执行不同的实例化策略，主要是包括如下四种策略：</p><ol><li>Supplier 回调</li><li><code>instantiateUsingFactoryMethod(...)</code> 方法，工厂方法初始化</li><li><code>autowireConstructor(...)</code> 方法，构造函数自动注入初始化</li><li><code>instantiateBean(...)</code> 方法，默认构造函数注入</li></ol><p>其实无论哪种策略，他们的实现逻辑都差不多：确定构造函数和构造方法，然后实例化。</p><p>只不过相对于 <strong>Supplier 回调</strong>和<strong>默认构造函数</strong>注入而言，<strong>工厂方法初始化</strong>和<strong>构造函数</strong>自动注入初始化会比较复杂，因为他们<strong>构造函数和构造参数的不确定性</strong>，Spring 需要花大量的精力来确定构造函数和构造参数，如果确定了则好办，直接选择实例化策略即可。当然在实例化的时候会根据<strong>是否有需要覆盖或者动态替换掉的方法</strong>，因为存在覆盖或者织入的话需要创建<strong>动态代理</strong>将方法织入，这个时候就只能选择 CGLIB 的方式来实例化，否则直接利用<strong>反射</strong>的方式即可。</p><h5 id="属性填充"><a href="#属性填充" class="headerlink" title="属性填充"></a>属性填充</h5><p>属性填充其实就是将 BeanDefinition 的属性值赋值给 BeanWrapper 实例对象的过程。在填充的过程需要根据注入的类型不同来区分是根据<strong>类型注入</strong>还是<strong>名字注入</strong>，当然在这个过程还会涉及<strong>循环依赖</strong>的问题的。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">populateBean</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> BeanWrapper bw)</span> </span>&#123;    <span class="hljs-comment">// 没有实例化对象</span>    <span class="hljs-keyword">if</span> (bw == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// 有属性，则抛出 BeanCreationException 异常</span>        <span class="hljs-keyword">if</span> (mbd.hasPropertyValues()) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(                    mbd.getResourceDescription(), beanName, <span class="hljs-string">&quot;Cannot apply property values to null instance&quot;</span>);            <span class="hljs-comment">// 没有属性，直接 return 返回</span>        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">return</span>;        &#125;    &#125;    <span class="hljs-comment">// &lt;1&gt; 在设置属性之前给 InstantiationAwareBeanPostProcessors 最后一次改变 bean 的机会</span>    <span class="hljs-keyword">boolean</span> continueWithPropertyPopulation = <span class="hljs-keyword">true</span>;    <span class="hljs-keyword">if</span> (!mbd.isSynthetic()  <span class="hljs-comment">// bean 不是&quot;合成&quot;的，即未由应用程序本身定义</span>            &amp;&amp; hasInstantiationAwareBeanPostProcessors()) &#123; <span class="hljs-comment">// 是否持有 InstantiationAwareBeanPostProcessor</span>        <span class="hljs-comment">// 迭代所有的 BeanPostProcessors</span>        <span class="hljs-keyword">for</span> (BeanPostProcessor bp : getBeanPostProcessors()) &#123;            <span class="hljs-keyword">if</span> (bp <span class="hljs-keyword">instanceof</span> InstantiationAwareBeanPostProcessor) &#123; <span class="hljs-comment">// 如果为 InstantiationAwareBeanPostProcessor</span>                InstantiationAwareBeanPostProcessor ibp = (InstantiationAwareBeanPostProcessor) bp;                <span class="hljs-comment">// 返回值为是否继续填充 bean</span>                <span class="hljs-comment">// postProcessAfterInstantiation：如果应该在 bean上面设置属性则返回 true，否则返回 false</span>                <span class="hljs-comment">// 一般情况下，应该是返回true 。</span>                <span class="hljs-comment">// 返回 false 的话，将会阻止在此 Bean 实例上调用任何后续的 InstantiationAwareBeanPostProcessor 实例。</span>                <span class="hljs-keyword">if</span> (!ibp.postProcessAfterInstantiation(bw.getWrappedInstance(), beanName)) &#123;                    continueWithPropertyPopulation = <span class="hljs-keyword">false</span>;                    <span class="hljs-keyword">break</span>;                &#125;            &#125;        &#125;    &#125;    <span class="hljs-comment">// 如果后续处理器发出停止填充命令，则终止后续操作</span>    <span class="hljs-keyword">if</span> (!continueWithPropertyPopulation) &#123;        <span class="hljs-keyword">return</span>;    &#125;    <span class="hljs-comment">// bean 的属性值</span>    PropertyValues pvs = (mbd.hasPropertyValues() ? mbd.getPropertyValues() : <span class="hljs-keyword">null</span>);    <span class="hljs-comment">// &lt;2&gt; 自动注入</span>    <span class="hljs-keyword">if</span> (mbd.getResolvedAutowireMode() == AUTOWIRE_BY_NAME || mbd.getResolvedAutowireMode() == AUTOWIRE_BY_TYPE) &#123;        <span class="hljs-comment">// 将 PropertyValues 封装成 MutablePropertyValues 对象</span>        <span class="hljs-comment">// MutablePropertyValues 允许对属性进行简单的操作，并提供构造函数以支持Map的深度复制和构造。</span>        MutablePropertyValues newPvs = <span class="hljs-keyword">new</span> MutablePropertyValues(pvs);        <span class="hljs-comment">// Add property values based on autowire by name if applicable.</span>        <span class="hljs-comment">// 根据名称自动注入</span>        <span class="hljs-keyword">if</span> (mbd.getResolvedAutowireMode() == AUTOWIRE_BY_NAME) &#123;            autowireByName(beanName, mbd, bw, newPvs);        &#125;        <span class="hljs-comment">// Add property values based on autowire by type if applicable.</span>        <span class="hljs-comment">// 根据类型自动注入</span>        <span class="hljs-keyword">if</span> (mbd.getResolvedAutowireMode() == AUTOWIRE_BY_TYPE) &#123;            autowireByType(beanName, mbd, bw, newPvs);        &#125;        pvs = newPvs;    &#125;    <span class="hljs-comment">// 是否已经注册了 InstantiationAwareBeanPostProcessors</span>    <span class="hljs-keyword">boolean</span> hasInstAwareBpps = hasInstantiationAwareBeanPostProcessors();    <span class="hljs-comment">// 是否需要进行【依赖检查】</span>    <span class="hljs-keyword">boolean</span> needsDepCheck = (mbd.getDependencyCheck() != AbstractBeanDefinition.DEPENDENCY_CHECK_NONE);    <span class="hljs-comment">// &lt;3&gt; BeanPostProcessor 处理</span>    PropertyDescriptor[] filteredPds = <span class="hljs-keyword">null</span>;    <span class="hljs-keyword">if</span> (hasInstAwareBpps) &#123;        <span class="hljs-keyword">if</span> (pvs == <span class="hljs-keyword">null</span>) &#123;            pvs = mbd.getPropertyValues();        &#125;        <span class="hljs-comment">// 遍历 BeanPostProcessor 数组</span>        <span class="hljs-keyword">for</span> (BeanPostProcessor bp : getBeanPostProcessors()) &#123;            <span class="hljs-keyword">if</span> (bp <span class="hljs-keyword">instanceof</span> InstantiationAwareBeanPostProcessor) &#123;                InstantiationAwareBeanPostProcessor ibp = (InstantiationAwareBeanPostProcessor) bp;                <span class="hljs-comment">// 对所有需要依赖检查的属性进行后处理</span>                PropertyValues pvsToUse = ibp.postProcessProperties(pvs, bw.getWrappedInstance(), beanName);                <span class="hljs-keyword">if</span> (pvsToUse == <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-comment">// 从 bw 对象中提取 PropertyDescriptor 结果集</span>                    <span class="hljs-comment">// PropertyDescriptor：可以通过一对存取方法提取一个属性</span>                    <span class="hljs-keyword">if</span> (filteredPds == <span class="hljs-keyword">null</span>) &#123;                        filteredPds = filterPropertyDescriptorsForDependencyCheck(bw, mbd.allowCaching);                    &#125;                    pvsToUse = ibp.postProcessPropertyValues(pvs, filteredPds, bw.getWrappedInstance(), beanName);                    <span class="hljs-keyword">if</span> (pvsToUse == <span class="hljs-keyword">null</span>) &#123;                        <span class="hljs-keyword">return</span>;                    &#125;                &#125;                pvs = pvsToUse;            &#125;        &#125;    &#125;        <span class="hljs-comment">// &lt;4&gt; 依赖检查</span>    <span class="hljs-keyword">if</span> (needsDepCheck) &#123;        <span class="hljs-keyword">if</span> (filteredPds == <span class="hljs-keyword">null</span>) &#123;            filteredPds = filterPropertyDescriptorsForDependencyCheck(bw, mbd.allowCaching);        &#125;        <span class="hljs-comment">// 依赖检查，对应 depends-on 属性</span>        checkDependencies(beanName, mbd, filteredPds, pvs);    &#125;    <span class="hljs-comment">// &lt;5&gt; 将属性应用到 bean 中</span>    <span class="hljs-keyword">if</span> (pvs != <span class="hljs-keyword">null</span>) &#123;        applyPropertyValues(beanName, mbd, bw, pvs);    &#125;&#125;</code></pre><p>主要过程：</p><ul><li><p><code>&lt;1&gt;</code> ，根据 <code>hasInstantiationAwareBeanPostProcessors</code> 属性来判断，是否需要在注入属性之前给 InstantiationAwareBeanPostProcessors 最后一次改变 bean 的机会。<strong>此过程可以控制 Spring 是否继续进行属性填充</strong>。</p></li><li><p>统一存入到 PropertyValues 中，PropertyValues 用于描述 bean 的属性。</p><ul><li><code>&lt;2&gt;</code> ，根据注入类型( <code>AbstractBeanDefinition.getResolvedAutowireMode()</code> 方法的返回值 )的不同来判断：是根据名称来自动注入<code>autowireByName()</code>，还是根据类型来自动注入<code>autowireByType()</code>。</li><li><code>&lt;3&gt;</code> ，进行 BeanPostProcessor 处理。</li><li><code>&lt;4&gt;</code> ，依赖检测。</li></ul></li><li><p><code>&lt;5&gt;</code> ，将所有 PropertyValues 中的属性，填充到 BeanWrapper 中。</p></li></ul><h5 id="初始化Bean"><a href="#初始化Bean" class="headerlink" title="初始化Bean"></a>初始化Bean</h5><p>初始化 bean 为 <code>createBean(...)</code> 方法的最后一个过程：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">initializeBean</span><span class="hljs-params">(<span class="hljs-keyword">final</span> String beanName, <span class="hljs-keyword">final</span> Object bean, <span class="hljs-meta">@Nullable</span> RootBeanDefinition mbd)</span> </span>&#123;    <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123; <span class="hljs-comment">// 安全模式</span>        AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;            <span class="hljs-comment">// &lt;1&gt; 激活 Aware 方法，对特殊的 bean 处理：Aware、BeanClassLoaderAware、BeanFactoryAware</span>            invokeAwareMethods(beanName, bean);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;, getAccessControlContext());    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// &lt;1&gt; 激活 Aware 方法，对特殊的 bean 处理：Aware、BeanClassLoaderAware、BeanFactoryAware</span>        invokeAwareMethods(beanName, bean);    &#125;    <span class="hljs-comment">// &lt;2&gt; 后处理器，before</span>    Object wrappedBean = bean;    <span class="hljs-keyword">if</span> (mbd == <span class="hljs-keyword">null</span> || !mbd.isSynthetic()) &#123;        wrappedBean = applyBeanPostProcessorsBeforeInitialization(wrappedBean, beanName);    &#125;    <span class="hljs-comment">// &lt;3&gt; 激活用户自定义的 init 方法</span>    <span class="hljs-keyword">try</span> &#123;        invokeInitMethods(beanName, wrappedBean, mbd);    &#125; <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(                (mbd != <span class="hljs-keyword">null</span> ? mbd.getResourceDescription() : <span class="hljs-keyword">null</span>),                beanName, <span class="hljs-string">&quot;Invocation of init method failed&quot;</span>, ex);    &#125;    <span class="hljs-comment">// &lt;2&gt; 后处理器，after</span>    <span class="hljs-keyword">if</span> (mbd == <span class="hljs-keyword">null</span> || !mbd.isSynthetic()) &#123;        wrappedBean = applyBeanPostProcessorsAfterInitialization(wrappedBean, beanName);    &#125;    <span class="hljs-keyword">return</span> wrappedBean;&#125;</code></pre><p>初始化 bean 的方法其实就是三个步骤的处理，而这三个步骤主要还是根据<strong>用户设定</strong>的来进行初始化，这三个过程为：</p><ul><li><code>&lt;1&gt;</code> 激活 Aware 方法。</li><li><code>&lt;3&gt;</code> 后置处理器的应用。</li><li><code>&lt;2&gt;</code> 激活自定义的 init 方法。</li></ul><h4 id="从Bean实例中获取对象"><a href="#从Bean实例中获取对象" class="headerlink" title="从Bean实例中获取对象"></a>从Bean实例中获取对象</h4><p>无论是从单例缓存中获取的 bean 实例 还是通过 <code>createBean(...)</code> 方法来创建的 bean 实例，最终都会调用 <code>getObjectForBeanInstance(...)</code> 方法来根据传入的 bean 实例获取对象，按照 Spring 的传统，该方法也只是做一些检测工作，真正的实现逻辑是委托给 <code>getObjectFromFactoryBean(...)</code> 方法来实现。代码如下：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">getObjectFromFactoryBean</span><span class="hljs-params">(FactoryBean&lt;?&gt; factory, String beanName, <span class="hljs-keyword">boolean</span> shouldPostProcess)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 为单例模式且缓存中存在</span>    <span class="hljs-keyword">if</span> (factory.isSingleton() &amp;&amp; containsSingleton(beanName)) &#123;        <span class="hljs-keyword">synchronized</span> (getSingletonMutex()) &#123; <span class="hljs-comment">// &lt;1.1&gt; 单例锁</span>            <span class="hljs-comment">// &lt;1.2&gt; 从缓存中获取指定的 factoryBean</span>            Object object = <span class="hljs-keyword">this</span>.factoryBeanObjectCache.get(beanName);            <span class="hljs-keyword">if</span> (object == <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">// 为空，则从 FactoryBean 中获取对象</span>                object = doGetObjectFromFactoryBean(factory, beanName);                <span class="hljs-comment">// 从缓存中获取</span>                Object alreadyThere = <span class="hljs-keyword">this</span>.factoryBeanObjectCache.get(beanName);                <span class="hljs-keyword">if</span> (alreadyThere != <span class="hljs-keyword">null</span>) &#123;                    object = alreadyThere;                &#125; <span class="hljs-keyword">else</span> &#123;                    <span class="hljs-comment">// &lt;1.3&gt; 需要后续处理</span>                    <span class="hljs-keyword">if</span> (shouldPostProcess) &#123;                        <span class="hljs-comment">// 若该 Bean 处于创建中，则返回非处理对象，而不是存储它</span>                        <span class="hljs-keyword">if</span> (isSingletonCurrentlyInCreation(beanName)) &#123;                            <span class="hljs-comment">// Temporarily return non-post-processed object, not storing it yet..</span>                            <span class="hljs-keyword">return</span> object;                        &#125;                        <span class="hljs-comment">// 单例 Bean 的前置处理</span>                        beforeSingletonCreation(beanName);                        <span class="hljs-keyword">try</span> &#123;                            <span class="hljs-comment">// 对从 FactoryBean 获取的对象进行后处理</span>                            <span class="hljs-comment">// 生成的对象将暴露给 bean 引用</span>                            object = postProcessObjectFromFactoryBean(object, beanName);                        &#125; <span class="hljs-keyword">catch</span> (Throwable ex) &#123;                            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(beanName,                                    <span class="hljs-string">&quot;Post-processing of FactoryBean&#x27;s singleton object failed&quot;</span>, ex);                        &#125; <span class="hljs-keyword">finally</span> &#123;                            <span class="hljs-comment">// 单例 Bean 的后置处理</span>                            afterSingletonCreation(beanName);                        &#125;                    &#125;                    <span class="hljs-comment">// &lt;1.4&gt; 添加到 factoryBeanObjectCache 中，进行缓存</span>                    <span class="hljs-keyword">if</span> (containsSingleton(beanName)) &#123;                        <span class="hljs-keyword">this</span>.factoryBeanObjectCache.put(beanName, object);                    &#125;                &#125;            &#125;            <span class="hljs-keyword">return</span> object;        &#125;    <span class="hljs-comment">// &lt;2&gt;</span>    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 为空，则从 FactoryBean 中获取对象</span>        Object object = doGetObjectFromFactoryBean(factory, beanName);        <span class="hljs-comment">// 需要后续处理</span>        <span class="hljs-keyword">if</span> (shouldPostProcess) &#123;            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">// 对从 FactoryBean 获取的对象进行后处理</span>                <span class="hljs-comment">// 生成的对象将暴露给 bean 引用</span>                object = postProcessObjectFromFactoryBean(object, beanName);            &#125;            <span class="hljs-keyword">catch</span> (Throwable ex) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(beanName, <span class="hljs-string">&quot;Post-processing of FactoryBean&#x27;s object failed&quot;</span>, ex);            &#125;        &#125;        <span class="hljs-keyword">return</span> object;    &#125;&#125;</code></pre><p>主要流程：</p><ul><li>若为单例且单例 Bean 缓存中存在 <code>beanName</code> ，则 <code>&lt;1&gt;</code> 进行后续处理（跳转到下一步），否则，则 <code>&lt;2&gt;</code> 从 FactoryBean 中获取 Bean 实例对象。</li><li><code>&lt;1.1&gt;</code> 首先，获取锁。其实我们在前面篇幅中发现了大量的同步锁，锁住的对象都是 <code>this.singletonObjects</code>，主要是因为在单例模式中必须要<strong>保证全局唯一</strong>。</li><li><code>&lt;1.2&gt;</code> 然后，从 <code>factoryBeanObjectCache</code> 缓存中获取实例对象 <code>object</code> 。若 <code>object</code> 为空，则调用 <code>doGetObjectFromFactoryBean(FactoryBean factory, String beanName)</code> 方法，从 FactoryBean 获取对象，其实内部就是调用 <code>FactoryBean.getObject()</code> 方法。</li><li><code>&lt;1.3&gt;</code> 如果需要后续处理( <code>shouldPostProcess = true</code> )，则进行进一步处理：<ul><li>若该 Bean 处于创建中（<code>isSingletonCurrentlyInCreation(String beanName)</code> 方法返回 <code>true</code> ），则返回<strong>非处理的 Bean 对象</strong>，而不是存储它。</li><li>调用 <code>beforeSingletonCreation(String beanName)</code> 方法，进行创建之前的处理。默认实现将该 Bean 标志为当前创建的。</li><li>调用 <code>postProcessObjectFromFactoryBean(Object object, String beanName)</code> 方法，对从 FactoryBean 获取的 Bean 实例对象进行后置处理。</li><li>调用 <code>afterSingletonCreation(String beanName)</code> 方法，进行创建 Bean 之后的处理，默认实现是将该 bean 标记为不再在创建中。</li></ul></li></ul><ul><li><code>&lt;1.4&gt;</code> 最后，加入到 <code>factoryBeanObjectCache</code> 缓存中。</li></ul>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>创建Bean之初始化Bean</title>
    <link href="/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E5%88%9D%E5%A7%8B%E5%8C%96Bean/"/>
    <url>/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E5%88%9D%E5%A7%8B%E5%8C%96Bean/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">创建Bean之初始化Bean</font></div><p>一个 bean 经历了 <code>createBeanInstance(String beanName, RootBeanDefinition mbd, Object[] args)</code> 方法，被创建出来，然后又经过了一番属性注入，依赖处理，接下来执行最后一步就是<strong>初始化</strong>，即执行<code>initializeBean(final String beanName, final Object bean, RootBeanDefinition mbd)</code> 方法，这也是<code>doCreateBean(...)</code> 方法的中最后一步：初始化 bean 对象。</p><h4 id="initializeBean-方法"><a href="#initializeBean-方法" class="headerlink" title="initializeBean()方法"></a><code>initializeBean()</code>方法</h4><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">initializeBean</span><span class="hljs-params">(String beanName, Object bean, <span class="hljs-meta">@Nullable</span> RootBeanDefinition mbd)</span> </span>&#123;    <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123; <span class="hljs-comment">// 安全模式</span>        AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;            <span class="hljs-comment">// &lt;1&gt; 激活 Aware 方法，对特殊的 bean 处理：Aware、BeanClassLoaderAware、BeanFactoryAware</span>            invokeAwareMethods(beanName, bean);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;, getAccessControlContext());    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// &lt;1&gt; 激活 Aware 方法，对特殊的 bean 处理：Aware、BeanClassLoaderAware、BeanFactoryAware</span>        invokeAwareMethods(beanName, bean);    &#125;    <span class="hljs-comment">// &lt;2&gt; 后置处理器，before</span>    Object wrappedBean = bean;    <span class="hljs-keyword">if</span> (mbd == <span class="hljs-keyword">null</span> || !mbd.isSynthetic()) &#123;        wrappedBean = applyBeanPostProcessorsBeforeInitialization(wrappedBean, beanName);    &#125;    <span class="hljs-comment">// &lt;3&gt; 激活用户自定义的 init 方法</span>    <span class="hljs-keyword">try</span> &#123;        invokeInitMethods(beanName, wrappedBean, mbd);    &#125;    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(            (mbd != <span class="hljs-keyword">null</span> ? mbd.getResourceDescription() : <span class="hljs-keyword">null</span>),            beanName, <span class="hljs-string">&quot;Invocation of init method failed&quot;</span>, ex);    &#125;    <span class="hljs-comment">// &lt;2&gt; 后置处理器，after</span>    <span class="hljs-keyword">if</span> (mbd == <span class="hljs-keyword">null</span> || !mbd.isSynthetic()) &#123;        wrappedBean = applyBeanPostProcessorsAfterInitialization(wrappedBean, beanName);    &#125;    <span class="hljs-keyword">return</span> wrappedBean;&#125;</code></pre><p>初始化 bean 的方法其实就是三个步骤的处理，而这三个步骤主要还是根据<strong>用户设定</strong>的来进行初始化，这三个过程为：</p><ul><li><code>&lt;1&gt;</code> 激活 Aware 方法。</li><li><code>&lt;3&gt;</code> 后置处理器的应用。</li><li><code>&lt;2&gt;</code> 激活自定义的 init 方法。</li></ul><h5 id="激活-Aware-方法"><a href="#激活-Aware-方法" class="headerlink" title="激活 Aware 方法"></a>激活 Aware 方法</h5><p>Spring 提供了诸多 Aware 接口，用于辅助 Spring Bean 以编程的方式调用 Spring 容器，通过实现这些接口，可以增强 Spring Bean 的功能。</p><p>Spring 提供了如下系列的 Aware 接口：</p><ul><li>LoadTimeWeaverAware：加载Spring Bean时织入第三方模块，如AspectJ</li><li>BeanClassLoaderAware：加载Spring Bean的类加载器</li><li>BootstrapContextAware：资源适配器BootstrapContext，如JCA,CCI</li><li>ResourceLoaderAware：底层访问资源的加载器</li><li>BeanFactoryAware：声明BeanFactory</li><li>PortletConfigAware：PortletConfig</li><li>PortletContextAware：PortletContext</li><li>ServletConfigAware：ServletConfig</li><li>ServletContextAware：ServletContext</li><li>MessageSourceAware：国际化</li><li>ApplicationEventPublisherAware：应用事件</li><li>NotificationPublisherAware：JMX通知</li><li>BeanNameAware：声明Spring Bean的名字</li></ul><p><code>invokeAwareMethods()</code>方法：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-comment">//主要是处理 BeanNameAware、BeanClassLoaderAware、BeanFactoryAware 。</span><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">invokeAwareMethods</span><span class="hljs-params">(String beanName, Object bean)</span> </span>&#123;    <span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> Aware) &#123;        <span class="hljs-comment">// BeanNameAware</span>        <span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> BeanNameAware) &#123;            ((BeanNameAware) bean).setBeanName(beanName);        &#125;        <span class="hljs-comment">// BeanClassLoaderAware</span>        <span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> BeanClassLoaderAware) &#123;            ClassLoader bcl = getBeanClassLoader();            <span class="hljs-keyword">if</span> (bcl != <span class="hljs-keyword">null</span>) &#123;                ((BeanClassLoaderAware) bean).setBeanClassLoader(bcl);            &#125;        &#125;        <span class="hljs-comment">// BeanFactoryAware</span>        <span class="hljs-keyword">if</span> (bean <span class="hljs-keyword">instanceof</span> BeanFactoryAware) &#123;            ((BeanFactoryAware) bean).setBeanFactory(AbstractAutowireCapableBeanFactory.<span class="hljs-keyword">this</span>);        &#125;    &#125;&#125;</code></pre><h5 id="后置处理器的应用"><a href="#后置处理器的应用" class="headerlink" title="后置处理器的应用"></a>后置处理器的应用</h5><p>BeanPostProcessor 在前面介绍 bean 加载的过程已多次遇到。</p><p>BeanPostProcessor 的作用是：如果我们想要在 Spring 容器完成 Bean 的实例化，配置和其他的初始化后添加一些自己的逻辑处理，那么请使用该接口，这个接口给与了用户充足的权限去更改或者扩展 Spring，是我们对 Spring 进行扩展和增强处理一个必不可少的接口。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">applyBeanPostProcessorsBeforeInitialization</span><span class="hljs-params">(Object existingBean, String beanName)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> BeansException </span>&#123;    Object result = existingBean;    <span class="hljs-comment">// 遍历 BeanPostProcessor 数组</span>    <span class="hljs-keyword">for</span> (BeanPostProcessor processor : getBeanPostProcessors()) &#123;        <span class="hljs-comment">// 处理</span>        Object current = processor.postProcessBeforeInitialization(result, beanName);        <span class="hljs-comment">// 返回空，则返回 result</span>        <span class="hljs-keyword">if</span> (current == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> result;        &#125;        <span class="hljs-comment">// 修改 result</span>        result = current;    &#125;    <span class="hljs-keyword">return</span> result;&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">applyBeanPostProcessorsAfterInitialization</span><span class="hljs-params">(Object existingBean, String beanName)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> BeansException </span>&#123;    Object result = existingBean;    <span class="hljs-comment">// 遍历 BeanPostProcessor</span>    <span class="hljs-keyword">for</span> (BeanPostProcessor processor : getBeanPostProcessors()) &#123;        <span class="hljs-comment">// 处理</span>        Object current = processor.postProcessAfterInitialization(result, beanName);        <span class="hljs-comment">// 返回空，则返回 result</span>        <span class="hljs-keyword">if</span> (current == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> result;        &#125;        result = current;    &#125;    <span class="hljs-keyword">return</span> result;&#125;</code></pre><p>其实，逻辑就是通过 <code>getBeanPostProcessors()</code> 方法，获取定义的 BeanPostProcessor ，然后分别调用其 <code>postProcessBeforeInitialization(...)</code>、<code>postProcessAfterInitialization(...)</code> 方法，进行<strong>自定义</strong>的业务处理。</p><h5 id="激活自定义的-init-方法"><a href="#激活自定义的-init-方法" class="headerlink" title="激活自定义的 init 方法"></a>激活自定义的 init 方法</h5><p>spring配置文件中的<code>&lt;bean&gt;</code>标签，有一个属性<code>init-method</code>，该属性的方法值就是通过以下方法执行的：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">invokeInitMethods</span><span class="hljs-params">(String beanName, Object bean, <span class="hljs-meta">@Nullable</span> RootBeanDefinition mbd)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> Throwable </span>&#123;    <span class="hljs-comment">// 首先会检查是否是 InitializingBean ，如果是的话需要调用 afterPropertiesSet()</span>    <span class="hljs-keyword">boolean</span> isInitializingBean = (bean <span class="hljs-keyword">instanceof</span> InitializingBean);    <span class="hljs-keyword">if</span> (isInitializingBean &amp;&amp; (mbd == <span class="hljs-keyword">null</span> || !mbd.isExternallyManagedInitMethod(<span class="hljs-string">&quot;afterPropertiesSet&quot;</span>))) &#123;        <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;            logger.trace(<span class="hljs-string">&quot;Invoking afterPropertiesSet() on bean with name &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);        &#125;        <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123;<span class="hljs-comment">// 安全模式</span>            <span class="hljs-keyword">try</span> &#123;                AccessController.doPrivileged((PrivilegedExceptionAction&lt;Object&gt;) () -&gt; &#123;                    <span class="hljs-comment">// &lt;1&gt; 属性初始化的处理</span>                    ((InitializingBean) bean).afterPropertiesSet();                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;                &#125;, getAccessControlContext());            &#125;            <span class="hljs-keyword">catch</span> (PrivilegedActionException pae) &#123;                <span class="hljs-keyword">throw</span> pae.getException();            &#125;        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// &lt;1&gt; 属性初始化的处理</span>            ((InitializingBean) bean).afterPropertiesSet();        &#125;    &#125;    <span class="hljs-keyword">if</span> (mbd != <span class="hljs-keyword">null</span> &amp;&amp; bean.getClass() != NullBean.class) &#123;        String initMethodName = mbd.getInitMethodName();        <span class="hljs-keyword">if</span> (StringUtils.hasLength(initMethodName) &amp;&amp;            !(isInitializingBean &amp;&amp; <span class="hljs-string">&quot;afterPropertiesSet&quot;</span>.equals(initMethodName)) &amp;&amp;            !mbd.isExternallyManagedInitMethod(initMethodName)) &#123;            <span class="hljs-comment">// &lt;2&gt; 激活用户自定义的初始化方法</span>            invokeCustomInitMethod(beanName, bean, mbd);        &#125;    &#125;&#125;</code></pre><ul><li>首先，检查是否为 InitializingBean 。如果<strong>是</strong>的话，需要执行 <code>afterPropertiesSet()</code> 方法，因为我们除了可以使用 <code>init-method</code> 来自定初始化方法外，还可以实现 InitializingBean 接口。接口仅有一个 <code>afterPropertiesSet()</code> 方法。</li><li>两者的执行先后顺序是先 <code>&lt;1&gt;</code> 的 <code>afterPropertiesSet()</code> 方法，后 <code>&lt;2&gt;</code> 的 <code>init-method</code> 对应的方法。</li></ul>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>创建Bean之循环依赖</title>
    <link href="/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E5%BE%AA%E7%8E%AF%E4%BE%9D%E8%B5%96/"/>
    <url>/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E5%BE%AA%E7%8E%AF%E4%BE%9D%E8%B5%96/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">创建Bean之循环依赖</font></div><p><code>doCreateBean()</code>的第三步：<strong>循环依赖处理</strong>，其他步骤已经在前边介绍了。</p><p>循环依赖并不仅仅只是在 <code>doCreateBean(...)</code> 方法中处理，而是在整个加载 bean 的过程中都有涉及。所以，本文内容并不仅仅只局限于 <code>doCreateBean(...)</code> 方法，而是从整个 Bean 的加载过程进行分析。</p><h4 id="什么是循环依赖"><a href="#什么是循环依赖" class="headerlink" title="什么是循环依赖"></a>什么是循环依赖</h4><p>循环依赖，其实就是循环引用，就是两个或者两个以上的 bean 互相引用对方，最终形成一个闭环，如 A 依赖 B，B 依赖 C，C 依赖 A。</p><p>其实就是一个<strong>死循环</strong>的过程，在初始化 A 的时候发现引用了 B，这时就会去初始化 B，然后又发现 B 引用 C，跑去初始化 C，初始化 C 的时候发现引用了 A，则又会去初始化 A，依次循环永不退出，除非有<strong>终结条件</strong>。</p><p>循环依赖的情况有两种：</p><ul><li>构造器的循环依赖。</li><li>field属性的循环依赖。</li></ul><p><strong>注</strong>：对于构造器的循环依赖，Spring 是无法解决的，只能抛出 <code>BeanCurrentlyInCreationException</code> 异常表示循环依赖。</p><p>在《开启Bean的加载》中已说到：Spring 只解决 scope 为 singleton 的循环依赖。对于scope 为 prototype 的 bean ，Spring 无法解决，直接抛出 BeanCurrentlyInCreationException 异常。所以本文说的解决循环依赖就是解决scope为singleton的循环依赖。</p><h4 id="解决循环依赖"><a href="#解决循环依赖" class="headerlink" title="解决循环依赖"></a>解决循环依赖</h4><h5 id="getSingleton-方法"><a href="#getSingleton-方法" class="headerlink" title="getSingleton()方法"></a><code>getSingleton()</code>方法</h5><p>从加载bean的最初始方法开始：<code>AbstractBeanFactory.getBean()</code>—&gt;<code>AbstractBeanFactory.doGetBean()</code>.</p><p>在 <code>doGetBean(...)</code> 方法中，首先会根据 <code>beanName</code> 从单例 <code>bean 缓存</code>中获取，<strong>如果不为空则直接返回</strong>。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span>Object sharedInstance = getSingleton(beanName);</code></pre><p>调用 <code>getSingleton(String beanName, boolean allowEarlyReference)</code> 方法，从单例缓存中获取。</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">getSingleton</span><span class="hljs-params">(String beanName, <span class="hljs-keyword">boolean</span> allowEarlyReference)</span> </span>&#123;    <span class="hljs-comment">// 从单例缓冲中加载 bean，//先从一级缓存拿</span>    <span class="hljs-comment">//singletonObjects 就是Spring内部用来存放单例Bean的对象池,key为beanName，value为Bean</span>    Object singletonObject = <span class="hljs-keyword">this</span>.singletonObjects.get(beanName);    <span class="hljs-comment">// 缓存中的 bean 为空，且当前 bean 正在创建，属性还没有DI（依赖注入）</span>    <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span> &amp;&amp; isSingletonCurrentlyInCreation(beanName)) &#123;        <span class="hljs-comment">//从二级缓存中拿</span>        <span class="hljs-comment">// earlySingletonObjects 是早期单例Bean的缓存池,此时Bean已经被创建(newInstance),但是还没有完成初始化</span>        singletonObject = <span class="hljs-keyword">this</span>.earlySingletonObjects.get(beanName);        <span class="hljs-comment">//如果还拿不到，并且允许bean提前引用（解决循环依赖）</span>        <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span> &amp;&amp; allowEarlyReference) &#123;            <span class="hljs-comment">// 加锁</span>            <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.singletonObjects) &#123;                <span class="hljs-comment">// Consistent creation of early reference within full singleton lock</span>                singletonObject = <span class="hljs-keyword">this</span>.singletonObjects.get(beanName);                <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span>) &#123;                    singletonObject = <span class="hljs-keyword">this</span>.earlySingletonObjects.get(beanName);                    <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span>) &#123;                        <span class="hljs-comment">//从三级缓存中拿到对象工厂</span>                        <span class="hljs-comment">//singletonFactories 单例工厂的缓存,key为beanName,value 为ObjectFactory</span>                        ObjectFactory&lt;?&gt; singletonFactory = <span class="hljs-keyword">this</span>.singletonFactories.get(beanName);                        <span class="hljs-keyword">if</span> (singletonFactory != <span class="hljs-keyword">null</span>) &#123;                            <span class="hljs-comment">//从工厂中拿到对象</span>                            singletonObject = singletonFactory.getObject();                            <span class="hljs-comment">//升级到二级缓存</span>                            <span class="hljs-keyword">this</span>.earlySingletonObjects.put(beanName, singletonObject);                            <span class="hljs-comment">// 从 singletonFactories 中移除对应的 ObjectFactory</span>                            <span class="hljs-keyword">this</span>.singletonFactories.remove(beanName);                        &#125;                    &#125;                &#125;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> singletonObject;&#125;</code></pre><p>这个方法主要是从三个缓存中获取，分别是：<code>singletonObjects</code>、<code>earlySingletonObjects</code>、<code>singletonFactories</code> 。</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span>        <span class="hljs-comment">/**</span><span class="hljs-comment"> * 一级缓存，存放的是单例 bean 的映射。</span><span class="hljs-comment"> * 注意，这里的 bean 是已经创建完成的。</span><span class="hljs-comment"> * 对应关系为 bean name --&gt; bean instance</span><span class="hljs-comment"> */</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, Object&gt; singletonObjects = <span class="hljs-keyword">new</span> ConcurrentHashMap&lt;&gt;(<span class="hljs-number">256</span>);    <span class="hljs-comment">/**</span><span class="hljs-comment"> * 二级缓存，存放的是未初始化完的 bean，对应关系也是 bean name --&gt; bean instance。</span><span class="hljs-comment"> * 它与 &#123;<span class="hljs-doctag">@link</span> #singletonObjects&#125; 区别在于， 它自己存放的 bean 不一定是完整。</span><span class="hljs-comment"> * 这个 Map 也是【循环依赖】的关键所在。</span><span class="hljs-comment"> */</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, Object&gt; earlySingletonObjects = <span class="hljs-keyword">new</span> HashMap&lt;&gt;(<span class="hljs-number">16</span>);    <span class="hljs-comment">/**</span><span class="hljs-comment"> * 三级缓存，存放的是 ObjectFactory，可以理解为创建未初始化完的 bean 的 factory ，最终添加到二级缓存 &#123;<span class="hljs-doctag">@link</span> earlySingletonObjects&#125; 中</span><span class="hljs-comment"> * 对应关系是 bean name --&gt; ObjectFactory</span><span class="hljs-comment"> * 这个 Map 也是【循环依赖】的关键所在。</span><span class="hljs-comment"> */</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, ObjectFactory&lt;?&gt;&gt; singletonFactories = <span class="hljs-keyword">new</span> HashMap&lt;&gt;(<span class="hljs-number">16</span>);</code></pre><p>以上三个缓存变量是解决<code>singleton bean</code>的关键，成为<strong>三级缓存</strong>：</p><p>一级缓存为<code>singletonObjects</code>，二级缓存为<code>earlySingletonObjects</code>，三级缓存为<code>singletonFactories</code>。</p><p>在<code>getSingleton()</code>方法中其中涉及了一个<code>isSingletonCurrentlyInCreation(String beanName)</code>方法和<code>allowEarlyReference</code>变量：</p><ul><li><p><code>isSingletonCurrentlyInCreation(String beanName)</code>方法：判断当前 singleton bean 是否处于创建中。bean 处于创建中，也就是说 bean 在初始化但是没有完成初始化，有一个这样的过程其实和 Spring 解决 bean 循环依赖的理念相辅相成。<strong>因为 Spring 解决 singleton bean 的核心就在于提前曝光 bean</strong> 。</p></li><li><p><code>allowEarlyReference</code> 变量：从字面意思上面理解就是允许提前拿到引用。其实真正的意思是，是否允许从 <code>singletonFactories</code> 缓存中通过 <code>getObject()</code> 方法，拿到对象。为什么会有这样一个字段呢？<strong>原因就在于 <code>singletonFactories</code> 才是 Spring 解决 singleton bean 的诀窍所在</strong>。</p></li></ul><p><code>getSingleton(String beanName, boolean allowEarlyReference)</code>方法，过程如下：</p><ul><li><p>首先，从一级缓存 <code>singletonObjects</code> 获取。</p></li><li><p>如果没有，且当前指定的 <code>beanName</code> 正在创建，就再从二级缓存 <code>earlySingletonObjects</code> 中获取。</p></li><li><p>如果还是没有获取到，并且允许 <code>singletonFactories</code> 通过 <code>getObject()</code> 获取，则从三级缓存 <code>singletonFactories</code> 获取。如果获取到，则通过其 <code>getObject()</code> 方法，获取对象，并将其加入到二级缓存 <code>earlySingletonObjects</code> 中，并从三级缓存 <code>singletonFactories</code> 删除：</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span>singletonObject = singletonFactory.getObject();<span class="hljs-keyword">this</span>.earlySingletonObjects.put(beanName, singletonObject);<span class="hljs-keyword">this</span>.singletonFactories.remove(beanName);</code></pre><p>这样，就从三级缓存<strong>升级</strong>到二级缓存了。</p><p>所以，二级缓存存在的<strong>意义</strong>：就是缓存三级缓存中的 ObjectFactory 的 <code>getObject()</code> 方法的执行结果，提早曝光的<strong>单例</strong> Bean 对象。</p></li></ul><h5 id="addSingletonFactory-方法"><a href="#addSingletonFactory-方法" class="headerlink" title="addSingletonFactory()方法"></a><code>addSingletonFactory()</code>方法</h5><p>上面是从缓存中获取，但是缓存中的数据从哪里添加进来的呢？一直往下跟会发现在 AbstractAutowireCapableBeanFactory 的 <code>doCreateBean(final String beanName, final RootBeanDefinition mbd, final Object[] args)</code> 方法中：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-keyword">boolean</span> earlySingletonExposure = (mbd.isSingleton() &amp;&amp; <span class="hljs-comment">// 单例模式</span>        <span class="hljs-keyword">this</span>.allowCircularReferences &amp;&amp; <span class="hljs-comment">// 运行循环依赖</span>        isSingletonCurrentlyInCreation(beanName));<span class="hljs-comment">// 当前单例 bean 是否正在被创建</span><span class="hljs-keyword">if</span> (earlySingletonExposure) &#123;    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;        logger.trace(<span class="hljs-string">&quot;Eagerly caching bean &#x27;&quot;</span> + beanName +                     <span class="hljs-string">&quot;&#x27; to allow for resolving potential circular references&quot;</span>);    &#125;    <span class="hljs-comment">// 提前将创建的 bean 实例加入到 singletonFactories 中</span>    <span class="hljs-comment">// 这里是为了后期避免循环依赖</span>    addSingletonFactory(beanName, () -&gt; getEarlyBeanReference(beanName, mbd, bean));&#125;</code></pre><p>当一个Bean满足<code>单例的</code>、<code>运行提前暴露bean</code>、<code>当前bean正在创建</code>三个条件，就将它添加到缓存中。</p><p><code>addSingletonFactory()</code>如下：</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">addSingletonFactory</span><span class="hljs-params">(String beanName, ObjectFactory&lt;?&gt; singletonFactory)</span> </span>&#123;Assert.notNull(singletonFactory, <span class="hljs-string">&quot;Singleton factory must not be null&quot;</span>);<span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.singletonObjects) &#123;<span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.singletonObjects.containsKey(beanName)) &#123;<span class="hljs-keyword">this</span>.singletonFactories.put(beanName, singletonFactory);<span class="hljs-keyword">this</span>.earlySingletonObjects.remove(beanName);<span class="hljs-keyword">this</span>.registeredSingletons.add(beanName);&#125;&#125;&#125;</code></pre><ul><li>从这段代码我们可以看出，<code>singletonFactories</code> 这个三级缓存才是解决 Spring Bean 循环依赖的诀窍所在。同时这段代码发生在 <code>createBeanInstance(...)</code> 方法之后，也就是说这个 bean 其实已经被创建出来了，<strong>但是它还不是很完美（没有进行属性填充和初始化），而这对于其他依赖它的对象而言已经足够了（可以根据对象引用定位到堆中对象），能够被认出来了</strong>。所以 Spring 在这个时候，选择将该对象提前曝光出来让大家认识认识。</li></ul><p>另外，<code>addSingletonFactory()</code>中的回调函数<code>getEarlyBeanReference(String beanName, RootBeanDefinition mbd, Object bean)</code> 方法也<strong>非常重要</strong>，这里会创建早期初始化 Bean 可能存在的 AOP 代理等等。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-comment">/**</span><span class="hljs-comment"> * 对创建的早期半成品（未初始化）的 Bean 处理引用</span><span class="hljs-comment"> * 例如说，AOP 就是在这里动态织入，创建其代理 Bean 返回</span><span class="hljs-comment"> */</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">getEarlyBeanReference</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, Object bean)</span> </span>&#123;Object exposedObject = bean;<span class="hljs-keyword">if</span> (!mbd.isSynthetic() &amp;&amp; hasInstantiationAwareBeanPostProcessors()) &#123;<span class="hljs-keyword">for</span> (BeanPostProcessor bp : getBeanPostProcessors()) &#123;<span class="hljs-keyword">if</span> (bp <span class="hljs-keyword">instanceof</span> SmartInstantiationAwareBeanPostProcessor) &#123;SmartInstantiationAwareBeanPostProcessor ibp = (SmartInstantiationAwareBeanPostProcessor) bp;exposedObject = ibp.getEarlyBeanReference(exposedObject, beanName);&#125;&#125;&#125;<span class="hljs-keyword">return</span> exposedObject;&#125;</code></pre><ul><li>这也是为什么 Spring 需要额外增加 <code>singletonFactories</code> 三级缓存的原因，解决 Spring 循环依赖情况下的 Bean 存在动态代理等情况，不然循环注入到别人的 Bean 就是原始的，而不是经过动态代理的！</li></ul><h5 id="addSingleton-方法"><a href="#addSingleton-方法" class="headerlink" title="addSingleton()方法"></a><code>addSingleton()</code>方法</h5><p>二级缓存<code>earlySingletonObjects</code>、三级缓存<code>singletonFactory</code>如上设置，一级缓存如何设置呢？</p><p>在类 DefaultSingletonBeanRegistry 中，可以发现这个 <code>addSingleton(String beanName, Object singletonObject)</code> 方法：</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">addSingleton</span><span class="hljs-params">(String beanName, Object singletonObject)</span> </span>&#123;<span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.singletonObjects) &#123;<span class="hljs-keyword">this</span>.singletonObjects.put(beanName, singletonObject);<span class="hljs-keyword">this</span>.singletonFactories.remove(beanName);<span class="hljs-keyword">this</span>.earlySingletonObjects.remove(beanName);<span class="hljs-keyword">this</span>.registeredSingletons.add(beanName);&#125;&#125;</code></pre><ul><li><p>添加至一级缓存，同时从二级、三级缓存中删除。</p><p>该方法何时被引用的？之前介绍《各scope的Bean创建》中，<code>doGetBean()</code>方法中处理不同scope时，如果是singleton，则调用<code>getSingleton()</code>，和上边<code>getSingleton()</code>不同。</p><p><img src="/images/spring_IOC/getSingleton.jpeg" alt="getSingleton"></p></li></ul><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">getSingleton</span><span class="hljs-params">(String beanName, ObjectFactory&lt;?&gt; singletonFactory)</span> </span>&#123;    Assert.notNull(beanName, <span class="hljs-string">&quot;Bean name must not be null&quot;</span>);    <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.singletonObjects) &#123;        Object singletonObject = <span class="hljs-keyword">this</span>.singletonObjects.get(beanName);        <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">//....</span>            <span class="hljs-keyword">try</span> &#123;                singletonObject = singletonFactory.getObject();                newSingleton = <span class="hljs-keyword">true</span>;            &#125;            <span class="hljs-comment">//.....</span>            <span class="hljs-keyword">if</span> (newSingleton) &#123;                addSingleton(beanName, singletonObject);            &#125;        &#125;        <span class="hljs-keyword">return</span> singletonObject;    &#125;&#125;</code></pre><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>最上边循环依赖Spring解决过程如下：</p><ul><li>首先 A 完成初始化第一步并将自己提前曝光出来（通过 ObjectFactory 将自己提前曝光），在初始化的时候，发现自己依赖对象 B，此时就会去尝试 get(B)，这个时候发现 B 还没有被创建出来。</li><li>然后 B 就走创建流程，在 B 初始化的时候，同样发现自己依赖 C，C 也没有被创建出来。</li><li>这个时候 C 又开始初始化进程，但是在初始化的过程中发现自己依赖 A，于是尝试 get(A)，这个时候由于 A 已经添加至缓存中（一般都是添加至三级缓存 <code>singletonFactories</code> ），通过 ObjectFactory 提前曝光，所以可以通过 <code>ObjectFactory.getObject()</code> 方法来拿到 A 对象，C 拿到 A 对象后顺利完成初始化，然后将自己添加到一级缓存中。</li><li>回到 B ，B 也可以拿到 C 对象，完成初始化，A 可以顺利拿到 B 完成初始化。到这里整个链路就已经完成了初始化过程了。</li></ul><p><img src="/images/spring_IOC/%E5%A4%84%E7%90%86%E5%BE%AA%E5%9D%8F%E4%BE%9D%E8%B5%96.png" alt="处理循坏依赖"></p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>创建Bean之属性填充</title>
    <link href="/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E5%B1%9E%E6%80%A7%E5%A1%AB%E5%85%85/"/>
    <url>/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E5%B1%9E%E6%80%A7%E5%A1%AB%E5%85%85/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">创建Bean之属性填充</font></div><p><code>doCreateBean(...)</code> 方法，主要用于完成 bean 的创建和初始化工作，我们可以将其分为四个过程：</p><ul><li><code>createBeanInstance(String beanName, RootBeanDefinition mbd, Object[] args)</code> 方法，实例化 bean 。</li><li>循环依赖的处理。</li><li><code>populateBean(String beanName, RootBeanDefinition mbd, BeanWrapper bw)</code> 方法，进行属性填充。</li><li><code>initializeBean(final String beanName, final Object bean, RootBeanDefinition mbd)</code> 方法，初始化 Bean 。</li></ul><h4 id="populateBean-方法"><a href="#populateBean-方法" class="headerlink" title="populateBean()方法"></a><code>populateBean()</code>方法</h4><p>该函数的作用是将 BeanDefinition 中的属性值赋值给 BeanWrapper 实例对象。</p><pre><code class="hljs java"><span class="hljs-meta">@SuppressWarnings(&quot;deprecation&quot;)</span>  <span class="hljs-comment">// for postProcessPropertyValues</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">populateBean</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> BeanWrapper bw)</span> </span>&#123;    <span class="hljs-comment">// 没有实例化对象</span>    <span class="hljs-keyword">if</span> (bw == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// 有属性，则抛出 BeanCreationException 异常</span>        <span class="hljs-keyword">if</span> (mbd.hasPropertyValues()) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(                mbd.getResourceDescription(), beanName, <span class="hljs-string">&quot;Cannot apply property values to null instance&quot;</span>);        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// Skip property population phase for null instance.</span>            <span class="hljs-comment">// 没有属性，直接 return 返回</span>            <span class="hljs-keyword">return</span>;        &#125;    &#125;    <span class="hljs-comment">// &lt;1&gt; 在设置属性之前给 InstantiationAwareBeanPostProcessors 最后一次改变 bean 的机会</span>    <span class="hljs-keyword">if</span> (!mbd.isSynthetic()  <span class="hljs-comment">// bean 不是&quot;合成&quot;的，即未由应用程序本身定义</span>        &amp;&amp; hasInstantiationAwareBeanPostProcessors()) &#123;<span class="hljs-comment">// 是否持有 InstantiationAwareBeanPostProcessor</span>        <span class="hljs-comment">// 迭代所有的 BeanPostProcessors</span>        <span class="hljs-keyword">for</span> (InstantiationAwareBeanPostProcessor bp : getBeanPostProcessorCache().instantiationAware) &#123;            <span class="hljs-comment">//此处for循环内和之前版本不一样，该处使用了缓存</span>            <span class="hljs-comment">// 返回值为是否继续填充 bean</span>            <span class="hljs-comment">// postProcessAfterInstantiation：如果应该在 bean上面设置属性则返回 true，否则返回 false</span>            <span class="hljs-comment">// 一般情况下，应该是返回true 。</span>            <span class="hljs-comment">// 返回 false 的话，将会阻止在此 Bean 实例上调用任何后续的 InstantiationAwareBeanPostProcessor 实例。</span>            <span class="hljs-keyword">if</span> (!bp.postProcessAfterInstantiation(bw.getWrappedInstance(), beanName)) &#123;                <span class="hljs-keyword">return</span>;            &#125;        &#125;    &#125;    <span class="hljs-comment">// bean 的属性值</span>    PropertyValues pvs = (mbd.hasPropertyValues() ? mbd.getPropertyValues() : <span class="hljs-keyword">null</span>);    <span class="hljs-comment">// &lt;2&gt; 自动注入</span>    <span class="hljs-keyword">int</span> resolvedAutowireMode = mbd.getResolvedAutowireMode();    <span class="hljs-keyword">if</span> (resolvedAutowireMode == AUTOWIRE_BY_NAME || resolvedAutowireMode == AUTOWIRE_BY_TYPE) &#123;        <span class="hljs-comment">// 将 PrbuopertyValues 封装成 MutablePropertyValues 对象</span>        <span class="hljs-comment">// MutablePropertyValues 允许对属性进行简单的操作，并提供构造函数以支持Map的深度复制和构造。</span>        MutablePropertyValues newPvs = <span class="hljs-keyword">new</span> MutablePropertyValues(pvs);        <span class="hljs-comment">// Add property values based on autowire by name if applicable.</span>        <span class="hljs-comment">// 根据名称自动注入</span>        <span class="hljs-keyword">if</span> (resolvedAutowireMode == AUTOWIRE_BY_NAME) &#123;            autowireByName(beanName, mbd, bw, newPvs);        &#125;        <span class="hljs-comment">// Add property values based on autowire by type if applicable.</span>        <span class="hljs-comment">// 根据类型自动注入</span>        <span class="hljs-keyword">if</span> (resolvedAutowireMode == AUTOWIRE_BY_TYPE) &#123;            autowireByType(beanName, mbd, bw, newPvs);        &#125;        pvs = newPvs;    &#125;    <span class="hljs-comment">// 是否已经注册了 InstantiationAwareBeanPostProcessors</span>    <span class="hljs-keyword">boolean</span> hasInstAwareBpps = hasInstantiationAwareBeanPostProcessors();    <span class="hljs-comment">// 是否需要进行【依赖检查】</span>    <span class="hljs-keyword">boolean</span> needsDepCheck = (mbd.getDependencyCheck() != AbstractBeanDefinition.DEPENDENCY_CHECK_NONE);    <span class="hljs-comment">// &lt;3&gt; BeanPostProcessor 处理</span>    PropertyDescriptor[] filteredPds = <span class="hljs-keyword">null</span>;    <span class="hljs-keyword">if</span> (hasInstAwareBpps) &#123;        <span class="hljs-keyword">if</span> (pvs == <span class="hljs-keyword">null</span>) &#123;            pvs = mbd.getPropertyValues();        &#125;        <span class="hljs-comment">// 遍历 BeanPostProcessor 数组</span>        <span class="hljs-comment">// 此处for循环内和之前版本不一样，该处使用了缓存</span>        <span class="hljs-keyword">for</span> (InstantiationAwareBeanPostProcessor bp : getBeanPostProcessorCache().instantiationAware) &#123;            <span class="hljs-comment">// 对所有需要依赖检查的属性进行后处理</span>            PropertyValues pvsToUse = bp.postProcessProperties(pvs, bw.getWrappedInstance(), beanName);            <span class="hljs-keyword">if</span> (pvsToUse == <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">// 从 bw 对象中提取 PropertyDescriptor 结果集</span>                <span class="hljs-comment">// PropertyDescriptor：可以通过一对存取方法提取一个属性</span>                <span class="hljs-keyword">if</span> (filteredPds == <span class="hljs-keyword">null</span>) &#123;                    filteredPds = filterPropertyDescriptorsForDependencyCheck(bw, mbd.allowCaching);                &#125;                pvsToUse = bp.postProcessPropertyValues(pvs, filteredPds, bw.getWrappedInstance(), beanName);                <span class="hljs-keyword">if</span> (pvsToUse == <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-keyword">return</span>;                &#125;            &#125;            pvs = pvsToUse;        &#125;    &#125;    <span class="hljs-comment">// &lt;4&gt; 依赖检查</span>    <span class="hljs-keyword">if</span> (needsDepCheck) &#123;        <span class="hljs-keyword">if</span> (filteredPds == <span class="hljs-keyword">null</span>) &#123;            filteredPds = filterPropertyDescriptorsForDependencyCheck(bw, mbd.allowCaching);        &#125;        <span class="hljs-comment">// 依赖检查，对应 depends-on 属性</span>        checkDependencies(beanName, mbd, filteredPds, pvs);    &#125;    <span class="hljs-comment">// &lt;5&gt; 将属性应用到 bean 中</span>    <span class="hljs-keyword">if</span> (pvs != <span class="hljs-keyword">null</span>) &#123;        applyPropertyValues(beanName, mbd, bw, pvs);    &#125;&#125;</code></pre><p>处理过程如下：</p><ul><li><p><code>&lt;1&gt;</code> ，根据 <code>hasInstantiationAwareBeanPostProcessors</code> 属性来判断，是否需要在注入属性之前给 InstantiationAwareBeanPostProcessors 最后一次改变 bean 的机会。<strong>此过程可以控制 Spring 是否继续进行属性填充</strong>。</p></li><li><p>统一存入到 PropertyValues 中，PropertyValues 用于描述 bean 的属性。</p><p><code>&lt;2&gt;</code> ，根据注入类型( <code>AbstractBeanDefinition.getResolvedAutowireMode()</code> 方法的返回值 )的不同来判断：是根据名称来自动注入（autowireByName(…)），还是根据类型来自动注入（autowireByType(…)）。</p></li></ul><ul><li><code>&lt;3&gt;</code> ，进行 BeanPostProcessor 处理。</li><li><code>&lt;4&gt;</code> ，依赖检测。</li><li><code>&lt;5&gt;</code> ，将所有 PropertyValues 中的属性，填充到 BeanWrapper 中。</li></ul><h5 id="自动注入"><a href="#自动注入" class="headerlink" title="自动注入"></a>自动注入</h5><p>Spring 会根据注入类型（ <code>byName</code> / <code>byType</code> ）的不同，调用不同的方法来注入属性值。</p><pre><code class="hljs java"><span class="hljs-comment">//AbstractAutowireCapableBeanFactory</span><span class="hljs-keyword">int</span> resolvedAutowireMode = mbd.getResolvedAutowireMode();<span class="hljs-comment">// AbstractBeanDefinition.java</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> autowireMode = AUTOWIRE_NO;<span class="hljs-comment">//0</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">getResolvedAutowireMode</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.autowireMode == AUTOWIRE_AUTODETECT) &#123;<span class="hljs-comment">// 自动检测模式，获得对应的检测模式</span>        Constructor&lt;?&gt;[] constructors = getBeanClass().getConstructors();        <span class="hljs-keyword">for</span> (Constructor&lt;?&gt; constructor : constructors) &#123;            <span class="hljs-keyword">if</span> (constructor.getParameterCount() == <span class="hljs-number">0</span>) &#123;                <span class="hljs-keyword">return</span> AUTOWIRE_BY_TYPE;<span class="hljs-comment">//2</span>            &#125;        &#125;        <span class="hljs-keyword">return</span> AUTOWIRE_CONSTRUCTOR;<span class="hljs-comment">//3</span>    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.autowireMode;    &#125;&#125;</code></pre><h6 id="autowireByName-方法"><a href="#autowireByName-方法" class="headerlink" title="autowireByName()方法"></a><code>autowireByName()</code>方法</h6><p>该方法，是根据<strong>属性名称</strong>，完成自动依赖注入的。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">autowireByName</span><span class="hljs-params">(String beanName, AbstractBeanDefinition mbd, BeanWrapper bw, MutablePropertyValues pvs)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 对 Bean 对象中非简单属性</span>    <span class="hljs-comment">// 非简单属性：类型为对象类型的属性，但这里并不是将所有的对象类型都会找到，比如8个原始类型，String类型、Number类型、Date类型等会被忽略</span>    String[] propertyNames = unsatisfiedNonSimpleProperties(mbd, bw);    <span class="hljs-comment">// 遍历 propertyName 数组</span>    <span class="hljs-keyword">for</span> (String propertyName : propertyNames) &#123;        <span class="hljs-comment">// 如果容器中包含指定名称的 bean，则将该 bean 注入到 bean中</span>        <span class="hljs-keyword">if</span> (containsBean(propertyName)) &#123;            <span class="hljs-comment">// 递归初始化相关 bean</span>            Object bean = getBean(propertyName);            <span class="hljs-comment">// 为指定名称的属性赋予属性值</span>            pvs.add(propertyName, bean);            <span class="hljs-comment">// &lt;2&gt; 属性依赖注入</span>            registerDependentBean(propertyName, beanName);            <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                logger.trace(<span class="hljs-string">&quot;Added autowiring by name from bean name &#x27;&quot;</span> + beanName +                             <span class="hljs-string">&quot;&#x27; via property &#x27;&quot;</span> + propertyName + <span class="hljs-string">&quot;&#x27; to bean named &#x27;&quot;</span> + propertyName + <span class="hljs-string">&quot;&#x27;&quot;</span>);            &#125;        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                logger.trace(<span class="hljs-string">&quot;Not autowiring property &#x27;&quot;</span> + propertyName + <span class="hljs-string">&quot;&#x27; of bean &#x27;&quot;</span> + beanName +                             <span class="hljs-string">&quot;&#x27; by name: no matching bean found&quot;</span>);            &#125;        &#125;    &#125;&#125;</code></pre><ul><li><p><code>&lt;1&gt;</code> 处，获取该 bean 的非简单属性。非简单属性就是类型为对象类型的属性，但是这里并不是将所有的对象类型都都会找到，比如 8 个原始类型，String 类型 ，Number类型、Date类型、URL类型、URI类型等都会被忽略。</p><pre><code class="hljs java"><span class="hljs-keyword">protected</span> String[] unsatisfiedNonSimpleProperties(AbstractBeanDefinition mbd, BeanWrapper bw) &#123;    <span class="hljs-comment">// 创建 result 集合</span>    Set&lt;String&gt; result = <span class="hljs-keyword">new</span> TreeSet&lt;&gt;();    PropertyValues pvs = mbd.getPropertyValues();    <span class="hljs-comment">// 遍历 PropertyDescriptor 数组</span>    PropertyDescriptor[] pds = bw.getPropertyDescriptors();    <span class="hljs-keyword">for</span> (PropertyDescriptor pd : pds) &#123;        <span class="hljs-keyword">if</span> (pd.getWriteMethod() != <span class="hljs-keyword">null</span> <span class="hljs-comment">// 有可写方法</span>            &amp;&amp; !isExcludedFromDependencyCheck(pd) <span class="hljs-comment">// 依赖检测中没有被忽略</span>            &amp;&amp; !pvs.contains(pd.getName()) &amp;&amp;<span class="hljs-comment">// pvs 不包含该属性名</span>            !BeanUtils.isSimpleProperty(pd.getPropertyType())) &#123; <span class="hljs-comment">// 不是简单属性类型</span>            result.add(pd.getName());<span class="hljs-comment">// 添加到 result 中</span>        &#125;    &#125;    <span class="hljs-keyword">return</span> StringUtils.toStringArray(result);&#125;</code></pre><ul><li>过滤<strong>条件</strong>为：有可写方法、依赖检测中没有被忽略、不是简单属性类型。</li><li>过滤<strong>结果</strong>为：其实这里获取的就是需要依赖注入的属性。</li></ul></li><li><p>&lt;2&gt;处，获取需要依赖注入的属性后，通过迭代、递归的方式初始化相关的 bean ，然后调用 <code>registerDependentBean(String beanName, String dependentBeanName)</code> 方法，完成注册依赖。该方法之前《parentBeanFactory和依赖处理》已经分析过了。</p></li></ul><h6 id="autowireByType-方法"><a href="#autowireByType-方法" class="headerlink" title="autowireByType()方法"></a><code>autowireByType()</code>方法</h6><p>该方法，是根据<strong>属性类型</strong>，完成自动依赖注入的。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">autowireByType</span><span class="hljs-params">(String beanName, AbstractBeanDefinition mbd, BeanWrapper bw, MutablePropertyValues pvs)</span> </span>&#123;    <span class="hljs-comment">// 获取 TypeConverter 实例</span>    <span class="hljs-comment">// 使用自定义的 TypeConverter，用于取代默认的 PropertyEditor 机制</span>    TypeConverter converter = getCustomTypeConverter();    <span class="hljs-keyword">if</span> (converter == <span class="hljs-keyword">null</span>) &#123;        converter = bw;    &#125;    Set&lt;String&gt; autowiredBeanNames = <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;(<span class="hljs-number">4</span>);    <span class="hljs-comment">// 获取非简单属性</span>    String[] propertyNames = unsatisfiedNonSimpleProperties(mbd, bw);    <span class="hljs-comment">// 遍历 PROPERTYNAME 数组</span>    <span class="hljs-keyword">for</span> (String propertyName : propertyNames) &#123;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">// 获取 PropertyDescriptor 实例</span>            PropertyDescriptor pd = bw.getPropertyDescriptor(propertyName);            <span class="hljs-comment">// 不要尝试按类型</span>            <span class="hljs-keyword">if</span> (Object.class != pd.getPropertyType()) &#123;                <span class="hljs-comment">// 探测指定属性的 set 方法</span>                MethodParameter methodParam = BeanUtils.getWriteMethodParameter(pd);                <span class="hljs-keyword">boolean</span> eager = !(bw.getWrappedInstance() <span class="hljs-keyword">instanceof</span> PriorityOrdered);                DependencyDescriptor desc = <span class="hljs-keyword">new</span> AutowireByTypeDependencyDescriptor(methodParam, eager);                <span class="hljs-comment">// &lt;1&gt;解析指定 beanName 的属性所匹配的值，并把解析到的属性名称存储在 autowiredBeanNames 中</span>                <span class="hljs-comment">// 当属性存在多个封装 bean 时将会找到所有匹配的 bean 并将其注入</span>                Object autowiredArgument = resolveDependency(desc, beanName, autowiredBeanNames, converter);                <span class="hljs-keyword">if</span> (autowiredArgument != <span class="hljs-keyword">null</span>) &#123;                    pvs.add(propertyName, autowiredArgument);                &#125;                <span class="hljs-comment">// 遍历 autowiredBeanName 数组</span>                <span class="hljs-keyword">for</span> (String autowiredBeanName : autowiredBeanNames) &#123;                    <span class="hljs-comment">// 属性依赖注入</span>                    registerDependentBean(autowiredBeanName, beanName);                    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                        logger.trace(<span class="hljs-string">&quot;Autowiring by type from bean name &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; via property &#x27;&quot;</span> +                                     propertyName + <span class="hljs-string">&quot;&#x27; to bean named &#x27;&quot;</span> + autowiredBeanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);                    &#125;                &#125;                <span class="hljs-comment">// 清空 autowiredBeanName 数组</span>                autowiredBeanNames.clear();            &#125;        &#125;        <span class="hljs-keyword">catch</span> (BeansException ex) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> UnsatisfiedDependencyException(mbd.getResourceDescription(), beanName, propertyName, ex);        &#125;    &#125;&#125;</code></pre><p>其实主要过程和根据名称自动注入<strong>差不多</strong>，都是找到需要依赖注入的属性，然后通过迭代的方式寻找所匹配的 bean，最后调用 <code>registerDependentBean(...)</code> 方法，来注册依赖。</p><p>**resolveDependency()**方法。</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultListableBeanFactory.java</span><span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">resolveDependency</span><span class="hljs-params">(DependencyDescriptor descriptor, <span class="hljs-meta">@Nullable</span> String requestingBeanName,<span class="hljs-meta">@Nullable</span> Set&lt;String&gt; autowiredBeanNames, <span class="hljs-meta">@Nullable</span> TypeConverter typeConverter)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-comment">// 初始化参数名称发现器，该方法并不会在这个时候尝试检索参数名称</span>    <span class="hljs-comment">// getParameterNameDiscoverer 返回 parameterNameDiscoverer 实例，parameterNameDiscoverer 方法参数名称的解析器</span>    descriptor.initParameterNameDiscovery(getParameterNameDiscoverer());    <span class="hljs-comment">// 依赖类型为 Optional 类型</span>    <span class="hljs-keyword">if</span> (Optional.class == descriptor.getDependencyType()) &#123;        <span class="hljs-keyword">return</span> createOptionalDependency(descriptor, requestingBeanName);    &#125;    <span class="hljs-comment">// 依赖类型为ObjectFactory、ObjectProvider</span>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (ObjectFactory.class == descriptor.getDependencyType() ||             ObjectProvider.class == descriptor.getDependencyType()) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> DependencyObjectProvider(descriptor, requestingBeanName);    &#125;    <span class="hljs-comment">// javaxInjectProviderClass 类注入的特殊处理</span>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (javaxInjectProviderClass == descriptor.getDependencyType()) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> Jsr330Factory().createDependencyProvider(descriptor, requestingBeanName);    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 为实际依赖关系目标的延迟解析构建代理</span>        <span class="hljs-comment">// 默认实现返回 null</span>        Object result = getAutowireCandidateResolver().getLazyResolutionProxyIfNecessary(            descriptor, requestingBeanName);        <span class="hljs-keyword">if</span> (result == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">// 通用处理逻辑</span>            result = doResolveDependency(descriptor, requestingBeanName, autowiredBeanNames, typeConverter);        &#125;        <span class="hljs-keyword">return</span> result;    &#125;&#125;</code></pre><p><strong>通用处理逻辑</strong><code>doResolveDependency()</code>：</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultListableBeanFactory.java</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">doResolveDependency</span><span class="hljs-params">(DependencyDescriptor descriptor, <span class="hljs-meta">@Nullable</span> String beanName,<span class="hljs-meta">@Nullable</span> Set&lt;String&gt; autowiredBeanNames, <span class="hljs-meta">@Nullable</span> TypeConverter typeConverter)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-comment">// 注入点</span>    InjectionPoint previousInjectionPoint = ConstructorResolver.setCurrentInjectionPoint(descriptor);    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// 针对给定的工厂给定一个快捷实现的方式，例如考虑一些预先解析的信息</span>        <span class="hljs-comment">// 在进入所有bean的常规类型匹配算法之前，解析算法将首先尝试通过此方法解析快捷方式。</span>        <span class="hljs-comment">// 子类可以覆盖此方法</span>        Object shortcut = descriptor.resolveShortcut(<span class="hljs-keyword">this</span>);        <span class="hljs-keyword">if</span> (shortcut != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">// 返回快捷的解析信息</span>            <span class="hljs-keyword">return</span> shortcut;        &#125;        <span class="hljs-comment">// 依赖的类型</span>        Class&lt;?&gt; type = descriptor.getDependencyType();        <span class="hljs-comment">// 支持 Spring 的注解 @value</span>        Object value = getAutowireCandidateResolver().getSuggestedValue(descriptor);        <span class="hljs-keyword">if</span> (value != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">if</span> (value <span class="hljs-keyword">instanceof</span> String) &#123;                String strVal = resolveEmbeddedValue((String) value);                BeanDefinition bd = (beanName != <span class="hljs-keyword">null</span> &amp;&amp; containsBean(beanName) ?                                     getMergedBeanDefinition(beanName) : <span class="hljs-keyword">null</span>);                value = evaluateBeanDefinitionString(strVal, bd);            &#125;            TypeConverter converter = (typeConverter != <span class="hljs-keyword">null</span> ? typeConverter : getTypeConverter());            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-keyword">return</span> converter.convertIfNecessary(value, type, descriptor.getTypeDescriptor());            &#125;            <span class="hljs-keyword">catch</span> (UnsupportedOperationException ex) &#123;                <span class="hljs-comment">// A custom TypeConverter which does not support TypeDescriptor resolution...</span>                <span class="hljs-keyword">return</span> (descriptor.getField() != <span class="hljs-keyword">null</span> ?                        converter.convertIfNecessary(value, type, descriptor.getField()) :                        converter.convertIfNecessary(value, type, descriptor.getMethodParameter()));            &#125;        &#125;        <span class="hljs-comment">// 解析复合 bean，其实就是对 bean 的属性进行解析</span>        <span class="hljs-comment">// 包括：数组、Collection 、Map 类型</span>        Object multipleBeans = resolveMultipleBeans(descriptor, beanName, autowiredBeanNames, typeConverter);        <span class="hljs-keyword">if</span> (multipleBeans != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> multipleBeans;        &#125;        <span class="hljs-comment">// 查找与类型相匹配的 bean</span>        <span class="hljs-comment">// 返回值构成为：key = 匹配的 beanName，value = beanName 对应的实例化 bean</span>        Map&lt;String, Object&gt; matchingBeans = findAutowireCandidates(beanName, type, descriptor);        <span class="hljs-comment">// 没有找到，检验 @autowire  的 require 是否为 true</span>        <span class="hljs-keyword">if</span> (matchingBeans.isEmpty()) &#123;            <span class="hljs-comment">// 如果 @autowire 的 require 属性为 true ，但是没有找到相应的匹配项，则抛出异常</span>            <span class="hljs-keyword">if</span> (isRequired(descriptor)) &#123;                raiseNoMatchingBeanFound(type, descriptor.getResolvableType(), descriptor);            &#125;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;        String autowiredBeanName;        Object instanceCandidate;        <span class="hljs-keyword">if</span> (matchingBeans.size() &gt; <span class="hljs-number">1</span>) &#123;            <span class="hljs-comment">// 确认给定 bean autowire 的候选者</span>            <span class="hljs-comment">// 按照 @Primary 和 @Priority 的顺序</span>            autowiredBeanName = determineAutowireCandidate(matchingBeans, descriptor);            <span class="hljs-keyword">if</span> (autowiredBeanName == <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">if</span> (isRequired(descriptor) || !indicatesMultipleBeans(type)) &#123;                    <span class="hljs-comment">// 唯一性处理</span>                    <span class="hljs-keyword">return</span> descriptor.resolveNotUnique(descriptor.getResolvableType(), matchingBeans);                &#125;                <span class="hljs-keyword">else</span> &#123;                    <span class="hljs-comment">// In case of an optional Collection/Map, silently ignore a non-unique case:</span>                    <span class="hljs-comment">// possibly it was meant to be an empty collection of multiple regular beans</span>                    <span class="hljs-comment">// (before 4.3 in particular when we didn&#x27;t even look for collection beans).</span>                    <span class="hljs-comment">// 在可选的Collection / Map的情况下，默默地忽略一个非唯一的情况：可能它是一个多个常规bean的空集合</span>                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;                &#125;            &#125;            instanceCandidate = matchingBeans.get(autowiredBeanName);        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// We have exactly one match.</span>            Map.Entry&lt;String, Object&gt; entry = matchingBeans.entrySet().iterator().next();            autowiredBeanName = entry.getKey();            instanceCandidate = entry.getValue();        &#125;        <span class="hljs-keyword">if</span> (autowiredBeanNames != <span class="hljs-keyword">null</span>) &#123;            autowiredBeanNames.add(autowiredBeanName);        &#125;        <span class="hljs-keyword">if</span> (instanceCandidate <span class="hljs-keyword">instanceof</span> Class) &#123;            instanceCandidate = descriptor.resolveCandidate(autowiredBeanName, type, <span class="hljs-keyword">this</span>);        &#125;        Object result = instanceCandidate;        <span class="hljs-keyword">if</span> (result <span class="hljs-keyword">instanceof</span> NullBean) &#123;            <span class="hljs-keyword">if</span> (isRequired(descriptor)) &#123;                raiseNoMatchingBeanFound(type, descriptor.getResolvableType(), descriptor);            &#125;            result = <span class="hljs-keyword">null</span>;        &#125;        <span class="hljs-keyword">if</span> (!ClassUtils.isAssignableValue(type, result)) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanNotOfRequiredTypeException(autowiredBeanName, type, instanceCandidate.getClass());        &#125;        <span class="hljs-keyword">return</span> result;    &#125;    <span class="hljs-keyword">finally</span> &#123;        ConstructorResolver.setCurrentInjectionPoint(previousInjectionPoint);    &#125;&#125;</code></pre><p>到这里就已经完成了所有属性的注入了。</p><ul><li>下一步，则是对依赖 bean 的依赖检测和 PostProcessor 处理（该分析放在最后）。</li></ul><ul><li>最后一步：<code>applyPropertyValues(String beanName, BeanDefinition mbd, BeanWrapper bw, PropertyValues pvs)</code> 方法。</li></ul><h5 id="applyPropertyValues-方法"><a href="#applyPropertyValues-方法" class="headerlink" title="applyPropertyValues()方法"></a><code>applyPropertyValues()</code>方法</h5><p>上面只是完成了所有注入属性的获取，将获取的属性封装在 PropertyValues 的实例对象 <code>pvs</code> 中，并没有应用到已经实例化的 bean 中。该方法是<code>populateBean()</code>的最后一步&lt;5&gt;，将属性添加到Bean。</p><pre><code class="hljs java"><span class="hljs-comment">//AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">applyPropertyValues</span><span class="hljs-params">(String beanName, BeanDefinition mbd, BeanWrapper bw, PropertyValues pvs)</span> </span>&#123;    <span class="hljs-keyword">if</span> (pvs.isEmpty()) &#123;        <span class="hljs-keyword">return</span>;    &#125;    <span class="hljs-comment">// 设置 BeanWrapperImpl 的 SecurityContext 属性</span>    <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span> &amp;&amp; bw <span class="hljs-keyword">instanceof</span> BeanWrapperImpl) &#123;        ((BeanWrapperImpl) bw).setSecurityContext(getAccessControlContext());    &#125;    <span class="hljs-comment">// MutablePropertyValues 类型属性</span>    MutablePropertyValues mpvs = <span class="hljs-keyword">null</span>;    <span class="hljs-comment">// 原始类型</span>    List&lt;PropertyValue&gt; original;    <span class="hljs-comment">// 获得 original</span>    <span class="hljs-keyword">if</span> (pvs <span class="hljs-keyword">instanceof</span> MutablePropertyValues) &#123;        mpvs = (MutablePropertyValues) pvs;        <span class="hljs-comment">// 属性值已经转换</span>        <span class="hljs-keyword">if</span> (mpvs.isConverted()) &#123;            <span class="hljs-comment">// Shortcut: use the pre-converted values as-is.</span>            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">// 为实例化对象设置属性值 ，依赖注入真真正正地实现在此！！！！！</span>                bw.setPropertyValues(mpvs);                <span class="hljs-keyword">return</span>;            &#125;            <span class="hljs-keyword">catch</span> (BeansException ex) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(                    mbd.getResourceDescription(), beanName, <span class="hljs-string">&quot;Error setting property values&quot;</span>, ex);            &#125;        &#125;        original = mpvs.getPropertyValueList();    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 如果 pvs 不是 MutablePropertyValues 类型，则直接使用原始类型</span>        original = Arrays.asList(pvs.getPropertyValues());    &#125;    <span class="hljs-comment">// 获取 TypeConverter = 获取用户自定义的类型转换</span>    TypeConverter converter = getCustomTypeConverter();    <span class="hljs-keyword">if</span> (converter == <span class="hljs-keyword">null</span>) &#123;        converter = bw;    &#125;    <span class="hljs-comment">// 获取对应的解析器</span>    BeanDefinitionValueResolver valueResolver = <span class="hljs-keyword">new</span> BeanDefinitionValueResolver(<span class="hljs-keyword">this</span>, beanName, mbd, converter);    <span class="hljs-comment">// Create a deep copy, resolving any references for values.</span>    List&lt;PropertyValue&gt; deepCopy = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;(original.size());    <span class="hljs-keyword">boolean</span> resolveNecessary = <span class="hljs-keyword">false</span>;    <span class="hljs-comment">// 遍历属性，将属性转换为对应类的对应属性的类型</span>    <span class="hljs-keyword">for</span> (PropertyValue pv : original) &#123;        <span class="hljs-comment">// 属性值不需要转换</span>        <span class="hljs-keyword">if</span> (pv.isConverted()) &#123;            deepCopy.add(pv);        &#125;        <span class="hljs-comment">// 属性值需要转换</span>        <span class="hljs-keyword">else</span> &#123;            String propertyName = pv.getName();            Object originalValue = pv.getValue();<span class="hljs-comment">// 原始的属性值，即转换之前的属性值</span>            <span class="hljs-keyword">if</span> (originalValue == AutowiredPropertyMarker.INSTANCE) &#123;                Method writeMethod = bw.getPropertyDescriptor(propertyName).getWriteMethod();                <span class="hljs-keyword">if</span> (writeMethod == <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(<span class="hljs-string">&quot;Autowire marker for property without write method: &quot;</span> + pv);                &#125;                originalValue = <span class="hljs-keyword">new</span> DependencyDescriptor(<span class="hljs-keyword">new</span> MethodParameter(writeMethod, <span class="hljs-number">0</span>), <span class="hljs-keyword">true</span>);            &#125;            Object resolvedValue = valueResolver.resolveValueIfNecessary(pv, originalValue); <span class="hljs-comment">// 转换属性值，例如将引用转换为IoC容器中实例化对象引用 ！！！！！ 对属性值的解析！！</span>            Object convertedValue = resolvedValue;<span class="hljs-comment">// 转换之后的属性值</span>            <span class="hljs-keyword">boolean</span> convertible = bw.isWritableProperty(propertyName) &amp;&amp;                !PropertyAccessorUtils.isNestedOrIndexedProperty(propertyName);<span class="hljs-comment">// 属性值是否可以转换</span>            <span class="hljs-keyword">if</span> (convertible) &#123;                <span class="hljs-comment">// 使用用户自定义的类型转换器转换属性值</span>                convertedValue = convertForProperty(resolvedValue, propertyName, bw, converter);            &#125;            <span class="hljs-comment">// Possibly store converted value in merged bean definition,</span>            <span class="hljs-comment">// in order to avoid re-conversion for every created bean instance.</span>            <span class="hljs-comment">// 存储转换后的属性值，避免每次属性注入时的转换工作</span>            <span class="hljs-keyword">if</span> (resolvedValue == originalValue) &#123;                <span class="hljs-keyword">if</span> (convertible) &#123;                    <span class="hljs-comment">// 设置属性转换之后的值</span>                    pv.setConvertedValue(convertedValue);                &#125;                deepCopy.add(pv);            &#125;            <span class="hljs-comment">// 属性是可转换的，且属性原始值是字符串类型，且属性的原始类型值不是</span>            <span class="hljs-comment">// 动态生成的字符串，且属性的原始值不是集合或者数组类型</span>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (convertible &amp;&amp; originalValue <span class="hljs-keyword">instanceof</span> TypedStringValue &amp;&amp;                     !((TypedStringValue) originalValue).isDynamic() &amp;&amp;                     !(convertedValue <span class="hljs-keyword">instanceof</span> Collection || ObjectUtils.isArray(convertedValue))) &#123;                pv.setConvertedValue(convertedValue);                deepCopy.add(pv);            &#125;            <span class="hljs-keyword">else</span> &#123;                resolveNecessary = <span class="hljs-keyword">true</span>;                <span class="hljs-comment">// 重新封装属性的值</span>                deepCopy.add(<span class="hljs-keyword">new</span> PropertyValue(pv, convertedValue));            &#125;        &#125;    &#125;    <span class="hljs-comment">// 标记属性值已经转换过</span>    <span class="hljs-keyword">if</span> (mpvs != <span class="hljs-keyword">null</span> &amp;&amp; !resolveNecessary) &#123;        mpvs.setConverted();    &#125;    <span class="hljs-comment">// Set our (possibly massaged) deep copy.</span>    <span class="hljs-comment">// 进行属性依赖注入，依赖注入的真真正正实现依赖的注入方法在此！！！</span>    <span class="hljs-keyword">try</span> &#123;        bw.setPropertyValues(<span class="hljs-keyword">new</span> MutablePropertyValues(deepCopy));    &#125;    <span class="hljs-keyword">catch</span> (BeansException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(            mbd.getResourceDescription(), beanName, <span class="hljs-string">&quot;Error setting property values&quot;</span>, ex);    &#125;&#125;</code></pre><p>以上完成了属性转换：</p><ul><li>属性值类型<strong>不需要</strong>转换时，不需要解析属性值，直接准备进行依赖注入。</li><li>属性值<strong>需要</strong>进行类型转换时，如对其他对象的引用等，首先需要解析属性值，然后对解析后的属性值进行依赖注入。</li></ul><p>其中调用了 <code>resolveValueIfNecessary(...)</code>方法对属性值的解析。</p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>创建Bean之实例化Bean对象二</title>
    <link href="/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E5%AE%9E%E4%BE%8B%E5%8C%96Bean%E5%AF%B9%E8%B1%A12/"/>
    <url>/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E5%AE%9E%E4%BE%8B%E5%8C%96Bean%E5%AF%B9%E8%B1%A12/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">创建Bean之实例化Bean对象二</font></div><p>createBeanInstance(String beanName, RootBeanDefinition mbd, Object[] args)` 方法，用于实例化 Bean 对象。它会根据不同情况，选择不同的实例化策略来完成 Bean 的初始化，主要包括：</p><ul><li>Supplier 回调：<code>obtainFromSupplier(final String beanName, final RootBeanDefinition mbd)</code> 方法。</li><li>工厂方法初始化：<code>instantiateUsingFactoryMethod(String beanName, RootBeanDefinition mbd, @Nullable Object[] explicitArgs)</code> 方法。</li><li>构造函数自动注入初始化：<code>autowireConstructor(final String beanName, final RootBeanDefinition mbd, Constructor[] chosenCtors, final Object[] explicitArgs)</code> 方法。</li><li>默认构造函数注入：<code>instantiateBean(final String beanName, final RootBeanDefinition mbd)</code> 方法。</li></ul><h4 id="autowireConstructor"><a href="#autowireConstructor" class="headerlink" title="autowireConstructor()"></a><code>autowireConstructor()</code></h4><p>这个初始化方法，我们可以简单理解为是<strong>带有参数的构造方法</strong>，来初始化 Bean 对象。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> BeanWrapper <span class="hljs-title">autowireConstructor</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> Constructor&lt;?&gt;[] ctors, <span class="hljs-meta">@Nullable</span> Object[] explicitArgs)</span> </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> ConstructorResolver(<span class="hljs-keyword">this</span>).autowireConstructor(beanName, mbd, ctors, explicitArgs);&#125;<span class="hljs-comment">// ConstructorResolver.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> BeanWrapper <span class="hljs-title">autowireConstructor</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd,</span></span><span class="hljs-function"><span class="hljs-params">    <span class="hljs-meta">@Nullable</span> Constructor&lt;?&gt;[] chosenCtors, <span class="hljs-meta">@Nullable</span> Object[] explicitArgs)</span> </span>&#123;    <span class="hljs-comment">// 封装 BeanWrapperImpl 对象，并完成初始化</span>    BeanWrapperImpl bw = <span class="hljs-keyword">new</span> BeanWrapperImpl();    <span class="hljs-keyword">this</span>.beanFactory.initBeanWrapper(bw);    <span class="hljs-comment">// 获得 constructorToUse、argsHolderToUse、argsToUse</span>    Constructor&lt;?&gt; constructorToUse = <span class="hljs-keyword">null</span>;<span class="hljs-comment">// 构造函数</span>    ArgumentsHolder argsHolderToUse = <span class="hljs-keyword">null</span>;<span class="hljs-comment">// 构造参数</span>    Object[] argsToUse = <span class="hljs-keyword">null</span>;<span class="hljs-comment">// 构造参数</span>    <span class="hljs-comment">// 确定构造参数</span>    <span class="hljs-comment">// 如果 getBean() 已经传递，则直接使用</span>    <span class="hljs-keyword">if</span> (explicitArgs != <span class="hljs-keyword">null</span>) &#123;        argsToUse = explicitArgs;    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 尝试从缓存中获取</span>        Object[] argsToResolve = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">synchronized</span> (mbd.constructorArgumentLock) &#123;            <span class="hljs-comment">// 缓存中的构造函数或者工厂方法</span>            constructorToUse = (Constructor&lt;?&gt;) mbd.resolvedConstructorOrFactoryMethod;            <span class="hljs-keyword">if</span> (constructorToUse != <span class="hljs-keyword">null</span> &amp;&amp; mbd.constructorArgumentsResolved) &#123;                <span class="hljs-comment">// Found a cached constructor...</span>                <span class="hljs-comment">// 缓存中的构造参数</span>                argsToUse = mbd.resolvedConstructorArguments;                <span class="hljs-keyword">if</span> (argsToUse == <span class="hljs-keyword">null</span>) &#123;                    argsToResolve = mbd.preparedConstructorArguments;                &#125;            &#125;        &#125;        <span class="hljs-comment">// 缓存中存在,则解析存储在 BeanDefinition 中的参数</span>        <span class="hljs-comment">// 如给定方法的构造函数 A(int ,int )，则通过此方法后就会把配置文件中的(&quot;1&quot;,&quot;1&quot;)转换为 (1,1)</span>        <span class="hljs-comment">// 缓存中的值可能是原始值也有可能是最终值</span>        <span class="hljs-keyword">if</span> (argsToResolve != <span class="hljs-keyword">null</span>) &#123;            argsToUse = resolvePreparedArguments(beanName, mbd, bw, constructorToUse, argsToResolve);        &#125;    &#125;    <span class="hljs-comment">// 没有缓存，则尝试从配置文件中获取参数</span>    <span class="hljs-keyword">if</span> (constructorToUse == <span class="hljs-keyword">null</span> || argsToUse == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// Take specified constructors, if any.</span>        <span class="hljs-comment">// 如果 chosenCtors 未传入，则获取构造方法们</span>        Constructor&lt;?&gt;[] candidates = chosenCtors;        <span class="hljs-keyword">if</span> (candidates == <span class="hljs-keyword">null</span>) &#123;            Class&lt;?&gt; beanClass = mbd.getBeanClass();            <span class="hljs-keyword">try</span> &#123;                candidates = (mbd.isNonPublicAccessAllowed() ?                              beanClass.getDeclaredConstructors() : beanClass.getConstructors());            &#125;            <span class="hljs-keyword">catch</span> (Throwable ex) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                                <span class="hljs-string">&quot;Resolution of declared constructors on bean Class [&quot;</span> + beanClass.getName() +                                                <span class="hljs-string">&quot;] from ClassLoader [&quot;</span> + beanClass.getClassLoader() + <span class="hljs-string">&quot;] failed&quot;</span>, ex);            &#125;        &#125;        <span class="hljs-comment">//创建 Bean</span>        <span class="hljs-keyword">if</span> (candidates.length == <span class="hljs-number">1</span> &amp;&amp; explicitArgs == <span class="hljs-keyword">null</span> &amp;&amp; !mbd.hasConstructorArgumentValues()) &#123;            Constructor&lt;?&gt; uniqueCandidate = candidates[<span class="hljs-number">0</span>];            <span class="hljs-keyword">if</span> (uniqueCandidate.getParameterCount() == <span class="hljs-number">0</span>) &#123;                <span class="hljs-keyword">synchronized</span> (mbd.constructorArgumentLock) &#123;                    mbd.resolvedConstructorOrFactoryMethod = uniqueCandidate;                    mbd.constructorArgumentsResolved = <span class="hljs-keyword">true</span>;                    mbd.resolvedConstructorArguments = EMPTY_ARGS;                &#125;                bw.setBeanInstance(instantiate(beanName, mbd, uniqueCandidate, EMPTY_ARGS));                <span class="hljs-keyword">return</span> bw;            &#125;        &#125;        <span class="hljs-comment">// Need to resolve the constructor.</span>        <span class="hljs-comment">// 是否需要解析构造器</span>        <span class="hljs-keyword">boolean</span> autowiring = (chosenCtors != <span class="hljs-keyword">null</span> ||                              mbd.getResolvedAutowireMode() == AutowireCapableBeanFactory.AUTOWIRE_CONSTRUCTOR);        <span class="hljs-comment">// 用于承载解析后的构造函数参数的值</span>        ConstructorArgumentValues resolvedValues = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">int</span> minNrOfArgs;        <span class="hljs-keyword">if</span> (explicitArgs != <span class="hljs-keyword">null</span>) &#123;            minNrOfArgs = explicitArgs.length;        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// 从 BeanDefinition 中获取构造参数，也就是从配置文件中提取构造参数</span>            ConstructorArgumentValues cargs = mbd.getConstructorArgumentValues();            resolvedValues = <span class="hljs-keyword">new</span> ConstructorArgumentValues();            <span class="hljs-comment">// 解析构造函数的参数</span>            <span class="hljs-comment">// 将该 bean 的构造函数参数解析为 resolvedValues 对象，其中会涉及到其他 bean</span>            minNrOfArgs = resolveConstructorArguments(beanName, mbd, bw, cargs, resolvedValues);        &#125;        <span class="hljs-comment">// 对构造函数进行排序处理</span>        <span class="hljs-comment">// public 构造函数优先参数数量降序，非public 构造函数参数数量降序</span>        AutowireUtils.sortConstructors(candidates);        <span class="hljs-comment">// 最小参数类型权重</span>        <span class="hljs-keyword">int</span> minTypeDiffWeight = Integer.MAX_VALUE;        Set&lt;Constructor&lt;?&gt;&gt; ambiguousConstructors = <span class="hljs-keyword">null</span>;        Deque&lt;UnsatisfiedDependencyException&gt; causes = <span class="hljs-keyword">null</span>;        <span class="hljs-comment">// 迭代所有构造函数</span>        <span class="hljs-keyword">for</span> (Constructor&lt;?&gt; candidate : candidates) &#123;            <span class="hljs-comment">// 获取该构造函数的参数类型</span>            <span class="hljs-keyword">int</span> parameterCount = candidate.getParameterCount();            <span class="hljs-comment">// 如果已经找到选用的构造函数或者需要的参数个数小于当前的构造函数参数个数，则终止。</span>            <span class="hljs-comment">// 因为，已经按照参数个数降序排列了</span>            <span class="hljs-keyword">if</span> (constructorToUse != <span class="hljs-keyword">null</span> &amp;&amp; argsToUse != <span class="hljs-keyword">null</span> &amp;&amp; argsToUse.length &gt; parameterCount) &#123;                <span class="hljs-comment">// Already found greedy constructor that can be satisfied -&gt;</span>                <span class="hljs-comment">// do not look any further, there are only less greedy constructors left.</span>                <span class="hljs-keyword">break</span>;            &#125;            <span class="hljs-comment">// 参数个数不等，继续</span>            <span class="hljs-keyword">if</span> (parameterCount &lt; minNrOfArgs) &#123;                <span class="hljs-keyword">continue</span>;            &#125;            <span class="hljs-comment">// 参数持有者 ArgumentsHolder 对象</span>            ArgumentsHolder argsHolder;            Class&lt;?&gt;[] paramTypes = candidate.getParameterTypes();            <span class="hljs-keyword">if</span> (resolvedValues != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">try</span> &#123;                    <span class="hljs-comment">// 注释上获取参数名称</span>                    String[] paramNames = ConstructorPropertiesChecker.evaluate(candidate, parameterCount);                    <span class="hljs-keyword">if</span> (paramNames == <span class="hljs-keyword">null</span>) &#123;                        <span class="hljs-comment">// 获取构造函数、方法参数的探测器</span>                        ParameterNameDiscoverer pnd = <span class="hljs-keyword">this</span>.beanFactory.getParameterNameDiscoverer();                        <span class="hljs-keyword">if</span> (pnd != <span class="hljs-keyword">null</span>) &#123;                            <span class="hljs-comment">// 通过探测器获取构造函数的参数名称</span>                            paramNames = pnd.getParameterNames(candidate);                        &#125;                    &#125;                    <span class="hljs-comment">// 根据构造函数和构造参数，创建参数持有者 ArgumentsHolder 对象</span>                    argsHolder = createArgumentArray(beanName, mbd, resolvedValues, bw, paramTypes, paramNames,                                                     getUserDeclaredConstructor(candidate), autowiring, candidates.length == <span class="hljs-number">1</span>);                &#125;                <span class="hljs-keyword">catch</span> (UnsatisfiedDependencyException ex) &#123;                    <span class="hljs-comment">// 若发生 UnsatisfiedDependencyException 异常，添加到 causes 中。</span>                    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                        logger.trace(<span class="hljs-string">&quot;Ignoring constructor [&quot;</span> + candidate + <span class="hljs-string">&quot;] of bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;: &quot;</span> + ex);                    &#125;                    <span class="hljs-comment">// Swallow and try next constructor.</span>                    <span class="hljs-keyword">if</span> (causes == <span class="hljs-keyword">null</span>) &#123;                        causes = <span class="hljs-keyword">new</span> ArrayDeque&lt;&gt;(<span class="hljs-number">1</span>);                    &#125;                    causes.add(ex);                    <span class="hljs-keyword">continue</span>;<span class="hljs-comment">// continue ，继续执行</span>                &#125;            &#125;            <span class="hljs-keyword">else</span> &#123;                <span class="hljs-comment">// 构造函数没有参数</span>                <span class="hljs-comment">// Explicit arguments given -&gt; arguments length must match exactly.</span>                <span class="hljs-keyword">if</span> (parameterCount != explicitArgs.length) &#123;                    <span class="hljs-keyword">continue</span>;                &#125;                <span class="hljs-comment">// 根据 explicitArgs ，创建 ArgumentsHolder 对象</span>                argsHolder = <span class="hljs-keyword">new</span> ArgumentsHolder(explicitArgs);            &#125;            <span class="hljs-comment">// isLenientConstructorResolution 判断解析构造函数的时候是否以宽松模式还是严格模式</span>            <span class="hljs-comment">// 严格模式：解析构造函数时，必须所有的都需要匹配，否则抛出异常</span>            <span class="hljs-comment">// 宽松模式：使用具有&quot;最接近的模式&quot;进行匹配</span>            <span class="hljs-comment">// typeDiffWeight：类型差异权重</span>            <span class="hljs-keyword">int</span> typeDiffWeight = (mbd.isLenientConstructorResolution() ?                                  argsHolder.getTypeDifferenceWeight(paramTypes) : argsHolder.getAssignabilityWeight(paramTypes));            <span class="hljs-comment">// Choose this constructor if it represents the closest match.</span>            <span class="hljs-comment">// 如果它代表着当前最接近的匹配则选择其作为构造函数</span>            <span class="hljs-keyword">if</span> (typeDiffWeight &lt; minTypeDiffWeight) &#123;                constructorToUse = candidate;                argsHolderToUse = argsHolder;                argsToUse = argsHolder.arguments;                minTypeDiffWeight = typeDiffWeight;                ambiguousConstructors = <span class="hljs-keyword">null</span>;            &#125;            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (constructorToUse != <span class="hljs-keyword">null</span> &amp;&amp; typeDiffWeight == minTypeDiffWeight) &#123;                <span class="hljs-keyword">if</span> (ambiguousConstructors == <span class="hljs-keyword">null</span>) &#123;                    ambiguousConstructors = <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;();                    ambiguousConstructors.add(constructorToUse);                &#125;                ambiguousConstructors.add(candidate);            &#125;        &#125;        <span class="hljs-comment">// 没有可执行的工厂方法，抛出异常</span>        <span class="hljs-keyword">if</span> (constructorToUse == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">if</span> (causes != <span class="hljs-keyword">null</span>) &#123;                UnsatisfiedDependencyException ex = causes.removeLast();                <span class="hljs-keyword">for</span> (Exception cause : causes) &#123;                    <span class="hljs-keyword">this</span>.beanFactory.onSuppressedException(cause);                &#125;                <span class="hljs-keyword">throw</span> ex;            &#125;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                            <span class="hljs-string">&quot;Could not resolve matching constructor &quot;</span> +                                            <span class="hljs-string">&quot;(hint: specify index/type/name arguments for simple parameters to avoid type ambiguities)&quot;</span>);        &#125;        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (ambiguousConstructors != <span class="hljs-keyword">null</span> &amp;&amp; !mbd.isLenientConstructorResolution()) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                            <span class="hljs-string">&quot;Ambiguous constructor matches found in bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; &quot;</span> +                                            <span class="hljs-string">&quot;(hint: specify index/type/name arguments for simple parameters to avoid type ambiguities): &quot;</span> +                                            ambiguousConstructors);        &#125;        <span class="hljs-comment">// 将解析的构造函数加入缓存</span>        <span class="hljs-keyword">if</span> (explicitArgs == <span class="hljs-keyword">null</span> &amp;&amp; argsHolderToUse != <span class="hljs-keyword">null</span>) &#123;            argsHolderToUse.storeCache(mbd, constructorToUse);        &#125;    &#125;    Assert.state(argsToUse != <span class="hljs-keyword">null</span>, <span class="hljs-string">&quot;Unresolved constructor arguments&quot;</span>);    <span class="hljs-comment">// 创建 Bean 对象，并设置到 bw 中</span>    bw.setBeanInstance(instantiate(beanName, mbd, constructorToUse, argsToUse));    <span class="hljs-keyword">return</span> bw;&#125;</code></pre><ul><li>一句话概括：首先确定构造函数参数、构造函数，然后调用相应的初始化策略进行 bean 的初始化。关于如何确定构造函数、构造参数，该部分逻辑和 <code>instantiateUsingFactoryMethod(...)</code> 方法，基本一致。</li></ul><h5 id="instantiate"><a href="#instantiate" class="headerlink" title="instantiate"></a><code>instantiate</code></h5><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> Object <span class="hljs-title">instantiate</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd,</span></span><span class="hljs-function"><span class="hljs-params">    <span class="hljs-meta">@Nullable</span> Object factoryBean, Method factoryMethod, Object[] args)</span> </span>&#123;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt;                                                 <span class="hljs-keyword">this</span>.beanFactory.getInstantiationStrategy().instantiate(    mbd, beanName, <span class="hljs-keyword">this</span>.beanFactory, factoryBean, factoryMethod, args),                                                 <span class="hljs-keyword">this</span>.beanFactory.getAccessControlContext());        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.beanFactory.getInstantiationStrategy().instantiate(                mbd, beanName, <span class="hljs-keyword">this</span>.beanFactory, factoryBean, factoryMethod, args);        &#125;    &#125;    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                        <span class="hljs-string">&quot;Bean instantiation via factory method failed&quot;</span>, ex);    &#125;&#125;</code></pre><ul><li>首先，是获取实例化 Bean 的策略 InstantiationStrategy 对象。</li><li>然后，调用其 <code>instantiate(RootBeanDefinition bd, String beanName, BeanFactory owner, Constructor ctor, Object... args)</code> 方法，该方法在 SimpleInstantiationStrategy 中实现。</li></ul><pre><code class="hljs java"><span class="hljs-comment">// SimpleInstantiationStrategy.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">instantiate</span><span class="hljs-params">(RootBeanDefinition bd, <span class="hljs-meta">@Nullable</span> String beanName, BeanFactory owner,<span class="hljs-keyword">final</span> Constructor&lt;?&gt; ctor, Object... args)</span> </span>&#123;    <span class="hljs-comment">// &lt;x1&gt; 没有覆盖，直接使用反射实例化即可</span>    <span class="hljs-keyword">if</span> (!bd.hasMethodOverrides()) &#123;        <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">// use own privileged to change accessibility (when security is on)</span>            <span class="hljs-comment">// 设置构造方法，可访问</span>            AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;                ReflectionUtils.makeAccessible(ctor);                <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;            &#125;);        &#125;        <span class="hljs-comment">// 通过 BeanUtils 直接使用构造器对象实例化 Bean 对象</span>        <span class="hljs-keyword">return</span> BeanUtils.instantiateClass(ctor, args);    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// &lt;x2&gt; 生成 CGLIB 创建的子类对象</span>        <span class="hljs-keyword">return</span> instantiateWithMethodInjection(bd, beanName, owner, ctor, args);    &#125;&#125;</code></pre><ul><li><code>&lt;1&gt;</code> 如果该 bean 没有配置 <code>lookup-method</code>、<code>replaced-method</code> 标签或者 <code>@Lookup</code> 注解，则直接通过<strong>反射</strong>的方式实例化 Bean 对象即可，方便快捷。</li><li><code>&lt;2&gt;</code> 但是，如果存在需要覆盖的方法或者动态替换的方法时，则需要使用 CGLIB 进行动态代理，因为可以在创建代理的同时将动态方法织入类中。</li></ul><h6 id="反射创建-Bean-对象"><a href="#反射创建-Bean-对象" class="headerlink" title="反射创建 Bean 对象"></a>反射创建 Bean 对象</h6><p>调用工具类 BeanUtils 的 <code>instantiateClass(Constructor ctor, Object... args)</code> 方法，完成反射工作，创建对象。</p><pre><code class="hljs java"><span class="hljs-comment">// BeanUtils.java</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; <span class="hljs-function">T <span class="hljs-title">instantiateClass</span><span class="hljs-params">(Constructor&lt;T&gt; ctor, Object... args)</span> <span class="hljs-keyword">throws</span> BeanInstantiationException </span>&#123;    Assert.notNull(ctor, <span class="hljs-string">&quot;Constructor must not be null&quot;</span>);    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// 设置构造方法，可访问</span>        ReflectionUtils.makeAccessible(ctor);        <span class="hljs-keyword">if</span> (KotlinDetector.isKotlinReflectPresent() &amp;&amp; KotlinDetector.isKotlinType(ctor.getDeclaringClass())) &#123;            <span class="hljs-comment">// 使用构造方法，创建对象</span>            <span class="hljs-keyword">return</span> KotlinDelegate.instantiateClass(ctor, args);        &#125;        <span class="hljs-keyword">else</span> &#123;            Class&lt;?&gt;[] parameterTypes = ctor.getParameterTypes();            Assert.isTrue(args.length &lt;= parameterTypes.length, <span class="hljs-string">&quot;Can&#x27;t specify more arguments than constructor parameters&quot;</span>);            Object[] argsWithDefaultValues = <span class="hljs-keyword">new</span> Object[args.length];            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span> ; i &lt; args.length; i++) &#123;                <span class="hljs-keyword">if</span> (args[i] == <span class="hljs-keyword">null</span>) &#123;                    Class&lt;?&gt; parameterType = parameterTypes[i];                    argsWithDefaultValues[i] = (parameterType.isPrimitive() ? DEFAULT_TYPE_VALUES.get(parameterType) : <span class="hljs-keyword">null</span>);                &#125;                <span class="hljs-keyword">else</span> &#123;                    argsWithDefaultValues[i] = args[i];                &#125;            &#125;            <span class="hljs-keyword">return</span> ctor.newInstance(argsWithDefaultValues);        &#125;    &#125;    <span class="hljs-comment">// 各种异常的翻译，最终统一抛出 BeanInstantiationException 异常</span>    <span class="hljs-keyword">catch</span> (InstantiationException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInstantiationException(ctor, <span class="hljs-string">&quot;Is it an abstract class?&quot;</span>, ex);    &#125;    <span class="hljs-keyword">catch</span> (IllegalAccessException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInstantiationException(ctor, <span class="hljs-string">&quot;Is the constructor accessible?&quot;</span>, ex);    &#125;    <span class="hljs-keyword">catch</span> (IllegalArgumentException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInstantiationException(ctor, <span class="hljs-string">&quot;Illegal arguments for constructor&quot;</span>, ex);    &#125;    <span class="hljs-keyword">catch</span> (InvocationTargetException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInstantiationException(ctor, <span class="hljs-string">&quot;Constructor threw exception&quot;</span>, ex.getTargetException());    &#125;&#125;</code></pre><h6 id="CGLIB-创建-Bean-对象"><a href="#CGLIB-创建-Bean-对象" class="headerlink" title="CGLIB 创建 Bean 对象"></a>CGLIB 创建 Bean 对象</h6><pre><code class="hljs java"><span class="hljs-comment">// SimpleInstantiationStrategy.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">instantiateWithMethodInjection</span><span class="hljs-params">(RootBeanDefinition bd, <span class="hljs-meta">@Nullable</span> String beanName, BeanFactory owner)</span> </span>&#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> UnsupportedOperationException(<span class="hljs-string">&quot;Method Injection not supported in SimpleInstantiationStrategy&quot;</span>);&#125;</code></pre><p>方法默认是<strong>没有实现</strong>的，具体过程由其子类 <code>org.springframework.beans.factory.support.CglibSubclassingInstantiationStrategy</code> 来实现。</p><pre><code class="hljs java"><span class="hljs-comment">// CglibSubclassingInstantiationStrategy.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">instantiateWithMethodInjection</span><span class="hljs-params">(RootBeanDefinition bd, <span class="hljs-meta">@Nullable</span> String beanName, BeanFactory owner)</span> </span>&#123;    <span class="hljs-keyword">return</span> instantiateWithMethodInjection(bd, beanName, owner, <span class="hljs-keyword">null</span>);&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">instantiateWithMethodInjection</span><span class="hljs-params">(RootBeanDefinition bd, <span class="hljs-meta">@Nullable</span> String beanName, BeanFactory owner, <span class="hljs-meta">@Nullable</span> Constructor&lt;?&gt; ctor, Object... args)</span> </span>&#123;    <span class="hljs-comment">// Must generate CGLIB subclass...</span>    <span class="hljs-comment">// 通过CGLIB生成一个子类对象</span>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> CglibSubclassCreator(bd, owner).instantiate(ctor, args);&#125;</code></pre><ul><li><p>创建一个 CglibSubclassCreator 对象，后调用其 <code>instantiate(Constructor ctor, Object... args)</code> 方法，生成其子类对象。</p><p><strong>注：</strong><code>CglibSubclassCreator</code>是个内部类。</p></li></ul><pre><code class="hljs java"><span class="hljs-comment">// CglibSubclassingInstantiationStrategy.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">instantiate</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Constructor&lt;?&gt; ctor, Object... args)</span> </span>&#123;    <span class="hljs-comment">// 通过 Cglib 创建一个代理类</span>    Class&lt;?&gt; subclass = createEnhancedSubclass(<span class="hljs-keyword">this</span>.beanDefinition);    Object instance;    <span class="hljs-comment">// 没有构造器，通过 BeanUtils 使用默认构造器创建一个bean实例</span>    <span class="hljs-keyword">if</span> (ctor == <span class="hljs-keyword">null</span>) &#123;        instance = BeanUtils.instantiateClass(subclass);    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">// 获取代理类对应的构造器对象，并实例化 bean</span>            Constructor&lt;?&gt; enhancedSubclassConstructor = subclass.getConstructor(ctor.getParameterTypes());            instance = enhancedSubclassConstructor.newInstance(args);        &#125; <span class="hljs-keyword">catch</span> (Exception ex) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInstantiationException(<span class="hljs-keyword">this</span>.beanDefinition.getBeanClass(),                    <span class="hljs-string">&quot;Failed to invoke constructor for CGLIB enhanced subclass [&quot;</span> + subclass.getName() + <span class="hljs-string">&quot;]&quot;</span>, ex);        &#125;    &#125;    <span class="hljs-comment">// SPR-10785: set callbacks directly on the instance instead of in the</span>    <span class="hljs-comment">// enhanced class (via the Enhancer) in order to avoid memory leaks.</span>    <span class="hljs-comment">// 为了避免 memory leaks 异常，直接在 bean 实例上设置回调对象</span>    Factory factory = (Factory) instance;    factory.setCallbacks(<span class="hljs-keyword">new</span> Callback[] &#123;NoOp.INSTANCE,            <span class="hljs-keyword">new</span> LookupOverrideMethodInterceptor(<span class="hljs-keyword">this</span>.beanDefinition, <span class="hljs-keyword">this</span>.owner),            <span class="hljs-keyword">new</span> ReplaceOverrideMethodInterceptor(<span class="hljs-keyword">this</span>.beanDefinition, <span class="hljs-keyword">this</span>.owner)&#125;);    <span class="hljs-keyword">return</span> instance;&#125;</code></pre><h4 id="instantiateBean-方法"><a href="#instantiateBean-方法" class="headerlink" title="instantiateBean()方法"></a><code>instantiateBean()</code>方法</h4><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> BeanWrapper <span class="hljs-title">instantiateBean</span><span class="hljs-params">(<span class="hljs-keyword">final</span> String beanName, <span class="hljs-keyword">final</span> RootBeanDefinition mbd)</span> </span>&#123;    <span class="hljs-keyword">try</span> &#123;        Object beanInstance;        <span class="hljs-keyword">final</span> BeanFactory parent = <span class="hljs-keyword">this</span>;        <span class="hljs-comment">// 安全模式</span>        <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123;            beanInstance = AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt;                    <span class="hljs-comment">// 获得 InstantiationStrategy 对象，并使用它，创建 Bean 对象</span>                    getInstantiationStrategy().instantiate(mbd, beanName, parent),                    getAccessControlContext());        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// 获得 InstantiationStrategy 对象，并使用它，创建 Bean 对象</span>            beanInstance = getInstantiationStrategy().instantiate(mbd, beanName, parent);        &#125;        <span class="hljs-comment">// 封装 BeanWrapperImpl  并完成初始化</span>        BeanWrapper bw = <span class="hljs-keyword">new</span> BeanWrapperImpl(beanInstance);        initBeanWrapper(bw);        <span class="hljs-keyword">return</span> bw;    &#125; <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(                mbd.getResourceDescription(), beanName, <span class="hljs-string">&quot;Instantiation of bean failed&quot;</span>, ex);    &#125;&#125;</code></pre><h5 id="instantiate-方法"><a href="#instantiate-方法" class="headerlink" title="instantiate()方法"></a><code>instantiate()</code>方法</h5><pre><code class="hljs java"><span class="hljs-comment">// SimpleInstantiationStrategy.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">instantiate</span><span class="hljs-params">(RootBeanDefinition bd, <span class="hljs-meta">@Nullable</span> String beanName, BeanFactory owner)</span> </span>&#123;    <span class="hljs-comment">// Don&#x27;t override the class with CGLIB if no overrides.</span>    <span class="hljs-comment">// 没有覆盖，直接使用反射实例化即可</span>    <span class="hljs-keyword">if</span> (!bd.hasMethodOverrides()) &#123;        Constructor&lt;?&gt; constructorToUse;        <span class="hljs-keyword">synchronized</span> (bd.constructorArgumentLock) &#123;            <span class="hljs-comment">// 获得构造方法 constructorToUse</span>            constructorToUse = (Constructor&lt;?&gt;) bd.resolvedConstructorOrFactoryMethod;            <span class="hljs-keyword">if</span> (constructorToUse == <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">final</span> Class&lt;?&gt; clazz = bd.getBeanClass();                <span class="hljs-comment">// 如果是接口，抛出 BeanInstantiationException 异常</span>                <span class="hljs-keyword">if</span> (clazz.isInterface()) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInstantiationException(clazz, <span class="hljs-string">&quot;Specified class is an interface&quot;</span>);                &#125;                <span class="hljs-keyword">try</span> &#123;                    <span class="hljs-comment">// 从 clazz 中，获得构造方法</span>                    <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123; <span class="hljs-comment">// 安全模式</span>                        constructorToUse = AccessController.doPrivileged(                                (PrivilegedExceptionAction&lt;Constructor&lt;?&gt;&gt;) clazz::getDeclaredConstructor);                    &#125; <span class="hljs-keyword">else</span> &#123;                        constructorToUse =  clazz.getDeclaredConstructor();                    &#125;                    <span class="hljs-comment">// 标记 resolvedConstructorOrFactoryMethod 属性</span>                    bd.resolvedConstructorOrFactoryMethod = constructorToUse;                &#125; <span class="hljs-keyword">catch</span> (Throwable ex) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInstantiationException(clazz, <span class="hljs-string">&quot;No default constructor found&quot;</span>, ex);                &#125;            &#125;        &#125;        <span class="hljs-comment">// 通过 BeanUtils 直接使用构造器对象实例化 Bean 对象</span>        <span class="hljs-keyword">return</span> BeanUtils.instantiateClass(constructorToUse);    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// Must generate CGLIB subclass.</span>        <span class="hljs-comment">// 生成 CGLIB 创建的子类对象</span>        <span class="hljs-keyword">return</span> instantiateWithMethodInjection(bd, beanName, owner);    &#125;&#125;</code></pre><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>对于 <code>createBeanInstance(...)</code> 方法而言，他就是<strong>选择合适实例化策略</strong>来为 bean 创建实例对象，具体的策略有：</p><ul><li>Supplier 回调方式</li><li>工厂方法初始化</li><li>构造函数自动注入初始化</li><li>默认构造函数注入。</li></ul><p>其中，工厂方法初始化和构造函数自动注入初始化两种方式<strong>最为复杂</strong>，主要是因为构造函数和构造参数的不确定性，Spring 需要花大量的精力来确定构造函数和构造参数，如果确定了则好办，直接选择实例化策略即可。</p><p>当然，在实例化的时候会根据是否有需要覆盖或者动态替换掉的方法，因为存在覆盖或者织入的话需要创建动态代理将方法织入，这个时候就只能选择 CGLIB 的方式来实例化，否则直接利用反射的方式即可，方便快捷。</p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>创建Bean之实例化Bean对象一</title>
    <link href="/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E5%AE%9E%E4%BE%8B%E5%8C%96Bean%E5%AF%B9%E8%B1%A11/"/>
    <url>/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E5%AE%9E%E4%BE%8B%E5%8C%96Bean%E5%AF%B9%E8%B1%A11/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">创建Bean之实例化Bean对象一</font></div><h4 id="createBeanInstance-方法"><a href="#createBeanInstance-方法" class="headerlink" title="createBeanInstance()方法"></a><code>createBeanInstance()</code>方法</h4><p>创建Bean实例过程的第一步：实例化Bean，方法为<code>createBeanInstance()</code>：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> BeanWrapper <span class="hljs-title">createBeanInstance</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> Object[] args)</span> </span>&#123;    <span class="hljs-comment">// 解析 bean ，将 bean 类名解析为 class 引用。</span>    Class&lt;?&gt; beanClass = resolveBeanClass(mbd, beanName);    <span class="hljs-keyword">if</span> (beanClass != <span class="hljs-keyword">null</span> &amp;&amp; !Modifier.isPublic(beanClass.getModifiers()) &amp;&amp; !mbd.isNonPublicAccessAllowed()) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                        <span class="hljs-string">&quot;Bean class isn&#x27;t public, and non-public access not allowed: &quot;</span> + beanClass.getName());    &#125;    <span class="hljs-comment">// &lt;1&gt; 如果存在 Supplier 回调，则使用给定的回调方法初始化策略</span>    Supplier&lt;?&gt; instanceSupplier = mbd.getInstanceSupplier();    <span class="hljs-keyword">if</span> (instanceSupplier != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> obtainFromSupplier(instanceSupplier, beanName);    &#125;    <span class="hljs-comment">// &lt;2&gt; 使用 FactoryBean 的 factory-method 来创建，支持静态工厂和实例工厂</span>    <span class="hljs-keyword">if</span> (mbd.getFactoryMethodName() != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> instantiateUsingFactoryMethod(beanName, mbd, args);    &#125;    <span class="hljs-keyword">boolean</span> resolved = <span class="hljs-keyword">false</span>;    <span class="hljs-keyword">boolean</span> autowireNecessary = <span class="hljs-keyword">false</span>;    <span class="hljs-keyword">if</span> (args == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// constructorArgumentLock 构造函数的常用锁</span>        <span class="hljs-keyword">synchronized</span> (mbd.constructorArgumentLock) &#123;            <span class="hljs-comment">// 如果已缓存的解析的构造函数或者工厂方法不为空，则可以利用构造函数解析</span>            <span class="hljs-comment">// 因为需要根据参数确认到底使用哪个构造函数，该过程比较消耗性能，所有采用缓存机制</span>            <span class="hljs-keyword">if</span> (mbd.resolvedConstructorOrFactoryMethod != <span class="hljs-keyword">null</span>) &#123;                resolved = <span class="hljs-keyword">true</span>;                autowireNecessary = mbd.constructorArgumentsResolved;            &#125;        &#125;    &#125;    <span class="hljs-comment">// 已经解析好了，直接注入即可</span>    <span class="hljs-keyword">if</span> (resolved) &#123;        <span class="hljs-keyword">if</span> (autowireNecessary) &#123;            <span class="hljs-comment">// &lt;3.1&gt; autowire 自动注入，调用构造函数自动注入</span>            <span class="hljs-keyword">return</span> autowireConstructor(beanName, mbd, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">null</span>);        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// &lt;3.2&gt; 使用默认构造函数构造</span>            <span class="hljs-keyword">return</span> instantiateBean(beanName, mbd);        &#125;    &#125;    <span class="hljs-comment">// &lt;4&gt; 确定解析的构造函数</span>    <span class="hljs-comment">// 主要是检查已经注册的 SmartInstantiationAwareBeanPostProcessor</span>    Constructor&lt;?&gt;[] ctors = determineConstructorsFromBeanPostProcessors(beanClass, beanName);    <span class="hljs-comment">// &lt;4.1&gt; 有参数情况时，创建 Bean 。先利用参数个数，类型等，确定最精确匹配的构造方法。</span>    <span class="hljs-keyword">if</span> (ctors != <span class="hljs-keyword">null</span> || mbd.getResolvedAutowireMode() == AUTOWIRE_CONSTRUCTOR ||        mbd.hasConstructorArgumentValues() || !ObjectUtils.isEmpty(args)) &#123;        <span class="hljs-keyword">return</span> autowireConstructor(beanName, mbd, ctors, args);    &#125;    <span class="hljs-comment">// &lt;4.1&gt; 选择构造方法，创建 Bean 。</span>    ctors = mbd.getPreferredConstructors();    <span class="hljs-keyword">if</span> (ctors != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> autowireConstructor(beanName, mbd, ctors, <span class="hljs-keyword">null</span>);    &#125;    <span class="hljs-comment">// &lt;4.2&gt; 有参数时，又没获取到构造方法，则只能调用无参构造方法来创建实例了(兜底方法)</span>    <span class="hljs-keyword">return</span> instantiateBean(beanName, mbd);&#125;</code></pre><p>实例化 Bean 对象，主要的逻辑为：</p><ul><li><p><code>&lt;1&gt;</code> 处，如果存在 Supplier 回调，则调用 <code>obtainFromSupplier(Supplier instanceSupplier, String beanName)</code> 方法，进行初始化。</p></li><li><p><code>&lt;2&gt;</code> 处，如果存在工厂方法，则使用工厂方法进行初始化。</p></li><li><p><code>&lt;3&gt;</code> 处，首先判断缓存，如果<strong>缓存中存在</strong>，即已经解析过了，则直接使用已经解析了的。根据 <code>constructorArgumentsResolved</code> 参数来判断：</p><p><code>&lt;3.1&gt;</code> 处，是使用构造函数自动注入，即调用 <code>autowireConstructor(String beanName, RootBeanDefinition mbd, Constructor[] ctors, Object[] explicitArgs)</code> 方法。</p><p><code>&lt;3.2&gt;</code> 处，还是默认构造函数，即调用 <code>instantiateBean(final String beanName, final RootBeanDefinition mbd)</code> 方法。</p></li><li><p><code>&lt;4&gt;</code> 处，如果<strong>缓存中没有</strong>，则需要先确定到底使用哪个构造函数来完成解析工作，因为一个类有多个构造函数，每个构造函数都有不同的构造参数，所以需要根据参数来锁定构造函数并完成初始化。</p><p><code>&lt;4.1&gt;</code> 处，如果存在参数，则使用相应的带有参数的构造函数，即调用 <code>autowireConstructor(String beanName, RootBeanDefinition mbd, Constructor[] ctors, Object[] explicitArgs)</code> 方法。</p><p><code>&lt;4.2&gt;</code> 处，否则，使用默认构造函数，即调用 <code>instantiateBean(final String beanName, final RootBeanDefinition mbd)</code> 方法。</p></li></ul><h5 id="obtainFromSupplier-方法"><a href="#obtainFromSupplier-方法" class="headerlink" title="obtainFromSupplier()方法"></a><code>obtainFromSupplier()</code>方法</h5><p>如果存在 Supplier 回调，则使用给定的回调方法初始化策略。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span>Supplier&lt;?&gt; instanceSupplier = mbd.getInstanceSupplier();<span class="hljs-keyword">if</span> (instanceSupplier != <span class="hljs-keyword">null</span>) &#123;    <span class="hljs-keyword">return</span> obtainFromSupplier(instanceSupplier, beanName);&#125;</code></pre><p>首先,从 BeanDefinition 中获取 Supplier 对象。如果不为空，则调用 <code>obtainFromSupplier(final String beanName, final RootBeanDefinition mbd)</code> 方法。</p><h6 id="Supplier"><a href="#Supplier" class="headerlink" title="Supplier"></a><code>Supplier</code></h6><p><code>Supplier</code>：java1.8的一个函数式接口，提供者。用于指定创建 bean 的回调。如果我们设置了这样的回调，那么其他的构造器或者工厂方法都会没有用。</p><pre><code class="hljs java"><span class="hljs-meta">@FunctionalInterface</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Supplier</span>&lt;<span class="hljs-title">T</span>&gt; </span>&#123;    <span class="hljs-function">T <span class="hljs-title">get</span><span class="hljs-params">()</span></span>;&#125;</code></pre><p>如何设置<code>Supplier</code>参数，有对应的<code>setter</code>方法：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanDefinition.java</span><span class="hljs-comment">/**</span><span class="hljs-comment"> * 创建 Bean 的 Supplier 对象</span><span class="hljs-comment"> */</span><span class="hljs-meta">@Nullable</span><span class="hljs-keyword">private</span> Supplier&lt;?&gt; instanceSupplier;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setInstanceSupplier</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Supplier&lt;?&gt; instanceSupplier)</span> </span>&#123;<span class="hljs-keyword">this</span>.instanceSupplier = instanceSupplier;&#125;</code></pre><p>在构造 BeanDefinition 对象的时候，设置了 <code>instanceSupplier</code> 该值，（以 RootBeanDefinition 为例）：</p><pre><code class="hljs java"><span class="hljs-comment">// RootBeanDefinition.java</span><span class="hljs-keyword">public</span> &lt;T&gt; RootBeanDefinition(<span class="hljs-meta">@Nullable</span> Class&lt;T&gt; beanClass, String scope, <span class="hljs-meta">@Nullable</span> Supplier&lt;T&gt; instanceSupplier) &#123;<span class="hljs-keyword">super</span>();setBeanClass(beanClass);setScope(scope);<span class="hljs-comment">// 设置 instanceSupplier 属性</span>setInstanceSupplier(instanceSupplier);&#125;</code></pre><h6 id="obtainFromSupplier-方法-1"><a href="#obtainFromSupplier-方法-1" class="headerlink" title="obtainFromSupplier()方法"></a><code>obtainFromSupplier()</code>方法</h6><p>如果设置了 <code>instanceSupplier</code> 属性，则可以调用 <code>obtainFromSupplier(Supplier instanceSupplier, String beanName)</code> 方法，完成 Bean 的初始化：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> BeanWrapper <span class="hljs-title">obtainFromSupplier</span><span class="hljs-params">(Supplier&lt;?&gt; instanceSupplier, String beanName)</span> </span>&#123;    Object instance;    <span class="hljs-comment">// 获得原创建的 Bean 的对象名</span>    String outerBean = <span class="hljs-keyword">this</span>.currentlyCreatedBean.get();    <span class="hljs-comment">// 设置新的 Bean 的对象名，到 currentlyCreatedBean 中</span>    <span class="hljs-keyword">this</span>.currentlyCreatedBean.set(beanName);    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// &lt;1&gt; 调用 Supplier 的 get()，返回一个 Bean 对象</span>        instance = instanceSupplier.get();    &#125;    <span class="hljs-keyword">finally</span> &#123;        <span class="hljs-comment">// 设置原创建的 Bean 的对象名，到 currentlyCreatedBean 中</span>        <span class="hljs-keyword">if</span> (outerBean != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">this</span>.currentlyCreatedBean.set(outerBean);        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">this</span>.currentlyCreatedBean.remove();        &#125;    &#125;    <span class="hljs-comment">// 未创建 Bean 对象，则创建 NullBean 对象</span>    <span class="hljs-keyword">if</span> (instance == <span class="hljs-keyword">null</span>) &#123;        instance = <span class="hljs-keyword">new</span> NullBean();    &#125;    <span class="hljs-comment">// &lt;2&gt; 创建 BeanWrapper 对象</span>    BeanWrapper bw = <span class="hljs-keyword">new</span> BeanWrapperImpl(instance);    <span class="hljs-comment">// &lt;3&gt; 初始化 BeanWrapper 对象</span>    initBeanWrapper(bw);    <span class="hljs-keyword">return</span> bw;&#125;</code></pre><p>流程如下：</p><ul><li><code>&lt;1&gt;</code> 首先，调用 Supplier 的 <code>get()</code> 方法，获得一个 Bean 实例对象。</li><li><code>&lt;2&gt;</code> 然后，根据该实例对象构造一个 BeanWrapper 对象 <code>bw</code> 。</li><li><code>&lt;3&gt;</code> 最后，初始化该对象。</li></ul><h5 id="instantiateUsingFactoryMethod-方法"><a href="#instantiateUsingFactoryMethod-方法" class="headerlink" title="instantiateUsingFactoryMethod()方法"></a><code>instantiateUsingFactoryMethod()</code>方法</h5><p>如果存在工厂方法，使用 FactoryBean 的 factory-method 来创建，支持静态工厂和实例工厂：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-comment">// &lt;2&gt; 使用 FactoryBean 的 factory-method 来创建，支持静态工厂和实例工厂</span><span class="hljs-keyword">if</span> (mbd.getFactoryMethodName() != <span class="hljs-keyword">null</span>) &#123;    <span class="hljs-keyword">return</span> instantiateUsingFactoryMethod(beanName, mbd, args);&#125;<span class="hljs-comment">/**</span><span class="hljs-comment"> *构造一个 ConstructorResolver 对象，然后调用其 instantiateUsingFactoryMethod(EvaluationContext context, String typeName, List&lt;TypeDescriptor&gt; argumentTypes) 方法。</span><span class="hljs-comment">*/</span><span class="hljs-function"><span class="hljs-keyword">protected</span> BeanWrapper <span class="hljs-title">instantiateUsingFactoryMethod</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> Object[] explicitArgs)</span> </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> ConstructorResolver(<span class="hljs-keyword">this</span>).instantiateUsingFactoryMethod(beanName, mbd, explicitArgs);&#125;</code></pre><h6 id="ConstructorResolver"><a href="#ConstructorResolver" class="headerlink" title="ConstructorResolver"></a><code>ConstructorResolver</code></h6><p><code>org.springframework.expression.ConstructorResolver</code> 是构造方法或者工厂类初始化 bean 的委托类。</p><pre><code class="hljs java"><span class="hljs-comment">// ConstructorResolver.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> BeanWrapper <span class="hljs-title">instantiateUsingFactoryMethod</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> Object[] explicitArgs)</span> </span>&#123;    <span class="hljs-comment">// 构造 BeanWrapperImpl 对象</span>    BeanWrapperImpl bw = <span class="hljs-keyword">new</span> BeanWrapperImpl();    <span class="hljs-comment">// 初始化 BeanWrapperImpl</span>    <span class="hljs-comment">// 向BeanWrapper对象中添加 ConversionService 对象和属性编辑器 PropertyEditor 对象</span>    <span class="hljs-keyword">this</span>.beanFactory.initBeanWrapper(bw);    <span class="hljs-comment">// &lt;1&gt; 获得 factoryBean、factoryClass、isStatic、factoryBeanName 属性</span>    Object factoryBean;    Class&lt;?&gt; factoryClass;    <span class="hljs-keyword">boolean</span> isStatic;    String factoryBeanName = mbd.getFactoryBeanName();    <span class="hljs-comment">// 工厂名不为空</span>    <span class="hljs-keyword">if</span> (factoryBeanName != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">if</span> (factoryBeanName.equals(beanName)) &#123;<span class="hljs-comment">// 抛出 BeanDefinitionStoreException 异常</span>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(mbd.getResourceDescription(), beanName,                                                   <span class="hljs-string">&quot;factory-bean reference points back to the same bean definition&quot;</span>);        &#125;        <span class="hljs-comment">// 获取工厂实例</span>        factoryBean = <span class="hljs-keyword">this</span>.beanFactory.getBean(factoryBeanName);        <span class="hljs-keyword">if</span> (mbd.isSingleton() &amp;&amp; <span class="hljs-keyword">this</span>.beanFactory.containsSingleton(beanName)) &#123;<span class="hljs-comment">// 抛出 ImplicitlyAppearedSingletonException 异常</span>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ImplicitlyAppearedSingletonException();        &#125;        <span class="hljs-keyword">this</span>.beanFactory.registerDependentBean(factoryBeanName, beanName);        factoryClass = factoryBean.getClass();        isStatic = <span class="hljs-keyword">false</span>;    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 工厂名为空，则其可能是一个静态工厂</span>        <span class="hljs-comment">// 静态工厂创建bean，必须要提供工厂的全类名</span>        <span class="hljs-keyword">if</span> (!mbd.hasBeanClass()) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(mbd.getResourceDescription(), beanName,                                                   <span class="hljs-string">&quot;bean definition declares neither a bean class nor a factory-bean reference&quot;</span>);        &#125;        factoryBean = <span class="hljs-keyword">null</span>;        factoryClass = mbd.getBeanClass();        isStatic = <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-comment">// &lt;2&gt; 获得 factoryMethodToUse、argsHolderToUse、argsToUse 属性</span>    Method factoryMethodToUse = <span class="hljs-keyword">null</span>;<span class="hljs-comment">// 工厂方法</span>    ArgumentsHolder argsHolderToUse = <span class="hljs-keyword">null</span>;    Object[] argsToUse = <span class="hljs-keyword">null</span>;<span class="hljs-comment">// 参数</span>    <span class="hljs-comment">// &lt;2.1&gt; 如果指定了构造参数则直接使用</span>    <span class="hljs-comment">// 在调用 getBean 方法的时候指定了方法参数</span>    <span class="hljs-keyword">if</span> (explicitArgs != <span class="hljs-keyword">null</span>) &#123;        argsToUse = explicitArgs;    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 没有指定，则尝试从配置文件中解析</span>        Object[] argsToResolve = <span class="hljs-keyword">null</span>;        <span class="hljs-comment">// &lt;2.2&gt; 首先尝试从缓存中获取</span>        <span class="hljs-keyword">synchronized</span> (mbd.constructorArgumentLock) &#123;            <span class="hljs-comment">// 获取缓存中的构造函数或者工厂方法</span>            factoryMethodToUse = (Method) mbd.resolvedConstructorOrFactoryMethod;            <span class="hljs-keyword">if</span> (factoryMethodToUse != <span class="hljs-keyword">null</span> &amp;&amp; mbd.constructorArgumentsResolved) &#123;                <span class="hljs-comment">// Found a cached factory method...</span>                <span class="hljs-comment">// 获取缓存中的构造参数</span>                argsToUse = mbd.resolvedConstructorArguments;                <span class="hljs-keyword">if</span> (argsToUse == <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-comment">// 获取缓存中的构造函数参数的包可见字段</span>                    argsToResolve = mbd.preparedConstructorArguments;                &#125;            &#125;        &#125;        <span class="hljs-comment">// 缓存中存在,则解析存储在 BeanDefinition 中的参数</span>        <span class="hljs-comment">// 如给定方法的构造函数 A(int ,int )，则通过此方法后就会把配置文件中的(&quot;1&quot;,&quot;1&quot;)转换为 (1,1)</span>        <span class="hljs-comment">// 缓存中的值可能是原始值也有可能是最终值</span>        <span class="hljs-keyword">if</span> (argsToResolve != <span class="hljs-keyword">null</span>) &#123;            argsToUse = resolvePreparedArguments(beanName, mbd, bw, factoryMethodToUse, argsToResolve);        &#125;    &#125;    <span class="hljs-comment">// &lt;3&gt;</span>    <span class="hljs-keyword">if</span> (factoryMethodToUse == <span class="hljs-keyword">null</span> || argsToUse == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// 获取工厂方法的类全名称</span>        factoryClass = ClassUtils.getUserClass(factoryClass);        List&lt;Method&gt; candidates = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">if</span> (mbd.isFactoryMethodUnique) &#123;            <span class="hljs-keyword">if</span> (factoryMethodToUse == <span class="hljs-keyword">null</span>) &#123;                factoryMethodToUse = mbd.getResolvedFactoryMethod();            &#125;            <span class="hljs-keyword">if</span> (factoryMethodToUse != <span class="hljs-keyword">null</span>) &#123;                candidates = Collections.singletonList(factoryMethodToUse);            &#125;        &#125;        <span class="hljs-keyword">if</span> (candidates == <span class="hljs-keyword">null</span>) &#123;            candidates = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();            <span class="hljs-comment">// 获取所有待定方法</span>            Method[] rawCandidates = getCandidateMethods(factoryClass, mbd);            <span class="hljs-keyword">for</span> (Method candidate : rawCandidates) &#123;                <span class="hljs-comment">// 检索所有方法，这里是对方法进行过滤</span>                <span class="hljs-keyword">if</span> (Modifier.isStatic(candidate.getModifiers()) == isStatic &amp;&amp; mbd.isFactoryMethod(candidate)) &#123;                    candidates.add(candidate);                &#125;            &#125;        &#125;        <span class="hljs-comment">// 创建 Bean</span>        <span class="hljs-keyword">if</span> (candidates.size() == <span class="hljs-number">1</span> &amp;&amp; explicitArgs == <span class="hljs-keyword">null</span> &amp;&amp; !mbd.hasConstructorArgumentValues()) &#123;            Method uniqueCandidate = candidates.get(<span class="hljs-number">0</span>);            <span class="hljs-keyword">if</span> (uniqueCandidate.getParameterCount() == <span class="hljs-number">0</span>) &#123;                mbd.factoryMethodToIntrospect = uniqueCandidate;                <span class="hljs-keyword">synchronized</span> (mbd.constructorArgumentLock) &#123;                    mbd.resolvedConstructorOrFactoryMethod = uniqueCandidate;                    mbd.constructorArgumentsResolved = <span class="hljs-keyword">true</span>;                    mbd.resolvedConstructorArguments = EMPTY_ARGS;                &#125;                bw.setBeanInstance(instantiate(beanName, mbd, factoryBean, uniqueCandidate, EMPTY_ARGS));                <span class="hljs-keyword">return</span> bw;            &#125;        &#125;        <span class="hljs-comment">// 排序构造函数</span>        <span class="hljs-comment">// public 构造函数优先参数数量降序，非 public 构造函数参数数量降序</span>        <span class="hljs-keyword">if</span> (candidates.size() &gt; <span class="hljs-number">1</span>) &#123;  <span class="hljs-comment">// explicitly skip immutable singletonList</span>            candidates.sort(AutowireUtils.EXECUTABLE_COMPARATOR);        &#125;        <span class="hljs-comment">// 用于承载解析后的构造函数参数的值</span>        ConstructorArgumentValues resolvedValues = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">boolean</span> autowiring = (mbd.getResolvedAutowireMode() == AutowireCapableBeanFactory.AUTOWIRE_CONSTRUCTOR);        <span class="hljs-keyword">int</span> minTypeDiffWeight = Integer.MAX_VALUE;        Set&lt;Method&gt; ambiguousFactoryMethods = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">int</span> minNrOfArgs;        <span class="hljs-keyword">if</span> (explicitArgs != <span class="hljs-keyword">null</span>) &#123;            minNrOfArgs = explicitArgs.length;        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// &lt;2.3&gt; getBean() 没有传递参数，则需要解析保存在 BeanDefinition 构造函数中指定的参数</span>            <span class="hljs-keyword">if</span> (mbd.hasConstructorArgumentValues()) &#123;                <span class="hljs-comment">// 构造函数的参数</span>                ConstructorArgumentValues cargs = mbd.getConstructorArgumentValues();                resolvedValues = <span class="hljs-keyword">new</span> ConstructorArgumentValues();                <span class="hljs-comment">// 解析构造函数的参数</span>                <span class="hljs-comment">// 将该 bean 的构造函数参数解析为 resolvedValues 对象，其中会涉及到其他 bean</span>                minNrOfArgs = resolveConstructorArguments(beanName, mbd, bw, cargs, resolvedValues);            &#125;            <span class="hljs-keyword">else</span> &#123;                minNrOfArgs = <span class="hljs-number">0</span>;            &#125;        &#125;        Deque&lt;UnsatisfiedDependencyException&gt; causes = <span class="hljs-keyword">null</span>;<span class="hljs-comment">// 记录 UnsatisfiedDependencyException 异常的集合</span>        <span class="hljs-comment">// 遍历 candidates 数组</span>        <span class="hljs-keyword">for</span> (Method candidate : candidates) &#123;            <span class="hljs-keyword">int</span> parameterCount = candidate.getParameterCount();            <span class="hljs-keyword">if</span> (parameterCount &gt;= minNrOfArgs) &#123;                <span class="hljs-comment">// 保存参数的对象</span>                ArgumentsHolder argsHolder;                <span class="hljs-comment">// 方法体的参数</span>                Class&lt;?&gt;[] paramTypes = candidate.getParameterTypes();                <span class="hljs-comment">// #getBean(...) 传递了参数</span>                <span class="hljs-keyword">if</span> (explicitArgs != <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-comment">// 显示给定参数，参数长度必须完全匹配</span>                    <span class="hljs-keyword">if</span> (paramTypes.length != explicitArgs.length) &#123;                        <span class="hljs-keyword">continue</span>;                    &#125;                    <span class="hljs-comment">// 根据参数创建参数持有者 ArgumentsHolder 对象</span>                    argsHolder = <span class="hljs-keyword">new</span> ArgumentsHolder(explicitArgs);                &#125;                <span class="hljs-keyword">else</span> &#123;                    <span class="hljs-comment">// 为提供参数，解析构造参数</span>                    <span class="hljs-keyword">try</span> &#123;                        String[] paramNames = <span class="hljs-keyword">null</span>;                        <span class="hljs-comment">// 获取 ParameterNameDiscoverer 对象</span>                        <span class="hljs-comment">// ParameterNameDiscoverer 是用于解析方法和构造函数的参数名称的接口，为参数名称探测器</span>                        ParameterNameDiscoverer pnd = <span class="hljs-keyword">this</span>.beanFactory.getParameterNameDiscoverer();                        <span class="hljs-comment">// 获取指定构造函数的参数名称</span>                        <span class="hljs-keyword">if</span> (pnd != <span class="hljs-keyword">null</span>) &#123;                            paramNames = pnd.getParameterNames(candidate);                        &#125;                        <span class="hljs-comment">// 在已经解析的构造函数参数值的情况下，创建一个参数持有者 ArgumentsHolder 对象</span>                        argsHolder = createArgumentArray(beanName, mbd, resolvedValues, bw,                                                         paramTypes, paramNames, candidate, autowiring, candidates.size() == <span class="hljs-number">1</span>);                    &#125;                    <span class="hljs-keyword">catch</span> (UnsatisfiedDependencyException ex) &#123;                        <span class="hljs-comment">// 若发生 UnsatisfiedDependencyException 异常，添加到 causes 中。</span>                        <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                            logger.trace(<span class="hljs-string">&quot;Ignoring factory method [&quot;</span> + candidate + <span class="hljs-string">&quot;] of bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;: &quot;</span> + ex);                        &#125;                        <span class="hljs-comment">// Swallow and try next overloaded factory method.</span>                        <span class="hljs-keyword">if</span> (causes == <span class="hljs-keyword">null</span>) &#123;                            causes = <span class="hljs-keyword">new</span> ArrayDeque&lt;&gt;(<span class="hljs-number">1</span>);                        &#125;                        causes.add(ex);                        <span class="hljs-keyword">continue</span>; <span class="hljs-comment">// continue ，继续执行</span>                    &#125;                &#125;                <span class="hljs-comment">// isLenientConstructorResolution 判断解析构造函数的时候是否以宽松模式还是严格模式</span>                <span class="hljs-comment">// 严格模式：解析构造函数时，必须所有的都需要匹配，否则抛出异常</span>                <span class="hljs-comment">// 宽松模式：使用具有&quot;最接近的模式&quot;进行匹配</span>                <span class="hljs-comment">// typeDiffWeight：类型差异权重</span>                <span class="hljs-keyword">int</span> typeDiffWeight = (mbd.isLenientConstructorResolution() ?                                      argsHolder.getTypeDifferenceWeight(paramTypes) : argsHolder.getAssignabilityWeight(paramTypes));                <span class="hljs-comment">// Choose this factory method if it represents the closest match.</span>                <span class="hljs-comment">// 代表最接近的类型匹配，则选择作为构造函数</span>                <span class="hljs-keyword">if</span> (typeDiffWeight &lt; minTypeDiffWeight) &#123;                    factoryMethodToUse = candidate;                    argsHolderToUse = argsHolder;                    argsToUse = argsHolder.arguments;                    minTypeDiffWeight = typeDiffWeight;                    ambiguousFactoryMethods = <span class="hljs-keyword">null</span>;                &#125;                <span class="hljs-comment">// 如果具有相同参数数量的方法具有相同的类型差异权重，则收集此类型选项</span>                <span class="hljs-comment">// 但是，仅在非宽松构造函数解析模式下执行该检查，并显式忽略重写方法（具有相同的参数签名）</span>                <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (factoryMethodToUse != <span class="hljs-keyword">null</span> &amp;&amp; typeDiffWeight == minTypeDiffWeight &amp;&amp;                         !mbd.isLenientConstructorResolution() &amp;&amp;                         paramTypes.length == factoryMethodToUse.getParameterCount() &amp;&amp;                         !Arrays.equals(paramTypes, factoryMethodToUse.getParameterTypes())) &#123;                    <span class="hljs-comment">// 查找到多个可匹配的方法</span>                    <span class="hljs-keyword">if</span> (ambiguousFactoryMethods == <span class="hljs-keyword">null</span>) &#123;                        ambiguousFactoryMethods = <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;();                        ambiguousFactoryMethods.add(factoryMethodToUse);                    &#125;                    ambiguousFactoryMethods.add(candidate);                &#125;            &#125;        &#125;        <span class="hljs-comment">// 没有可执行的工厂方法，抛出异常</span>        <span class="hljs-keyword">if</span> (factoryMethodToUse == <span class="hljs-keyword">null</span> || argsToUse == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">if</span> (causes != <span class="hljs-keyword">null</span>) &#123;                UnsatisfiedDependencyException ex = causes.removeLast();                <span class="hljs-keyword">for</span> (Exception cause : causes) &#123;                    <span class="hljs-keyword">this</span>.beanFactory.onSuppressedException(cause);                &#125;                <span class="hljs-keyword">throw</span> ex;            &#125;            List&lt;String&gt; argTypes = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;(minNrOfArgs);            <span class="hljs-keyword">if</span> (explicitArgs != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">for</span> (Object arg : explicitArgs) &#123;                    argTypes.add(arg != <span class="hljs-keyword">null</span> ? arg.getClass().getSimpleName() : <span class="hljs-string">&quot;null&quot;</span>);                &#125;            &#125;            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (resolvedValues != <span class="hljs-keyword">null</span>) &#123;                Set&lt;ValueHolder&gt; valueHolders = <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;(resolvedValues.getArgumentCount());                valueHolders.addAll(resolvedValues.getIndexedArgumentValues().values());                valueHolders.addAll(resolvedValues.getGenericArgumentValues());                <span class="hljs-keyword">for</span> (ValueHolder value : valueHolders) &#123;                    String argType = (value.getType() != <span class="hljs-keyword">null</span> ? ClassUtils.getShortName(value.getType()) :                                      (value.getValue() != <span class="hljs-keyword">null</span> ? value.getValue().getClass().getSimpleName() : <span class="hljs-string">&quot;null&quot;</span>));                    argTypes.add(argType);                &#125;            &#125;            String argDesc = StringUtils.collectionToCommaDelimitedString(argTypes);            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                            <span class="hljs-string">&quot;No matching factory method found: &quot;</span> +                                            (mbd.getFactoryBeanName() != <span class="hljs-keyword">null</span> ?                                             <span class="hljs-string">&quot;factory bean &#x27;&quot;</span> + mbd.getFactoryBeanName() + <span class="hljs-string">&quot;&#x27;; &quot;</span> : <span class="hljs-string">&quot;&quot;</span>) +                                            <span class="hljs-string">&quot;factory method &#x27;&quot;</span> + mbd.getFactoryMethodName() + <span class="hljs-string">&quot;(&quot;</span> + argDesc + <span class="hljs-string">&quot;)&#x27;. &quot;</span> +                                            <span class="hljs-string">&quot;Check that a method with the specified name &quot;</span> +                                            (minNrOfArgs &gt; <span class="hljs-number">0</span> ? <span class="hljs-string">&quot;and arguments &quot;</span> : <span class="hljs-string">&quot;&quot;</span>) +                                            <span class="hljs-string">&quot;exists and that it is &quot;</span> +                                            (isStatic ? <span class="hljs-string">&quot;static&quot;</span> : <span class="hljs-string">&quot;non-static&quot;</span>) + <span class="hljs-string">&quot;.&quot;</span>);        &#125;        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-keyword">void</span>.class == factoryMethodToUse.getReturnType()) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                            <span class="hljs-string">&quot;Invalid factory method &#x27;&quot;</span> + mbd.getFactoryMethodName() +                                            <span class="hljs-string">&quot;&#x27;: needs to have a non-void return type!&quot;</span>);        &#125;        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (ambiguousFactoryMethods != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                            <span class="hljs-string">&quot;Ambiguous factory method matches found in bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; &quot;</span> +                                            <span class="hljs-string">&quot;(hint: specify index/type/name arguments for simple parameters to avoid type ambiguities): &quot;</span> +                                            ambiguousFactoryMethods);        &#125;        <span class="hljs-keyword">if</span> (explicitArgs == <span class="hljs-keyword">null</span> &amp;&amp; argsHolderToUse != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">// 将解析的构造函数加入缓存</span>            mbd.factoryMethodToIntrospect = factoryMethodToUse;            argsHolderToUse.storeCache(mbd, factoryMethodToUse);        &#125;    &#125;    <span class="hljs-comment">// 创建 Bean 对象，并设置到 bw 中</span>    bw.setBeanInstance(instantiate(beanName, mbd, factoryBean, factoryMethodToUse, argsToUse));    <span class="hljs-keyword">return</span> bw;&#125;<span class="hljs-function"><span class="hljs-keyword">private</span> Object <span class="hljs-title">instantiate</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd,</span></span><span class="hljs-function"><span class="hljs-params">   <span class="hljs-meta">@Nullable</span> Object factoryBean, Method factoryMethod, Object[] args)</span> </span>&#123;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt;                                            <span class="hljs-keyword">this</span>.beanFactory.getInstantiationStrategy().instantiate(mbd, beanName, <span class="hljs-keyword">this</span>.beanFactory, factoryBean, factoryMethod, args),                                      <span class="hljs-keyword">this</span>.beanFactory.getAccessControlContext());        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.beanFactory.getInstantiationStrategy().instantiate(                mbd, beanName, <span class="hljs-keyword">this</span>.beanFactory, factoryBean, factoryMethod, args);        &#125;    &#125;    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                        <span class="hljs-string">&quot;Bean instantiation via factory method failed&quot;</span>, ex);    &#125;&#125;</code></pre><p>以上该方法：<strong>确定工厂对象，然后获取构造函数和构造参数，最后调用 InstantiationStrategy 对象的 <code>instantiate(RootBeanDefinition bd, String beanName, BeanFactory owner, Constructor ctor, Object... args)</code> 方法，来创建 Bean 实例</strong>。</p><ul><li><p><strong>一、&lt;1&gt;确定工厂对象</strong></p><p>首先获取工厂方法名：</p><ul><li>若工厂方法名不为空，则调用 <code>AbstractAutowireCapableBeanFactory.getBean(String name)</code> 方法，获取工厂对象，</li><li>若为空，则可能为一个静态工厂，对于静态工厂则必须提供工厂类的全类名，同时设置 <code>factoryBean = null</code> 。</li></ul></li><li><p><strong>二、&lt;2&gt;构造参数确认</strong></p><p>工厂对象确定后，则是确认构造参数。构造参数的确认主要分为<strong>三种</strong>情况：</p><ul><li><p><code>explicitArgs</code> 参数</p></li><li><p>缓存中获取</p></li><li><p>配置文件中解析</p><p>&lt;2.1&gt;explicitArgs 参数</p><p><code>explicitArgs</code> 参数，是我们调用 <code>getBean(...)</code> 方法时传递进来的。一般该参数，该参数就是用于初始化 Bean 时所传递的参数。如果该参数不为空，则可以确定构造函数的参数就是它了。</p><p>&lt;2.2&gt;缓存中获取</p><p>在该方法的最后，我们会发现这样一段 <code>argsHolderToUse.storeCache(mbd, factoryMethodToUse)</code> 代码。这段代码主要是将构造函数、构造参数保存到缓存中:</p><pre><code class="hljs java"><span class="hljs-comment">// ConstructorResolver.ArgumentsHolder.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">storeCache</span><span class="hljs-params">(RootBeanDefinition mbd, Executable constructorOrFactoryMethod)</span> </span>&#123;<span class="hljs-keyword">synchronized</span> (mbd.constructorArgumentLock) &#123;mbd.resolvedConstructorOrFactoryMethod = constructorOrFactoryMethod;mbd.constructorArgumentsResolved = <span class="hljs-keyword">true</span>;<span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.resolveNecessary) &#123;mbd.preparedConstructorArguments = <span class="hljs-keyword">this</span>.preparedArguments;&#125; <span class="hljs-keyword">else</span> &#123;mbd.resolvedConstructorArguments = <span class="hljs-keyword">this</span>.arguments;&#125;&#125;&#125;<span class="hljs-comment">// RootBeanDefinition.java</span><span class="hljs-comment">// 构造函数的缓存锁</span><span class="hljs-keyword">final</span> Object constructorArgumentLock = <span class="hljs-keyword">new</span> Object();<span class="hljs-comment">// 缓存已经解析的构造函数或者工厂方法</span><span class="hljs-meta">@Nullable</span>Executable resolvedConstructorOrFactoryMethod;<span class="hljs-comment">// 标记字段，标记构造函数、参数已经解析了。默认为 `false`</span><span class="hljs-keyword">boolean</span> constructorArgumentsResolved = <span class="hljs-keyword">false</span>;<span class="hljs-comment">// 缓存已经解析的构造函数参数，包可见字段。</span><span class="hljs-meta">@Nullable</span>Object[] resolvedConstructorArguments;</code></pre><p>从缓存中获取就是提取以上几个参数的值。</p><pre><code class="hljs java"><span class="hljs-comment">// ConstructorResolver.java</span><span class="hljs-comment">// 没有指定，则尝试从配置文件中解析</span>Object[] argsToResolve = <span class="hljs-keyword">null</span>;<span class="hljs-comment">// &lt;2.2&gt; 首先尝试从缓存中获取</span><span class="hljs-keyword">synchronized</span> (mbd.constructorArgumentLock) &#123;    <span class="hljs-comment">// 获取缓存中的构造函数或者工厂方法</span>    factoryMethodToUse = (Method) mbd.resolvedConstructorOrFactoryMethod;    <span class="hljs-keyword">if</span> (factoryMethodToUse != <span class="hljs-keyword">null</span> &amp;&amp; mbd.constructorArgumentsResolved) &#123;        <span class="hljs-comment">// Found a cached factory method...</span>        <span class="hljs-comment">// 获取缓存中的构造参数</span>        argsToUse = mbd.resolvedConstructorArguments;        <span class="hljs-keyword">if</span> (argsToUse == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">// 获取缓存中的构造函数参数的包可见字段</span>            argsToResolve = mbd.preparedConstructorArguments;        &#125;    &#125;&#125;<span class="hljs-comment">// 缓存中存在,则解析存储在 BeanDefinition 中的参数</span><span class="hljs-comment">// 如给定方法的构造函数 A(int ,int )，则通过此方法后就会把配置文件中的(&quot;1&quot;,&quot;1&quot;)转换为 (1,1)</span><span class="hljs-comment">// 缓存中的值可能是原始值也有可能是最终值</span><span class="hljs-keyword">if</span> (argsToResolve != <span class="hljs-keyword">null</span>) &#123;    argsToUse = resolvePreparedArguments(beanName, mbd, bw, factoryMethodToUse, argsToResolve, <span class="hljs-keyword">true</span>);&#125;</code></pre><ul><li>如果缓存中存在构造参数，则需要调用<code>resolvePreparedArguments(String beanName, RootBeanDefinition mbd, BeanWrapper bw, Executable executable, Object[] argsToResolve, boolean fallback)</code>方法，进行转换。</li><li>因为缓存中的值有可能是最终值，<strong>也有可能不是最终值</strong>。比如我们构造函数中的类型为 Integer 类型的 1 ，但是原始的参数类型有可能是 String 类型的 <code>&quot;1&quot;</code> ，所以即便是从缓存中得到了构造参数，也<strong>需要经过一番的类型转换确保参数类型完全对应</strong>。</li></ul><p>&lt;2.3&gt;配置文件中解析</p><p>即没有通过传递参数的方式传递构造参数，缓存中也没有，那就只能通过解析配置文件获取构造参数了。在 bean 解析类的博文中我们了解了，配置文件中的信息都会转换到 BeanDefinition 实例对象中，所以配置文件中的参数可以直接通过 BeanDefinition 对象获取。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-comment">// &lt;2.3&gt; getBean() 没有传递参数，则需要解析保存在 BeanDefinition 构造函数中指定的参数</span><span class="hljs-keyword">if</span> (mbd.hasConstructorArgumentValues()) &#123;    <span class="hljs-comment">// &lt;2.3.1&gt; 构造函数的参数</span>    ConstructorArgumentValues cargs = mbd.getConstructorArgumentValues();    resolvedValues = <span class="hljs-keyword">new</span> ConstructorArgumentValues();    <span class="hljs-comment">// &lt;2.3.2&gt; 解析构造函数的参数</span>    <span class="hljs-comment">// 将该 bean 的构造函数参数解析为 resolvedValues 对象，其中会涉及到其他 bean</span>    minNrOfArgs = resolveConstructorArguments(beanName, mbd, bw, cargs, resolvedValues);&#125;</code></pre><ul><li><code>&lt;2.3.1&gt;</code> ，通过 BeanDefinition 的 <code>getConstructorArgumentValues()</code> 方法，就可以获取构造信息了。</li><li><code>&lt;2.3.2&gt;</code> ，有了构造信息就可以获取相关的参数值信息了，获取的参数信息包括直接值和引用，这一步骤的处理交由 <code>resolveConstructorArguments(String beanName, RootBeanDefinition mbd, BeanWrapper bw, ConstructorArgumentValues cargs, ConstructorArgumentValues resolvedValues)</code> 方法来完成。该方法会将构造参数信息解析为 <code>resolvedValues</code> 对象 并返回解析到的参数个数 <code>minNrOfArgs</code> 。</li></ul></li></ul></li><li><p>&lt;3&gt;<strong>构造函数</strong></p><p>确定构造参数后，下一步则是确定构造函数。</p><ul><li>第一步，是通过 <code>getCandidateMethods()</code> 方法，获取所有的构造方法，同时对构造方法进行刷选。</li><li>然后，在对其进行排序处理（<code>candidates.sort(AutowireUtils.EXECUTABLE_COMPARATOR)</code>）。排序的主要目的，是为了能够<strong>更加方便的</strong>找到匹配的构造函数，因为构造函数的确认是根据参数个数确认的。排序的规则是：先按照 <code>public</code> / 非 <code>public</code> 构造函数<strong>升序</strong>，再按照构造参数数量<strong>降序</strong>。</li></ul><p>通过迭代 <code>candidates</code>（包含了所有要匹配的构造函数）的方式，依次比较其参数：</p><ul><li>如果显示提供了参数（<code>explicitArgs != null</code>），则直接比较两者<strong>长度</strong>是否相等，如果相等则表示找到了，否则继续比较。</li><li>如果没有显示提供参数，则需要获取 <code>org.springframework.core.ParameterNameDiscoverer</code> 对象。该对象为参数名称探测器，主要用于发现方法和构造函数的参数名称。</li></ul><p>将参数包装成 <code>ConstructorResolver.ArgumentsHolder</code> 对象。该对象用于保存参数，我们称之为参数持有者。当将对象包装成 ArgumentsHolder 对象后，我们就可以通过它来进行构造函数<strong>匹配</strong>。匹配分为严格模式和宽松模式：</p><ul><li>严格模式：解析构造函数时，必须所有参数都需要匹配，否则抛出异常。</li><li>宽松模式：使用具有”最接近的模式”进行匹配。</li></ul><p>判断的依据是根据 BeanDefinition 的 <code>isLenientConstructorResolution</code> 属性（该参数是我们在构造 AbstractBeanDefinition 对象是传递的）来获取类型差异权重（<code>typeDiffWeight</code>） 的。</p><ul><li>如果 <code>typeDiffWeight &lt; minTypeDiffWeight</code> ，则代表“最接近的模式”，选择其作为构造函数。</li><li>否则，只有两者具有相同的参数数量，且类型差异权重相等才会纳入考虑范围。</li></ul></li><li><p><strong>&lt;4&gt;创建Bean实例</strong></p><p>工厂对象、构造函数、构造参数都已经确认了，则最后一步就是调用 <code>org.springframework.beans.factory.support.InstantiationStrategy</code> 对象的 <code>.instantiate(RootBeanDefinition bd, String beanName, BeanFactory owner, Object factoryBean, final Method factoryMethod, @Nullable Object... args)</code> 方法，来创建 bean 实例。</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">instantiate</span><span class="hljs-params">(RootBeanDefinition bd, <span class="hljs-meta">@Nullable</span> String beanName, BeanFactory owner,<span class="hljs-meta">@Nullable</span> Object factoryBean, <span class="hljs-keyword">final</span> Method factoryMethod, Object... args)</span> </span>&#123;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// 设置 Method 可访问</span>        <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123;            AccessController.doPrivileged((PrivilegedAction&lt;Object&gt;) () -&gt; &#123;                ReflectionUtils.makeAccessible(factoryMethod);                <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;            &#125;);        &#125;        <span class="hljs-keyword">else</span> &#123;            ReflectionUtils.makeAccessible(factoryMethod);        &#125;        <span class="hljs-comment">// 获得原 Method 对象</span>        Method priorInvokedFactoryMethod = currentlyInvokedFactoryMethod.get();        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">// 设置新的 Method 对象，到 currentlyInvokedFactoryMethod 中</span>            currentlyInvokedFactoryMethod.set(factoryMethod);            <span class="hljs-comment">// &lt;x&gt; 创建 Bean 对象</span>            Object result = factoryMethod.invoke(factoryBean, args);            <span class="hljs-comment">// 未创建，则创建 NullBean 对象</span>            <span class="hljs-keyword">if</span> (result == <span class="hljs-keyword">null</span>) &#123;                result = <span class="hljs-keyword">new</span> NullBean();            &#125;            <span class="hljs-keyword">return</span> result;        &#125;        <span class="hljs-keyword">finally</span> &#123;            <span class="hljs-comment">// 设置老的 Method 对象，到 currentlyInvokedFactoryMethod 中</span>            <span class="hljs-keyword">if</span> (priorInvokedFactoryMethod != <span class="hljs-keyword">null</span>) &#123;                currentlyInvokedFactoryMethod.set(priorInvokedFactoryMethod);            &#125;            <span class="hljs-keyword">else</span> &#123;                currentlyInvokedFactoryMethod.remove();            &#125;        &#125;    &#125;    <span class="hljs-keyword">catch</span> (IllegalArgumentException ex) &#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInstantiationException(factoryMethod,                                             <span class="hljs-string">&quot;Illegal arguments to factory method &#x27;&quot;</span> + factoryMethod.getName() + <span class="hljs-string">&quot;&#x27;; &quot;</span> +                                             <span class="hljs-string">&quot;args: &quot;</span> + StringUtils.arrayToCommaDelimitedString(args), ex);    &#125;    <span class="hljs-keyword">catch</span> (IllegalAccessException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInstantiationException(factoryMethod,                                             <span class="hljs-string">&quot;Cannot access factory method &#x27;&quot;</span> + factoryMethod.getName() + <span class="hljs-string">&quot;&#x27;; is it public?&quot;</span>, ex);    &#125;    <span class="hljs-keyword">catch</span> (InvocationTargetException ex) &#123;        String msg = <span class="hljs-string">&quot;Factory method &#x27;&quot;</span> + factoryMethod.getName() + <span class="hljs-string">&quot;&#x27; threw exception&quot;</span>;        <span class="hljs-keyword">if</span> (bd.getFactoryBeanName() != <span class="hljs-keyword">null</span> &amp;&amp; owner <span class="hljs-keyword">instanceof</span> ConfigurableBeanFactory &amp;&amp;            ((ConfigurableBeanFactory) owner).isCurrentlyInCreation(bd.getFactoryBeanName())) &#123;            msg = <span class="hljs-string">&quot;Circular reference involving containing bean &#x27;&quot;</span> + bd.getFactoryBeanName() + <span class="hljs-string">&quot;&#x27; - consider &quot;</span> +                <span class="hljs-string">&quot;declaring the factory method as static for independence from its containing instance. &quot;</span> + msg;        &#125;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanInstantiationException(factoryMethod, msg, ex.getTargetException());    &#125;&#125;</code></pre><p>核心的部分，在于 <code>&lt;x&gt;</code> 处，利用 Java 反射执行工厂方法并返回创建好的实例：</p><pre><code class="hljs java"><span class="hljs-comment">// SimpleInstantiationStrategy.java</span>Object result = factoryMethod.invoke(factoryBean, args);</code></pre></li></ul>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>创建Bean之主流程</title>
    <link href="/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E4%B8%BB%E6%B5%81%E7%A8%8B/"/>
    <url>/2021/02/24/%E5%88%9B%E5%BB%BABean%E4%B9%8B%E4%B8%BB%E6%B5%81%E7%A8%8B/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">创建Bean之主流程</font></div><p>承接各个scope创建bean，不论那个scope，创建Bean都会以自己的方式执行<code>createBean()</code>方法。</p><h4 id="createBean-抽象方法"><a href="#createBean-抽象方法" class="headerlink" title="createBean()抽象方法"></a><code>createBean()</code>抽象方法</h4><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">abstract</span> Object <span class="hljs-title">createBean</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> Object[] args)</span> <span class="hljs-keyword">throws</span> BeanCreationException</span>;</code></pre><ul><li>该方法定义在 AbstractBeanFactory 中，其含义是根据给定的 <code>BeanDefinition</code> 和 <code>args</code> 实例化一个 Bean 对象。</li><li>如果该 BeanDefinition 存在父类，则该 BeanDefinition 已经合并了父类的属性。</li><li>所有 Bean 实例的创建，都会委托给该方法实现。</li><li>该方法接受三个方法参数：<ul><li><code>beanName</code> ：bean 的名字。</li><li><code>mbd</code> ：已经合并了父类属性的（如果有的话）BeanDefinition 对象。</li><li><code>args</code> ：用于构造函数或者工厂方法创建 Bean 实例对象的参数。</li></ul></li></ul><h4 id="createBean-默认实现"><a href="#createBean-默认实现" class="headerlink" title="createBean()默认实现"></a><code>createBean()</code>默认实现</h4><p>该抽象方法的默认实现是在类 AbstractAutowireCapableBeanFactory 中实现：</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">createBean</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> Object[] args)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> BeanCreationException </span>&#123;    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;        logger.trace(<span class="hljs-string">&quot;Creating instance of bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);    &#125;    RootBeanDefinition mbdToUse = mbd;    <span class="hljs-comment">// &lt;1&gt; 确保此时的 bean 已经被解析了</span>    <span class="hljs-comment">// 如果获取的class 属性不为null，则克隆该 BeanDefinition</span>    <span class="hljs-comment">// 主要是因为该动态解析的 class 无法保存到到共享的 BeanDefinition</span>    Class&lt;?&gt; resolvedClass = resolveBeanClass(mbd, beanName);    <span class="hljs-keyword">if</span> (resolvedClass != <span class="hljs-keyword">null</span> &amp;&amp; !mbd.hasBeanClass() &amp;&amp; mbd.getBeanClassName() != <span class="hljs-keyword">null</span>) &#123;        mbdToUse = <span class="hljs-keyword">new</span> RootBeanDefinition(mbd);        mbdToUse.setBeanClass(resolvedClass);    &#125;    <span class="hljs-comment">// &lt;2&gt; 验证和准备覆盖方法</span>    <span class="hljs-keyword">try</span> &#123;        mbdToUse.prepareMethodOverrides();    &#125;    <span class="hljs-keyword">catch</span> (BeanDefinitionValidationException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(mbdToUse.getResourceDescription(),                                               beanName, <span class="hljs-string">&quot;Validation of method overrides failed&quot;</span>, ex);    &#125;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// &lt;3&gt; 实例化的前置处理</span>        <span class="hljs-comment">// 给 BeanPostProcessors 一个机会用来返回一个代理类而不是真正的类实例</span>        <span class="hljs-comment">// AOP 的功能就是基于这个地方</span>        Object bean = resolveBeforeInstantiation(beanName, mbdToUse);        <span class="hljs-keyword">if</span> (bean != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> bean;        &#125;    &#125;    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbdToUse.getResourceDescription(), beanName,                                        <span class="hljs-string">&quot;BeanPostProcessor before instantiation of bean failed&quot;</span>, ex);    &#125;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// &lt;4&gt; 创建 Bean 对象</span>        Object beanInstance = doCreateBean(beanName, mbdToUse, args);        <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;            logger.trace(<span class="hljs-string">&quot;Finished creating instance of bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);        &#125;        <span class="hljs-keyword">return</span> beanInstance;    &#125;    <span class="hljs-keyword">catch</span> (BeanCreationException | ImplicitlyAppearedSingletonException ex) &#123;        <span class="hljs-keyword">throw</span> ex;    &#125;    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(            mbdToUse.getResourceDescription(), beanName, <span class="hljs-string">&quot;Unexpected exception during bean creation&quot;</span>, ex);    &#125;&#125;</code></pre><p>过程如下：</p><ul><li><code>&lt;1&gt;</code> 处，解析指定 BeanDefinition 的 class 属性。</li><li><code>&lt;2&gt;</code> 处，处理 <code>override</code> 属性。</li><li><code>&lt;3&gt;</code> 处，实例化的前置处理。</li><li><code>&lt;4&gt;</code> 处，创建 Bean 对象。</li></ul><h5 id="解析指定-BeanDefinition-的-class-属性"><a href="#解析指定-BeanDefinition-的-class-属性" class="headerlink" title="解析指定 BeanDefinition 的 class 属性"></a>解析指定 BeanDefinition 的 class 属性</h5><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span>Class&lt;?&gt; resolvedClass = resolveBeanClass(mbd, beanName);<span class="hljs-keyword">if</span> (resolvedClass != <span class="hljs-keyword">null</span> &amp;&amp; !mbd.hasBeanClass() &amp;&amp; mbd.getBeanClassName() != <span class="hljs-keyword">null</span>) &#123;    mbdToUse = <span class="hljs-keyword">new</span> RootBeanDefinition(mbd);    mbdToUse.setBeanClass(resolvedClass);&#125;</code></pre><ul><li><code>resolveBeanClass(final RootBeanDefinition mbd, String beanName, final Class... typesToMatch)</code> 方法，主要是解析 bean definition 的 class 类，并将已经解析的 Class 存储在 bean definition 中以供后面使用。</li><li>如果解析的 class 不为空，则会将该 BeanDefinition 进行设置到 <code>mbdToUse</code> 中。这样做的主要目的是，以为动态解析的 class 是无法保存到共享的 BeanDefinition 中。</li></ul><h5 id="处理override属性"><a href="#处理override属性" class="headerlink" title="处理override属性"></a>处理<code>override</code>属性</h5><p> <code>lookup-method</code> 和 <code>replace-method</code> 这两个标签的用法和解析过程之前有分析，这两个配置存放在 BeanDefinition 中的 <code>methodOverrides</code> 属性中。</p><p>我们知道在 bean 实例化的过程中如果检测到存在 <code>methodOverrides</code> ，则会动态地位为当前 bean 生成代理并使用对应的拦截器为 bean 做<strong>增强处理</strong>。</p><p>之后会做具体分析，此处先看<code> mbdToUse.prepareMethodOverrides();</code>：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanDefinition.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">prepareMethodOverrides</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> BeanDefinitionValidationException </span>&#123;    <span class="hljs-comment">// Check that lookup methods exist and determine their overloaded status.</span>    <span class="hljs-keyword">if</span> (hasMethodOverrides()) &#123;        <span class="hljs-comment">// 循环，执行 prepareMethodOverride，之前版本执行forEach的时候会先synchronized加锁</span>        getMethodOverrides().getOverrides().forEach(<span class="hljs-keyword">this</span>::prepareMethodOverride);    &#125;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> MethodOverrides <span class="hljs-title">getMethodOverrides</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.methodOverrides;&#125;<span class="hljs-keyword">private</span> MethodOverrides methodOverrides = <span class="hljs-keyword">new</span> MethodOverrides();<span class="hljs-comment">// MethodOverrides.java</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Set&lt;MethodOverride&gt; overrides = <span class="hljs-keyword">new</span> CopyOnWriteArraySet&lt;&gt;();</code></pre><p>如果存在 <code>methodOverrides</code> ，则获取所有的 override method ，然后通过迭代的方法一次调用 <code>prepareMethodOverride(MethodOverride mo)</code> 方法：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">prepareMethodOverride</span><span class="hljs-params">(MethodOverride mo)</span> <span class="hljs-keyword">throws</span> BeanDefinitionValidationException </span>&#123;    <span class="hljs-keyword">int</span> count = ClassUtils.getMethodCountForName(getBeanClass(), mo.getMethodName());    <span class="hljs-keyword">if</span> (count == <span class="hljs-number">0</span>) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionValidationException(            <span class="hljs-string">&quot;Invalid method override: no method with name &#x27;&quot;</span> + mo.getMethodName() +            <span class="hljs-string">&quot;&#x27; on class [&quot;</span> + getBeanClassName() + <span class="hljs-string">&quot;]&quot;</span>);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (count == <span class="hljs-number">1</span>) &#123;        <span class="hljs-comment">// Mark override as not overloaded, to avoid the overhead of arg type checking.</span>        mo.setOverloaded(<span class="hljs-keyword">false</span>);    &#125;&#125;</code></pre><ul><li>根据方法名称，从 class 中获取该方法名的个数：<ul><li>如果个数为 0 ，则抛出 BeanDefinitionValidationException 异常。</li><li>如果个数为 1 ，则设置该重载方法<strong>没有被重载</strong>。</li></ul></li></ul><ul><li>若一个类中存在<strong>多个</strong>重载方法，则在方法调用的时候还需要根据参数类型来判断到底重载的是哪个方法。在设置重载的时候其实这里做了一个<strong>小小优化</strong>，那就是当 <code>count == 1</code> 时，设置 <code>overloaded = false</code> ，这样表示该方法没有重载。这样，在后续调用的时候，便可以直接找到方法而不需要进行方法参数的校验。</li></ul><h5 id="实例化的前置处理"><a href="#实例化的前置处理" class="headerlink" title="实例化的前置处理"></a>实例化的前置处理</h5><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanDefinition.java</span><span class="hljs-comment">// &lt;3&gt; 实例化的前置处理</span><span class="hljs-comment">// 给 BeanPostProcessors 一个机会用来返回一个代理类而不是真正的类实例</span><span class="hljs-comment">// AOP 的功能就是基于这个地方</span>Object bean = resolveBeforeInstantiation(beanName, mbdToUse);<span class="hljs-keyword">if</span> (bean != <span class="hljs-keyword">null</span>) &#123;    <span class="hljs-keyword">return</span> bean;&#125;</code></pre><p>如果代理对象不为空，则直接返回代理对象，这一步骤有非常重要的作用，Spring 后续实现 AOP 就是基于这个地方判断的。</p><p><code>resolveBeforeInstantiation()</code>如下：</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">resolveBeforeInstantiation</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd)</span> </span>&#123;    Object bean = <span class="hljs-keyword">null</span>;    <span class="hljs-keyword">if</span> (!Boolean.FALSE.equals(mbd.beforeInstantiationResolved)) &#123;        <span class="hljs-comment">// Make sure bean class is actually resolved at this point.</span>        <span class="hljs-keyword">if</span> (!mbd.isSynthetic() &amp;&amp; hasInstantiationAwareBeanPostProcessors()) &#123;            Class&lt;?&gt; targetType = determineTargetType(beanName, mbd);            <span class="hljs-keyword">if</span> (targetType != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">//前置</span>                bean = applyBeanPostProcessorsBeforeInstantiation(targetType, beanName);                <span class="hljs-keyword">if</span> (bean != <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-comment">//后置</span>                    bean = applyBeanPostProcessorsAfterInitialization(bean, beanName);                &#125;            &#125;        &#125;        mbd.beforeInstantiationResolved = (bean != <span class="hljs-keyword">null</span>);    &#125;    <span class="hljs-keyword">return</span> bean;&#125;</code></pre><p>这个方法核心就在于 <code>applyBeanPostProcessorsBeforeInstantiation()</code> 和 <code>applyBeanPostProcessorsAfterInitialization()</code> 两个方法，before 为实例化前的后处理器应用，after 为实例化后的后处理器应用。</p><h5 id="执行doCreateBean-创建Bean"><a href="#执行doCreateBean-创建Bean" class="headerlink" title="执行doCreateBean()创建Bean"></a>执行<code>doCreateBean()</code>创建Bean</h5><p>如果没有代理对象，就只能走常规的路线进行 bean 的创建了，该过程有 <code>doCreateBean(final String beanName, final RootBeanDefinition mbd, final @Nullable Object[] args)</code> 方法来实现。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractAutowireCapableBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">doCreateBean</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> Object[] args)</span> <span class="hljs-keyword">throws</span> BeanCreationException </span>&#123;    <span class="hljs-comment">// Instantiate the bean.</span>    <span class="hljs-comment">// BeanWrapper 是对 Bean 的包装，其接口中所定义的功能很简单包括设置获取被包装的对象，获取被包装 bean 的属性描述器</span>    BeanWrapper instanceWrapper = <span class="hljs-keyword">null</span>;    <span class="hljs-comment">// &lt;1&gt; 单例模型，则从未完成的 FactoryBean 缓存中删除</span>    <span class="hljs-keyword">if</span> (mbd.isSingleton()) &#123;        instanceWrapper = <span class="hljs-keyword">this</span>.factoryBeanInstanceCache.remove(beanName);    &#125;    <span class="hljs-comment">// &lt;2&gt; 使用合适的实例化策略来创建新的实例：工厂方法、构造函数自动注入、简单初始化</span>    <span class="hljs-keyword">if</span> (instanceWrapper == <span class="hljs-keyword">null</span>) &#123;        instanceWrapper = createBeanInstance(beanName, mbd, args);    &#125;    <span class="hljs-comment">// 包装的实例对象</span>    Object bean = instanceWrapper.getWrappedInstance();    <span class="hljs-comment">// 包装的实例对象的类型</span>    Class&lt;?&gt; beanType = instanceWrapper.getWrappedClass();    <span class="hljs-keyword">if</span> (beanType != NullBean.class) &#123;        mbd.resolvedTargetType = beanType;    &#125;    <span class="hljs-comment">// Allow post-processors to modify the merged bean definition.</span>    <span class="hljs-comment">// &lt;3&gt; 判断是否有后置处理</span>    <span class="hljs-comment">// 如果有后置处理，则允许后置处理修改 BeanDefinition</span>    <span class="hljs-keyword">synchronized</span> (mbd.postProcessingLock) &#123;        <span class="hljs-keyword">if</span> (!mbd.postProcessed) &#123;            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">// 后置处理修改 BeanDefinition</span>                applyMergedBeanDefinitionPostProcessors(mbd, beanType, beanName);            &#125;            <span class="hljs-keyword">catch</span> (Throwable ex) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                                                <span class="hljs-string">&quot;Post-processing of merged bean definition failed&quot;</span>, ex);            &#125;            mbd.postProcessed = <span class="hljs-keyword">true</span>;        &#125;    &#125;    <span class="hljs-comment">// Eagerly cache singletons to be able to resolve circular references</span>    <span class="hljs-comment">// even when triggered by lifecycle interfaces like BeanFactoryAware.</span>    <span class="hljs-comment">// &lt;4&gt; 解决单例模式的循环依赖</span>    <span class="hljs-keyword">boolean</span> earlySingletonExposure = (mbd.isSingleton() &amp;&amp; <span class="hljs-comment">// 单例模式</span>                                      <span class="hljs-keyword">this</span>.allowCircularReferences &amp;&amp; <span class="hljs-comment">// 运行循环依赖</span>                                      isSingletonCurrentlyInCreation(beanName));<span class="hljs-comment">// 当前单例 bean 是否正在被创建</span>    <span class="hljs-keyword">if</span> (earlySingletonExposure) &#123;        <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;            logger.trace(<span class="hljs-string">&quot;Eagerly caching bean &#x27;&quot;</span> + beanName +                         <span class="hljs-string">&quot;&#x27; to allow for resolving potential circular references&quot;</span>);        &#125;        <span class="hljs-comment">// 提前将创建的 bean 实例加入到 singletonFactories 中</span>        <span class="hljs-comment">// 这里是为了后期避免循环依赖</span>        addSingletonFactory(beanName, () -&gt; getEarlyBeanReference(beanName, mbd, bean));    &#125;    <span class="hljs-comment">// Initialize the bean instance.</span>    <span class="hljs-comment">// 开始初始化 bean 实例对象</span>    Object exposedObject = bean;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// &lt;5&gt; 对 bean 进行填充，将各个属性值注入，其中，可能存在依赖于其他 bean 的属性</span>        <span class="hljs-comment">// 则会递归初始依赖 bean</span>        populateBean(beanName, mbd, instanceWrapper);        <span class="hljs-comment">// &lt;6&gt; 调用初始化方法</span>        exposedObject = initializeBean(beanName, exposedObject, mbd);    &#125;    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">if</span> (ex <span class="hljs-keyword">instanceof</span> BeanCreationException &amp;&amp; beanName.equals(((BeanCreationException) ex).getBeanName())) &#123;            <span class="hljs-keyword">throw</span> (BeanCreationException) ex;        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(                mbd.getResourceDescription(), beanName, <span class="hljs-string">&quot;Initialization of bean failed&quot;</span>, ex);        &#125;    &#125;    <span class="hljs-comment">// &lt;7&gt; 循环依赖处理</span>    <span class="hljs-keyword">if</span> (earlySingletonExposure) &#123;        <span class="hljs-comment">// 获取 earlySingletonReference</span>        Object earlySingletonReference = getSingleton(beanName, <span class="hljs-keyword">false</span>);        <span class="hljs-comment">// 只有在存在循环依赖的情况下，earlySingletonReference 才不会为空</span>        <span class="hljs-keyword">if</span> (earlySingletonReference != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">// 如果 exposedObject 没有在初始化方法中被改变，也就是没有被增强</span>            <span class="hljs-keyword">if</span> (exposedObject == bean) &#123;                exposedObject = earlySingletonReference;                <span class="hljs-comment">// 处理依赖</span>            &#125;            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.allowRawInjectionDespiteWrapping &amp;&amp; hasDependentBean(beanName)) &#123;                String[] dependentBeans = getDependentBeans(beanName);                Set&lt;String&gt; actualDependentBeans = <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;(dependentBeans.length);                <span class="hljs-keyword">for</span> (String dependentBean : dependentBeans) &#123;                    <span class="hljs-keyword">if</span> (!removeSingletonIfCreatedForTypeCheckOnly(dependentBean)) &#123;                        actualDependentBeans.add(dependentBean);                    &#125;                &#125;                <span class="hljs-keyword">if</span> (!actualDependentBeans.isEmpty()) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCurrentlyInCreationException(beanName,                                                               <span class="hljs-string">&quot;Bean with name &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; has been injected into other beans [&quot;</span> +                                                               StringUtils.collectionToCommaDelimitedString(actualDependentBeans) +                                                               <span class="hljs-string">&quot;] in its raw version as part of a circular reference, but has eventually been &quot;</span> +                                                               <span class="hljs-string">&quot;wrapped. This means that said other beans do not use the final version of the &quot;</span> +                                                               <span class="hljs-string">&quot;bean. This is often the result of over-eager type matching - consider using &quot;</span> +                                                               <span class="hljs-string">&quot;&#x27;getBeanNamesForType&#x27; with the &#x27;allowEagerInit&#x27; flag turned off, for example.&quot;</span>);                &#125;            &#125;        &#125;    &#125;    <span class="hljs-comment">// Register bean as disposable.</span>    <span class="hljs-comment">// &lt;8&gt; 注册 bean</span>    <span class="hljs-keyword">try</span> &#123;        registerDisposableBeanIfNecessary(beanName, bean, mbd);    &#125;    <span class="hljs-keyword">catch</span> (BeanDefinitionValidationException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(            mbd.getResourceDescription(), beanName, <span class="hljs-string">&quot;Invalid destruction signature&quot;</span>, ex);    &#125;    <span class="hljs-keyword">return</span> exposedObject;&#125;</code></pre><p>整体流程：</p><ul><li><code>&lt;1&gt;</code> 处，如果是单例模式，则清除缓存。</li><li><code>&lt;2&gt;</code> 处，调用 <code>createBeanInstance(String beanName, RootBeanDefinition mbd, Object[] args)</code> 方法，实例化 bean ，主要是将 BeanDefinition 转换为 <code>org.springframework.beans.BeanWrapper</code> 对象。</li><li><code>&lt;3&gt;</code> 处，MergedBeanDefinitionPostProcessor 的应用。</li><li><code>&lt;4&gt;</code> 处，单例模式的循环依赖处理。</li><li><code>&lt;5&gt;</code> 处，调用 <code>populateBean(String beanName, RootBeanDefinition mbd, BeanWrapper bw)</code> 方法，进行属性填充。将所有属性填充至 bean 的实例中。</li><li><code>&lt;6&gt;</code> 处，调用 <code>initializeBean(final String beanName, final Object bean, RootBeanDefinition mbd)</code> 方法，初始化 bean 。</li><li><code>&lt;7&gt;</code> 处，依赖检查。</li><li><code>&lt;8&gt;</code> 处，注册 DisposableBean 。</li></ul>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>各scope的Bean创建</title>
    <link href="/2021/02/24/%E5%90%84scope%E7%9A%84Bean%E5%88%9B%E5%BB%BA/"/>
    <url>/2021/02/24/%E5%90%84scope%E7%9A%84Bean%E5%88%9B%E5%BB%BA/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">各scope的Bean创建</font></div><p>﻿在 Spring 中存在着不同的 scope，默认是 singleton ，还有 prototype、request 等其他的 scope 。</p><h4 id="singleton"><a href="#singleton" class="headerlink" title="singleton"></a><code>singleton</code></h4><p>Spring 的 scope 默认为 singleton ：</p><pre><code class="hljs java"><span class="hljs-comment">//AbstractBeanFactory.java</span><span class="hljs-keyword">if</span> (mbd.isSingleton()) &#123;<span class="hljs-comment">// 单例模式</span>    sharedInstance = getSingleton(beanName, () -&gt; &#123;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-keyword">return</span> createBean(beanName, mbd, args);        &#125;        <span class="hljs-keyword">catch</span> (BeansException ex) &#123;            destroySingleton(beanName);            <span class="hljs-keyword">throw</span> ex;        &#125;    &#125;);    beanInstance = getObjectForBeanInstance(sharedInstance, name, beanName, mbd);&#125;</code></pre><p>如果缓存中<strong>不存在</strong>bean呢？则需要从头开始加载 Bean ，这个过程由 <code>getSingleton(String beanName, ObjectFactory singletonFactory)</code> 方法来实现。</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">getSingleton</span><span class="hljs-params">(String beanName, ObjectFactory&lt;?&gt; singletonFactory)</span> </span>&#123;    Assert.notNull(beanName, <span class="hljs-string">&quot;Bean name must not be null&quot;</span>);    <span class="hljs-comment">// 全局加锁</span>    <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.singletonObjects) &#123;        <span class="hljs-comment">// &lt;1&gt; 从缓存中检查一遍</span>        <span class="hljs-comment">// 因为 singleton 模式其实就是复用已经创建的 bean 所以这步骤必须检查</span>        Object singletonObject = <span class="hljs-keyword">this</span>.singletonObjects.get(beanName);        <span class="hljs-comment">//  为空，开始加载过程</span>        <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.singletonsCurrentlyInDestruction) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationNotAllowedException(beanName,                                                          <span class="hljs-string">&quot;Singleton bean creation not allowed while singletons of this factory are in destruction &quot;</span> +                                                          <span class="hljs-string">&quot;(Do not request a bean from a BeanFactory in a destroy method implementation!)&quot;</span>);            &#125;            <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                logger.debug(<span class="hljs-string">&quot;Creating shared instance of singleton bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);            &#125;            <span class="hljs-comment">// &lt;2&gt; 加载前置处理</span>            beforeSingletonCreation(beanName);            <span class="hljs-keyword">boolean</span> newSingleton = <span class="hljs-keyword">false</span>;            <span class="hljs-keyword">boolean</span> recordSuppressedExceptions = (<span class="hljs-keyword">this</span>.suppressedExceptions == <span class="hljs-keyword">null</span>);            <span class="hljs-keyword">if</span> (recordSuppressedExceptions) &#123;                <span class="hljs-keyword">this</span>.suppressedExceptions = <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;();            &#125;            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">// &lt;3&gt; 初始化 bean</span>                <span class="hljs-comment">// 这个过程其实是调用 createBean() 方法</span>                singletonObject = singletonFactory.getObject();                newSingleton = <span class="hljs-keyword">true</span>;            &#125;            <span class="hljs-keyword">catch</span> (IllegalStateException ex) &#123;                <span class="hljs-comment">// Has the singleton object implicitly appeared in the meantime -&gt;</span>                <span class="hljs-comment">// if yes, proceed with it since the exception indicates that state.</span>                singletonObject = <span class="hljs-keyword">this</span>.singletonObjects.get(beanName);                <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-keyword">throw</span> ex;                &#125;            &#125;            <span class="hljs-keyword">catch</span> (BeanCreationException ex) &#123;                <span class="hljs-keyword">if</span> (recordSuppressedExceptions) &#123;                    <span class="hljs-keyword">for</span> (Exception suppressedException : <span class="hljs-keyword">this</span>.suppressedExceptions) &#123;                        ex.addRelatedCause(suppressedException);                    &#125;                &#125;                <span class="hljs-keyword">throw</span> ex;            &#125;            <span class="hljs-keyword">finally</span> &#123;                <span class="hljs-keyword">if</span> (recordSuppressedExceptions) &#123;                    <span class="hljs-keyword">this</span>.suppressedExceptions = <span class="hljs-keyword">null</span>;                &#125;                <span class="hljs-comment">// &lt;4&gt; 后置处理</span>                afterSingletonCreation(beanName);            &#125;            <span class="hljs-comment">// &lt;5&gt; 加入缓存中</span>            <span class="hljs-keyword">if</span> (newSingleton) &#123;                addSingleton(beanName, singletonObject);            &#125;        &#125;        <span class="hljs-keyword">return</span> singletonObject;    &#125;&#125;<span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">addSingleton</span><span class="hljs-params">(String beanName, Object singletonObject)</span> </span>&#123;    <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.singletonObjects) &#123;        <span class="hljs-keyword">this</span>.singletonObjects.put(beanName, singletonObject);<span class="hljs-comment">//添加到单例 bean 的缓存。</span>        <span class="hljs-keyword">this</span>.singletonFactories.remove(beanName);<span class="hljs-comment">//从单例 bean Factory 的缓存中移除。</span>        <span class="hljs-keyword">this</span>.earlySingletonObjects.remove(beanName);<span class="hljs-comment">//从预加载单例 bean 的缓存中移除。</span>        <span class="hljs-keyword">this</span>.registeredSingletons.add(beanName);<span class="hljs-comment">//添加到已经注册的单例缓存。</span>    &#125;&#125;</code></pre><p>这个过程并没有真正创建 Bean 对象，仅仅只是做了一部分准备和预处理步骤。真正获取单例 bean 的方法，其实是由 <code>&lt;3&gt;</code> 处的 <code>singletonFactory.getObject()</code> 这部分代码块来实现，而 <code>singletonFactory</code> 由回调方法产生。</p><p>该方法做的准备：</p><ul><li><code>&lt;1&gt;</code> 处，再次检查缓存是否已经加载过，如果已经加载了则直接返回，否则开始加载过程。</li><li><code>&lt;2&gt;</code> 处，调用 <code>beforeSingletonCreation(String beanName)</code> 方法，记录加载单例 bean 之前的加载状态，即前置处理，标记为正在创建中。在从缓存中获取单例Bean已经介绍。</li><li><code>&lt;3&gt;</code> 处，调用参数传递的 ObjectFactory 的 <code>getObject()</code> 方法，实例化 bean 。</li><li><code>&lt;4&gt;</code> 处，调用 <code>afterSingletonCreation(String beanName)</code> 方法，进行加载单例后的后置处理，删除标记为正在创建中。</li><li><code>&lt;5&gt;</code> 处，调用 <code>addSingleton(String beanName, Object singletonObject)</code> 方法，将结果记录并加入值���存中，同时删除加载 bean 过程中所记录的一些辅助状态。</li></ul><p>加载完单例Bean后，调用 <code>getObjectForBeanInstance(Object beanInstance, String name, String beanName, RootBeanDefinition mbd)</code> 方法，从 bean 实例中获取对象。在从缓存中获取单例Bean已经介绍。</p><h4 id="prototype"><a href="#prototype" class="headerlink" title="prototype"></a><code>prototype</code></h4><pre><code class="hljs java"><span class="hljs-comment">//AbstractBeanFactory.java</span><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (mbd.isPrototype()) &#123;<span class="hljs-comment">// 原型模式</span>    <span class="hljs-comment">// It&#x27;s a prototype -&gt; create a new instance.</span>    Object prototypeInstance = <span class="hljs-keyword">null</span>;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// &lt;1&gt; 加载前置处理</span>        beforePrototypeCreation(beanName);        <span class="hljs-comment">// &lt;2&gt; 创建 Bean 对象</span>        prototypeInstance = createBean(beanName, mbd, args);    &#125;    <span class="hljs-keyword">finally</span> &#123;        <span class="hljs-comment">// &lt;3&gt; 加载后置处理</span>        afterPrototypeCreation(beanName);    &#125;    <span class="hljs-comment">// &lt;4&gt; 从 Bean 实例中获取对象</span>    beanInstance = getObjectForBeanInstance(prototypeInstance, name, beanName, mbd);&#125;</code></pre><p>原型模式的初始化过程很简单：直接创建一个<strong>新</strong>的 Bean 的实例就可以了。</p><ul><li>在 <code>&lt;1&gt;</code> 处，调用 <code>beforePrototypeCreation(String beanName)</code> 方法，记录加载原型模式 bean 之前的加载状态，即前置处理。</li><li>在 <code>&lt;2&gt;</code> 处，调用 <code>createBean(String beanName)</code> 方法，创建一个 bean 实例对象。</li><li>在 <code>&lt;3&gt;</code> 处，调用 <code>afterSingletonCreation(String beanName)</code> 方法，进行加载原型模式 bean 后的后置处理。</li><li>在 <code>&lt;4&gt;</code> 处，加载了单例 bean 后，调用 <code>getObjectForBeanInstance(Object beanInstance, String name, String beanName, RootBeanDefinition mbd)</code> 方法，从 bean 实例中获取对象。在从缓存中获取单例Bean已经介绍。</li></ul><h5 id="beforePrototypeCreation-方法"><a href="#beforePrototypeCreation-方法" class="headerlink" title="beforePrototypeCreation()方法"></a><code>beforePrototypeCreation()</code>方法</h5><p>注册为当前正在创建。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">beforePrototypeCreation</span><span class="hljs-params">(String beanName)</span> </span>&#123;    Object curVal = <span class="hljs-keyword">this</span>.prototypesCurrentlyInCreation.get();    <span class="hljs-keyword">if</span> (curVal == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">this</span>.prototypesCurrentlyInCreation.set(beanName);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (curVal <span class="hljs-keyword">instanceof</span> String) &#123;        Set&lt;String&gt; beanNameSet = <span class="hljs-keyword">new</span> HashSet&lt;&gt;(<span class="hljs-number">2</span>);        beanNameSet.add((String) curVal);        beanNameSet.add(beanName);        <span class="hljs-keyword">this</span>.prototypesCurrentlyInCreation.set(beanNameSet);    &#125;    <span class="hljs-keyword">else</span> &#123;        Set&lt;String&gt; beanNameSet = (Set&lt;String&gt;) curVal;        beanNameSet.add(beanName);    &#125;&#125;</code></pre><h5 id="afterPrototypeCreation-方法"><a href="#afterPrototypeCreation-方法" class="headerlink" title="afterPrototypeCreation()方法"></a><code>afterPrototypeCreation()</code>方法</h5><p>标记为不在创建中。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">afterPrototypeCreation</span><span class="hljs-params">(String beanName)</span> </span>&#123;    Object curVal = <span class="hljs-keyword">this</span>.prototypesCurrentlyInCreation.get();    <span class="hljs-keyword">if</span> (curVal <span class="hljs-keyword">instanceof</span> String) &#123;        <span class="hljs-keyword">this</span>.prototypesCurrentlyInCreation.remove();    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (curVal <span class="hljs-keyword">instanceof</span> Set) &#123;        Set&lt;String&gt; beanNameSet = (Set&lt;String&gt;) curVal;        beanNameSet.remove(beanName);        <span class="hljs-keyword">if</span> (beanNameSet.isEmpty()) &#123;            <span class="hljs-keyword">this</span>.prototypesCurrentlyInCreation.remove();        &#125;    &#125;&#125;</code></pre><h4 id="其他scope"><a href="#其他scope" class="headerlink" title="其他scope"></a>其他scope</h4><pre><code class="hljs java"><span class="hljs-keyword">else</span> &#123;<span class="hljs-comment">// 从指定的 scope 下创建 bean</span>    <span class="hljs-comment">// 获得 scopeName 对应的 Scope 对象</span>    String scopeName = mbd.getScope();    <span class="hljs-keyword">if</span> (!StringUtils.hasLength(scopeName)) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">&quot;No scope name defined for bean ´&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);    &#125;    Scope scope = <span class="hljs-keyword">this</span>.scopes.get(scopeName);    <span class="hljs-keyword">if</span> (scope == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">&quot;No Scope registered for scope name &#x27;&quot;</span> + scopeName + <span class="hljs-string">&quot;&#x27;&quot;</span>);    &#125;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// 从指定的 scope 下创建 bean</span>        Object scopedInstance = scope.get(beanName, () -&gt; &#123;            <span class="hljs-comment">// 加载前置处理</span>            beforePrototypeCreation(beanName);            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">// 创建 Bean 对象</span>                <span class="hljs-keyword">return</span> createBean(beanName, mbd, args);            &#125;            <span class="hljs-keyword">finally</span> &#123;                <span class="hljs-comment">// 加载后缀处理</span>                afterPrototypeCreation(beanName);            &#125;        &#125;);        <span class="hljs-comment">// 从 Bean 实例中获取对象</span>        beanInstance = getObjectForBeanInstance(scopedInstance, name, beanName, mbd);    &#125;    <span class="hljs-keyword">catch</span> (IllegalStateException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ScopeNotActiveException(beanName, scopeName, ex);    &#125;&#125;</code></pre><p><strong>核心流程和原型模式一样</strong>，只不过获取 bean 实例是由 <code>Scope.get(String name, ObjectFactory objectFactory)</code> 方法来实现，处理方式和单例模式一样。</p><pre><code class="hljs java"><span class="hljs-comment">// SimpleThreadScope.java</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ThreadLocal&lt;Map&lt;String, Object&gt;&gt; threadScope =    <span class="hljs-keyword">new</span> NamedThreadLocal&lt;Map&lt;String, Object&gt;&gt;(<span class="hljs-string">&quot;SimpleThreadScope&quot;</span>) &#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">protected</span> Map&lt;String, Object&gt; <span class="hljs-title">initialValue</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> HashMap&lt;&gt;();    &#125;&#125;;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">get</span><span class="hljs-params">(String name, ObjectFactory&lt;?&gt; objectFactory)</span> </span>&#123;    <span class="hljs-comment">// 获取 scope 缓存</span>    Map&lt;String, Object&gt; scope = <span class="hljs-keyword">this</span>.threadScope.get();    Object scopedObject = scope.get(name);    <span class="hljs-keyword">if</span> (scopedObject == <span class="hljs-keyword">null</span>) &#123;        scopedObject = objectFactory.getObject();        <span class="hljs-comment">// 加入缓存</span>        scope.put(name, scopedObject);    &#125;    <span class="hljs-keyword">return</span> scopedObject;&#125;</code></pre><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>比较重要的两个：</p><ul><li><code>createBean(String beanName, RootBeanDefinition mbd, Object[] args)</code> 方法。</li><li><code>getObjectForBeanInstance(Object beanInstance, String name, String beanName, RootBeanDefinition mbd)</code> 方法。在从缓存中获取单例Bean已经介绍。</li></ul><p>以上两个方法在单例、原型、其他模式中都有调用。不过在单例和其他模式中，<code>createBean()</code>是以回调方式出现的。</p><blockquote><p><code>getObjectForBeanInstance()</code>这个方法主要是验证以下我们得到的 bean 的正确性，其实就是检测当前 bean 是否是 FactoryBean 类型的 bean 。</p><p>如果是，那么需要调用该 bean 对应的 FactoryBean 实例的 <code>getObject()</code> 方法，作为返回值。</p><p>无论是从缓存中获得到的 bean 还是通过不同的 scope 策略加载的 bean 都只是最原始的 bean 状态，并不一定就是我们最终想要的 bean。</p><p>举个例子，加入我们需要对工厂 bean 进行处理，那么这里得到的其实是工厂 bean 的初始状态，但是我们真正需要的是工厂 bean 中定义 <code>factory-method</code> 方法中返回的 bean，而 <code>getObjectForBeanInstance(Object beanInstance, String name, String beanName, RootBeanDefinition mbd)</code> 方法，就是完成这个工作的。</p></blockquote>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>parentBeanFactory和依赖处理</title>
    <link href="/2021/02/24/parentBeanFactory%E5%92%8C%E4%BE%9D%E8%B5%96%E5%A4%84%E7%90%86/"/>
    <url>/2021/02/24/parentBeanFactory%E5%92%8C%E4%BE%9D%E8%B5%96%E5%A4%84%E7%90%86/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">parentBeanFactory和依赖处理</font></div><p>如果从单例缓存中没有获取到单例 Bean 对象，则说明两种两种情况：</p><ol><li>该 Bean 的 scope 不是 singleton</li><li>该 Bean 的 scope 是 singleton ，但是没有初始化完成。</li></ol><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">//protected &lt;T&gt; T doGetBean(final String name, final Class&lt;T&gt; requiredType, final Object[] args, boolean typeCheckOnly)</span><span class="hljs-comment">// ... 省略很多代码</span><span class="hljs-comment">// &lt;3&gt; 因为 Spring 只解决单例模式下得循环依赖，在原型模式下如果存在循环依赖则会抛出异常。</span><span class="hljs-keyword">if</span> (isPrototypeCurrentlyInCreation(beanName)) &#123;    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCurrentlyInCreationException(beanName);&#125;<span class="hljs-comment">// &lt;4&gt; 如果容器中没有找到，则从父类容器中加载</span>BeanFactory parentBeanFactory = getParentBeanFactory();<span class="hljs-keyword">if</span> (parentBeanFactory != <span class="hljs-keyword">null</span> &amp;&amp; !containsBeanDefinition(beanName)) &#123;    String nameToLookup = originalBeanName(name);    <span class="hljs-keyword">if</span> (parentBeanFactory <span class="hljs-keyword">instanceof</span> AbstractBeanFactory) &#123;        <span class="hljs-keyword">return</span> ((AbstractBeanFactory) parentBeanFactory).doGetBean(                nameToLookup, requiredType, args, typeCheckOnly);    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (args != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// Delegation to parent with explicit args.</span>        <span class="hljs-keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup, args);    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (requiredType != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// No args -&gt; delegate to standard getBean method.</span>        <span class="hljs-keyword">return</span> parentBeanFactory.getBean(nameToLookup, requiredType);    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup);    &#125;&#125;<span class="hljs-comment">// &lt;5&gt; 如果不是仅仅做类型检查则是创建bean，这里需要记录</span><span class="hljs-keyword">if</span> (!typeCheckOnly) &#123;    markBeanAsCreated(beanName);&#125;<span class="hljs-keyword">try</span> &#123;    <span class="hljs-comment">// &lt;6&gt; 从容器中获取 beanName 相应的 GenericBeanDefinition 对象，并将其转换为 RootBeanDefinition 对象</span>    <span class="hljs-keyword">final</span> RootBeanDefinition mbd = getMergedLocalBeanDefinition(beanName);    <span class="hljs-comment">// 检查给定的合并的 BeanDefinition</span>    checkMergedBeanDefinition(mbd, beanName, args);    <span class="hljs-comment">// Guarantee initialization of beans that the current bean depends on.</span>    <span class="hljs-comment">// &lt;7&gt; 处理所依赖的 bean</span>    String[] dependsOn = mbd.getDependsOn();    <span class="hljs-keyword">if</span> (dependsOn != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">for</span> (String dep : dependsOn) &#123;            <span class="hljs-comment">// 若给定的依赖 bean 已经注册为依赖给定的 bean</span>            <span class="hljs-comment">// 循环依赖的情况</span>            <span class="hljs-keyword">if</span> (isDependent(beanName, dep)) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                        <span class="hljs-string">&quot;Circular depends-on relationship between &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; and &#x27;&quot;</span> + dep + <span class="hljs-string">&quot;&#x27;&quot;</span>);            &#125;            <span class="hljs-comment">// 缓存依赖调用</span>            registerDependentBean(dep, beanName);            <span class="hljs-keyword">try</span> &#123;                getBean(dep);            &#125; <span class="hljs-keyword">catch</span> (NoSuchBeanDefinitionException ex) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                        <span class="hljs-string">&quot;&#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; depends on missing bean &#x27;&quot;</span> + dep + <span class="hljs-string">&quot;&#x27;&quot;</span>, ex);            &#125;        &#125;    &#125;&#125;<span class="hljs-comment">// ... 省略很多代码</span></code></pre><p>该段主要处理了：</p><p>1、检测，若当前 Bean 在创建，则抛出 BeanCurrentlyInCreationException 异常。</p><p>2、如果 beanDefinitionMap 中不存在 beanName 的 BeanDefinition（即在 Spring bean 初始化过程中没有加载），则尝试从 parentBeanFactory 中加载。</p><p>3、判断是否为类型检查。</p><p>4、从 <code>mergedBeanDefinitions</code> 中获取 <code>beanName</code> 对应的 RootBeanDefinition 对象。如果这个 BeanDefinition 是子 Bean 的话，则会合并父类的相关属性。</p><p>5、依赖处理。</p><h4 id="检测"><a href="#检测" class="headerlink" title="检测"></a>检测</h4><p>Spring 只解决单例模式下的循环依赖，对于原型模式的循环依赖则是抛出 BeanCurrentlyInCreationException 异常，所以首先检查该 <code>beanName</code> 是否处于原型模式下的循环依赖。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">//正在创建的原型Bean</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ThreadLocal&lt;Object&gt; prototypesCurrentlyInCreation =    <span class="hljs-keyword">new</span> NamedThreadLocal&lt;&gt;(<span class="hljs-string">&quot;Prototype beans currently in creation&quot;</span>);<span class="hljs-keyword">if</span> (isPrototypeCurrentlyInCreation(beanName)) &#123;    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCurrentlyInCreationException(beanName);&#125;<span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isPrototypeCurrentlyInCreation</span><span class="hljs-params">(String beanName)</span> </span>&#123;    Object curVal = <span class="hljs-keyword">this</span>.prototypesCurrentlyInCreation.get();    <span class="hljs-keyword">return</span> (curVal != <span class="hljs-keyword">null</span> &amp;&amp;            (curVal.equals(beanName) || (curVal <span class="hljs-keyword">instanceof</span> Set &amp;&amp; ((Set&lt;?&gt;) curVal).contains(beanName))));&#125;</code></pre><h4 id="检查父类BeanFactory"><a href="#检查父类BeanFactory" class="headerlink" title="检查父类BeanFactory"></a>检查父类<code>BeanFactory</code></h4><p>若 <code>containsBeanDefinition(String beanName)</code> 方法中不存在 <code>beanName</code> 相对应的 BeanDefinition 对象时，则从 <code>parentBeanFactory</code> 中获取。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">// 获取 parentBeanFactory</span>BeanFactory parentBeanFactory = getParentBeanFactory();<span class="hljs-comment">// parentBeanFactory 不为空且 beanDefinitionMap 中不存该 name 的 BeanDefinition</span><span class="hljs-keyword">if</span> (parentBeanFactory != <span class="hljs-keyword">null</span> &amp;&amp; !containsBeanDefinition(beanName)) &#123;    <span class="hljs-comment">// 确定原始 beanName</span>    String nameToLookup = originalBeanName(name);    <span class="hljs-comment">// 若为 AbstractBeanFactory 类型，委托父类处理</span>    <span class="hljs-keyword">if</span> (parentBeanFactory <span class="hljs-keyword">instanceof</span> AbstractBeanFactory) &#123;        <span class="hljs-keyword">return</span> ((AbstractBeanFactory) parentBeanFactory).doGetBean(                nameToLookup, requiredType, args, typeCheckOnly);    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (args != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// 委托给构造���数 getBean() 处理</span>        <span class="hljs-keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup, args);    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 没有 args，委托给标准的 getBean() 处理</span>        <span class="hljs-keyword">return</span> parentBeanFactory.getBean(nameToLookup, requiredType);    &#125;&#125;</code></pre><p>在获取实例Bean之前，先通过<code>originalBeanName()</code>方法对<code>name</code>进行了处理用来获取原始BeanName。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> String <span class="hljs-title">originalBeanName</span><span class="hljs-params">(String name)</span> </span>&#123;    String beanName = transformedBeanName(name);    <span class="hljs-keyword">if</span> (name.startsWith(FACTORY_BEAN_PREFIX)) &#123;        beanName = FACTORY_BEAN_PREFIX + beanName;    &#125;    <span class="hljs-keyword">return</span> beanName;&#125;</code></pre><p>优先调用了<code>transformedBeanName()</code>方法（<code>doGetBean()</code>方法第一步就是调用了该方法），然后再对返回的beanname进行处理，因为上一步将<code>&amp;</code>去掉了。</p><h4 id="类型检查"><a href="#类型检查" class="headerlink" title="类型检查"></a>类型检查</h4><p>方法参数 <code>typeCheckOnly</code> ，是用来判断调用 <code>getBean(...)</code> 方法时，表示是否为<strong>仅仅</strong>进行类型检查获取 Bean 对象。如果不是仅仅做类型检查，而是创建 Bean 对象，则需要调用 <code>markBeanAsCreated(String beanName)</code> 方法，进行记录。</p><pre><code class="hljs java"><span class="hljs-comment">//AbstractBeanFactory.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">getBean</span><span class="hljs-params">(String name)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-keyword">return</span> doGetBean(name, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">false</span>);&#125;<span class="hljs-keyword">protected</span> &lt;T&gt; <span class="hljs-function">T <span class="hljs-title">doGetBean</span><span class="hljs-params">(String name, <span class="hljs-meta">@Nullable</span> Class&lt;T&gt; requiredType, <span class="hljs-meta">@Nullable</span> Object[] args, <span class="hljs-keyword">boolean</span> typeCheckOnly)</span></span><span class="hljs-function"><span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-comment">/********省略**********/</span>    <span class="hljs-comment">//标记当前bean正在创建中，alreadyCreated中</span>    <span class="hljs-keyword">if</span> (!typeCheckOnly) &#123;        markBeanAsCreated(beanName);    &#125;    <span class="hljs-comment">/********省略**********/</span>&#125;</code></pre><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">markBeanAsCreated</span><span class="hljs-params">(String beanName)</span> </span>&#123;    <span class="hljs-comment">// 没有创建</span>    <span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.alreadyCreated.contains(beanName)) &#123;        <span class="hljs-comment">// 加上全局锁</span>        <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.mergedBeanDefinitions) &#123;            <span class="hljs-comment">// 再次检查一次：DCL 双检查模式</span>            <span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.alreadyCreated.contains(beanName)) &#123;                <span class="hljs-comment">// Let the bean definition get re-merged now that we&#x27;re actually creating</span>                <span class="hljs-comment">// the bean... just in case some of its metadata changed in the meantime.</span>                <span class="hljs-comment">// 从 mergedBeanDefinitions 中删除 beanName，并在下次访问时重新创建它。</span>                clearMergedBeanDefinition(beanName);                <span class="hljs-comment">// 添加到已创建 bean 集合中</span>                <span class="hljs-keyword">this</span>.alreadyCreated.add(beanName);            &#125;        &#125;    &#125;&#125;<span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">clearMergedBeanDefinition</span><span class="hljs-params">(String beanName)</span> </span>&#123;    RootBeanDefinition bd = <span class="hljs-keyword">this</span>.mergedBeanDefinitions.get(beanName);    <span class="hljs-keyword">if</span> (bd != <span class="hljs-keyword">null</span>) &#123;        bd.stale = <span class="hljs-keyword">true</span>;    &#125;&#125;</code></pre><h4 id="获取RootBeanDefinition"><a href="#获取RootBeanDefinition" class="headerlink" title="获取RootBeanDefinition"></a>获取<code>RootBeanDefinition</code></h4><pre><code class="hljs java"><span class="hljs-comment">// 从容器中获取 beanName 相应的 GenericBeanDefinition 对象，并将其转换为 RootBeanDefinition 对象</span><span class="hljs-comment">// 因为从 XML 配置文件中读取到的 Bean 信息是存储在GenericBeanDefinition 中的。但是，所有的 Bean 后续处理都是针对于 RootBeanDefinition 的，所以这里需要进行一个转换。</span><span class="hljs-comment">// 转换的同时，如果父类 bean 不为空的话，则会一并合并父类的属性。</span>RootBeanDefinition mbd = getMergedLocalBeanDefinition(beanName);<span class="hljs-comment">//检查是否为抽象类</span>checkMergedBeanDefinition(mbd, beanName, args);</code></pre><p>调用 <code>getMergedLocalBeanDefinition(String beanName)</code> 方法，获取相对应的 BeanDefinition 对象。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> RootBeanDefinition <span class="hljs-title">getMergedLocalBeanDefinition</span><span class="hljs-params">(String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-comment">// Quick check on the concurrent map first, with minimal locking.</span>    <span class="hljs-comment">// 快速从缓存中获取，如果不为空，则直接返回</span>    RootBeanDefinition mbd = <span class="hljs-keyword">this</span>.mergedBeanDefinitions.get(beanName);    <span class="hljs-keyword">if</span> (mbd != <span class="hljs-keyword">null</span> &amp;&amp; !mbd.stale) &#123;        <span class="hljs-keyword">return</span> mbd;    &#125;    <span class="hljs-comment">// 获取 RootBeanDefinition，</span>    <span class="hljs-comment">// 如果返回的 BeanDefinition 是子类 bean 的话，则合并父类相关属性</span>    <span class="hljs-keyword">return</span> getMergedBeanDefinition(beanName, getBeanDefinition(beanName));&#125;</code></pre><p>调用 <code>checkMergedBeanDefinition()</code> 方法，检查给定的合并的 BeanDefinition 对象。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">checkMergedBeanDefinition</span><span class="hljs-params">(RootBeanDefinition mbd, String beanName, <span class="hljs-meta">@Nullable</span> Object[] args)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;    <span class="hljs-keyword">if</span> (mbd.isAbstract()) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanIsAbstractException(beanName);    &#125;&#125;</code></pre><h4 id="处理依赖"><a href="#处理依赖" class="headerlink" title="处理依赖"></a>处理依赖</h4><p>如果一个 Bean 有依赖 Bean 的话，那么在初始化该 Bean 时是需要先初始化它所依赖的 Bean 。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">// 处理所依赖的 bean</span>String[] dependsOn = mbd.getDependsOn();<span class="hljs-keyword">if</span> (dependsOn != <span class="hljs-keyword">null</span>) &#123;    <span class="hljs-keyword">for</span> (String dep : dependsOn) &#123;        <span class="hljs-comment">// &lt;1&gt; 若给定的依赖 bean 已经注册为依赖给定的 bean</span>        <span class="hljs-comment">// 即循环依赖的情况，抛出 BeanCreationException 异常</span>        <span class="hljs-keyword">if</span> (isDependent(beanName, dep)) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                    <span class="hljs-string">&quot;Circular depends-on relationship between &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; and &#x27;&quot;</span> + dep + <span class="hljs-string">&quot;&#x27;&quot;</span>);        &#125;        <span class="hljs-comment">// &lt;2&gt; 缓存依赖调用</span>        registerDependentBean(dep, beanName);        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">// &lt;3&gt; 递归处理依赖 Bean</span>            getBean(dep);        &#125; <span class="hljs-keyword">catch</span> (NoSuchBeanDefinitionException ex) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                    <span class="hljs-string">&quot;&#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; depends on missing bean &#x27;&quot;</span> + dep + <span class="hljs-string">&quot;&#x27;&quot;</span>, ex);        &#125;    &#125;&#125;</code></pre><p>通过迭代的方式依次对依赖 bean 进行检测、校验。如果通过，则调用 <code>getBean(String beanName)</code> 方法，实例化<strong>依赖</strong>的 Bean 对象。</p><h5 id="getDependsOn"><a href="#getDependsOn" class="headerlink" title="getDependsOn()"></a><code>getDependsOn()</code></h5><p>获取所有依赖，<code>RootBeanDefinition</code>继承自<code>AbstractBeanDefinition</code>所以委托给了父类处理：</p><pre><code class="hljs java"><span class="hljs-comment">//AbstractBeanDefinition.java</span><span class="hljs-meta">@Nullable</span><span class="hljs-keyword">private</span> String[] dependsOn;<span class="hljs-keyword">public</span> String[] getDependsOn() &#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.dependsOn;&#125;</code></pre><h5 id="isDependent"><a href="#isDependent" class="headerlink" title="isDependent()"></a><code>isDependent()</code></h5><p>校验该依赖是否已经注册给当前 Bean 。</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isDependent</span><span class="hljs-params">(String beanName, String dependentBeanName)</span> </span>&#123;    <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.dependentBeanMap) &#123;        <span class="hljs-keyword">return</span> isDependent(beanName, dependentBeanName, <span class="hljs-keyword">null</span>);    &#125;&#125;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isDependent</span><span class="hljs-params">(String beanName, String dependentBeanName, <span class="hljs-meta">@Nullable</span> Set&lt;String&gt; alreadySeen)</span> </span>&#123;    <span class="hljs-comment">// alreadySeen 已经检测的依赖 bean</span>    <span class="hljs-keyword">if</span> (alreadySeen != <span class="hljs-keyword">null</span> &amp;&amp; alreadySeen.contains(beanName)) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;    <span class="hljs-comment">// 获取原始 beanName</span>    String canonicalName = canonicalName(beanName);    <span class="hljs-comment">// 获取当前 beanName 的依赖集合</span>    Set&lt;String&gt; dependentBeans = <span class="hljs-keyword">this</span>.dependentBeanMap.get(canonicalName);    <span class="hljs-keyword">if</span> (dependentBeans == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;    <span class="hljs-comment">// 存在，则证明存在已经注册的依赖</span>    <span class="hljs-keyword">if</span> (dependentBeans.contains(dependentBeanName)) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-comment">// 递归检测依赖</span>    <span class="hljs-keyword">for</span> (String transitiveDependency : dependentBeans) &#123;        <span class="hljs-keyword">if</span> (alreadySeen == <span class="hljs-keyword">null</span>) &#123;            alreadySeen = <span class="hljs-keyword">new</span> HashSet&lt;&gt;();        &#125;        alreadySeen.add(beanName);        <span class="hljs-comment">//递归</span>        <span class="hljs-keyword">if</span> (isDependent(transitiveDependency, dependentBeanName, alreadySeen)) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;&#125;</code></pre><h5 id="registerDependentBean-dep-beanName-方法"><a href="#registerDependentBean-dep-beanName-方法" class="headerlink" title="registerDependentBean(dep, beanName)方法;"></a><code>registerDependentBean(dep, beanName)方法;</code></h5><p>注册依赖记录</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerDependentBean</span><span class="hljs-params">(String beanName, String dependentBeanName)</span> </span>&#123;    <span class="hljs-comment">// 获取 beanName</span>    String canonicalName = canonicalName(beanName);    <span class="hljs-comment">// 添加 &lt;canonicalName, &lt;dependentBeanName&gt;&gt; 到 dependentBeanMap 中</span>    <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.dependentBeanMap) &#123;        Set&lt;String&gt; dependentBeans =            <span class="hljs-keyword">this</span>.dependentBeanMap.computeIfAbsent(canonicalName, k -&gt; <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;(<span class="hljs-number">8</span>));        <span class="hljs-keyword">if</span> (!dependentBeans.add(dependentBeanName)) &#123;            <span class="hljs-keyword">return</span>;        &#125;    &#125;    <span class="hljs-comment">// 添加 &lt;dependentBeanName, &lt;canonicalName&gt;&gt; 到 dependenciesForBeanMap 中</span>    <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.dependenciesForBeanMap) &#123;        Set&lt;String&gt; dependenciesForBean =            <span class="hljs-keyword">this</span>.dependenciesForBeanMap.computeIfAbsent(dependentBeanName, k -&gt; <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;(<span class="hljs-number">8</span>));        dependenciesForBean.add(canonicalName);    &#125;&#125;</code></pre><h4 id="getBean-方法"><a href="#getBean-方法" class="headerlink" title="getBean()方法"></a><code>getBean()</code>方法</h4><p>最后调用 <code>getBean(String beanName)</code> 方法，实例化依赖 Bean 对象。</p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>从单例缓存中获取Bean</title>
    <link href="/2021/02/24/%E4%BB%8E%E5%8D%95%E4%BE%8B%E7%BC%93%E5%AD%98%E4%B8%AD%E8%8E%B7%E5%8F%96Bean/"/>
    <url>/2021/02/24/%E4%BB%8E%E5%8D%95%E4%BE%8B%E7%BC%93%E5%AD%98%E4%B8%AD%E8%8E%B7%E5%8F%96Bean/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">从单例缓存中获取Bean</font></div><p><code>getBean()</code>的第一个过程，获取<code>beanName</code>上篇已经介绍：</p><pre><code class="hljs java">Object sharedInstance = getSingleton(beanName);<span class="hljs-comment">/*</span><span class="hljs-comment">* 如果容器中包含对应的bean则直接返回</span><span class="hljs-comment">* 如果 sharedInstance = null，则说明缓存里没有对应的实例，表明这个实例还没创建。</span><span class="hljs-comment">* BeanFactory 并不会在一开始就将所有的单例 bean 实例化好，而是在调用 getBean 获取</span><span class="hljs-comment">* bean 时再实例化，也就是懒加载。</span><span class="hljs-comment">* getBean 方法有很多重载，比如 getBean(String name, Object... args)，我们在首次获取</span><span class="hljs-comment">* 某个 bean 时，可以传入用于初始化 bean 的参数数组（args），BeanFactory 会根据这些参数</span><span class="hljs-comment">* 去匹配合适的构造方法构造 bean 实例。当然，如果单例 bean 早已创建好，这里的 args 就没有</span><span class="hljs-comment">* 用了，BeanFactory 不会多次实例化单例 bean。</span><span class="hljs-comment">*/</span><span class="hljs-keyword">if</span> (sharedInstance != <span class="hljs-keyword">null</span> &amp;&amp; args == <span class="hljs-keyword">null</span>) &#123;    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;<span class="hljs-comment">//日志</span>        <span class="hljs-keyword">if</span> (isSingletonCurrentlyInCreation(beanName)) &#123;            logger.trace(<span class="hljs-string">&quot;Returning eagerly cached instance of singleton bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; that is not fully initialized yet - a consequence of a circular reference&quot;</span>);        &#125;        <span class="hljs-keyword">else</span> &#123;            logger.trace(<span class="hljs-string">&quot;Returning cached instance of singleton bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);        &#125;    &#125;<span class="hljs-comment">/*</span><span class="hljs-comment">* 如果 sharedInstance 是普通的单例 bean，下面的方法会直接返回。但如果</span><span class="hljs-comment">* sharedInstance 是 FactoryBean 类型的，则需调用 getObject 工厂方法获取真正的</span><span class="hljs-comment">* bean 实例。如果用户想获取 FactoryBean 本身，这里也不会做特别的处理，直接返回</span><span class="hljs-comment">* 即可。毕竟 FactoryBean 的实现类本身也是一种 bean，只不过具有一点特殊的功能而已。</span><span class="hljs-comment">*/</span>    beanInstance = getObjectForBeanInstance(sharedInstance, name, beanName, <span class="hljs-keyword">null</span>);&#125;</code></pre><h4 id="getSingleton-方法"><a href="#getSingleton-方法" class="headerlink" title="getSingleton()方法"></a><code>getSingleton()</code>方法</h4><p>Spring 对单例模式的 bean 只会创建一次。后续，如果再获取该 Bean ，则是直接从单例缓存中获取：</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span><span class="hljs-comment">//singletonObjects 就是Spring内部用来存放单例Bean的对象池,key为beanName，value为Bean</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, Object&gt; singletonObjects = <span class="hljs-keyword">new</span> ConcurrentHashMap&lt;&gt;(<span class="hljs-number">256</span>);<span class="hljs-comment">//singletonFactories 单例工厂的缓存,key为beanName,value 为ObjectFactory</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, ObjectFactory&lt;?&gt;&gt; singletonFactories = <span class="hljs-keyword">new</span> HashMap&lt;&gt;(<span class="hljs-number">16</span>);<span class="hljs-comment">// earlySingletonObjects 是早期单例Bean的缓存池,此时Bean已经被创建(newInstance),但是还没有完成初始化</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, Object&gt; earlySingletonObjects = <span class="hljs-keyword">new</span> ConcurrentHashMap&lt;&gt;(<span class="hljs-number">16</span>);<span class="hljs-comment">// singletonsCurrentlyInCreation 存放了当前正在创建的bean的BeanName</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Set&lt;String&gt; singletonsCurrentlyInCreation =    Collections.newSetFromMap(<span class="hljs-keyword">new</span> ConcurrentHashMap&lt;&gt;(<span class="hljs-number">16</span>));<span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">getSingleton</span><span class="hljs-params">(String beanName)</span> </span>&#123;    <span class="hljs-keyword">return</span> getSingleton(beanName, <span class="hljs-keyword">true</span>);&#125;<span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">getSingleton</span><span class="hljs-params">(String beanName, <span class="hljs-keyword">boolean</span> allowEarlyReference)</span> </span>&#123;    <span class="hljs-comment">// Quick check for existing instance without full singleton lock</span>    <span class="hljs-comment">// 从单例缓冲中加载 bean，//先从一级缓存拿</span>    <span class="hljs-comment">//singletonObjects 就是Spring内部用来存放单例Bean的对象池,key为beanName，value为Bean</span>    Object singletonObject = <span class="hljs-keyword">this</span>.singletonObjects.get(beanName);    <span class="hljs-comment">// 缓存中的 bean 为空，且当前 bean 正在创建，属性还没有DI（依赖注入）</span>    <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span> &amp;&amp; isSingletonCurrentlyInCreation(beanName)) &#123;        <span class="hljs-comment">//从二级缓存中拿</span>        <span class="hljs-comment">// earlySingletonObjects 是早期单例Bean的缓存池,此时Bean已经被创建(newInstance),但是还没有完成初始化</span>        singletonObject = <span class="hljs-keyword">this</span>.earlySingletonObjects.get(beanName);        <span class="hljs-comment">//如果还拿不到，并且允许bean提前引用（解决循环依赖）</span>        <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span> &amp;&amp; allowEarlyReference) &#123;            <span class="hljs-comment">// 加锁</span>            <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.singletonObjects) &#123;                <span class="hljs-comment">// Consistent creation of early reference within full singleton lock</span>                singletonObject = <span class="hljs-keyword">this</span>.singletonObjects.get(beanName);                <span class="hljs-keyword">if</span> (singletonObject == <span class="hljs-keyword">null</span>) &#123;                    singletonObject = <span class="hljs-keyword">this</span>.earlySingletonObjects.get(beanName);                    <span class="hljs-keyword">if</span> (sing也letonObject == <span class="hljs-keyword">null</span>) &#123;                        <span class="hljs-comment">//从三级缓存中拿到对象工厂</span>                        <span class="hljs-comment">//singletonFactories 单例工厂的缓存,key为beanName,value 为ObjectFactory</span>                        ObjectFactory&lt;?&gt; singletonFactory = <span class="hljs-keyword">this</span>.singletonFactories.get(beanName);                        <span class="hljs-keyword">if</span> (singletonFactory != <span class="hljs-keyword">null</span>) &#123;                            <span class="hljs-comment">//从工厂中拿到对象</span>                            singletonObject = singletonFactory.getObject();                            <span class="hljs-comment">//升级到二级缓存</span>                            <span class="hljs-keyword">this</span>.earlySingletonObjects.put(beanName, singletonObject);                            <span class="hljs-comment">// 从 singletonFactories 中移除对应的 ObjectFactory</span>                            <span class="hljs-keyword">this</span>.singletonFactories.remove(beanName);                        &#125;                    &#125;                &#125;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> singletonObject;&#125;</code></pre><p>从缓存中获取单例Bean的流程：</p><ul><li>第一步，从<code>singletonObjects</code>中获取Bean对象</li><li>第二步，如果获取不到且Bean正在创建中，从<code>earlySingletonObjects</code>获取Bean对象</li><li>第三步，如果获取不到且允许提前创建,从<code>singletonFactories</code>获取FactoryBean</li><li>第四步，如果不为null，则通过<code>FactoryBean.getObject()</code>获取Bean，然后将其加入到 <code>earlySingletonObjects</code> ，并且从 <code>singletonFactories</code> 删除，两者是互斥的，主要用来解决循环依赖的问题</li><li>总结就是：从这三个Map依次去取，取不到就取下一个Map</li></ul><h4 id="getObjectForBeanInstance-方法"><a href="#getObjectForBeanInstance-方法" class="headerlink" title="getObjectForBeanInstance()方法"></a><code>getObjectForBeanInstance()</code>方法</h4><p>执行完<code>getSingleton()</code>如果<code>sharedInstance != null</code>然后会执行该方法获取真正的实例Bean，执行该方法是因为：</p><pre><code class="hljs txt">如果 sharedInstance 是普通的单例 bean，该方法会直接返回。但如果sharedInstance 是 FactoryBean 类型的，则需调用 getObject 工厂方法获取真正的bean 实例。如果用户想获取 FactoryBean 本身，这里也不会做特别的处理，直接返回即可。毕竟 FactoryBean 的实现类本身也是一种 bean，只不过具有一点特殊的功能而已。</code></pre><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">getObjectForBeanInstance</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">    Object beanInstance, String name, String beanName, <span class="hljs-meta">@Nullable</span> RootBeanDefinition mbd)</span> </span>&#123;    <span class="hljs-comment">// 判断是否为工厂类引用（name 以 &amp; 开头）</span>    <span class="hljs-keyword">if</span> (BeanFactoryUtils.isFactoryDereference(name)) &#123;        <span class="hljs-comment">// 如果是 NullBean，则直接返回</span>        <span class="hljs-keyword">if</span> (beanInstance <span class="hljs-keyword">instanceof</span> NullBean) &#123;            <span class="hljs-keyword">return</span> beanInstance;        &#125;        <span class="hljs-comment">// 如果 beanInstance 不是 FactoryBean 类型，则抛出异常</span>        <span class="hljs-keyword">if</span> (!(beanInstance <span class="hljs-keyword">instanceof</span> FactoryBean)) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanIsNotAFactoryException(beanName, beanInstance.getClass());        &#125;        <span class="hljs-keyword">if</span> (mbd != <span class="hljs-keyword">null</span>) &#123;<span class="hljs-comment">//doGetBean中调用默认为null</span>            mbd.isFactoryBean = <span class="hljs-keyword">true</span>;        &#125;        <span class="hljs-keyword">return</span> beanInstance;    &#125;     <span class="hljs-keyword">if</span> (!(beanInstance <span class="hljs-keyword">instanceof</span> FactoryBean)) &#123;        <span class="hljs-keyword">return</span> beanInstance;    &#125;    Object object = <span class="hljs-keyword">null</span>;    <span class="hljs-keyword">if</span> (mbd != <span class="hljs-keyword">null</span>) &#123;        mbd.isFactoryBean = <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 尝试从缓存中加载bean</span>        object = getCachedObjectForFactoryBean(beanName);    &#125;    <span class="hljs-keyword">if</span> (object == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// 若 object 依然为空，则可以确认，beanInstance 一定是 FactoryBean 。从而，使用 FactoryBean 获得 Bean 对象</span>        <span class="hljs-comment">// Return bean instance from factory.</span>        FactoryBean&lt;?&gt; factory = (FactoryBean&lt;?&gt;) beanInstance;         <span class="hljs-comment">// containsBeanDefinition 检测 beanDefinitionMap 中也就是在所有已经加载的类中</span>        <span class="hljs-comment">// 检测是否定义 beanName</span>        <span class="hljs-keyword">if</span> (mbd == <span class="hljs-keyword">null</span> &amp;&amp; containsBeanDefinition(beanName)) &#123;            <span class="hljs-comment">// 将存储 XML 配置文件的 GenericBeanDefinition 转换为 RootBeanDefinition，</span>            <span class="hljs-comment">// 如果指定 BeanName 是子 Bean 的话同时会合并父类的相关属性</span>            mbd = getMergedLocalBeanDefinition(beanName);        &#125;        <span class="hljs-comment">// 是否是用户定义的，而不是应用程序本身定义的</span>        <span class="hljs-keyword">boolean</span> synthetic = (mbd != <span class="hljs-keyword">null</span> &amp;&amp; mbd.isSynthetic());        <span class="hljs-comment">// 核心处理方法，使用 FactoryBean 获得 Bean 对象</span>        object = getObjectFromFactoryBean(factory, beanName, !synthetic);    &#125;    <span class="hljs-keyword">return</span> object;&#125;</code></pre><h5 id="getObjectFromFactoryBean-方法"><a href="#getObjectFromFactoryBean-方法" class="headerlink" title="getObjectFromFactoryBean()方法"></a><code>getObjectFromFactoryBean()</code>方法</h5><p>从上面可以看出， <code>getObjectForBeanInstance(Object beanInstance, String name, String beanName,RootBeanDefinition mbd)</code> 方法，分成两种情况：</p><ul><li>第一种，当该实例对象为<strong>非 FactoryBean 类型</strong>，<strong>直接</strong>返回给定的 Bean 实例对象 <code>beanInstance</code> 。</li><li>第二种，当该实例对象为<strong>FactoryBean 类型</strong>，从 FactoryBean ( <code>beanInstance</code> ) 中，获取 Bean 实例对象，即通过<code>getObjectFromFactoryBean()</code>来获取。</li></ul><pre><code class="hljs java"><span class="hljs-comment">//缓存 FactoryBean 创建的单例 Bean 对象的映射</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, Object&gt; factoryBeanObjectCache = <span class="hljs-keyword">new</span> ConcurrentHashMap&lt;&gt;(<span class="hljs-number">16</span>);<span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">getObjectFromFactoryBean</span><span class="hljs-params">(FactoryBean&lt;?&gt; factory, String beanName, <span class="hljs-keyword">boolean</span> shouldPostProcess)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 为单例模式且缓存中存在</span>    <span class="hljs-keyword">if</span> (factory.isSingleton() &amp;&amp; containsSingleton(beanName)) &#123;        <span class="hljs-keyword">synchronized</span> (getSingletonMutex()) &#123;            <span class="hljs-comment">// &lt;1.2&gt; 从缓存中获取指定的 factoryBean</span>            Object object = <span class="hljs-keyword">this</span>.factoryBeanObjectCache.get(beanName);            <span class="hljs-keyword">if</span> (object == <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">// 为空，则从 FactoryBean 中获取对象</span>                object = doGetObjectFromFactoryBean(factory, beanName);                Object alreadyThere = <span class="hljs-keyword">this</span>.factoryBeanObjectCache.get(beanName);                <span class="hljs-keyword">if</span> (alreadyThere != <span class="hljs-keyword">null</span>) &#123;                    object = alreadyThere;                &#125;                <span class="hljs-keyword">else</span> &#123;                    <span class="hljs-comment">// &lt;1.3&gt; 需要后续处理</span>                    <span class="hljs-keyword">if</span> (shouldPostProcess) &#123;                        <span class="hljs-comment">// 若该 Bean 处于创建中，则返回非处理对象，而不是存储它</span>                        <span class="hljs-keyword">if</span> (isSingletonCurrentlyInCreation(beanName)) &#123;                            <span class="hljs-keyword">return</span> object;                        &#125;                        <span class="hljs-comment">// 单例 Bean 的前置处理</span>                        beforeSingletonCreation(beanName);                        <span class="hljs-keyword">try</span> &#123;                            <span class="hljs-comment">// 对从 FactoryBean 获取的对象进行后处理</span>                            <span class="hljs-comment">// 生成的对象将暴露给 bean 引用</span>                            object = postProcessObjectFromFactoryBean(object, beanName);                        &#125;                        <span class="hljs-keyword">catch</span> (Throwable ex) &#123;                            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(beanName,                                                            <span class="hljs-string">&quot;Post-processing of FactoryBean&#x27;s singleton object failed&quot;</span>, ex);                        &#125;                        <span class="hljs-keyword">finally</span> &#123;                            <span class="hljs-comment">// 单例 Bean 的后置处理</span>                            afterSingletonCreation(beanName);                        &#125;                    &#125;                    <span class="hljs-comment">// &lt;1.4&gt; 添加到 factoryBeanObjectCache 中，进行缓存</span>                    <span class="hljs-keyword">if</span> (containsSingleton(beanName)) &#123;                        <span class="hljs-keyword">this</span>.factoryBeanObjectCache.put(beanName, object);                    &#125;                &#125;            &#125;            <span class="hljs-keyword">return</span> object;        &#125;    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 为空，则从 FactoryBean 中获取对象</span>        Object object = doGetObjectFromFactoryBean(factory, beanName);        <span class="hljs-comment">// 需要后续处理</span>        <span class="hljs-keyword">if</span> (shouldPostProcess) &#123;            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">// 对从 FactoryBean 获取的对象进行后处理</span>                <span class="hljs-comment">// 生成的对象将暴露给 bean 引用</span>                object = postProcessObjectFromFactoryBean(object, beanName);            &#125;            <span class="hljs-keyword">catch</span> (Throwable ex) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(beanName, <span class="hljs-string">&quot;Post-processing of FactoryBean&#x27;s object failed&quot;</span>, ex);            &#125;        &#125;        <span class="hljs-keyword">return</span> object;    &#125;&#125;</code></pre><p>主要流程如下：</p><ul><li>若为单例且单例 Bean 缓存中存在 <code>beanName</code> ，进行后续处理，否则直接调用<code>doGetObjectFromFactoryBean()</code>从 FactoryBean 中获取 Bean 实例对象。</li><li> 若为单例且单例 Bean 缓存中存在 <code>beanName</code> ，进行后续处理：第一步，获取锁。其实我们可以发现大量的同步锁，锁住的对象都是 <code>this.singletonObjects</code>，主要是因为在单例模式中必须要<strong>保证全局唯一</strong>。</li></ul><pre><code class="hljs java"><span class="hljs-comment">//DefaultSingletonBeanRegistry.java</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, Object&gt; singletonObjects = <span class="hljs-keyword">new</span> ConcurrentHashMap&lt;&gt;(<span class="hljs-number">256</span>);<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> Object <span class="hljs-title">getSingletonMutex</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.singletonObjects;&#125;</code></pre><ul><li>第二步，从 <code>factoryBeanObjectCache</code> 缓存中获取实例对象 <code>object</code> 。若 <code>object</code> 为空，则调用 <code>doGetObjectFromFactoryBean()</code> 方法，从 FactoryBean 获取 Bean 实例对象，底层是调用 <code>FactoryBean.getObject()</code> 方法，获取 Bean 对象。。</li></ul><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> Object <span class="hljs-title">doGetObjectFromFactoryBean</span><span class="hljs-params">(FactoryBean&lt;?&gt; factory, String beanName)</span> <span class="hljs-keyword">throws</span> BeanCreationException </span>&#123;    Object object;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// 需要权限验证</span>        <span class="hljs-keyword">if</span> (System.getSecurityManager() != <span class="hljs-keyword">null</span>) &#123;            AccessControlContext acc = getAccessControlContext();            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">// &lt;x&gt; 从 FactoryBean 中，获得 Bean 对象</span>                object = AccessController.doPrivileged((PrivilegedExceptionAction&lt;Object&gt;) factory::getObject, acc);            &#125;            <span class="hljs-keyword">catch</span> (PrivilegedActionException pae) &#123;                <span class="hljs-keyword">throw</span> pae.getException();            &#125;        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// &lt;x&gt; 从 FactoryBean 中，获得 Bean 对象</span>            object = factory.getObject();        &#125;    &#125;    <span class="hljs-keyword">catch</span> (FactoryBeanNotInitializedException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCurrentlyInCreationException(beanName, ex.toString());    &#125;    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(beanName, <span class="hljs-string">&quot;FactoryBean threw exception on object creation&quot;</span>, ex);    &#125;    <span class="hljs-comment">// Do not accept a null value for a FactoryBean that&#x27;s not fully</span>    <span class="hljs-comment">// initialized yet: Many FactoryBeans just return null then.</span>    <span class="hljs-keyword">if</span> (object == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">if</span> (isSingletonCurrentlyInCreation(beanName)) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCurrentlyInCreationException(                beanName, <span class="hljs-string">&quot;FactoryBean which is currently in creation returned null from getObject&quot;</span>);        &#125;        object = <span class="hljs-keyword">new</span> NullBean();    &#125;    <span class="hljs-keyword">return</span> object;&#125;</code></pre><ul><li>第三步，如果需要后续处理( <code>shouldPostProcess = true</code> )，则进行进一步处理，步骤如下：<ul><li>若该 Bean 处于创建中（<code>isSingletonCurrentlyInCreation(String beanName)</code> 方法返回 <code>true</code> ），则返回<strong>非处理的 Bean 对象</strong>，而不是存储它。</li><li>调用 <code>beforeSingletonCreation(String beanName)</code> 方法，进行创建之前的处理。默认实现将该 Bean 标志为当前创建的。</li><li>调用 <code>postProcessObjectFromFactoryBean(Object object, String beanName)</code> 方法，对从 FactoryBean 获取的 Bean 实例对象进行后置处理。</li><li>调用 <code>afterSingletonCreation(String beanName)</code> 方法，进行创建 Bean 之后的处理，默认实现是将该 bean 标记为不在创建中。</li></ul></li><li>第四步，加入到 <code>factoryBeanObjectCache</code> 缓存中。</li></ul><h5 id="isSingletonCurrentlyInCreation-方法"><a href="#isSingletonCurrentlyInCreation-方法" class="headerlink" title="isSingletonCurrentlyInCreation()方法"></a><code>isSingletonCurrentlyInCreation()</code>方法</h5><p>该方法是用于检测当前 Bean 是否处于创建之中。</p><ul><li><code>beforeSingletonCreation(String beanName)</code> 方法，用于添加标志，当前 bean <strong>正处于</strong>创建中</li><li><code>afterSingletonCreation(String beanName)</code> 方法，用于移除标记，当前 Bean <strong>不处于</strong>创建中。</li></ul><p>以上两个方法<strong>记录了 Bean 的加载状态，是检测当前 Bean 是否处于创建中的关键之处，对解决 Bean 循环依赖起着关键作用</strong>。</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span><span class="hljs-comment">// singletonsCurrentlyInCreation 存放了当前正在创建的bean的BeanName</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Set&lt;String&gt; singletonsCurrentlyInCreation =    Collections.newSetFromMap(<span class="hljs-keyword">new</span> ConcurrentHashMap&lt;&gt;(<span class="hljs-number">16</span>));<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isSingletonCurrentlyInCreation</span><span class="hljs-params">(String beanName)</span> </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.singletonsCurrentlyInCreation.contains(beanName);&#125;</code></pre><p>该集合的元素是通过<code>beforeSingletonCreation(String beanName)</code> 方法添加的：</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">beforeSingletonCreation</span><span class="hljs-params">(String beanName)</span> </span>&#123;    <span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.inCreationCheckExclusions.contains(beanName) &amp;&amp; !<span class="hljs-keyword">this</span>.singletonsCurrentlyInCreation.add(beanName)) &#123;<span class="hljs-comment">// 添加</span>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCurrentlyInCreationException(beanName);<span class="hljs-comment">// 如果添加失败，则抛出 BeanCurrentlyInCreationException 异常。</span>    &#125;&#125;</code></pre><p>该集合的元素是通过<code>afterSingletonCreation(String beanName)</code> 方法移除的：</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">afterSingletonCreation</span><span class="hljs-params">(String beanName)</span> </span>&#123;    <span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.inCreationCheckExclusions.contains(beanName) &amp;&amp; !<span class="hljs-keyword">this</span>.singletonsCurrentlyInCreation.remove(beanName)) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">&quot;Singleton &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; isn&#x27;t currently in creation&quot;</span>);    &#125;&#125;</code></pre><h5 id="postProcessObjectFromFactoryBean-方法"><a href="#postProcessObjectFromFactoryBean-方法" class="headerlink" title="postProcessObjectFromFactoryBean()方法"></a><code>postProcessObjectFromFactoryBean()</code>方法</h5><p><code>postProcessObjectFromFactoryBean(Object object, String beanName)</code> 方法，对从 FactoryBean 处获取的 Bean 实例对象进行后置处理。其默认实现是直接返回 object 对象，不做任何处理。</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultSingletonBeanRegistry.java</span><span class="hljs-comment">/**</span><span class="hljs-comment">*机翻注释：对从FactoryBean获得的给定对象进行后处理。结果对象将暴露给bean引用。</span><span class="hljs-comment">* 默认实现只是按原样返回给定的对象。子类可以覆盖它，例如，以应用后处理器。</span><span class="hljs-comment">*/</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">postProcessObjectFromFactoryBean</span><span class="hljs-params">(Object object, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-keyword">return</span> object;&#125;</code></pre><p>方法注释里提到了<strong>应用后处理器</strong><code>AbstractAutowireCapableBeanFactory.java</code>。</p><p>该类就覆盖了上边的方法：</p><pre><code class="hljs java"><span class="hljs-comment">//AbstractAutowireCapableBeanFactory.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">postProcessObjectFromFactoryBean</span><span class="hljs-params">(Object object, String beanName)</span> </span>&#123;    <span class="hljs-keyword">return</span> applyBeanPostProcessorsAfterInitialization(object, beanName);&#125;</code></pre><p>尽可能保证所有 bean 初始化后都会调用注册的 <code>BeanPostProcessor.postProcessAfterInitialization(Object bean, String beanName)</code> 方法进行处理，在实际开发过程中大可以针对此特性设计自己的业务逻辑。</p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>开启Bean加载</title>
    <link href="/2021/02/24/%E5%BC%80%E5%90%AFBean%E5%8A%A0%E8%BD%BD/"/>
    <url>/2021/02/24/%E5%BC%80%E5%90%AFBean%E5%8A%A0%E8%BD%BD/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">开启Bean加载</font></div><p>Spring IOC容器的作用：它会以某种方式加载 Configuration Metadata，将其解析注册到容器内部，然后回根据这些信息绑定整个系统的对象，最终组装成一个可用的基于轻量级容器的应用系统。具体流程分为两个阶段：</p><p><strong>1、容器初始化阶段</strong></p><ul><li>首先，通过某种方式加载 Configuration Metadata (主要是依据 Resource、ResourceLoader 两个体系) 。</li><li>然后，容器会对加载的 Configuration MetaData 进行解析，并将解析的信息组装成 BeanDefinition 。</li><li>最后，将 BeanDefinition 保存注册到相应的 BeanDefinitionRegistry 中。</li><li>至此，Spring IoC 的初始化工作完成。</li></ul><p><strong>2、加载Bean阶段</strong></p><ul><li>经过容器初始化阶段后，应用程序中定义的 bean 信息已经全部加载到系统中了，当我们显示或者隐式地调用 <code>BeanFactory.getBean(...)</code> 方法时，则会触发加载 Bean 阶段。</li><li>在这阶段，容器会首先检查所请求的对象是否已经初始化完成了，如果没有，则会根据注册的 Bean 信息实例化请求的对象，并为其注册依赖，然后将其返回给请求方。</li></ul><h4 id="getBean-方法"><a href="#getBean-方法" class="headerlink" title="getBean()方法"></a><code>getBean()</code>方法</h4><p>当我们显示或者隐式地调用 <code>BeanFactory.getBean(...)</code> 方法时，则会触发加载 Bean 阶段。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">getBean</span><span class="hljs-params">(String name)</span> <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-keyword">return</span> doGetBean(name, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">false</span>);&#125;</code></pre><p>内部调用 <code>doGetBean(String name, final Class requiredType, Object[] args, boolean typeCheckOnly)</code> 方法。</p><h4 id="doGetBean-方法"><a href="#doGetBean-方法" class="headerlink" title="doGetBean()方法"></a><code>doGetBean()</code>方法</h4><p><code>getBean()</code>内部调用的是该方法，主要步骤总结在下方：</p><pre><code class="hljs java"><span class="hljs-comment">/**</span><span class="hljs-comment">* <span class="hljs-doctag">@param</span> name 要获取 Bean 的名字</span><span class="hljs-comment">* <span class="hljs-doctag">@param</span> requiredType 要获取 bean 的类型</span><span class="hljs-comment">* <span class="hljs-doctag">@param</span> args 创建 Bean 时传递的参数。这个参数仅限于创建 Bean 时使用。</span><span class="hljs-comment">* <span class="hljs-doctag">@param</span> typeCheckOnly 是否为类型检</span><span class="hljs-comment">* <span class="hljs-doctag">@return</span> an instance of the bean</span><span class="hljs-comment">* <span class="hljs-doctag">@throws</span> BeansException if the bean could not be created</span><span class="hljs-comment">*/</span><span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span><span class="hljs-keyword">protected</span> &lt;T&gt; <span class="hljs-function">T <span class="hljs-title">doGetBean</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">    String name, <span class="hljs-meta">@Nullable</span> Class&lt;T&gt; requiredType, <span class="hljs-meta">@Nullable</span> Object[] args, <span class="hljs-keyword">boolean</span> typeCheckOnly)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> BeansException </span>&#123;    <span class="hljs-comment">/*</span><span class="hljs-comment">* 通过 name 获取 beanName。这里不使用 name 直接作为 beanName 有两点原因：</span><span class="hljs-comment">* 1. name 可能会以 &amp; 字符开头，表明调用者想获取 FactoryBean 本身，而非 FactoryBean</span><span class="hljs-comment">* 实现类所创建的 bean。在 BeanFactory 中，FactoryBean 的实现类和其他的 bean 存储</span><span class="hljs-comment">* 方式是一致的，即 &lt;beanName, bean&gt;，beanName 中是没有 &amp; 这个字符的。所以我们需要</span><span class="hljs-comment">* 将 name 的首字符 &amp; 移除，这样才能从缓存里取到 FactoryBean 实例。</span><span class="hljs-comment">* 2. 若 name 是一个别名，则应将别名转换为具体的实例名，也就是 beanName。</span><span class="hljs-comment">*/</span>    <span class="hljs-comment">// ①获取beanName</span>    String beanName = transformedBeanName(name);    Object beanInstance;    <span class="hljs-comment">// ②从缓存中或者实例工厂中获取 Bean 对象</span>    <span class="hljs-comment">// Eagerly check singleton cache for manually registered singletons.</span>    Object sharedInstance = getSingleton(beanName);    <span class="hljs-comment">/*</span><span class="hljs-comment">* 如果容器中包含对应的bean则直接返回</span><span class="hljs-comment">* 如果 sharedInstance = null，则说明缓存里没有对应的实例，表明这个实例还没创建。</span><span class="hljs-comment">* BeanFactory 并不会在一开始就将所有的单例 bean 实例化好，而是在调用 getBean 获取</span><span class="hljs-comment">* bean 时再实例化，也就是懒加载。</span><span class="hljs-comment">* getBean 方法有很多重载，比如 getBean(String name, Object... args)，我们在首次获取</span><span class="hljs-comment">* 某个 bean 时，可以传入用于初始化 bean 的参数数组（args），BeanFactory 会根据这些参数</span><span class="hljs-comment">* 去匹配合适的构造方法构造 bean 实例。当然，如果单例 bean 早已创建好，这里的 args 就没有</span><span class="hljs-comment">* 用了，BeanFactory 不会多次实例化单例 bean。</span><span class="hljs-comment">*/</span>    <span class="hljs-keyword">if</span> (sharedInstance != <span class="hljs-keyword">null</span> &amp;&amp; args == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;<span class="hljs-comment">//日志</span>            <span class="hljs-keyword">if</span> (isSingletonCurrentlyInCreation(beanName)) &#123;                logger.trace(<span class="hljs-string">&quot;Returning eagerly cached instance of singleton bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; that is not fully initialized yet - a consequence of a circular reference&quot;</span>);            &#125;            <span class="hljs-keyword">else</span> &#123;                logger.trace(<span class="hljs-string">&quot;Returning cached instance of singleton bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);            &#125;        &#125;        <span class="hljs-comment">/*</span><span class="hljs-comment">* 如果 sharedInstance 是普通的单例 bean，下面的方法会直接返回。但如果</span><span class="hljs-comment">* sharedInstance 是 FactoryBean 类型的，则需调用 getObject 工厂方法获取真正的</span><span class="hljs-comment">* bean 实例。如果用户想获取 FactoryBean 本身，这里也不会做特别的处理，直接返回</span><span class="hljs-comment">* 即可。毕竟 FactoryBean 的实现类本身也是一种 bean，只不过具有一点特殊的功能而已。</span><span class="hljs-comment">*/</span>        beanInstance = getObjectForBeanInstance(sharedInstance, name, beanName, <span class="hljs-keyword">null</span>);    &#125;    <span class="hljs-comment">/*</span><span class="hljs-comment">* 如果上面的条件不满足，则表明 sharedInstance 可能为空，此时 beanName 对应的 bean</span><span class="hljs-comment">* 实例可能还未创建。这里还存在另一种可能，如果当前容器有父容器，beanName 对应的 bean 实例</span><span class="hljs-comment">* 可能是在父容器中被创建了，所以在创建���例前，需要先去父容器里检查一下。</span><span class="hljs-comment">*/</span>    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// Fail if we&#x27;re already creating this bean instance:</span>        <span class="hljs-comment">// We&#x27;re assumably within a circular reference.</span>        <span class="hljs-comment">// 检查bean是否正在创建， BeanFactory 不缓存 Prototype 类型的 bean，无法处理该类型 bean 的循环依赖问题</span>        <span class="hljs-comment">// ③因为 Spring 只解决单例模式下得循环依赖，在原型模式下如果存在循环依赖则会抛出异常。</span>        <span class="hljs-keyword">if</span> (isPrototypeCurrentlyInCreation(beanName)) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCurrentlyInCreationException(beanName);        &#125;        <span class="hljs-comment">// Check if bean definition exists in this factory.</span>        <span class="hljs-comment">// ④如果 sharedInstance = null，则到父容器中查找 bean 实例</span>        BeanFactory parentBeanFactory = getParentBeanFactory();        <span class="hljs-keyword">if</span> (parentBeanFactory != <span class="hljs-keyword">null</span> &amp;&amp; !containsBeanDefinition(beanName)) &#123;            <span class="hljs-comment">// Not found -&gt; check parent.</span>            <span class="hljs-comment">// 获取 name 对应的 beanName，如果 name 是以 &amp; 字符开头，则返回 &amp; + beanName</span>            String nameToLookup = originalBeanName(name);            <span class="hljs-keyword">if</span> (parentBeanFactory <span class="hljs-keyword">instanceof</span> AbstractBeanFactory) &#123;                <span class="hljs-keyword">return</span> ((AbstractBeanFactory) parentBeanFactory).doGetBean(                    nameToLookup, requiredType, args, typeCheckOnly);            &#125;            <span class="hljs-comment">// 根据 args 是否为空，以决定调用父容器哪个方法获取 bean</span>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (args != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">// Delegation to parent with explicit args.</span>                <span class="hljs-keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup, args);            &#125;            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (requiredType != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">// No args -&gt; delegate to standard getBean method.</span>                <span class="hljs-keyword">return</span> parentBeanFactory.getBean(nameToLookup, requiredType);            &#125;            <span class="hljs-keyword">else</span> &#123;                <span class="hljs-keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup);            &#125;        &#125;        <span class="hljs-comment">//⑤标记当前bean正在创建中，alreadyCreated中</span>        <span class="hljs-keyword">if</span> (!typeCheckOnly) &#123;            markBeanAsCreated(beanName);        &#125;        StartupStep beanCreation = <span class="hljs-keyword">this</span>.applicationStartup.start(<span class="hljs-string">&quot;spring.beans.instantiate&quot;</span>)            .tag(<span class="hljs-string">&quot;beanName&quot;</span>, name);        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-keyword">if</span> (requiredType != <span class="hljs-keyword">null</span>) &#123;                beanCreation.tag(<span class="hljs-string">&quot;beanType&quot;</span>, requiredType::toString);            &#125;            <span class="hljs-comment">// ⑥从容器中获取 beanName 相应的 GenericBeanDefinition 对象，并将其转换为 RootBeanDefinition 对象</span>            RootBeanDefinition mbd = getMergedLocalBeanDefinition(beanName);            <span class="hljs-comment">//检查是否为抽象类</span>            checkMergedBeanDefinition(mbd, beanName, args);            <span class="hljs-comment">// Guarantee initialization of beans that the current bean depends on.</span>            <span class="hljs-comment">// ⑦处理所依赖的 bean，先创建需要在当前bean创建之前必须创建的bean，通过@DependsOn(&quot;eventListener&quot;)注解设置该信息， 检查是否有 dependsOn 依赖，如果有则先初始化所依赖的 bean</span>            String[] dependsOn = mbd.getDependsOn();            <span class="hljs-keyword">if</span> (dependsOn != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">for</span> (String dep : dependsOn) &#123;                    <span class="hljs-comment">/*</span><span class="hljs-comment">* 检测是否存在 depends-on 循环依赖，若存在则抛异常。比如 A 依赖 B，</span><span class="hljs-comment">* B 又依赖 A，他们的配置如下：</span><span class="hljs-comment">*   &lt;bean id=&quot;beanA&quot; class=&quot;BeanA&quot; depends-on=&quot;beanB&quot;&gt;</span><span class="hljs-comment">*   &lt;bean id=&quot;beanB&quot; class=&quot;BeanB&quot; depends-on=&quot;beanA&quot;&gt;</span><span class="hljs-comment">* beanA 要求 beanB 在其之前被创建，但 beanB 又要求 beanA 先于它</span><span class="hljs-comment">* 创建。这个时候形成了循环，对于 depends-on 循环，Spring 会直接</span><span class="hljs-comment">* 抛出异常</span><span class="hljs-comment">*/</span>                    <span class="hljs-keyword">if</span> (isDependent(beanName, dep)) &#123;                        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,<span class="hljs-string">&quot;Circular depends-on relationship between &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; and &#x27;&quot;</span> + dep + <span class="hljs-string">&quot;&#x27;&quot;</span>);                    &#125;                    <span class="hljs-comment">// 缓存依赖调用，注册依赖记录</span>                    registerDependentBean(dep, beanName);                    <span class="hljs-keyword">try</span> &#123;                        <span class="hljs-comment">// 加载 depends-on 依赖</span>                        getBean(dep);                    &#125;                    <span class="hljs-keyword">catch</span> (NoSuchBeanDefinitionException ex) &#123;                        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,<span class="hljs-string">&quot;&#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; depends on missing bean &#x27;&quot;</span> + dep + <span class="hljs-string">&quot;&#x27;&quot;</span>, ex);                    &#125;                &#125;            &#125;            <span class="hljs-comment">// Create bean instance.</span>            <span class="hljs-comment">// ⑧创建 bean 实例</span>            <span class="hljs-keyword">if</span> (mbd.isSingleton()) &#123;<span class="hljs-comment">// 单例模式</span>                <span class="hljs-comment">/*jiu</span><span class="hljs-comment">* 这里并没有直接调用 createBean 方法创建 bean 实例，而是通过</span><span class="hljs-comment">* getSingleton(String, ObjectFactory) 方法获取 bean 实例。</span><span class="hljs-comment">* getSingleton(String, ObjectFactory) 方法会在内部调用</span><span class="hljs-comment">* ObjectFactory 的 getObject() 方法创建 bean，并会在创建完成后，</span><span class="hljs-comment">* 将 bean 放入缓存中。</span><span class="hljs-comment">*/</span>                sharedInstance = getSingleton(beanName, () -&gt; &#123;                    <span class="hljs-keyword">try</span> &#123;                        <span class="hljs-comment">// 创建 bean 实例</span>                        <span class="hljs-keyword">return</span> createBean(beanName, mbd, args);                    &#125;                    <span class="hljs-keyword">catch</span> (BeansException ex) &#123;                        <span class="hljs-comment">// 显式从单例缓存中删除 Bean 实例</span>                        <span class="hljs-comment">// 因为单例模式下为了解决循环依赖，可能他已经存在了，所以销毁它。</span>                        destroySingleton(beanName);                        <span class="hljs-keyword">throw</span> ex;                    &#125;                &#125;);                <span class="hljs-comment">// 如果 bean 是 FactoryBean 类型，则调用工厂方法获取真正的 bean 实例。否则直接返回 bean 实例</span>                beanInstance = getObjectForBeanInstance(sharedInstance, name, beanName, mbd);            &#125;            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (mbd.isPrototype()) &#123;<span class="hljs-comment">// 原型模式</span>                <span class="hljs-comment">// It&#x27;s a prototype -&gt; create a new instance.</span>                Object prototypeInstance = <span class="hljs-keyword">null</span>;                <span class="hljs-keyword">try</span> &#123;                    beforePrototypeCreation(beanName);                    prototypeInstance = createBean(beanName, mbd, args);                &#125;jiu                <span class="hljs-keyword">finally</span> &#123;                    afterPrototypeCreation(beanName);                &#125;                beanInstance = getObjectForBeanInstance(prototypeInstance, name, beanName, mbd);            &#125;            <span class="hljs-keyword">else</span> &#123;<span class="hljs-comment">// 从指定的 scope 下创建 bean</span>                String scopeName = mbd.getScope();                <span class="hljs-keyword">if</span> (!StringUtils.hasLength(scopeName)) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">&quot;No scope name defined for bean ´&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);                &#125;                Scope scope = <span class="hljs-keyword">this</span>.scopes.get(scopeName);                <span class="hljs-keyword">if</span> (scope == <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">&quot;No Scope registered for scope name &#x27;&quot;</span> + scopeName + <span class="hljs-string">&quot;&#x27;&quot;</span>);                &#125;jiu                <span class="hljs-keyword">try</span> &#123;                    Object scopedInstance = scope.get(beanName, () -&gt; &#123;                        beforePrototypeCreation(beanName);                        <span class="hljs-keyword">try</span> &#123;                            <span class="hljs-keyword">return</span> createBean(beanName, mbd, args);                        &#125;                        <span class="hljs-keyword">finally</span> &#123;                            afterPrototypeCreation(beanName);                        &#125;                    &#125;);                    beanInstance = getObjectForBeanInstance(scopedInstance, name, beanName, mbd);                &#125;                <span class="hljs-keyword">catch</span> (IllegalStateException ex) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ScopeNotActiveException(beanName, scopeName, ex);                &#125;            &#125;        &#125;        <span class="hljs-keyword">catch</span> (BeansException ex) &#123;            beanCreation.tag(<span class="hljs-string">&quot;exception&quot;</span>, ex.getClass().toString());            beanCreation.tag(<span class="hljs-string">&quot;message&quot;</span>, String.valueOf(ex.getMessage()));            cleanupAfterBeanCreationFailure(beanName);            <span class="hljs-keyword">throw</span> ex;        &#125;        <span class="hljs-keyword">finally</span> &#123;            beanCreation.end();        &#125;    &#125;    <span class="hljs-keyword">return</span> adaptBeanInstance(name, beanInstance, requiredType);&#125;jiu<span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span>&lt;T&gt; <span class="hljs-function">T <span class="hljs-title">adaptBeanInstance</span><span class="hljs-params">(String name, Object bean, <span class="hljs-meta">@Nullable</span> Class&lt;?&gt; requiredType)</span> </span>&#123;    <span class="hljs-comment">// Check if required type matches the type of the actual bean instance.</span>    <span class="hljs-comment">// ⑨检查需要的类型是否符合 bean 的实际类型，如果需要进行类型转换，则在此处进行转换。</span>    <span class="hljs-keyword">if</span> (requiredType != <span class="hljs-keyword">null</span> &amp;&amp; !requiredType.isInstance(bean)) &#123;        <span class="hljs-keyword">try</span> &#123;            Object convertedBean = getTypeConverter().convertIfNecessary(bean, requiredType);            <span class="hljs-keyword">if</span> (convertedBean == <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanNotOfRequiredTypeException(name, requiredType, bean.getClass());            &#125;            <span class="hljs-keyword">return</span> (T) convertedBean;        &#125;        <span class="hljs-keyword">catch</span> (TypeMismatchException ex) &#123;            <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                logger.trace(<span class="hljs-string">&quot;Failed to convert bean &#x27;&quot;</span> + name + <span class="hljs-string">&quot;&#x27; to required type &#x27;&quot;</span> +                             ClassUtils.getQualifiedName(requiredType) + <span class="hljs-string">&quot;&#x27;&quot;</span>, ex);            &#125;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanNotOfRequiredTypeException(name, requiredType, bean.getClass());        &#125;    &#125;    <span class="hljs-keyword">return</span> (T) bean;&#125;</code></pre><p><strong><code>doGetBean()</code>的流程：</strong></p><p>①、获取beanName</p><p>②、从缓存中或者实例工厂中获取 Bean 对象</p><p>③、原型模式依赖检查</p><p>④、从父类容器加载bean</p><p>⑤、标记当前bean为正在创建或者创建中</p><p>⑥、获取BeanDefinition</p><p>⑦、依赖bean处理</p><p>⑧、不同作用域的bean实例化</p><p>⑨、类型转换</p><h5 id="获取beanName"><a href="#获取beanName" class="headerlink" title="获取beanName"></a>获取beanName</h5><pre><code class="hljs java">String beanName = transformedBeanName(name);</code></pre><p>该<code>name</code>，不一定是<code>beanName</code>，可能是<code>aliasName</code>，也有可能是<code>FactoryBean</code>。所以需要执行该方法对<code>name</code>进行转换。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> String <span class="hljs-title">transformedBeanName</span><span class="hljs-params">(String name)</span> </span>&#123;    <span class="hljs-keyword">return</span> canonicalName(BeanFactoryUtils.transformedBeanName(name));&#125;</code></pre><h6 id="BeanFactoryUtils-transformedBeanName-name"><a href="#BeanFactoryUtils-transformedBeanName-name" class="headerlink" title="BeanFactoryUtils.transformedBeanName(name)"></a><code>BeanFactoryUtils.transformedBeanName(name)</code></h6><p>先调用<code>BeanFactoryUtils.transformedBeanName(name)</code>方法，去除<code>FactoryBean</code>的修饰符：</p><pre><code class="hljs java"><span class="hljs-comment">// BeanFactoryUtils.java</span><span class="hljs-comment">/**</span><span class="hljs-comment"> * 缓存 &#123;<span class="hljs-doctag">@link</span> #transformedBeanName(String)&#125; 已经转换好的结果。</span><span class="hljs-comment"> */</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Map&lt;String, String&gt; transformedBeanNameCache = <span class="hljs-keyword">new</span> ConcurrentHashMap&lt;&gt;();<span class="hljs-comment">/**</span><span class="hljs-comment"> * 去除 FactoryBean 的修饰符 &amp;</span><span class="hljs-comment"> * 如果 name 以 “&amp;” 为前缀，那么会去掉该 &quot;&amp;&quot; 。</span><span class="hljs-comment"> * 例如，name = &quot;&amp;studentService&quot; ，则会是 name = &quot;studentService&quot;。</span><span class="hljs-comment"> */</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> String <span class="hljs-title">transformedBeanName</span><span class="hljs-params">(String name)</span> </span>&#123;    Assert.notNull(name, <span class="hljs-string">&quot;&#x27;name&#x27; must not be null&quot;</span>);    <span class="hljs-keyword">if</span> (!name.startsWith(BeanFactory.FACTORY_BEAN_PREFIX)) &#123;<span class="hljs-comment">//FACTORY_BEAN_PREFIX=&amp;</span>        <span class="hljs-keyword">return</span> name;    &#125;    <span class="hljs-comment">// computeIfAbsent 方法，分成两种情况：</span>    <span class="hljs-comment">//      1. 未存在，则进行计算执行，并将结果添加到缓存、</span>    <span class="hljs-comment">//      2. 已存在，则直接返回，无需计算。</span>    <span class="hljs-keyword">return</span> transformedBeanNameCache.computeIfAbsent(name, beanName -&gt; &#123;        <span class="hljs-keyword">do</span> &#123;            beanName = beanName.substring(BeanFactory.FACTORY_BEAN_PREFIX.length());        &#125; <span class="hljs-keyword">while</span> (beanName.startsWith(BeanFactory.FACTORY_BEAN_PREFIX));        <span class="hljs-keyword">return</span> beanName;    &#125;);&#125;</code></pre><blockquote><p>tip：假设配置了一个 FactoryBean 的名字为 <code>&quot;abc&quot;</code> ，那么获取 FactoryBean 创建的 Bean 时，使用 <code>&quot;abc&quot;</code> ，如果获取 FactoryBean 本身，使用 <code>&quot;$abc&quot;</code> 。</p></blockquote><p><code>transformedBeanNameCache</code> 集合的存在，是为了缓存转换后的结果。下次再获取相同的 <code>name</code> 时，直接返回缓存中的结果即可。</p><h6 id="canonicalName"><a href="#canonicalName" class="headerlink" title="canonicalName(...)"></a><code>canonicalName(...)</code></h6><p>其次执行<code>canonicalName(String name)</code>：<code>AbstractBeanFactory</code>继承了<code>FactoryBeanRegistrySupport</code>，<code>FactoryBeanRegistrySupport</code>继承了<code>DefaultSingletonBeanRegistry</code>，<code>DefaultSingletonBeanRegistry</code>继承了<code>SimpleAliasRegistry</code>。</p><pre><code class="hljs java"><span class="hljs-comment">//SimpleAliasRegistry.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">canonicalName</span><span class="hljs-params">(String name)</span> </span>&#123;    String canonicalName = name;    <span class="hljs-comment">// Handle aliasing...</span>    String resolvedName;    <span class="hljs-comment">// 循环，从 aliasMap 中，获取到最终的 beanName</span>    <span class="hljs-comment">// 例如，别名 A 指向名称为 B 的 bean 则返回 B，若 别名 A 指向别名 B，别名 B 指向名称为 C 的 bean，则返回 C。</span>    <span class="hljs-keyword">do</span> &#123;        resolvedName = <span class="hljs-keyword">this</span>.aliasMap.get(canonicalName);        <span class="hljs-keyword">if</span> (resolvedName != <span class="hljs-keyword">null</span>) &#123;            canonicalName = resolvedName;        &#125;    &#125;    <span class="hljs-keyword">while</span> (resolvedName != <span class="hljs-keyword">null</span>);    <span class="hljs-keyword">return</span> canonicalName;&#125;</code></pre><h5 id="从缓存中或者实例工厂中获取-Bean-对象"><a href="#从缓存中或者实例工厂中获取-Bean-对象" class="headerlink" title="从缓存中或者实例工厂中获取 Bean 对象"></a>从缓存中或者实例工厂中获取 Bean 对象</h5><p>相应代码</p><pre><code class="hljs java">Object sharedInstance = getSingleton(beanName);<span class="hljs-comment">/*</span><span class="hljs-comment">* 如果容器中包含对应的bean则直接返回</span><span class="hljs-comment">* 如果 sharedInstance = null，则说明缓存里没有对应的实例，表明这个实例还没创建。</span><span class="hljs-comment">* BeanFactory 并不会在一开始就将所有的单例 bean 实例化好，而是在调用 getBean 获取</span><span class="hljs-comment">* bean 时再实例化，也就是懒加载。</span><span class="hljs-comment">* getBean 方法有很多重载，比如 getBean(String name, Object... args)，我们在首次获取</span><span class="hljs-comment">* 某个 bean 时，可以传入用于初始化 bean 的参数数组（args），BeanFactory 会根据这些参数</span><span class="hljs-comment">* 去匹配合适的构造方法构造 bean 实例。当然，如果单例 bean 早已创建好，这里的 args 就没有</span><span class="hljs-comment">* 用了，BeanFactory 不会多次实例化单例 bean。</span><span class="hljs-comment">*/</span><span class="hljs-keyword">if</span> (sharedInstance != <span class="hljs-keyword">null</span> &amp;&amp; args == <span class="hljs-keyword">null</span>) &#123;    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;<span class="hljs-comment">//日志</span>        <span class="hljs-keyword">if</span> (isSingletonCurrentlyInCreation(beanName)) &#123;            logger.trace(<span class="hljs-string">&quot;Returning eagerly cached instance of singleton bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; that is not fully initialized yet - a consequence of a circular reference&quot;</span>);        &#125;        <span class="hljs-keyword">else</span> &#123;            logger.trace(<span class="hljs-string">&quot;Returning cached instance of singleton bean &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27;&quot;</span>);        &#125;    &#125;<span class="hljs-comment">/*</span><span class="hljs-comment">* 如果 sharedInstance 是普通的单例 bean，下面的方法会直接返回。但如果</span><span class="hljs-comment">* sharedInstance 是 FactoryBean 类型的，则需调用 getObject 工厂方法获取真正的</span><span class="hljs-comment">* bean 实例。如果用户想获取 FactoryBean 本身，这里也不会做特别的处理，直接返回</span><span class="hljs-comment">* 即可。毕竟 FactoryBean 的实现类本身也是一种 bean，只不过具有一点特殊的功能而已。</span><span class="hljs-comment">*/</span>    beanInstance = getObjectForBeanInstance(sharedInstance, name, beanName, <span class="hljs-keyword">null</span>);&#125;</code></pre><p>单例模式的 Bean 在整个过程中只会被创建一次。第一次创建后会将该 Bean 加载到缓存中。后面，在获取 Bean 就会直接从单例缓存中获取。</p><p>如果从缓存中得到了 Bean 对象，则需要调用 <code>getObjectForBeanInstance(Object beanInstance, String name, String beanName, RootBeanDefinition mbd)</code> 方法，对 Bean 进行实例化处理。因为，缓存中记录的是最原始的 Bean 状态，我们得到的不一定是我们<strong>最终</strong>想要的 Bean 。</p><h5 id="原型模式依赖检查"><a href="#原型模式依赖检查" class="headerlink" title="原型模式依赖检查"></a>原型模式依赖检查</h5><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">// 因为 Spring 只解决单例模式下得循环依赖，在原型模式下如果存在循环依赖则会抛出异常。</span><span class="hljs-keyword">if</span> (isPrototypeCurrentlyInCreation(beanName)) &#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCurrentlyInCreationException(beanName);&#125;</code></pre><p>Spring 只处理单例模式下得循环依赖，对于原型模式的循环依赖直接抛出异常。主要原因还是在于，<strong>和 Spring 解决循环依赖的策略有关</strong>。</p><ul><li>对于<strong>单例( Singleton )模式</strong>， Spring 在创建 Bean 的时候并不是等 Bean 完全创建完成后才会将 Bean 添加至缓存中，而是不等 Bean 创建完成就会将创建 Bean 的 ObjectFactory 提早加入到缓存中，这样一旦下一个 Bean 创建的时候需要依赖 bean 时则直接使用 ObjectFactroy 。</li><li>但是<strong>原型( Prototype )模式</strong>，我们知道是没法使用缓存的，所以 Spring 对原型模式的循环依赖处理策略则是不处理。</li></ul><h5 id="从父类容器加载bean"><a href="#从父类容器加载bean" class="headerlink" title="从父类容器加载bean"></a>从父类容器加载bean</h5><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">// 如果当前容器中没有找到，则从父类容器中加载</span><span class="hljs-comment">// Check if bean definition exists in this factory.</span>BeanFactory parentBeanFactory = getParentBeanFactory();<span class="hljs-keyword">if</span> (parentBeanFactory != <span class="hljs-keyword">null</span> &amp;&amp; !containsBeanDefinition(beanName)) &#123;    <span class="hljs-comment">// Not found -&gt; check parent.</span>    String nameToLookup = originalBeanName(name);    <span class="hljs-comment">// 如果，父类容器为 AbstractBeanFactory ，直接递归查找</span>    <span class="hljs-keyword">if</span> (parentBeanFactory <span class="hljs-keyword">instanceof</span> AbstractBeanFactory) &#123;        <span class="hljs-keyword">return</span> ((AbstractBeanFactory) parentBeanFactory).doGetBean(                nameToLookup, requiredType, args, typeCheckOnly);    <span class="hljs-comment">// 用明确的 args 从 parentBeanFactory 中，获取 Bean 对象</span>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (args != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// Delegation to parent with explicit args.</span>        <span class="hljs-keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup, args);    <span class="hljs-comment">// 用明确的 requiredType 从 parentBeanFactory 中，获取 Bean 对象</span>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (requiredType != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// No args -&gt; delegate to standard getBean method.</span>        <span class="hljs-keyword">return</span> parentBeanFactory.getBean(nameToLookup, requiredType);    <span class="hljs-comment">// 直接使用 nameToLookup 从 parentBeanFactory 获取 Bean 对象</span>    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">return</span> (T) parentBeanFactory.getBean(nameToLookup);    &#125;&#125;</code></pre><p>如果<strong>当前</strong>容器缓存中没有相对应的 BeanDefinition 对象，则会尝试从父类工厂（<code>parentBeanFactory</code>）中加载，然后再去递归调用 <code>getBean(...)</code> 方法。</p><h5 id="标记当前bean为正在创建或者创建中"><a href="#标记当前bean为正在创建或者创建中" class="headerlink" title="标记当前bean为正在创建或者创建中"></a>标记当前bean为正在创建或者创建中</h5><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">// 如果不是仅仅做类型检查则是创建bean，这里需要记录</span><span class="hljs-keyword">if</span> (!typeCheckOnly) &#123;markBeanAsCreated(beanName);&#125;</code></pre><h5 id="获取BeanDefinition"><a href="#获取BeanDefinition" class="headerlink" title="获取BeanDefinition"></a>获取BeanDefinition</h5><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">// 从容器中获取 beanName 相应的 GenericBeanDefinition 对象，并将其转换为 RootBeanDefinition 对象</span><span class="hljs-keyword">final</span> RootBeanDefinition mbd = getMergedLocalBeanDefinition(beanName);<span class="hljs-comment">// 检查给定的合并的 BeanDefinition</span>checkMergedBeanDefinition(mbd, beanName, args);</code></pre><p>因为从 XML 配置文件中读取到的 Bean 信息是存储在GenericBeanDefinition 中的。但是，所有的 Bean 后续处理都是针对于 RootBeanDefinition 的，所以这里需要进行一个转换。转换的同时，如果父类 bean 不为空的话，则会一并合并父类的属性。</p><h5 id="依赖bean处理"><a href="#依赖bean处理" class="headerlink" title="依赖bean处理"></a>依赖bean处理</h5><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">// Guarantee initialization of beans that the current bean depends on.</span><span class="hljs-comment">// 处理所依赖的 bean</span>String[] dependsOn = mbd.getDependsOn();<span class="hljs-keyword">if</span> (dependsOn != <span class="hljs-keyword">null</span>) &#123;    <span class="hljs-keyword">for</span> (String dep : dependsOn) &#123;        <span class="hljs-comment">// 若给定的依赖 bean 已经注册为依赖给定的 bean</span>        <span class="hljs-comment">// 即循环依赖的情况，抛出 BeanCreationException 异常</span>        <span class="hljs-keyword">if</span> (isDependent(beanName, dep)) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                    <span class="hljs-string">&quot;Circular depends-on relationship between &#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; and &#x27;&quot;</span> + dep + <span class="hljs-string">&quot;&#x27;&quot;</span>);        &#125;        <span class="hljs-comment">// 缓存依赖调用</span>        registerDependentBean(dep, beanName);        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">// 递归处理依赖 Bean</span>            getBean(dep);        &#125; <span class="hljs-keyword">catch</span> (NoSuchBeanDefinitionException ex) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(mbd.getResourceDescription(), beanName,                    <span class="hljs-string">&quot;&#x27;&quot;</span> + beanName + <span class="hljs-string">&quot;&#x27; depends on missing bean &#x27;&quot;</span> + dep + <span class="hljs-string">&quot;&#x27;&quot;</span>, ex);        &#125;    &#125;&#125;</code></pre><ul><li>每个 Bean 都不是单独工作的，它会依赖其他 Bean，其他 Bean 也会依赖它。</li><li>对于依赖的 Bean ，它会优先加载，所以，在 Spring 的加载顺序中，在初始化某一个 Bean 的时候，首先会初始化这个 Bean 的依赖。</li></ul><h5 id="不同作用域的bean实例化"><a href="#不同作用域的bean实例化" class="headerlink" title="不同作用域的bean实例化"></a>不同作用域的bean实例化</h5><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">// bean 实例化</span><span class="hljs-comment">// Create bean instance.</span><span class="hljs-keyword">if</span> (mbd.isSingleton()) &#123; <span class="hljs-comment">// 单例模式</span>    sharedInstance = getSingleton(beanName, () -&gt; &#123;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-keyword">return</span> createBean(beanName, mbd, args);        &#125;        <span class="hljs-keyword">catch</span> (BeansException ex) &#123;            <span class="hljs-comment">// Explicitly remove instance from singleton cache: It might have been put there</span>            <span class="hljs-comment">// eagerly by the creation process, to allow for circular reference resolution.</span>            <span class="hljs-comment">// Also remove any beans that received a temporary reference to the bean.</span>            <span class="hljs-comment">// 显式从单例缓存中删除 Bean 实例</span>            <span class="hljs-comment">// 因为单例模式下为了解决循环依赖，可能他已经存在了，所以销毁它。 TODO 芋艿</span>            destroySingleton(beanName);            <span class="hljs-keyword">throw</span> ex;        &#125;    &#125;);    bean = getObjectForBeanInstance(sharedInstance, name, beanName, mbd);&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (mbd.isPrototype()) &#123; <span class="hljs-comment">// 原型模式</span>    <span class="hljs-comment">// It&#x27;s a prototype -&gt; create a new instance.</span>    Object prototypeInstance;    <span class="hljs-keyword">try</span> &#123;        beforePrototypeCreation(beanName);        prototypeInstance = createBean(beanName, mbd, args);    &#125; <span class="hljs-keyword">finally</span> &#123;        afterPrototypeCreation(beanName);    &#125;    bean = getObjectForBeanInstance(prototypeInstance, name, beanName, mbd);&#125; <span class="hljs-keyword">else</span> &#123;    <span class="hljs-comment">// 从指定的 scope 下创建 bean</span>    String scopeName = mbd.getScope();    <span class="hljs-keyword">final</span> Scope scope = <span class="hljs-keyword">this</span>.scopes.get(scopeName);    <span class="hljs-keyword">if</span> (scope == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">&quot;No Scope registered for scope name &#x27;&quot;</span> + scopeName + <span class="hljs-string">&quot;&#x27;&quot;</span>);    &#125;<span class="hljs-keyword">try</span> &#123;        Object scopedInstance = scope.get(beanName, () -&gt; &#123;            beforePrototypeCreation(beanName);            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-keyword">return</span> createBean(beanName, mbd, args);            &#125; <span class="hljs-keyword">finally</span> &#123;                afterPrototypeCreation(beanName);            &#125;        &#125;);        bean = getObjectForBeanInstance(scopedInstance, name, beanName, mbd);    &#125; <span class="hljs-keyword">catch</span> (IllegalStateException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanCreationException(beanName,                <span class="hljs-string">&quot;Scope &#x27;&quot;</span> + scopeName + <span class="hljs-string">&quot;&#x27; is not active for the current thread; consider &quot;</span> + <span class="hljs-string">&quot;defining a scoped proxy for this bean if you intend to refer to it from a singleton&quot;</span>,                ex);    &#125;&#125;</code></pre><ul><li>Spring Bean 的作用域默认为 singleton 。当然，还有其他作用域，如 prototype、request、session 等。</li><li>不同的作用域会有不同的初始化策略。</li></ul><h5 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h5><pre><code class="hljs java"><span class="hljs-comment">// AbstractBeanFactory.java</span><span class="hljs-comment">// 检查需要的类型是否符合 bean 的实际类型</span><span class="hljs-comment">// Check if required type matches the type of the actual bean instance.</span><span class="hljs-keyword">if</span> (requiredType != <span class="hljs-keyword">null</span> &amp;&amp; !requiredType.isInstance(bean)) &#123;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// 执行转换</span>        T convertedBean = getTypeConverter().convertIfNecessary(bean, requiredType);        <span class="hljs-comment">// 转换失败，抛出 BeanNotOfRequiredTypeException 异常</span>        <span class="hljs-keyword">if</span> (convertedBean == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanNotOfRequiredTypeException(name, requiredType, bean.getClass());        &#125;        <span class="hljs-keyword">return</span> convertedBean;    &#125; <span class="hljs-keyword">catch</span> (TypeMismatchException ex) &#123;        <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;            logger.trace(<span class="hljs-string">&quot;Failed to convert bean &#x27;&quot;</span> + name + <span class="hljs-string">&quot;&#x27; to required type &#x27;&quot;</span> +                    ClassUtils.getQualifiedName(requiredType) + <span class="hljs-string">&quot;&#x27;&quot;</span>, ex);        &#125;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanNotOfRequiredTypeException(name, requiredType, bean.getClass());    &#125;&#125;</code></pre><ul><li>在调用 <code>#doGetBean(...)</code> 方法时，<code>有一个 requiredTyp</code>e 参数。该参数的功能就是将返回的 Bean 转换为 <code>requiredType</code> 类型。</li><li>当然就一般而言，我们是不需要进行类型转换的，也就是 <code>requiredType</code> 为空（比如 <code>#getBean(String name)</code> 方法）。但有，可能会存在这种情况，比如我们返回的 Bean 类型为 String ，我们在使用的时候需要将其转换为 Integer，那么这个时候 <code>requiredType</code> 就有用武之地了。<strong>当然我们一般是不需要这样做的</strong>。</li></ul><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>以上是对Spring Bean加载过程的概览，主要过程：</p><ol><li>分析从缓存中获取单例 Bean ，以及对 Bean 的实例中获取对象。</li><li>如果从单例缓存中获取 Bean ，Spring 是怎么加载的呢？所以第二部分是分析 Bean 加载，以及 Bean 的依赖处理。</li><li>Bean 已经加载了，依赖也处理完毕了，第三部分则分析各个作用域的 Bean 初始化过程。</li></ol>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>装载BeanDefinition总结</title>
    <link href="/2021/02/24/%E8%A3%85%E8%BD%BDBeanDefinition%E6%80%BB%E7%BB%93/"/>
    <url>/2021/02/24/%E8%A3%85%E8%BD%BDBeanDefinition%E6%80%BB%E7%BB%93/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">装载BeanDefinition总结</font></div><p>该篇是对前边十几篇的总结。</p><p>IOC容器初始化的过程：<strong>Resource定位</strong>、<strong>BeanDefinition的装载和解析</strong>、<strong>BeanDefinition注册</strong>。</p><p><strong>Resource定位</strong>：一般使用外部资源描述<code>bean</code>对象，所以初始化IOC容器第一步需要定位这个外部资源（统一资源加载策略）。</p><p><strong>BeanDefinition的装载和解析</strong>：装载就是BeanDefinition的载入，通过BeanDefinitionReader读取、解析Resource资源，也就是将用户定义的Bean表示成IOC容器内部的数据结构–BeanDefinition。</p><blockquote><p>注：1.IOC容器内部维护着一个BeanDefinitionMap的数据结构。</p><p>​        2.配置文件中的每一个<bean>都对应着一个BeanDefinition对象。</p></blockquote><p><strong>BeanDefinition注册</strong>：向IOC容器内部维护的BeanDefinitionMap中注册第二步解析好的BeanDefinition对象，该过程通过BeanDefinitionRegistry接口实现。</p><blockquote><p>注：1.该过程并没有完成依赖注入（Bean创建），Bean创建发生在第一次调用<code>getBean()</code>方法，向容器索取             Bean时，因为默认是懒加载。</p><p>​        2.可以修改加载方式，即设置某个Bean的<code>lazyinit=false</code>，那么这个Bean的依赖注入会在IOC容器初始化的时候完成。</p></blockquote><p>在初始阶段，做过一个实例：</p><pre><code class="hljs java"><span class="hljs-comment">//①获取资源</span>ClassPathResource resource = <span class="hljs-keyword">new</span> ClassPathResource(<span class="hljs-string">&quot;bean.xml&quot;</span>);<span class="hljs-comment">//②获取 BeanFactory</span>DefaultListableBeanFactory factory = <span class="hljs-keyword">new</span> DefaultListableBeanFactory();<span class="hljs-comment">//③根据新建的 BeanFactory 创建一个 BeanDefinitionReader 对象，该 Reader 对象为资源的解析器</span>XmlBeanDefinitionReader reader = <span class="hljs-keyword">new</span> XmlBeanDefinitionReader(factory);<span class="hljs-comment">//④装载资源</span>reader.loadBeanDefinitions(resource);</code></pre><p><strong>①</strong>：根据 Xml 配置文件创建 Resource 资源对象。ClassPathResource 是 Resource 接口的子类，bean.xml 文件中的内容是我们定义的 Bean 信息。</p><p><strong>②</strong>：创建一个 BeanFactory 。DefaultListableBeanFactory 是 BeanFactory 的一个子类，BeanFactory 作为一个接口，其实它本身是不具有独立使用的功能的，而 DefaultListableBeanFactory 则是真正可以独立使用的 IoC 容器，它是整个 Spring IoC 的始祖。</p><p><strong>③</strong>：创建 XmlBeanDefinitionReader 读取器，用于载入和解析 BeanDefinition 。</p><p><strong>④</strong>：开始 BeanDefinition 的载入和注册进程，完成后的 BeanDefinition 放置在 IoC 容器中。</p><h4 id="Resource定位"><a href="#Resource定位" class="headerlink" title="Resource定位"></a><code>Resource</code>定位</h4><p>Spring为了解决资源定位问题，提供了两个接口：<strong>Resource</strong>、<strong>ResourceLoader</strong>。</p><blockquote><p>Resource：Spring 统一资源的抽象接口。</p><p>ResourceLoader：Spring 资源加载的统一抽象。</p></blockquote><p>Resource 资源的定位需要 Resource 和 ResourceLoader 两个接口互相配合，在上面那段代码中 <code>new ClassPathResource(&quot;bean.xml&quot;)</code> 为我们定义了Resource，那么 ResourceLoader 则是在什么时候初始化的呢？看 XmlBeanDefinitionReader 构造方法：</p><pre><code class="hljs java"><span class="hljs-comment">// XmlBeanDefinitionReader.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">XmlBeanDefinitionReader</span><span class="hljs-params">(BeanDefinitionRegistry registry)</span> </span>&#123;<span class="hljs-keyword">super</span>(registry);&#125;<span class="hljs-comment">//第③步执行了XmlBeanDefinitionReader reader = new XmlBeanDefinitionReader(factory);</span><span class="hljs-comment">//注：DefaultListableBeanFactory是BeanDefinitionRegistry的子类。</span></code></pre><p>直接调用父类 AbstractBeanDefinitionReader 构造方法：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-title">AbstractBeanDefinitionReader</span><span class="hljs-params">(BeanDefinitionRegistry registry)</span> </span>&#123;    Assert.notNull(registry, <span class="hljs-string">&quot;BeanDefinitionRegistry must not be null&quot;</span>);    <span class="hljs-keyword">this</span>.registry = registry;    <span class="hljs-comment">// 如果设置了 ResourceLoader 则用设置的，否则使用 PathMatchingResourcePatternResolver</span>    <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.registry <span class="hljs-keyword">instanceof</span> ResourceLoader) &#123;        <span class="hljs-keyword">this</span>.resourceLoader = (ResourceLoader) <span class="hljs-keyword">this</span>.registry;    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">this</span>.resourceLoader = <span class="hljs-keyword">new</span> PathMatchingResourcePatternResolver();    &#125;    <span class="hljs-comment">// Inherit Environment if possible</span>    <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.registry <span class="hljs-keyword">instanceof</span> EnvironmentCapable) &#123;        <span class="hljs-keyword">this</span>.environment = ((EnvironmentCapable) <span class="hljs-keyword">this</span>.registry).getEnvironment();    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">this</span>.environment = <span class="hljs-keyword">new</span> StandardEnvironment();    &#125;&#125;</code></pre><h4 id="BeanDefinition的装载和解析"><a href="#BeanDefinition的装载和解析" class="headerlink" title="BeanDefinition的装载和解析"></a><code>BeanDefinition</code>的装载和解析</h4><p>第④步<code>reader.loadBeanDefinitions(resource);</code>开始BeanDefinition的解析过程：</p><pre><code class="hljs java"><span class="hljs-comment">//XmlBeanDefinitionReader.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">loadBeanDefinitions</span><span class="hljs-params">(Resource resource)</span> <span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;    <span class="hljs-keyword">return</span> loadBeanDefinitions(<span class="hljs-keyword">new</span> EncodedResource(resource));&#125;</code></pre><p>在这个方法会将资源 Resource 包装成一个 <code>EncodedResource</code>实例对象，然后调用 <code>loadBeanDefinitions(EncodedResource encodedResource)</code> 方法。而将 Resource 封装成 EncodedResource 主要是为了对 Resource 进行<strong>编码</strong>，保证内容读取的正确性。</p><pre><code class="hljs java"><span class="hljs-comment">// XmlBeanDefinitionReader.java</span><span class="hljs-comment">// 具体过程查看之前的文章</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">loadBeanDefinitions</span><span class="hljs-params">(EncodedResource encodedResource)</span> <span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;<span class="hljs-comment">// ... 省略一些代码</span><span class="hljs-keyword">try</span> &#123;<span class="hljs-comment">// 将资源文件转为 InputStream 的 IO 流</span>InputStream inputStream = encodedResource.getResource().getInputStream();<span class="hljs-keyword">try</span> &#123;<span class="hljs-comment">// 从 InputStream 中得到 XML 的解析源</span>InputSource inputSource = <span class="hljs-keyword">new</span> InputSource(inputStream);<span class="hljs-keyword">if</span> (encodedResource.getEncoding() != <span class="hljs-keyword">null</span>) &#123;inputSource.setEncoding(encodedResource.getEncoding());&#125;<span class="hljs-comment">// 核心逻辑部分，执行加载 BeanDefinition</span><span class="hljs-keyword">return</span> doLoadBeanDefinitions(inputSource, encodedResource.getResource());&#125;<span class="hljs-keyword">finally</span> &#123;inputStream.close();&#125;&#125;<span class="hljs-comment">// 省略一些代码</span>&#125;</code></pre><p>从 <code>encodedResource</code> 源中获取 xml 的解析源，然后调用 <code>doLoadBeanDefinitions(InputSource inputSource, Resource resource)</code> 方法，执行具体的解析过程。</p><pre><code class="hljs java"><span class="hljs-comment">// XmlBeanDefinitionReader.java</span><span class="hljs-comment">// 具体过程查看之前的文章</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">int</span> <span class="hljs-title">doLoadBeanDefinitions</span><span class="hljs-params">(InputSource inputSource, Resource resource)</span></span><span class="hljs-function"><span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;<span class="hljs-keyword">try</span> &#123;<span class="hljs-comment">// 获取 XML Document 实例</span>Document doc = doLoadDocument(inputSource, resource);<span class="hljs-comment">// 根据 Document 实例，注册 BeanDefinition 信息</span><span class="hljs-keyword">int</span> count = registerBeanDefinitions(doc, resource);<span class="hljs-keyword">return</span> count;&#125;<span class="hljs-comment">// ... 省略一堆配置</span>&#125;</code></pre><p>在该方法中主要做两件事：</p><p>1、调用<code>doLoadDocument(inputSource, resource)</code>根据 xml 解析源获取相应的 Document 对象。</p><p>2、调用 <code>registerBeanDefinitions(Document doc, Resource resource)</code> 方法，开启 BeanDefinition 的解析注册过程。</p><h5 id="获取相应的Document对象"><a href="#获取相应的Document对象" class="headerlink" title="获取相应的Document对象"></a>获取相应的<code>Document</code>对象</h5><p>调用<code>doLoadDocument(inputSource, resource)</code>根据 xml 解析源获取相应的 Document 对象。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> Document <span class="hljs-title">doLoadDocument</span><span class="hljs-params">(InputSource inputSource, Resource resource)</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.documentLoader.loadDocument(inputSource, getEntityResolver(), <span class="hljs-keyword">this</span>.errorHandler,getValidationModeForResource(resource), isNamespaceAware());&#125;</code></pre><p>五个参数：</p><p>1、InputSource：加载 Document 的 Resource 源。</p><p>2、EntityResolver：解析文件的解析器，重点，前边已经分析</p><p>3、ErrorHandler：处理加载 Document 对象的过程的错误。</p><p>4、validationMode：验证模式，重点，前边已经分析，默认自动获取，值为VALIDATION_AUTO，1。</p><p>5、namespaceAware：命名空间支持。如果要提供对 XML 名称空间的支持，则为 <code>true</code> ，默认false。</p><p><code>loadDocument(InputSource inputSource, EntityResolver entityResolver, ErrorHandler errorHandler, int validationMode, boolean namespaceAware)</code> 方法，在类 DefaultDocumentLoader 中提供了实现。</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultDocumentLoader.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Document <span class="hljs-title">loadDocument</span><span class="hljs-params">(InputSource inputSource, EntityResolver entityResolver,</span></span><span class="hljs-function"><span class="hljs-params">ErrorHandler errorHandler, <span class="hljs-keyword">int</span> validationMode, <span class="hljs-keyword">boolean</span> namespaceAware)</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;<span class="hljs-comment">// 创建 DocumentBuilderFactory</span>DocumentBuilderFactory factory = createDocumentBuilderFactory(validationMode, namespaceAware);<span class="hljs-comment">// 创建 DocumentBuilder</span>DocumentBuilder builder = createDocumentBuilder(factory, entityResolver, errorHandler);<span class="hljs-comment">// 解析 XML InputSource 返回 Document 对象</span><span class="hljs-keyword">return</span> builder.parse(inputSource);&#125;</code></pre><h5 id="解析注册BeanDefinition信息"><a href="#解析注册BeanDefinition信息" class="headerlink" title="解析注册BeanDefinition信息"></a>解析注册<code>BeanDefinition</code>信息</h5><p>根据上个步骤转换的Document实例，解析注册 BeanDefinition 信息，该过程调用<code>registerBeanDefinitions(doc, resource);</code>实现：</p><pre><code class="hljs java"><span class="hljs-comment">// XmlBeanDefinitionReader.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">registerBeanDefinitions</span><span class="hljs-params">(Document doc, Resource resource)</span> <span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;<span class="hljs-comment">// 创建 BeanDefinitionDocumentReader 对象</span>BeanDefinitionDocumentReader documentReader = createBeanDefinitionDocumentReader();<span class="hljs-comment">// 获取已注册的 BeanDefinition 数量</span><span class="hljs-keyword">int</span> countBefore = getRegistry().getBeanDefinitionCount();<span class="hljs-comment">// 创建 XmlReaderContext 对象</span><span class="hljs-comment">// 注册 BeanDefinition</span>documentReader.registerBeanDefinitions(doc, createReaderContext(resource));<span class="hljs-comment">// 计算新注册的 BeanDefinition 数量</span><span class="hljs-keyword">return</span> getRegistry().getBeanDefinitionCount() - countBefore;&#125;</code></pre><p>1、首先，创建 BeanDefinition 的解析器 BeanDefinitionDocumentReader 。<br>2、然后，调用该 BeanDefinitionDocumentReader 的 <code>registerBeanDefinitions(Document doc, XmlReaderContext readerContext) </code>方法，开启解析过程，这里使用的是<strong>委派模式</strong>，具体的实现由子类 DefaultBeanDefinitionDocumentReader 完成。</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultBeanDefinitionDocumentReader.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerBeanDefinitions</span><span class="hljs-params">(Document doc, XmlReaderContext readerContext)</span> </span>&#123;    <span class="hljs-keyword">this</span>.readerContext = readerContext;    <span class="hljs-comment">// 获得 XML Document Root Element</span>    <span class="hljs-comment">// 执行注册 BeanDefinition</span>    doRegisterBeanDefinitions(doc.getDocumentElement());&#125;</code></pre><h6 id="对Document对象进行解析"><a href="#对Document对象进行解析" class="headerlink" title="对Document对象进行解析"></a>对<code>Document</code>对象进行解析</h6><p>从 Document 对象中获取<strong>根元素 root</strong>，然后调用 ``doRegisterBeanDefinitions(Element root)` 方法，开启真正的解析过程。</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultBeanDefinitionDocumentReader.java</span><span class="hljs-comment">//前边文章有分析</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">doRegisterBeanDefinitions</span><span class="hljs-params">(Element root)</span> </span>&#123;    <span class="hljs-comment">// ... 省略部分代码（非核心）</span>    <span class="hljs-keyword">this</span>.delegate = createDelegate(getReaderContext(), root, parent);    <span class="hljs-comment">// 解析前处理</span>    preProcessXml(root);    <span class="hljs-comment">// 解析</span>    parseBeanDefinitions(root, <span class="hljs-keyword">this</span>.delegate);    <span class="hljs-comment">// 解析后处理</span>    postProcessXml(root);&#125;</code></pre><p>注：<code>preProcessXml(Element root)</code>、#po<code>stProcessXml(Element root) </code>为前置、后置增强处理，目前 Spring 中都是空实现。</p><p><code>parseBeanDefinitions(Element root, BeanDefinitionParserDelegate delegate)</code> 是对根元素 root 的解析注册过程：</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultBeanDefinitionDocumentReader.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseBeanDefinitions</span><span class="hljs-params">(Element root, BeanDefinitionParserDelegate delegate)</span> </span>&#123;    <span class="hljs-comment">// 如果根节点使用默认命名空间，执行默认解析</span>    <span class="hljs-keyword">if</span> (delegate.isDefaultNamespace(root)) &#123;        <span class="hljs-comment">// 遍历子节点</span>        NodeList nl = root.getChildNodes();        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nl.getLength(); i++) &#123;            Node node = nl.item(i);            <span class="hljs-keyword">if</span> (node <span class="hljs-keyword">instanceof</span> Element) &#123;                Element ele = (Element) node;                <span class="hljs-comment">// 如果该节点使用默认命名空间，执行默认解析</span>                <span class="hljs-keyword">if</span> (delegate.isDefaultNamespace(ele)) &#123;                    parseDefaultElement(ele, delegate);                <span class="hljs-comment">// 如果该节点非默认命名空间，执行自定义解析</span>                &#125; <span class="hljs-keyword">else</span> &#123;                    delegate.parseCustomElement(ele);                &#125;            &#125;        &#125;    <span class="hljs-comment">// 如果根节点非默认命名空间，执行自定义解析</span>    &#125; <span class="hljs-keyword">else</span> &#123;        delegate.parseCustomElement(root);    &#125;&#125;</code></pre><p>迭代 root 元素的所有子节点，对其进行判断：</p><p>1、若节点为默认命名空间，则调用 <code>parseDefaultElement(Element ele, BeanDefinitionParserDelegate delegate)</code> 方法，开启默认标签的解析注册过程。</p><p>2、否则，调用 <code>BeanDefinitionParserDelegate.parseCustomElement(Element ele)</code> 方法，开启自定义标签的解析注册过程。</p><p>两个过程前边都有介绍。</p><p><strong>对默认标签的解析</strong></p><pre><code class="hljs java"><span class="hljs-comment">// DefaultBeanDefinitionDocumentReader.java</span><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseDefaultElement</span><span class="hljs-params">(Element ele, BeanDefinitionParserDelegate delegate)</span> </span>&#123;<span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, IMPORT_ELEMENT)) &#123; <span class="hljs-comment">// import</span>importBeanDefinitionResource(ele);&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, ALIAS_ELEMENT)) &#123; <span class="hljs-comment">// alias</span>processAliasRegistration(ele);&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, BEAN_ELEMENT)) &#123; <span class="hljs-comment">// bean</span>processBeanDefinition(ele, delegate);&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, NESTED_BEANS_ELEMENT)) &#123; <span class="hljs-comment">// beans</span><span class="hljs-comment">// recurse</span>doRegisterBeanDefinitions(ele);&#125;&#125;</code></pre><p>对四大标签：<code>&lt;import&gt;</code>、<code>&lt;alias&gt;</code>、<code>&lt;bean&gt;</code>、<code>&lt;beans&gt;</code> 进行解析。<strong>其中 <code>&lt;bean&gt;</code> 标签的解析为核心工作，解析为BeanDefinition</strong>。</p><p><strong>对自定义标签的解析</strong></p><pre><code class="hljs java"><span class="hljs-comment">// BeanDefinitionParserDelegate.java</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> BeanDefinition <span class="hljs-title">parseCustomElement</span><span class="hljs-params">(Element ele)</span> </span>&#123;    <span class="hljs-keyword">return</span> parseCustomElement(ele, <span class="hljs-keyword">null</span>);&#125;<span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> BeanDefinition <span class="hljs-title">parseCustomElement</span><span class="hljs-params">(Element ele, <span class="hljs-meta">@Nullable</span> BeanDefinition containingBd)</span> </span>&#123;    <span class="hljs-comment">// 获取 namespaceUri</span>    String namespaceUri = getNamespaceURI(ele);    <span class="hljs-keyword">if</span> (namespaceUri == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;    <span class="hljs-comment">// 根据 namespaceUri 获取相应的 Handler</span>    NamespaceHandler handler = <span class="hljs-keyword">this</span>.readerContext.getNamespaceHandlerResolver().resolve(namespaceUri);    <span class="hljs-keyword">if</span> (handler == <span class="hljs-keyword">null</span>) &#123;        error(<span class="hljs-string">&quot;Unable to locate Spring NamespaceHandler for XML schema namespace [&quot;</span> + namespaceUri + <span class="hljs-string">&quot;]&quot;</span>, ele);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;    <span class="hljs-comment">// 调用自定义的 Handler 处理</span>    <span class="hljs-keyword">return</span> handler.parse(ele, <span class="hljs-keyword">new</span> ParserContext(<span class="hljs-keyword">this</span>.readerContext, <span class="hljs-keyword">this</span>, containingBd));&#125;</code></pre><p>获取节点的 <code>namespaceUri</code>，然后根据该 <code>namespaceUri</code> 获取相对应的 NamespaceHandler，最后调用 NamespaceHandler 的 <code>parse(Element element, ParserContext parserContext)</code> 方法，即完成自定义标签的解析和注入。</p><h6 id="注册BeanDefinition"><a href="#注册BeanDefinition" class="headerlink" title="注册BeanDefinition"></a>注册<code>BeanDefinition</code></h6><p>将 Document 对象里面的 Bean 标签解析成了一个个的 BeanDefinition 后，下一步则是将这些 BeanDefinition 注册到 IoC 容器中。动作的触发是在解析 Bean 标签完成后：</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultBeanDefinitionDocumentReader.java</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">processBeanDefinition</span><span class="hljs-params">(Element ele, BeanDefinitionParserDelegate delegate)</span> </span>&#123;    <span class="hljs-comment">// 进行 bean 元素解析。</span>    <span class="hljs-comment">// 如果解析成功，则返回 BeanDefinitionHolder 对象。而 BeanDefinitionHolder 为 name 和 alias 的 BeanDefinition 对象</span>    <span class="hljs-comment">// 如果解析失败，则返回 null 。</span>    BeanDefinitionHolder bdHolder = delegate.parseBeanDefinitionElement(ele);    <span class="hljs-keyword">if</span> (bdHolder != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// 进行自定义标签处理</span>        bdHolder = delegate.decorateBeanDefinitionIfRequired(ele, bdHolder);        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">// 进行 BeanDefinition 的注册</span>            BeanDefinitionReaderUtils.registerBeanDefinition(bdHolder, getReaderContext().getRegistry());        &#125; <span class="hljs-keyword">catch</span> (BeanDefinitionStoreException ex) &#123;            getReaderContext().error(<span class="hljs-string">&quot;Failed to register bean definition with name &#x27;&quot;</span> +                    bdHolder.getBeanName() + <span class="hljs-string">&quot;&#x27;&quot;</span>, ele, ex);        &#125;        <span class="hljs-comment">// 发出响应事件，通知相关的监听器，已完成该 Bean 标签的解析。</span>        <span class="hljs-comment">// Send registration event.</span>        getReaderContext().fireComponentRegistered(<span class="hljs-keyword">new</span> BeanComponentDefinition(bdHolder));    &#125;&#125;</code></pre><p>调用 <code>BeanDefinitionReaderUtils.registerBeanDefinition()</code> 方法来注册。其实，这里面也是调用 BeanDefinitionRegistry 的 <code>.registerBeanDefinition(String beanName, BeanDefinition beanDefinition)</code> 方法，来注册 BeanDefinition 。不过，最终的实现是在 DefaultListableBeanFactory 中实现：</p><pre><code class="hljs java"><span class="hljs-comment">// DefaultListableBeanFactory.java</span><span class="hljs-comment">// 所谓注册，就是添加到了一个Map集合，key是beanName，value是BeanDefinition对象。</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerBeanDefinition</span><span class="hljs-params">(String beanName, BeanDefinition beanDefinition)</span></span><span class="hljs-function">        <span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;    <span class="hljs-comment">// ...省略校验相关的代码</span>    <span class="hljs-comment">// 从缓存中获取指定 beanName 的 BeanDefinition</span>    BeanDefinition existingDefinition = <span class="hljs-keyword">this</span>.beanDefinitionMap.get(beanName);    <span class="hljs-comment">// 如果已经存在</span>    <span class="hljs-keyword">if</span> (existingDefinition != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// 如果存在但是不允许覆盖，抛出异常</span>        <span class="hljs-keyword">if</span> (!isAllowBeanDefinitionOverriding()) &#123;             <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionOverrideException(beanName, beanDefinition, existingDefinition);        &#125; <span class="hljs-keyword">else</span> &#123;           <span class="hljs-comment">// ...省略 logger 打印日志相关的代码</span>        &#125;        <span class="hljs-comment">// 【重点】允许覆盖，直接覆盖原有的 BeanDefinition 到 beanDefinitionMap 中。</span>        <span class="hljs-keyword">this</span>.beanDefinitionMap.put(beanName, beanDefinition);    <span class="hljs-comment">// 如果未存在</span>    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// ... 省略非核心的代码</span>        <span class="hljs-comment">// 【重点】添加到 BeanDefinition 到 beanDefinitionMap 中。</span>        <span class="hljs-keyword">this</span>.beanDefinitionMap.put(beanName, beanDefinition);    &#125;    <span class="hljs-comment">// 重新设置 beanName 对应的缓存</span>    <span class="hljs-keyword">if</span> (existingDefinition != <span class="hljs-keyword">null</span> || containsSingleton(beanName)) &#123;        resetBeanDefinition(beanName);    &#125;&#125;</code></pre><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>现在 IoC 容器中已经建立了整个 Bean 的配置信息，这些 Bean 可以被检索、使用、维护，他们是控制反转的基础，是后面注入 Bean 的依赖。</p><p>流程图：</p><p><img src="/images/sprin_IOC/IOC%E5%AE%B9%E5%99%A8%E5%88%9D%E5%A7%8B%E5%8C%96%E6%B5%81%E7%A8%8B.png" alt="IOC容器初始化流程"></p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>BeanDefinition对象的注册</title>
    <link href="/2021/02/24/%E6%B3%A8%E5%86%8CBeanDefinition/"/>
    <url>/2021/02/24/%E6%B3%A8%E5%86%8CBeanDefinition/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">BeanDefinition对象的注册</font></div><p>在完成<code>BeanDifinition</code>对象的所有解析工作后，然后会执行<code>BeanDefinitionReaderUtils.registerBeanDefinition()</code>进行<code>BeanDefinition</code>对象的注册。</p><h4 id="BeanDefinitionReaderUtils-registerBeanDefinition-方法"><a href="#BeanDefinitionReaderUtils-registerBeanDefinition-方法" class="headerlink" title="BeanDefinitionReaderUtils.registerBeanDefinition()方法"></a><code>BeanDefinitionReaderUtils.registerBeanDefinition()</code>方法</h4><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerBeanDefinition</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">    BeanDefinitionHolder definitionHolder, BeanDefinitionRegistry registry)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;    <span class="hljs-comment">// Register bean definition under primary name.</span>    <span class="hljs-comment">// 注册beandefinition到对应的beanName</span>    String beanName = definitionHolder.getBeanName();    registry.registerBeanDefinition(beanName, definitionHolder.getBeanDefinition());    <span class="hljs-comment">// Register aliases for bean name, if any.</span>    <span class="hljs-comment">// 注册beanname的别名</span>    String[] aliases = definitionHolder.getAliases();    <span class="hljs-keyword">if</span> (aliases != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">for</span> (String alias : aliases) &#123;            registry.registerAlias(beanName, alias);        &#125;    &#125;&#125;</code></pre><h5 id="registerBeanDefinition-方法"><a href="#registerBeanDefinition-方法" class="headerlink" title="registerBeanDefinition()方法"></a><code>registerBeanDefinition()</code>方法</h5><p>注册beandefinition到对应的beanName是通过<code>BeanDefinitionRegistry.registerBeanDefinition()</code>方法：</p><pre><code class="hljs java"><span class="hljs-comment">//---------------------------------------------------------------------</span><span class="hljs-comment">// Implementation of BeanDefinitionRegistry interface</span><span class="hljs-comment">//---------------------------------------------------------------------</span><span class="hljs-comment">// DefaultListableBeanFactory.java</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerBeanDefinition</span><span class="hljs-params">(String beanName, BeanDefinition beanDefinition)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;    <span class="hljs-comment">// 校验 beanName 与 beanDefinition 非空</span>    Assert.hasText(beanName, <span class="hljs-string">&quot;Bean name must not be empty&quot;</span>);    Assert.notNull(beanDefinition, <span class="hljs-string">&quot;BeanDefinition must not be null&quot;</span>);    <span class="hljs-comment">// &lt;1&gt; 校验 BeanDefinition 。</span>    <span class="hljs-comment">// 这是注册前的最后一次校验了，主要是对属性 methodOverrides 进行校验。</span>    <span class="hljs-keyword">if</span> (beanDefinition <span class="hljs-keyword">instanceof</span> AbstractBeanDefinition) &#123;        <span class="hljs-keyword">try</span> &#123;            ((AbstractBeanDefinition) beanDefinition).validate();        &#125;        <span class="hljs-keyword">catch</span> (BeanDefinitionValidationException ex) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(beanDefinition.getResourceDescription(), beanName,                                                   <span class="hljs-string">&quot;Validation of bean definition failed&quot;</span>, ex);        &#125;    &#125;    <span class="hljs-comment">// &lt;2&gt; 从缓存中获取指定 beanName 的 BeanDefinition</span>    BeanDefinition existingDefinition = <span class="hljs-keyword">this</span>.beanDefinitionMap.get(beanName);    <span class="hljs-keyword">if</span> (existingDefinition != <span class="hljs-keyword">null</span>) &#123;<span class="hljs-comment">// 如果存在</span>        <span class="hljs-comment">// 如果存在但是不允许覆盖，抛出异常</span>        <span class="hljs-keyword">if</span> (!isAllowBeanDefinitionOverriding()) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionOverrideException(beanName, beanDefinition, existingDefinition);        &#125;        <span class="hljs-comment">// 覆盖 beanDefinition 大于 被覆盖的 beanDefinition 的 ROLE ，打印 info 日志</span>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (existingDefinition.getRole() &lt; beanDefinition.getRole()) &#123;            <span class="hljs-comment">// e.g. was ROLE_APPLICATION, now overriding with ROLE_SUPPORT or ROLE_INFRASTRUCTURE</span>            <span class="hljs-keyword">if</span> (logger.isInfoEnabled()) &#123;                logger.info(<span class="hljs-string">&quot;Overriding user-defined bean definition for bean &#x27;&quot;</span> + beanName +                            <span class="hljs-string">&quot;&#x27; with a framework-generated bean definition: replacing [&quot;</span> +                            existingDefinition + <span class="hljs-string">&quot;] with [&quot;</span> + beanDefinition + <span class="hljs-string">&quot;]&quot;</span>);            &#125;        &#125;        <span class="hljs-comment">// 覆盖 beanDefinition 与 被覆盖的 beanDefinition 不相同，打印 debug 日志</span>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (!beanDefinition.equals(existingDefinition)) &#123;            <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                logger.debug(<span class="hljs-string">&quot;Overriding bean definition for bean &#x27;&quot;</span> + beanName +                             <span class="hljs-string">&quot;&#x27; with a different definition: replacing [&quot;</span> + existingDefinition +                             <span class="hljs-string">&quot;] with [&quot;</span> + beanDefinition + <span class="hljs-string">&quot;]&quot;</span>);            &#125;        &#125;        <span class="hljs-comment">// 其它，打印 debug 日志</span>        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                logger.trace(<span class="hljs-string">&quot;Overriding bean definition for bean &#x27;&quot;</span> + beanName +                             <span class="hljs-string">&quot;&#x27; with an equivalent definition: replacing [&quot;</span> + existingDefinition +                             <span class="hljs-string">&quot;] with [&quot;</span> + beanDefinition + <span class="hljs-string">&quot;]&quot;</span>);            &#125;        &#125;        <span class="hljs-comment">// 允许覆盖，直接覆盖原有的 BeanDefinition 到 beanDefinitionMap 中。</span>        <span class="hljs-keyword">this</span>.beanDefinitionMap.put(beanName, beanDefinition);    &#125;    <span class="hljs-keyword">else</span> &#123;<span class="hljs-comment">// 如果不存在</span>        <span class="hljs-comment">// 检测创建 Bean 阶段是否已经开启，如果开启了则需要对 beanDefinitionMap 进行并发控制</span>        <span class="hljs-keyword">if</span> (hasBeanCreationStarted()) &#123;            <span class="hljs-comment">// beanDefinitionMap 为全局变量，避免并发情况</span>            <span class="hljs-comment">// Cannot modify startup-time collection elements anymore (for stable iteration)</span>            <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.beanDefinitionMap) &#123;                <span class="hljs-comment">// 添加到 BeanDefinition 到 beanDefinitionMap 中。</span>                <span class="hljs-keyword">this</span>.beanDefinitionMap.put(beanName, beanDefinition);                <span class="hljs-comment">// 添加 beanName 到 beanDefinitionNames 中</span>                List&lt;String&gt; updatedDefinitions = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;(<span class="hljs-keyword">this</span>.beanDefinitionNames.size() + <span class="hljs-number">1</span>);                updatedDefinitions.addAll(<span class="hljs-keyword">this</span>.beanDefinitionNames);                updatedDefinitions.add(beanName);                <span class="hljs-keyword">this</span>.beanDefinitionNames = updatedDefinitions;                <span class="hljs-comment">// 从 manualSingletonNames 移除 beanName</span>                removeManualSingletonName(beanName);            &#125;        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// Still in startup registration phase</span>            <span class="hljs-comment">// 添加到 BeanDefinition 到 beanDefinitionMap 中。</span>            <span class="hljs-keyword">this</span>.beanDefinitionMap.put(beanName, beanDefinition);            <span class="hljs-comment">// 添加 beanName 到 beanDefinitionNames 中</span>            <span class="hljs-keyword">this</span>.beanDefinitionNames.add(beanName);            <span class="hljs-comment">// 从 manualSingletonNames 移除 beanName</span>            removeManualSingletonName(beanName);        &#125;        <span class="hljs-keyword">this</span>.frozenBeanDefinitionNames = <span class="hljs-keyword">null</span>;    &#125;    <span class="hljs-comment">// &lt;5&gt; 重新设置 beanName 对应的缓存</span>    <span class="hljs-keyword">if</span> (existingDefinition != <span class="hljs-keyword">null</span> || containsSingleton(beanName)) &#123;        resetBeanDefinition(beanName);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (isConfigurationFrozen()) &#123;        clearByTypeCache();    &#125;&#125;</code></pre><p>过程：</p><ul><li><code>&lt;1&gt;</code> 对 BeanDefinition 进行校验，该校验也是注册过程中的最后一次校验了，主要是对 AbstractBeanDefinition 的 <code>methodOverrides</code> 属性进行校验。</li><li><code>&lt;2&gt;</code> 根据 <code>beanName</code> 从缓存中获取 BeanDefinition 对象。</li><li><code>&lt;3&gt;</code> 如果缓存中存在，则根据 <code>allowBeanDefinitionOverriding</code> 标志来判断是否允许覆盖。如果允许则直接覆盖。否则，抛出 BeanDefinitionStoreException 异常。</li><li><code>&lt;4&gt;</code> 若缓存中没有指定 <code>beanName</code> 的 BeanDefinition，则判断当前阶段是否已经开始了 Bean 的创建阶段？如果是，则需要对 beanDefinitionMap 进行加锁控制并发问题，否则直接设置即可。</li><li><code>&lt;5&gt;</code> 若缓存中存在该 <code>beanName</code> 或者单例 bean 集合中存在该 <code>beanName</code> ，则调用 <code>#resetBeanDefinition(String beanName)</code> 方法，重置 BeanDefinition 缓存。</li></ul><h5 id="registerAlias-方法"><a href="#registerAlias-方法" class="headerlink" title="registerAlias()方法"></a><code>registerAlias()</code>方法</h5><p>注册beanname的别名通过<code>BeanDefinitionRegistry.registerAlias()</code>方法进行注册的，实际执行的是<code>SimpleAliasRegistry.registerAlias()</code>：</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerAlias</span><span class="hljs-params">(String name, String alias)</span> </span>&#123;    <span class="hljs-comment">// 校验 name 、 alias</span>    Assert.hasText(name, <span class="hljs-string">&quot;&#x27;name&#x27; must not be empty&quot;</span>);    Assert.hasText(alias, <span class="hljs-string">&quot;&#x27;alias&#x27; must not be empty&quot;</span>);    <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>.aliasMap) &#123;        <span class="hljs-comment">// name == alias 则去掉alias</span>        <span class="hljs-keyword">if</span> (alias.equals(name)) &#123;            <span class="hljs-keyword">this</span>.aliasMap.remove(alias);            <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                logger.debug(<span class="hljs-string">&quot;Alias definition &#x27;&quot;</span> + alias + <span class="hljs-string">&quot;&#x27; ignored since it points to same name&quot;</span>);            &#125;        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// 获取 alias 已注册的 beanName</span>            String registeredName = <span class="hljs-keyword">this</span>.aliasMap.get(alias);            <span class="hljs-keyword">if</span> (registeredName != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">// 相同，则 return ，无需重复注册</span>                <span class="hljs-keyword">if</span> (registeredName.equals(name)) &#123;                    <span class="hljs-comment">// An existing alias - no need to re-register</span>                    <span class="hljs-keyword">return</span>;                &#125;                <span class="hljs-comment">// 不允许覆盖，则抛出 IllegalStateException 异常</span>                <span class="hljs-keyword">if</span> (!allowAliasOverriding()) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">&quot;Cannot define alias &#x27;&quot;</span> + alias + <span class="hljs-string">&quot;&#x27; for name &#x27;&quot;</span> +                                                    name + <span class="hljs-string">&quot;&#x27;: It is already registered for name &#x27;&quot;</span> + registeredName + <span class="hljs-string">&quot;&#x27;.&quot;</span>);                &#125;                <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                    logger.debug(<span class="hljs-string">&quot;Overriding alias &#x27;&quot;</span> + alias + <span class="hljs-string">&quot;&#x27; definition for registered name &#x27;&quot;</span> +                                 registeredName + <span class="hljs-string">&quot;&#x27; with new target name &#x27;&quot;</span> + name + <span class="hljs-string">&quot;&#x27;&quot;</span>);                &#125;            &#125;            <span class="hljs-comment">// 校验，是否存在循环指向</span>            checkForAliasCircle(name, alias);            <span class="hljs-keyword">this</span>.aliasMap.put(alias, name);            <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                logger.trace(<span class="hljs-string">&quot;Alias definition &#x27;&quot;</span> + alias + <span class="hljs-string">&quot;&#x27; registered for name &#x27;&quot;</span> + name + <span class="hljs-string">&quot;&#x27;&quot;</span>);            &#125;        &#125;    &#125;&#125;</code></pre><p>进行循环检测：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">checkForAliasCircle</span><span class="hljs-params">(String name, String alias)</span> </span>&#123;<span class="hljs-keyword">if</span> (hasAlias(alias, name)) &#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">&quot;Cannot register alias &#x27;&quot;</span> + alias +<span class="hljs-string">&quot;&#x27; for name &#x27;&quot;</span> + name + <span class="hljs-string">&quot;&#x27;: Circular reference - &#x27;&quot;</span> +name + <span class="hljs-string">&quot;&#x27; is a direct or indirect alias for &#x27;&quot;</span> + alias + <span class="hljs-string">&quot;&#x27; already&quot;</span>);&#125;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">hasAlias</span><span class="hljs-params">(String name, String alias)</span> </span>&#123;    String registeredName = <span class="hljs-keyword">this</span>.aliasMap.get(alias);    <span class="hljs-keyword">return</span> ObjectUtils.nullSafeEquals(registeredName, name) || (registeredName != <span class="hljs-keyword">null</span>                                                                &amp;&amp; hasAlias(name, registeredName));&#125;</code></pre>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>解析自定义标签一</title>
    <link href="/2021/02/24/%E8%A7%A3%E6%9E%90%E8%87%AA%E5%AE%9A%E4%B9%89%E6%A0%87%E7%AD%BE/"/>
    <url>/2021/02/24/%E8%A7%A3%E6%9E%90%E8%87%AA%E5%AE%9A%E4%B9%89%E6%A0%87%E7%AD%BE/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">解析自定义标签一</font></div><p>默认标签的解析过程：</p><p>DefaultBeanDefinitionDocumentReader 的 <code>processBeanDefinition(Element ele, BeanDefinitionParserDelegate delegate)</code> 方法，负责 <code>bean</code> 标签的解析：</p><ul><li>在解析过程中，首先调用 <code>BeanDefinitionParserDelegate.parseBeanDefinitionElement(Element ele)</code> 方法，完成<strong>默认标签</strong>的解析。</li><li>如果解析成功（返回的 <code>bdHolder != null</code> ），则调用 <code>BeanDefinitionParserDelegate.decorateBeanDefinitionIfRequired(Element ele, BeanDefinitionHolder definitionHolder)</code> 方法，完成<strong>自定义标签</strong>元素的解析。</li></ul><h4 id="decorateBeanDefinitionIfRequired-方法"><a href="#decorateBeanDefinitionIfRequired-方法" class="headerlink" title="decorateBeanDefinitionIfRequired()方法"></a><code>decorateBeanDefinitionIfRequired()</code>方法</h4><p>该方法处理自定义标签的解析：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> BeanDefinitionHolder <span class="hljs-title">decorateBeanDefinitionIfRequired</span><span class="hljs-params">(Element ele, BeanDefinitionHolder originalDef)</span> </span>&#123;    <span class="hljs-keyword">return</span> decorateBeanDefinitionIfRequired(ele, originalDef, <span class="hljs-keyword">null</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> BeanDefinitionHolder <span class="hljs-title">decorateBeanDefinitionIfRequired</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">    Element ele, BeanDefinitionHolder originalDef, <span class="hljs-meta">@Nullable</span> BeanDefinition containingBd)</span> </span>&#123;    BeanDefinitionHolder finalDefinition = originalDef;    <span class="hljs-comment">// Decorate based on custom attributes first.</span>    <span class="hljs-comment">// &lt;1&gt; 遍历属性，查看是否有适用于装饰的【属性】</span>    NamedNodeMap attributes = ele.getAttributes();    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; attributes.getLength(); i++) &#123;        Node node = attributes.item(i);        finalDefinition = decorateIfRequired(node, finalDefinition, containingBd);    &#125;    <span class="hljs-comment">// Decorate based on custom nested elements.</span>    <span class="hljs-comment">// &lt;2&gt; 遍历子节点，查看是否有适用于修饰的【子节点】</span>    NodeList children = ele.getChildNodes();    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; children.getLength(); i++) &#123;        Node node = children.item(i);        <span class="hljs-keyword">if</span> (node.getNodeType() == Node.ELEMENT_NODE) &#123;            finalDefinition = decorateIfRequired(node, finalDefinition, containingBd);        &#125;    &#125;    <span class="hljs-keyword">return</span> finalDefinition;&#125;</code></pre><p><code>&lt;1&gt;</code> 和 <code>&lt;2&gt;</code> 处，都是遍历，前者遍历的是属性( <code>attributes</code> )，后者遍历的是子节点( <code>childNodes</code> )，最终调用的都是 <code>#decorateIfRequired(Node node, BeanDefinitionHolder originalDef, BeanDefinition containingBd)</code> 方法，装饰对应的节点( Node )。</p><h5 id="decorateIfRequired-方法"><a href="#decorateIfRequired-方法" class="headerlink" title="decorateIfRequired()方法"></a><code>decorateIfRequired()</code>方法</h5><p>在<code>decorateBeanDefinitionIfRequired()</code>方法中分别遍历了<code>属性</code>和<code>子节点</code>后，都会执行该方法进行对应节点（Node）的装饰。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> BeanDefinitionHolder <span class="hljs-title">decorateIfRequired</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">    Node node, BeanDefinitionHolder originalDef, <span class="hljs-meta">@Nullable</span> BeanDefinition containingBd)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 获取自定义标签的命名空间</span>    String namespaceUri = getNamespaceURI(node);    <span class="hljs-comment">// &lt;2&gt; 过滤掉默认命名标签</span>    <span class="hljs-keyword">if</span> (namespaceUri != <span class="hljs-keyword">null</span> &amp;&amp; !isDefaultNamespace(namespaceUri)) &#123;        <span class="hljs-comment">// &lt;2&gt; 获取相应的处理器</span>        NamespaceHandler handler = <span class="hljs-keyword">this</span>.readerContext.getNamespaceHandlerResolver().resolve(namespaceUri);        <span class="hljs-keyword">if</span> (handler != <span class="hljs-keyword">null</span>) &#123;<span class="hljs-comment">//如果处理器存在，进行装饰处理</span>            BeanDefinitionHolder decorated =                handler.decorate(node, originalDef, <span class="hljs-keyword">new</span> ParserContext(<span class="hljs-keyword">this</span>.readerContext, <span class="hljs-keyword">this</span>, containingBd));            <span class="hljs-keyword">if</span> (decorated != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">return</span> decorated;            &#125;        &#125;        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (namespaceUri.startsWith(<span class="hljs-string">&quot;http://www.springframework.org/schema/&quot;</span>)) &#123;            error(<span class="hljs-string">&quot;Unable to locate Spring NamespaceHandler for XML schema namespace [&quot;</span> + namespaceUri + <span class="hljs-string">&quot;]&quot;</span>, node);        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// A custom namespace, not to be handled by Spring - maybe &quot;xml:...&quot;.</span>            <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                logger.debug(<span class="hljs-string">&quot;No Spring NamespaceHandler found for XML schema namespace [&quot;</span> + namespaceUri + <span class="hljs-string">&quot;]&quot;</span>);            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> originalDef;&#125;</code></pre>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>解析bean标签子元素二</title>
    <link href="/2021/02/24/%E8%A7%A3%E6%9E%90bean%E6%A0%87%E7%AD%BE%E5%AD%90%E5%85%83%E7%B4%A0%E4%BA%8C/"/>
    <url>/2021/02/24/%E8%A7%A3%E6%9E%90bean%E6%A0%87%E7%AD%BE%E5%AD%90%E5%85%83%E7%B4%A0%E4%BA%8C/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">解析bean标签子元素二</font></div><p>其他三个子元素：<code>constructor-arg</code>、<code>property</code>、<code>qualifier</code>。</p><h4 id="constructor-arg子元素"><a href="#constructor-arg子元素" class="headerlink" title="constructor-arg子元素"></a><code>constructor-arg</code>子元素</h4><h5 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h5><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Hello</span> </span>&#123;<span class="hljs-keyword">private</span> Integer age;<span class="hljs-keyword">private</span> String name;<span class="hljs-keyword">private</span> String address;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Hello</span><span class="hljs-params">(Integer age, String name, String address)</span> </span>&#123;<span class="hljs-keyword">this</span>.age = age;<span class="hljs-keyword">this</span>.name = name;<span class="hljs-keyword">this</span>.address = address;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">sayHello</span><span class="hljs-params">()</span> </span>&#123;System.out.println(<span class="hljs-keyword">this</span>.name + <span class="hljs-string">&quot;今年&quot;</span> + <span class="hljs-keyword">this</span>.age + <span class="hljs-string">&quot;岁，&quot;</span> + <span class="hljs-string">&quot;住在&quot;</span> + <span class="hljs-keyword">this</span>.address);&#125;&#125;<span class="hljs-comment">//spring配置文件</span>&lt;bean id=<span class="hljs-string">&quot;hello&quot;</span> <span class="hljs-class"><span class="hljs-keyword">class</span></span>=<span class="hljs-string">&quot;com.jievhaha.Hello&quot;</span>&gt;    &lt;constructor-arg type=<span class="hljs-string">&quot;java.lang.Integer&quot;</span> value=<span class="hljs-string">&quot;27&quot;</span>/&gt;    &lt;constructor-arg name=<span class="hljs-string">&quot;name&quot;</span> value=<span class="hljs-string">&quot;jievhaha&quot;</span>/&gt;    &lt;constructor-arg index=<span class="hljs-string">&quot;2&quot;</span> value=<span class="hljs-string">&quot;山西&quot;</span>/&gt;&lt;/bean&gt;</code></pre><h5 id="parseConstructorArgElements-方法"><a href="#parseConstructorArgElements-方法" class="headerlink" title="parseConstructorArgElements()方法"></a><code>parseConstructorArgElements()</code>方法</h5><p><code>BeanDefinitionParserDelegate.parseConstructorArgElements()</code>对<code>constructor-arg</code>子元素进行解析。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseConstructorArgElements</span><span class="hljs-params">(Element beanEle, BeanDefinition bd)</span> </span>&#123;    NodeList nl = beanEle.getChildNodes();    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nl.getLength(); i++) &#123;        Node node = nl.item(i);        <span class="hljs-keyword">if</span> (isCandidateElement(node) &amp;&amp; nodeNameEquals(node, CONSTRUCTOR_ARG_ELEMENT)) &#123;            parseConstructorArgElement((Element) node, bd);        &#125;    &#125;&#125;</code></pre><p>遍历所有的子元素，如果为<code>constructor-arg</code>标签，则执行<code>parseConstructorArgElement()</code>方法，和之前其他三个子元素解析不同的是，当匹配了某个标签后，直接在if段执行逻辑，此处单独提取了一个<code>parseConstructorArgElement()</code>方法。</p><h5 id="parseConstructorArgElement-方法"><a href="#parseConstructorArgElement-方法" class="headerlink" title="parseConstructorArgElement()方法"></a><code>parseConstructorArgElement()</code>方法</h5><p>该方法是真正解析constructor-arg的方法：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseConstructorArgElement</span><span class="hljs-params">(Element ele, BeanDefinition bd)</span> </span>&#123;    String indexAttr = ele.getAttribute(INDEX_ATTRIBUTE);<span class="hljs-comment">//index属性</span>    String typeAttr = ele.getAttribute(TYPE_ATTRIBUTE);<span class="hljs-comment">//type属性</span>    String nameAttr = ele.getAttribute(NAME_ATTRIBUTE);<span class="hljs-comment">//name属性</span>    <span class="hljs-comment">//是否设置了index属性</span>    <span class="hljs-comment">// index较type、name的区别：</span>    <span class="hljs-comment">// 1.构造ConstructorArgumentEntry对象时，index调用了有参构造函数</span>    <span class="hljs-comment">// 2.index最后保存到了indexedArgumentValues集合，另外两个保存到了genericArgumentValues集合中。</span>    <span class="hljs-keyword">if</span> (StringUtils.hasLength(indexAttr)) &#123;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-keyword">int</span> index = Integer.parseInt(indexAttr);            <span class="hljs-keyword">if</span> (index &lt; <span class="hljs-number">0</span>) &#123;                error(<span class="hljs-string">&quot;&#x27;index&#x27; cannot be lower than 0&quot;</span>, ele);            &#125;            <span class="hljs-keyword">else</span> &#123;                <span class="hljs-keyword">try</span> &#123;                    <span class="hljs-comment">//多上送了index，构造 ConstructorArgumentEntry 对象并将其加入到 ParseState 队列中。</span>                    <span class="hljs-keyword">this</span>.parseState.push(<span class="hljs-keyword">new</span> ConstructorArgumentEntry(index));                    <span class="hljs-comment">// &lt;2&gt; 解析 ele 对应的属性，返回结果值。</span>                    Object value = parsePropertyValue(ele, bd, <span class="hljs-keyword">null</span>);                    <span class="hljs-comment">// &lt;3&gt; 根据解析的属性元素构造一个 ValueHolder 对象</span>                    ConstructorArgumentValues.ValueHolder valueHolder = <span class="hljs-keyword">new</span> ConstructorArgumentValues.ValueHolder(value);                    <span class="hljs-comment">//将 type、name 设置到 ValueHolder 中</span>                    <span class="hljs-keyword">if</span> (StringUtils.hasLength(typeAttr)) &#123;                        valueHolder.setType(typeAttr);                    &#125;                    <span class="hljs-keyword">if</span> (StringUtils.hasLength(nameAttr)) &#123;                        valueHolder.setName(nameAttr);                    &#125;                    valueHolder.setSource(extractSource(ele));                    <span class="hljs-comment">// 不允许重复指定相同参数</span>                    <span class="hljs-keyword">if</span> (bd.getConstructorArgumentValues().hasIndexedArgumentValue(index)) &#123;                        error(<span class="hljs-string">&quot;Ambiguous constructor-arg entries for index &quot;</span> + index, ele);                    &#125;                    <span class="hljs-keyword">else</span> &#123;                        <span class="hljs-comment">// &lt;4&gt; 加入到 indexedArgumentValues 中</span>                        bd.getConstructorArgumentValues().addIndexedArgumentValue(index, valueHolder);                    &#125;                &#125;                <span class="hljs-keyword">finally</span> &#123;                    <span class="hljs-keyword">this</span>.parseState.pop();                &#125;            &#125;        &#125;        <span class="hljs-keyword">catch</span> (NumberFormatException ex) &#123;            error(<span class="hljs-string">&quot;Attribute &#x27;index&#x27; of tag &#x27;constructor-arg&#x27; must be an integer&quot;</span>, ele);        &#125;    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-keyword">this</span>.parseState.push(<span class="hljs-keyword">new</span> ConstructorArgumentEntry());            Object value = parsePropertyValue(ele, bd, <span class="hljs-keyword">null</span>);            ConstructorArgumentValues.ValueHolder valueHolder = <span class="hljs-keyword">new</span> ConstructorArgumentValues.ValueHolder(value);            <span class="hljs-keyword">if</span> (StringUtils.hasLength(typeAttr)) &#123;                valueHolder.setType(typeAttr);            &#125;            <span class="hljs-keyword">if</span> (StringUtils.hasLength(nameAttr)) &#123;                valueHolder.setName(nameAttr);            &#125;            valueHolder.setSource(extractSource(ele));            <span class="hljs-comment">//加入到genericArgumentValues集合中</span>            bd.getConstructorArgumentValues().addGenericArgumentValue(valueHolder);        &#125;        <span class="hljs-keyword">finally</span> &#123;            <span class="hljs-keyword">this</span>.parseState.pop();        &#125;    &#125;&#125;</code></pre><h5 id="parsePropertyValue-方法"><a href="#parsePropertyValue-方法" class="headerlink" title="parsePropertyValue()方法"></a><code>parsePropertyValue()</code>方法</h5><p>解析constructor-arg的过程中，调用了<code>parsePropertyValue()</code>方法，返回结果值。</p><pre><code class="hljs java"><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">parsePropertyValue</span><span class="hljs-params">(Element ele, BeanDefinition bd, <span class="hljs-meta">@Nullable</span> String propertyName)</span> </span>&#123;    String elementName = (propertyName != <span class="hljs-keyword">null</span> ?                          <span class="hljs-string">&quot;&lt;property&gt; element for property &#x27;&quot;</span> + propertyName + <span class="hljs-string">&quot;&#x27;&quot;</span> :                          <span class="hljs-string">&quot;&lt;constructor-arg&gt; element&quot;</span>);    <span class="hljs-comment">// Should only have one child element: ref, value, list, etc.</span>    <span class="hljs-comment">// &lt;1&gt; 查找子节点中，是否有 ref、value、list 等元素</span>    NodeList nl = ele.getChildNodes();    Element subElement = <span class="hljs-keyword">null</span>;    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nl.getLength(); i++) &#123;        Node node = nl.item(i);        <span class="hljs-comment">// meta 、description 不处理</span>        <span class="hljs-keyword">if</span> (node <span class="hljs-keyword">instanceof</span> Element &amp;&amp; !nodeNameEquals(node, DESCRIPTION_ELEMENT) &amp;&amp;            !nodeNameEquals(node, META_ELEMENT)) &#123;            <span class="hljs-comment">// Child element is what we&#x27;re looking for.</span>            <span class="hljs-keyword">if</span> (subElement != <span class="hljs-keyword">null</span>) &#123;                error(elementName + <span class="hljs-string">&quot; must not contain more than one sub-element&quot;</span>, ele);            &#125;            <span class="hljs-keyword">else</span> &#123;                subElement = (Element) node;            &#125;        &#125;    &#125;    <span class="hljs-comment">// &lt;1&gt; 是否有 ref 属性</span>    <span class="hljs-keyword">boolean</span> hasRefAttribute = ele.hasAttribute(REF_ATTRIBUTE);    <span class="hljs-comment">// &lt;1&gt; 是否有 value 属性</span>    <span class="hljs-keyword">boolean</span> hasValueAttribute = ele.hasAttribute(VALUE_ATTRIBUTE);    <span class="hljs-comment">// &lt;1&gt; 多个元素存在，报错，存在冲突。</span>    <span class="hljs-keyword">if</span> ((hasRefAttribute &amp;&amp; hasValueAttribute) ||<span class="hljs-comment">// 1. ref 和 value 都存在</span>        ((hasRefAttribute || hasValueAttribute) &amp;&amp; subElement != <span class="hljs-keyword">null</span>)) &#123;<span class="hljs-comment">// 2. ref 和 value 存在其一，并且 subElement 存在</span>        error(elementName +              <span class="hljs-string">&quot; is only allowed to contain either &#x27;ref&#x27; attribute OR &#x27;value&#x27; attribute OR sub-element&quot;</span>, ele);    &#125;    <span class="hljs-comment">// &lt;2&gt; 将 ref 属性值，构造为 RuntimeBeanReference 实例对象</span>    <span class="hljs-keyword">if</span> (hasRefAttribute) &#123;        String refName = ele.getAttribute(REF_ATTRIBUTE);        <span class="hljs-keyword">if</span> (!StringUtils.hasText(refName)) &#123;            error(elementName + <span class="hljs-string">&quot; contains empty &#x27;ref&#x27; attribute&quot;</span>, ele);        &#125;        RuntimeBeanReference ref = <span class="hljs-keyword">new</span> RuntimeBeanReference(refName);        ref.setSource(extractSource(ele));        <span class="hljs-keyword">return</span> ref;    &#125;    <span class="hljs-comment">// &lt;3&gt; 将 value 属性值，构造为 TypedStringValue 实例对象</span>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (hasValueAttribute) &#123;        TypedStringValue valueHolder = <span class="hljs-keyword">new</span> TypedStringValue(ele.getAttribute(VALUE_ATTRIBUTE));        valueHolder.setSource(extractSource(ele));        <span class="hljs-keyword">return</span> valueHolder;    &#125;    <span class="hljs-comment">// &lt;4&gt; 解析子元素</span>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (subElement != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> parsePropertySubElement(subElement, bd);    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// Neither child element nor &quot;ref&quot; or &quot;value&quot; attribute found.</span>        error(elementName + <span class="hljs-string">&quot; must specify a ref or value&quot;</span>, ele);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;&#125;</code></pre><h5 id="parsePropertySubElement-方法"><a href="#parsePropertySubElement-方法" class="headerlink" title="parsePropertySubElement()方法"></a><code>parsePropertySubElement()</code>方法</h5><p>在调用<code>parsePropertyValue()</code>返回结果值的时候，假如解析了子元素，会调用<code>parsePropertySubElement()</code>方法，主要是针对各种子标签进行不同的处理：</p><pre><code class="hljs java"><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">parsePropertySubElement</span><span class="hljs-params">(Element ele, <span class="hljs-meta">@Nullable</span> BeanDefinition bd, <span class="hljs-meta">@Nullable</span> String defaultValueType)</span> </span>&#123;    <span class="hljs-keyword">if</span> (!isDefaultNamespace(ele)) &#123;        <span class="hljs-keyword">return</span> parseNestedCustomElement(ele, bd);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nodeNameEquals(ele, BEAN_ELEMENT)) &#123;<span class="hljs-comment">// bean 标签</span>        BeanDefinitionHolder nestedBd = parseBeanDefinitionElement(ele, bd);        <span class="hljs-keyword">if</span> (nestedBd != <span class="hljs-keyword">null</span>) &#123;            nestedBd = decorateBeanDefinitionIfRequired(ele, nestedBd, bd);        &#125;        <span class="hljs-keyword">return</span> nestedBd;    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nodeNameEquals(ele, REF_ELEMENT)) &#123;<span class="hljs-comment">// ref 标签</span>        <span class="hljs-comment">// A generic reference to any name of any bean.</span>        String refName = ele.getAttribute(BEAN_REF_ATTRIBUTE);        <span class="hljs-keyword">boolean</span> toParent = <span class="hljs-keyword">false</span>;        <span class="hljs-keyword">if</span> (!StringUtils.hasLength(refName)) &#123;            <span class="hljs-comment">// A reference to the id of another bean in a parent context.</span>            refName = ele.getAttribute(PARENT_REF_ATTRIBUTE);            toParent = <span class="hljs-keyword">true</span>;            <span class="hljs-keyword">if</span> (!StringUtils.hasLength(refName)) &#123;                error(<span class="hljs-string">&quot;&#x27;bean&#x27; or &#x27;parent&#x27; is required for &lt;ref&gt; element&quot;</span>, ele);                <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;            &#125;        &#125;        <span class="hljs-keyword">if</span> (!StringUtils.hasText(refName)) &#123;            error(<span class="hljs-string">&quot;&lt;ref&gt; element contains empty target attribute&quot;</span>, ele);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;        RuntimeBeanReference ref = <span class="hljs-keyword">new</span> RuntimeBeanReference(refName, toParent);        ref.setSource(extractSource(ele));        <span class="hljs-keyword">return</span> ref;    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nodeNameEquals(ele, IDREF_ELEMENT)) &#123;<span class="hljs-comment">// idref 标签</span>        <span class="hljs-keyword">return</span> parseIdRefElement(ele);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nodeNameEquals(ele, VALUE_ELEMENT)) &#123;<span class="hljs-comment">// value 标签</span>        <span class="hljs-keyword">return</span> parseValueElement(ele, defaultValueType);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nodeNameEquals(ele, NULL_ELEMENT)) &#123;<span class="hljs-comment">// null 标签</span>        <span class="hljs-comment">// It&#x27;s a distinguished null value. Let&#x27;s wrap it in a TypedStringValue</span>        <span class="hljs-comment">// object in order to preserve the source location.</span>        TypedStringValue nullHolder = <span class="hljs-keyword">new</span> TypedStringValue(<span class="hljs-keyword">null</span>);        nullHolder.setSource(extractSource(ele));        <span class="hljs-keyword">return</span> nullHolder;    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nodeNameEquals(ele, ARRAY_ELEMENT)) &#123;<span class="hljs-comment">// array 标签</span>        <span class="hljs-keyword">return</span> parseArrayElement(ele, bd);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nodeNameEquals(ele, LIST_ELEMENT)) &#123;<span class="hljs-comment">// list 标签</span>        <span class="hljs-keyword">return</span> parseListElement(ele, bd);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nodeNameEquals(ele, SET_ELEMENT)) &#123;<span class="hljs-comment">// set 标签</span>        <span class="hljs-keyword">return</span> parseSetElement(ele, bd);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nodeNameEquals(ele, MAP_ELEMENT)) &#123;<span class="hljs-comment">// map 标签</span>        <span class="hljs-keyword">return</span> parseMapElement(ele, bd);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nodeNameEquals(ele, PROPS_ELEMENT)) &#123;<span class="hljs-comment">// props 标签</span>        <span class="hljs-keyword">return</span> parsePropsElement(ele);<span class="hljs-comment">// 未知标签</span>    &#125;    <span class="hljs-keyword">else</span> &#123;        error(<span class="hljs-string">&quot;Unknown property sub-element: [&quot;</span> + ele.getNodeName() + <span class="hljs-string">&quot;]&quot;</span>, ele);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;&#125;</code></pre><h4 id="property子元素"><a href="#property子元素" class="headerlink" title="property子元素"></a><code>property</code>子元素</h4><h5 id="实例-1"><a href="#实例-1" class="headerlink" title="实例"></a>实例</h5><pre><code class="hljs java"><span class="hljs-comment">//spring配置文件</span>&lt;bean id=<span class="hljs-string">&quot;hello&quot;</span> <span class="hljs-class"><span class="hljs-keyword">class</span></span>=<span class="hljs-string">&quot;com.jievhaha.Hello&quot;</span>&gt;    &lt;property name=<span class="hljs-string">&quot;name&quot;</span> value=<span class="hljs-string">&quot;chenssy&quot;</span>/&gt;    &lt;property name=<span class="hljs-string">&quot;age&quot;</span> value=<span class="hljs-string">&quot;18&quot;</span>/&gt;    &lt;property name=<span class="hljs-string">&quot;address&quot;</span> value=<span class="hljs-string">&quot;山西&quot;</span>/&gt;&lt;/bean&gt;</code></pre><h5 id="parsePropertyElement-方法"><a href="#parsePropertyElement-方法" class="headerlink" title="parsePropertyElement()方法"></a><code>parsePropertyElement()</code>方法</h5><p>和上边解析<code>constructor-arg</code>一样，遍历所有子元素，匹配到了<code>property</code>子元素，调用相应的逻辑方法<code>parsePropertyElement</code>。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parsePropertyElement</span><span class="hljs-params">(Element ele, BeanDefinition bd)</span> </span>&#123;    String propertyName = ele.getAttribute(NAME_ATTRIBUTE);<span class="hljs-comment">// 获取 name 属性</span>    <span class="hljs-keyword">if</span> (!StringUtils.hasLength(propertyName)) &#123;        error(<span class="hljs-string">&quot;Tag &#x27;property&#x27; must have a &#x27;name&#x27; attribute&quot;</span>, ele);        <span class="hljs-keyword">return</span>;    &#125;    <span class="hljs-keyword">this</span>.parseState.push(<span class="hljs-keyword">new</span> PropertyEntry(propertyName));    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// 如果存在相同的 name ，报错</span>        <span class="hljs-keyword">if</span> (bd.getPropertyValues().contains(propertyName)) &#123;            error(<span class="hljs-string">&quot;Multiple &#x27;property&#x27; definitions for property &#x27;&quot;</span> + propertyName + <span class="hljs-string">&quot;&#x27;&quot;</span>, ele);            <span class="hljs-keyword">return</span>;        &#125;        <span class="hljs-comment">// 解析属性值，解析constructor-arg子元素的时候，已经分析了该方法</span>        Object val = parsePropertyValue(ele, bd, propertyName);        <span class="hljs-comment">// 创建 PropertyValue 对象</span>        PropertyValue pv = <span class="hljs-keyword">new</span> PropertyValue(propertyName, val);        parseMetaElements(ele, pv);        pv.setSource(extractSource(ele));        <span class="hljs-comment">// 添加到 PropertyValue 集合中</span>        bd.getPropertyValues().addPropertyValue(pv);    &#125;    <span class="hljs-keyword">finally</span> &#123;        <span class="hljs-keyword">this</span>.parseState.pop();    &#125;&#125;</code></pre><h4 id="qualifier子元素"><a href="#qualifier子元素" class="headerlink" title="qualifier子元素"></a><code>qualifier</code>子元素</h4><h5 id="parseQualifierElements-方法"><a href="#parseQualifierElements-方法" class="headerlink" title="parseQualifierElements()方法"></a><code>parseQualifierElements()</code>方法</h5><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseQualifierElements</span><span class="hljs-params">(Element beanEle, AbstractBeanDefinition bd)</span> </span>&#123;    NodeList nl = beanEle.getChildNodes();    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nl.getLength(); i++) &#123;        Node node = nl.item(i);        <span class="hljs-keyword">if</span> (isCandidateElement(node) &amp;&amp; nodeNameEquals(node, QUALIFIER_ELEMENT)) &#123;            parseQualifierElement((Element) node, bd);        &#125;    &#125;&#125;</code></pre><h5 id="parseQualifierElement-方法"><a href="#parseQualifierElement-方法" class="headerlink" title="parseQualifierElement()方法"></a><code>parseQualifierElement()</code>方法</h5><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseQualifierElement</span><span class="hljs-params">(Element ele, AbstractBeanDefinition bd)</span> </span>&#123;   String typeName = ele.getAttribute(TYPE_ATTRIBUTE);<span class="hljs-comment">// 解析 type 属性</span>   <span class="hljs-keyword">if</span> (!StringUtils.hasLength(typeName)) &#123;<span class="hljs-comment">// 必须有 type</span>      error(<span class="hljs-string">&quot;Tag &#x27;qualifier&#x27; must have a &#x27;type&#x27; attribute&quot;</span>, ele);      <span class="hljs-keyword">return</span>;   &#125;   <span class="hljs-keyword">this</span>.parseState.push(<span class="hljs-keyword">new</span> QualifierEntry(typeName));   <span class="hljs-keyword">try</span> &#123;      <span class="hljs-comment">// 创建 AutowireCandidateQualifier 对象</span>      AutowireCandidateQualifier qualifier = <span class="hljs-keyword">new</span> AutowireCandidateQualifier(typeName);      qualifier.setSource(extractSource(ele));      <span class="hljs-comment">// 解析 value 属性，并设置到 AutowireCandidateQualifier 中</span>      String value = ele.getAttribute(VALUE_ATTRIBUTE);      <span class="hljs-keyword">if</span> (StringUtils.hasLength(value)) &#123;         qualifier.setAttribute(AutowireCandidateQualifier.VALUE_KEY, value);      &#125;      <span class="hljs-comment">// 遍历子节点</span>      NodeList nl = ele.getChildNodes();      <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nl.getLength(); i++) &#123;         Node node = nl.item(i);         <span class="hljs-keyword">if</span> (isCandidateElement(node) &amp;&amp; nodeNameEquals(node, QUALIFIER_ATTRIBUTE_ELEMENT)) &#123;<span class="hljs-comment">// attribute 标签</span>            Element attributeEle = (Element) node;            String attributeName = attributeEle.getAttribute(KEY_ATTRIBUTE);<span class="hljs-comment">// attribute 标签的 key 属性</span>            String attributeValue = attributeEle.getAttribute(VALUE_ATTRIBUTE);<span class="hljs-comment">// attribute 标签的 value 属性</span>            <span class="hljs-keyword">if</span> (StringUtils.hasLength(attributeName) &amp;&amp; StringUtils.hasLength(attributeValue)) &#123;               <span class="hljs-comment">// 创建 BeanMetadataAttribute 对象</span>               BeanMetadataAttribute attribute = <span class="hljs-keyword">new</span> BeanMetadataAttribute(attributeName, attributeValue);               attribute.setSource(extractSource(attributeEle));               <span class="hljs-comment">// 添加到 attributes 中</span>               qualifier.addMetadataAttribute(attribute);            &#125;            <span class="hljs-keyword">else</span> &#123;               error(<span class="hljs-string">&quot;Qualifier &#x27;attribute&#x27; tag must have a &#x27;name&#x27; and &#x27;value&#x27;&quot;</span>, attributeEle);               <span class="hljs-keyword">return</span>;            &#125;         &#125;      &#125;      <span class="hljs-comment">// 添加到 qualifiers 中</span>      bd.addQualifier(qualifier);   &#125;   <span class="hljs-keyword">finally</span> &#123;      <span class="hljs-keyword">this</span>.parseState.pop();   &#125;&#125;</code></pre>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>解析bean标签子元素一</title>
    <link href="/2021/02/24/%E8%A7%A3%E6%9E%90bean%E6%A0%87%E7%AD%BE%E5%AD%90%E5%85%83%E7%B4%A0%E4%B8%80/"/>
    <url>/2021/02/24/%E8%A7%A3%E6%9E%90bean%E6%A0%87%E7%AD%BE%E5%AD%90%E5%85%83%E7%B4%A0%E4%B8%80/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">解析bean标签子元素一</font></div><p>完成<code>bean</code>标签属性解析后，会进行子元素的解析。</p><p>本篇先看<code>meta</code>、<code>lookup-metod</code>、<code>replace-method</code>三个子元素是如何解析的。</p><h4 id="meta子元素"><a href="#meta子元素" class="headerlink" title="meta子元素"></a><code>meta</code>子元素</h4><p><strong>meta</strong> ：元数据。当需要使用里面的信息时可以通过 key 获取。</p><p>meta 所声明的 key 并不会在 Bean 中体现，只是一个额外的声明，当我们需要使用里面的信息时，通过调用 BeanDefinition 的 <code>getAttribute(String name)</code> 方法来获取。</p><p>通过<code>BeanDefinitionParserDelegate.parseMetaElements()</code>来完成对meta子元素的解析。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseMetaElements</span><span class="hljs-params">(Element ele, BeanMetadataAttributeAccessor attributeAccessor)</span> </span>&#123;    NodeList nl = ele.getChildNodes();    <span class="hljs-comment">// 遍历子节点</span>    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nl.getLength(); i++) &#123;        Node node = nl.item(i);        <span class="hljs-comment">//例：// &lt;meta key=&quot;special-data&quot; value=&quot;sprecial stragey&quot; /&gt;</span>        <span class="hljs-keyword">if</span> (isCandidateElement(node) &amp;&amp; nodeNameEquals(node, META_ELEMENT)) &#123;            Element metaElement = (Element) node;            String key = metaElement.getAttribute(KEY_ATTRIBUTE);            String value = metaElement.getAttribute(VALUE_ATTRIBUTE);            <span class="hljs-comment">// 创建 BeanMetadataAttribute 对象</span>            BeanMetadataAttribute attribute = <span class="hljs-keyword">new</span> BeanMetadataAttribute(key, value);            attribute.setSource(extractSource(metaElement));            <span class="hljs-comment">// 添加到 BeanMetadataAttributeAccessor 中</span>            attributeAccessor.addMetadataAttribute(attribute);        &#125;    &#125;&#125;</code></pre><p>解析过程：获取相应的 key - value 构建<code>BeanMetadataAttribute</code>对象，然后调用 <code>BeanMetadataAttributeAccessor.addMetadataAttribute(BeanMetadataAttribute) </code>方法，添加 <code>BeanMetadataAttribute</code>加入到<code>AbstractBeanDefinition</code>中。</p><p><code>AbstractBeanDefinition</code>继承<code>BeanMetadataAttributeAccessor</code>类。调用parseMetaElements()上送的就是AbstractBeanDefinition。</p><h5 id="addMetadataAttribute-方法"><a href="#addMetadataAttribute-方法" class="headerlink" title="addMetadataAttribute()方法"></a><code>addMetadataAttribute()</code>方法</h5><p>调用 <code>BeanMetadataAttributeAccessor.addMetadataAttribute(BeanMetadataAttribute)</code> 方法，添加 <code>BeanMetadataAttribute</code>加入到<code>AbstractBeanDefinition</code>中。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">addMetadataAttribute</span><span class="hljs-params">(BeanMetadataAttribute attribute)</span> </span>&#123;    <span class="hljs-keyword">super</span>.setAttribute(attribute.getName(), attribute);&#125;</code></pre><p><code>BeanMetadataAttributeAccessor </code>继承<code>AttributeAccessorSupport</code>类。<code>AttributeAccessorSupport</code>类实现了<code>AttributeAccessor</code>接口，如上篇提到，该接口提供了对属性的获取、设置、删除。</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setAttribute</span><span class="hljs-params">(String name, <span class="hljs-meta">@Nullable</span> Object value)</span> </span>&#123;    Assert.notNull(name, <span class="hljs-string">&quot;Name must not be null&quot;</span>);    <span class="hljs-keyword">if</span> (value != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">this</span>.attributes.put(name, value);    &#125;    <span class="hljs-keyword">else</span> &#123;        removeAttribute(name);    &#125;&#125;</code></pre><h5 id="getAttribute-方法"><a href="#getAttribute-方法" class="headerlink" title="getAttribute()方法"></a><code>getAttribute()</code>方法</h5><p>设置元数据后，则可以通过调用 BeanDefinition 的 <code>.getAttribute(String name)</code> 方法来获取属性。</p><p><code>AbstractBeanDefinition</code>继承<code>BeanMetadataAttributeAccessor</code>类。<code>BeanMetadataAttributeAccessor </code>继承<code>AttributeAccessorSupport</code>类。</p><pre><code class="hljs java"><span class="hljs-comment">//AttributeAccessorSupport.java</span><span class="hljs-comment">/** Map with String keys and Object values. */</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, Object&gt; attributes = <span class="hljs-keyword">new</span> LinkedHashMap&lt;&gt;();<span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">getAttribute</span><span class="hljs-params">(String name)</span> </span>&#123;    Assert.notNull(name, <span class="hljs-string">&quot;Name must not be null&quot;</span>);    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.attributes.get(name);&#125;</code></pre><h4 id="lookup-method子元素"><a href="#lookup-method子元素" class="headerlink" title="lookup-method子元素"></a><code>lookup-method</code>子元素</h4><p>依赖注入－方法注入，使用<lookup-method/>标签。和<replace-method/>类似，一个注入bean，一个替代原有方法。</p><h5 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h5><pre><code class="hljs java"><span class="hljs-comment">// 定义一个水果类</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Fruit</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Fruit</span><span class="hljs-params">()</span> </span>&#123;        System.out.println(<span class="hljs-string">&quot;I got Fruit&quot;</span>);    &#125;&#125;<span class="hljs-comment">// 苹果</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Apple</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Fruit</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Apple</span><span class="hljs-params">()</span> </span>&#123;        System.out.println(<span class="hljs-string">&quot;I got a fresh apple&quot;</span>);    &#125;&#125;<span class="hljs-comment">// 香蕉</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Bananer</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Fruit</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Bananer</span> <span class="hljs-params">()</span> </span>&#123;        System.out.println(<span class="hljs-string">&quot;I got a  fresh bananer&quot;</span>);    &#125;&#125;<span class="hljs-comment">// 水果盘，可以拿到水果</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FruitPlate</span></span>&#123;    <span class="hljs-comment">// 抽象方法获取新鲜水果</span>    <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">abstract</span> Fruit <span class="hljs-title">getFruit</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-comment">//spring配置</span>&lt;bean id=<span class="hljs-string">&quot;apple&quot;</span> <span class="hljs-class"><span class="hljs-keyword">class</span></span>=<span class="hljs-string">&quot;com.jievhaha.test.Apple&quot;</span> scope=<span class="hljs-string">&quot;prototype&quot;</span>/&gt;&lt;bean id=<span class="hljs-string">&quot;bananer&quot;</span> <span class="hljs-class"><span class="hljs-keyword">class</span></span>=<span class="hljs-string">&quot;com.jievhaha.test.Bananer &quot;</span> scope=<span class="hljs-string">&quot;prototype&quot;</span>/&gt; &lt;bean id=<span class="hljs-string">&quot;fruitPlate1&quot;</span> <span class="hljs-class"><span class="hljs-keyword">class</span></span>=<span class="hljs-string">&quot;com.jievhaha.test.FruitPlate&quot;</span>&gt;    &lt;lookup-method name=<span class="hljs-string">&quot;getFruit&quot;</span> bean=<span class="hljs-string">&quot;apple&quot;</span>/&gt;&lt;/bean&gt;&lt;bean id=<span class="hljs-string">&quot;fruitPlate2&quot;</span> <span class="hljs-class"><span class="hljs-keyword">class</span></span>=<span class="hljs-string">&quot;com.jievhaha.test.FruitPlate&quot;</span>&gt;    &lt;lookup-method name=<span class="hljs-string">&quot;getFruit&quot;</span> bean=<span class="hljs-string">&quot;bananer&quot;</span>/&gt;&lt;/bean&gt;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;    ApplicationContext app = <span class="hljs-keyword">new</span> ClassPathXmlApplicationContext(<span class="hljs-string">&quot;classpath:resource/applicationContext.xml&quot;</span>);    FruitPlate fp1= (FruitPlate)app.getBean(<span class="hljs-string">&quot;fruitPlate1&quot;</span>);    FruitPlate fp2 = (FruitPlate)app.getBean(<span class="hljs-string">&quot;fruitPlate2&quot;</span>);    fp1.getFruit();<span class="hljs-comment">//&quot;I got Fruit&quot;,&quot;I got a fresh apple&quot;</span>    fp2.getFruit();<span class="hljs-comment">//&quot;I got Fruit&quot;,&quot;I got a  fresh bananer&quot;</span>&#125;</code></pre><p>lookup-method为抽象方法指定返回类型，应用了CGLIB（动态代理）类库。</p><h5 id="parseLookupOverrideSubElements-方法"><a href="#parseLookupOverrideSubElements-方法" class="headerlink" title="parseLookupOverrideSubElements()方法"></a><code>parseLookupOverrideSubElements()</code>方法</h5><p>通过<code>BeanDefinitionParserDelegate.parseLookupOverrideSubElements()</code>来完成对lookup-method子元素的解析。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseLookupOverrideSubElements</span><span class="hljs-params">(Element beanEle, MethodOverrides overrides)</span> </span>&#123;    NodeList nl = beanEle.getChildNodes();    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nl.getLength(); i++) &#123;        <span class="hljs-comment">// 遍历子节点</span>        Node node = nl.item(i);        <span class="hljs-keyword">if</span> (isCandidateElement(node) &amp;&amp; nodeNameEquals(node, LOOKUP_METHOD_ELEMENT)) &#123;            Element ele = (Element) node;            String methodName = ele.getAttribute(NAME_ATTRIBUTE);<span class="hljs-comment">//name属性，指定了要返回指定类型对象的抽象方法</span>            String beanRef = ele.getAttribute(BEAN_ELEMENT);<span class="hljs-comment">//bean属性，指定了抽象方法要返回的对象的具体类型</span>            <span class="hljs-comment">// 创建 LookupOverride 对象</span>            LookupOverride override = <span class="hljs-keyword">new</span> LookupOverride(methodName, beanRef);            <span class="hljs-comment">//设置数据源配置文件，对象的确切类型取决于配置文件的配置机制</span>            override.setSource(extractSource(ele));            <span class="hljs-comment">// 添加到 MethodOverrides 中</span>            overrides.addOverride(override);        &#125;    &#125;&#125;</code></pre><p>解析过程和 <code>meta</code> 子元素没有多大区别，同样是解析 methodName、beanRef 构造一个 LookupOverride 对象，然后记录到 AbstractBeanDefinition 中的 <code>methodOverrides</code> 属性中。</p><h4 id="replace-method-方法"><a href="#replace-method-方法" class="headerlink" title="replace-method()方法"></a><code>replace-method()</code>方法</h4><p><strong>replaced-method</strong> ：可以在运行时调用新的方法替换现有的方法，还能动态的更新原有方法的逻辑。</p><p>该标签使用方法和<code>lookup-method</code>标签大同小异，不过替代原有方法的类需要实现<code>org.springframework.beans.factory.support.MethodReplacer</code> 接口，一个注入bean，一个替代原有方法。</p><h5 id="实例-1"><a href="#实例-1" class="headerlink" title="实例"></a>实例</h5><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Method</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">display</span><span class="hljs-params">()</span></span>&#123;        System.out.println(<span class="hljs-string">&quot;我是原始方法&quot;</span>);    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MethodReplace</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">MethodReplacer</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">reimplement</span><span class="hljs-params">(Object obj, Method method, Object[] args)</span> <span class="hljs-keyword">throws</span> Throwable </span>&#123;        System.out.println(<span class="hljs-string">&quot;我是替换方法&quot;</span>);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;&#125;<span class="hljs-comment">//spring配置文件内容</span>&lt;bean id=<span class="hljs-string">&quot;methodReplace&quot;</span> <span class="hljs-class"><span class="hljs-keyword">class</span></span>=<span class="hljs-string">&quot;com.jievhaha.test.MethodReplace&quot;</span>/&gt;&lt;bean id=<span class="hljs-string">&quot;method&quot;</span> <span class="hljs-class"><span class="hljs-keyword">class</span></span>=<span class="hljs-string">&quot;com.jievhaha.test.Method&quot;</span>&gt;    &lt;replaced-method name=<span class="hljs-string">&quot;display&quot;</span> replacer=<span class="hljs-string">&quot;methodReplace&quot;</span>/&gt;&lt;/bean&gt;<span class="hljs-comment">//测试</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;    ApplicationContext context = <span class="hljs-keyword">new</span> ClassPathXmlApplicationContext(<span class="hljs-string">&quot;classpath:spring.xml&quot;</span>);    Method method = (Method) context.getBean(<span class="hljs-string">&quot;method&quot;</span>);    method.display();<span class="hljs-comment">//&quot;我是替换方法&quot;</span>&#125;</code></pre><h5 id="parseReplacedMethodSubElements-方法"><a href="#parseReplacedMethodSubElements-方法" class="headerlink" title="parseReplacedMethodSubElements()方法"></a><code>parseReplacedMethodSubElements()</code>方法</h5><p>通过 <code>BeanDefinitionParserDelegate.parseReplacedMethodSubElements(Element beanEle, MethodOverrides overrides)</code>来解析replace-method标签：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseReplacedMethodSubElements</span><span class="hljs-params">(Element beanEle, MethodOverrides overrides)</span> </span>&#123;    NodeList nl = beanEle.getChildNodes();    <span class="hljs-comment">// 遍历子节点</span>    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nl.getLength(); i++) &#123;        Node node = nl.item(i);        <span class="hljs-keyword">if</span> (isCandidateElement(node) &amp;&amp; nodeNameEquals(node, REPLACED_METHOD_ELEMENT)) &#123;            Element replacedMethodEle = (Element) node;            String name = replacedMethodEle.getAttribute(NAME_ATTRIBUTE);<span class="hljs-comment">//name属性，替代的方法名</span>            String callback = replacedMethodEle.getAttribute(REPLACER_ATTRIBUTE);<span class="hljs-comment">//replacer，用哪个类的方法替代</span>            <span class="hljs-comment">// 创建 ReplaceOverride 对象</span>            ReplaceOverride replaceOverride = <span class="hljs-keyword">new</span> ReplaceOverride(name, callback);            <span class="hljs-comment">// Look for arg-type match elements.</span>            List&lt;Element&gt; argTypeEles = DomUtils.getChildElementsByTagName(replacedMethodEle, ARG_TYPE_ELEMENT);<span class="hljs-comment">// arg-type 子标签</span>            <span class="hljs-keyword">for</span> (Element argTypeEle : argTypeEles) &#123;                String match = argTypeEle.getAttribute(ARG_TYPE_MATCH_ATTRIBUTE);<span class="hljs-comment">// arg-type 子标签的 match 属性</span>                match = (StringUtils.hasText(match) ? match : DomUtils.getTextValue(argTypeEle));                <span class="hljs-keyword">if</span> (StringUtils.hasText(match)) &#123;                    replaceOverride.addTypeIdentifier(match);                &#125;            &#125;            replaceOverride.setSource(extractSource(replacedMethodEle));            <span class="hljs-comment">// 添加到 MethodOverrides 中</span>            overrides.addOverride(replaceOverride);        &#125;    &#125;&#125;</code></pre>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>解析bean标签BeanDefinition</title>
    <link href="/2021/02/24/%E8%A7%A3%E6%9E%90bean%E6%A0%87%E7%AD%BEBeanDefinition/"/>
    <url>/2021/02/24/%E8%A7%A3%E6%9E%90bean%E6%A0%87%E7%AD%BEBeanDefinition/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">解析bean标签BeanDefinition</font></div><p>bean标签的所有子元素在BeanDefinition对象中都有与之相对应的属性。</p><h4 id="BeanDefinition"><a href="#BeanDefinition" class="headerlink" title="BeanDefinition"></a>BeanDefinition</h4><p>BeanDefinition是一个接口，描述了Bean实例的定义。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">BeanDefinition</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AttributeAccessor</span>, <span class="hljs-title">BeanMetadataElement</span> </span>&#123;String SCOPE_SINGLETON = ConfigurableBeanFactory.SCOPE_SINGLETON;String SCOPE_PROTOTYPE = ConfigurableBeanFactory.SCOPE_PROTOTYPE;<span class="hljs-keyword">int</span> ROLE_APPLICATION = <span class="hljs-number">0</span>;<span class="hljs-keyword">int</span> ROLE_SUPPORT = <span class="hljs-number">1</span>;<span class="hljs-keyword">int</span> ROLE_INFRASTRUCTURE = <span class="hljs-number">2</span>;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setParentName</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String parentName)</span></span>;<span class="hljs-meta">@Nullable</span><span class="hljs-function">String <span class="hljs-title">getParentName</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setBeanClassName</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String beanClassName)</span></span>;<span class="hljs-meta">@Nullable</span><span class="hljs-function">String <span class="hljs-title">getBeanClassName</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setScope</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String scope)</span></span>;<span class="hljs-meta">@Nullable</span><span class="hljs-function">String <span class="hljs-title">getScope</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setLazyInit</span><span class="hljs-params">(<span class="hljs-keyword">boolean</span> lazyInit)</span></span>;<span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">isLazyInit</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setDependsOn</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String... dependsOn)</span></span>;<span class="hljs-meta">@Nullable</span>String[] getDependsOn();<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setAutowireCandidate</span><span class="hljs-params">(<span class="hljs-keyword">boolean</span> autowireCandidate)</span></span>;<span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">isAutowireCandidate</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setPrimary</span><span class="hljs-params">(<span class="hljs-keyword">boolean</span> primary)</span></span>;<span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">isPrimary</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setFactoryBeanName</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String factoryBeanName)</span></span>;<span class="hljs-meta">@Nullable</span><span class="hljs-function">String <span class="hljs-title">getFactoryBeanName</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setFactoryMethodName</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String factoryMethodName)</span></span>;<span class="hljs-meta">@Nullable</span><span class="hljs-function">String <span class="hljs-title">getFactoryMethodName</span><span class="hljs-params">()</span></span>;<span class="hljs-function">ConstructorArgumentValues <span class="hljs-title">getConstructorArgumentValues</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">default</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">hasConstructorArgumentValues</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> !getConstructorArgumentValues().isEmpty();&#125;<span class="hljs-function">MutablePropertyValues <span class="hljs-title">getPropertyValues</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">default</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">hasPropertyValues</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> !getPropertyValues().isEmpty();&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setInitMethodName</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String initMethodName)</span></span>;<span class="hljs-meta">@Nullable</span><span class="hljs-function">String <span class="hljs-title">getInitMethodName</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setDestroyMethodName</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String destroyMethodName)</span></span>;<span class="hljs-meta">@Nullable</span><span class="hljs-function">String <span class="hljs-title">getDestroyMethodName</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setRole</span><span class="hljs-params">(<span class="hljs-keyword">int</span> role)</span></span>;<span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">getRole</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setDescription</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String description)</span></span>;<span class="hljs-meta">@Nullable</span><span class="hljs-function">String <span class="hljs-title">getDescription</span><span class="hljs-params">()</span></span>;<span class="hljs-function">ResolvableType <span class="hljs-title">getResolvableType</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">isSingleton</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">isPrototype</span><span class="hljs-params">()</span></span>;<span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">isAbstract</span><span class="hljs-params">()</span></span>;<span class="hljs-meta">@Nullable</span><span class="hljs-function">String <span class="hljs-title">getResourceDescription</span><span class="hljs-params">()</span></span>;    <span class="hljs-meta">@Nullable</span><span class="hljs-function">BeanDefinition <span class="hljs-title">getOriginatingBeanDefinition</span><span class="hljs-params">()</span></span>;&#125;</code></pre><p><img src="/images/spring_IOC/beandefinition%E7%88%B6%E7%B1%BB%E5%AD%90%E7%B1%BB%E7%BB%93%E6%9E%84%E5%9B%BE.png" alt="beandefinition父类子类结构图"></p><h5 id="BeanDefinition的父类"><a href="#BeanDefinition的父类" class="headerlink" title="BeanDefinition的父类"></a>BeanDefinition的父类</h5><p>如上可以看出<code>BeanDefinition</code>继承自<code>AttributeAccessor</code>和<code>BeanMetadataElement</code>。</p><h6 id="AttributeAccessor"><a href="#AttributeAccessor" class="headerlink" title="AttributeAccessor"></a>AttributeAccessor</h6><p>该接口定义了与其他对象的元数据进行连接和访问的约定，即对属性的获取、设置、删除：</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">AttributeAccessor</span> </span>&#123;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setAttribute</span><span class="hljs-params">(String name, <span class="hljs-meta">@Nullable</span> Object value)</span></span>;<span class="hljs-meta">@Nullable</span><span class="hljs-function">Object <span class="hljs-title">getAttribute</span><span class="hljs-params">(String name)</span></span>;<span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span><span class="hljs-keyword">default</span> &lt;T&gt; <span class="hljs-function">T <span class="hljs-title">computeAttribute</span><span class="hljs-params">(String name, Function&lt;String, T&gt; computeFunction)</span> </span>&#123;Assert.notNull(name, <span class="hljs-string">&quot;Name must not be null&quot;</span>);Assert.notNull(computeFunction, <span class="hljs-string">&quot;Compute function must not be null&quot;</span>);Object value = getAttribute(name);<span class="hljs-keyword">if</span> (value == <span class="hljs-keyword">null</span>) &#123;value = computeFunction.apply(name);Assert.state(value != <span class="hljs-keyword">null</span>,() -&gt; String.format(<span class="hljs-string">&quot;Compute function must not return null for attribute named &#x27;%s&#x27;&quot;</span>, name));setAttribute(name, value);&#125;<span class="hljs-keyword">return</span> (T) value;&#125;<span class="hljs-meta">@Nullable</span><span class="hljs-function">Object <span class="hljs-title">removeAttribute</span><span class="hljs-params">(String name)</span></span>;<span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">hasAttribute</span><span class="hljs-params">(String name)</span></span>;String[] attributeNames();&#125;</code></pre><h6 id="BeanMetadataElement"><a href="#BeanMetadataElement" class="headerlink" title="BeanMetadataElement"></a>BeanMetadataElement</h6><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">BeanMetadataElement</span> </span>&#123;    <span class="hljs-comment">//返回此元数据元素的配置源，可能为null</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">default</span> Object <span class="hljs-title">getSource</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;&#125;</code></pre><h5 id="BeanDefinition的子类"><a href="#BeanDefinition的子类" class="headerlink" title="BeanDefinition的子类"></a>BeanDefinition的子类</h5><p>三个常用的子类：</p><p><code>package org.springframework.beans.factory.support.RootBeanDefinition</code></p><p><code>package org.springframework.beans.factory.support.ChildBeanDefinition</code></p><p><code>package org.springframework.beans.factory.support.RootBeanDefinition</code></p><p>如果配置文件中定义了父 <code>bean</code> 和 子 <code>bean</code> ，则父 <code>bean</code> 用 RootBeanDefinition 表示，子 <code>bean</code> 用 ChildBeanDefinition 表示，而没有父 <code>bean</code> 的就使用RootBeanDefinition 表示。</p><h4 id="解析bean标签"><a href="#解析bean标签" class="headerlink" title="解析bean标签"></a>解析bean标签</h4><h5 id="createBeanDefinition-方法"><a href="#createBeanDefinition-方法" class="headerlink" title="createBeanDefinition()方法"></a><code>createBeanDefinition()</code>方法</h5><p>在 <code>BeanDefinitionParserDelegate.parseBeanDefinitionElement(Element ele, String beanName, BeanDefinition containingBean)</code> 方法中进行<code>bean</code>标签解析的过程中，内部调用了该方法。</p><p><code>BeanDefinitionParserDelegate.createBeanDefinition()</code>返回<code>AbstractBeanDefinition</code>对象，方法内部调用的<code>BeanDefinitionReaderUtils.createBeanDefinition()</code>：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> AbstractBeanDefinition <span class="hljs-title">createBeanDefinition</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String className, <span class="hljs-meta">@Nullable</span> String parentName)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> ClassNotFoundException </span>&#123;    <span class="hljs-keyword">return</span> BeanDefinitionReaderUtils.createBeanDefinition(        parentName, className, <span class="hljs-keyword">this</span>.readerContext.getBeanClassLoader());&#125;<span class="hljs-comment">//该方法主要是，创建 GenericBeanDefinition 对象，并设置 parentName、className、beanClass 属性。</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> AbstractBeanDefinition <span class="hljs-title">createBeanDefinition</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String parentName, <span class="hljs-meta">@Nullable</span> String className, <span class="hljs-meta">@Nullable</span> ClassLoader classLoader)</span> <span class="hljs-keyword">throws</span> ClassNotFoundException </span>&#123;    <span class="hljs-comment">// 创建 GenericBeanDefinition 对象</span>    GenericBeanDefinition bd = <span class="hljs-keyword">new</span> GenericBeanDefinition();    <span class="hljs-comment">// 设置 parentName</span>    bd.setParentName(parentName);    <span class="hljs-keyword">if</span> (className != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">if</span> (classLoader != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">// 如果有给定的classLoader，设置 beanClass</span>            bd.setBeanClass(ClassUtils.forName(className, classLoader));        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// 设置 beanClassName</span>            bd.setBeanClassName(className);        &#125;    &#125;    <span class="hljs-keyword">return</span> bd;&#125;</code></pre><h5 id="parseBeanDefinitionAttributes-方法"><a href="#parseBeanDefinitionAttributes-方法" class="headerlink" title="parseBeanDefinitionAttributes()方法"></a><code>parseBeanDefinitionAttributes()</code>方法</h5><p>执行完<code>createBeanDefinition()</code>方法返回<code>AbstractBeanDefinition</code>对象后，进而执行该方法进行bean的属性设置。</p><p><code>BeanDefinitionParserDelegate.parseBeanDefinitionAttributes()</code>：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> AbstractBeanDefinition <span class="hljs-title">parseBeanDefinitionAttributes</span><span class="hljs-params">(Element ele, String beanName,</span></span><span class="hljs-function"><span class="hljs-params"><span class="hljs-meta">@Nullable</span> BeanDefinition containingBean, AbstractBeanDefinition bd)</span> </span>&#123;    <span class="hljs-comment">// 解析 scope 属性</span>    <span class="hljs-keyword">if</span> (ele.hasAttribute(SINGLETON_ATTRIBUTE)) &#123;        error(<span class="hljs-string">&quot;Old 1.x &#x27;singleton&#x27; attribute in use - upgrade to &#x27;scope&#x27; declaration&quot;</span>, ele);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (ele.hasAttribute(SCOPE_ATTRIBUTE)) &#123;        bd.setScope(ele.getAttribute(SCOPE_ATTRIBUTE));    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (containingBean != <span class="hljs-keyword">null</span>) &#123;        bd.setScope(containingBean.getScope());    &#125;    <span class="hljs-comment">// 解析 abstract 属性</span>    <span class="hljs-keyword">if</span> (ele.hasAttribute(ABSTRACT_ATTRIBUTE)) &#123;        bd.setAbstract(TRUE_VALUE.equals(ele.getAttribute(ABSTRACT_ATTRIBUTE)));    &#125;    <span class="hljs-comment">// 解析 lazy-init 属性</span>    String lazyInit = ele.getAttribute(LAZY_INIT_ATTRIBUTE);    <span class="hljs-keyword">if</span> (isDefaultValue(lazyInit)) &#123;        lazyInit = <span class="hljs-keyword">this</span>.defaults.getLazyInit();    &#125;    bd.setLazyInit(TRUE_VALUE.equals(lazyInit));    <span class="hljs-comment">// 解析 autowire 属性</span>    String autowire = ele.getAttribute(AUTOWIRE_ATTRIBUTE);    bd.setAutowireMode(getAutowireMode(autowire));    <span class="hljs-comment">// 解析 depends-on 属性</span>    <span class="hljs-keyword">if</span> (ele.hasAttribute(DEPENDS_ON_ATTRIBUTE)) &#123;        String dependsOn = ele.getAttribute(DEPENDS_ON_ATTRIBUTE);        bd.setDependsOn(StringUtils.tokenizeToStringArray(dependsOn, MULTI_VALUE_ATTRIBUTE_DELIMITERS));    &#125;    <span class="hljs-comment">// 解析 autowire-candidate 属性</span>    String autowireCandidate = ele.getAttribute(AUTOWIRE_CANDIDATE_ATTRIBUTE);    <span class="hljs-keyword">if</span> (isDefaultValue(autowireCandidate)) &#123;        String candidatePattern = <span class="hljs-keyword">this</span>.defaults.getAutowireCandidates();        <span class="hljs-keyword">if</span> (candidatePattern != <span class="hljs-keyword">null</span>) &#123;            String[] patterns = StringUtils.commaDelimitedListToStringArray(candidatePattern);            bd.setAutowireCandidate(PatternMatchUtils.simpleMatch(patterns, beanName));        &#125;    &#125;    <span class="hljs-keyword">else</span> &#123;        bd.setAutowireCandidate(TRUE_VALUE.equals(autowireCandidate));    &#125;    <span class="hljs-comment">// 解析 primary 标签</span>    <span class="hljs-keyword">if</span> (ele.hasAttribute(PRIMARY_ATTRIBUTE)) &#123;        bd.setPrimary(TRUE_VALUE.equals(ele.getAttribute(PRIMARY_ATTRIBUTE)));    &#125;    <span class="hljs-comment">// 解析 init-method 属性</span>    <span class="hljs-keyword">if</span> (ele.hasAttribute(INIT_METHOD_ATTRIBUTE)) &#123;        String initMethodName = ele.getAttribute(INIT_METHOD_ATTRIBUTE);        bd.setInitMethodName(initMethodName);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.defaults.getInitMethod() != <span class="hljs-keyword">null</span>) &#123;        bd.setInitMethodName(<span class="hljs-keyword">this</span>.defaults.getInitMethod());        bd.setEnforceInitMethod(<span class="hljs-keyword">false</span>);    &#125;    <span class="hljs-comment">// 解析 destroy-method 属性</span>    <span class="hljs-keyword">if</span> (ele.hasAttribute(DESTROY_METHOD_ATTRIBUTE)) &#123;        String destroyMethodName = ele.getAttribute(DESTROY_METHOD_ATTRIBUTE);        bd.setDestroyMethodName(destroyMethodName);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.defaults.getDestroyMethod() != <span class="hljs-keyword">null</span>) &#123;        bd.setDestroyMethodName(<span class="hljs-keyword">this</span>.defaults.getDestroyMethod());        bd.setEnforceDestroyMethod(<span class="hljs-keyword">false</span>);    &#125;    <span class="hljs-comment">// 解析 factory-method 属性</span>    <span class="hljs-keyword">if</span> (ele.hasAttribute(FACTORY_METHOD_ATTRIBUTE)) &#123;        bd.setFactoryMethodName(ele.getAttribute(FACTORY_METHOD_ATTRIBUTE));    &#125;    <span class="hljs-keyword">if</span> (ele.hasAttribute(FACTORY_BEAN_ATTRIBUTE)) &#123;        bd.setFactoryBeanName(ele.getAttribute(FACTORY_BEAN_ATTRIBUTE));    &#125;    <span class="hljs-keyword">return</span> bd;&#125;</code></pre><h4 id="BeanDefinition解析的整体流程"><a href="#BeanDefinition解析的整体流程" class="headerlink" title="BeanDefinition解析的整体流程"></a>BeanDefinition解析的整体流程</h4><p>下边四篇进行具体分析。</p><p>解析 BeanDefinition 的<strong>入口在DefaultBeanDefinitionDocumentReader的<code>.parseBeanDefinitions(Element root, BeanDefinitionParserDelegate delegate)</code> 方法</strong>。该方法会根据命令空间来判断标签是<strong>默认标签</strong>还是<strong>自定义标签</strong>，其中：</p><ul><li>默认标签，由 <code>parseDefaultElement(Element ele, BeanDefinitionParserDelegate delegate)</code> 方法来实现</li><li>自定义标签，由 BeanDefinitionParserDelegate 的 <code>parseCustomElement(Element ele, @Nullable BeanDefinition containingBd)</code> 方法来实现。</li></ul><p>在默认标签解析中，会根据标签名称的不同进行 <code>import</code>、<code>alias</code>、<code>bean</code>、<code>beans</code> 四大标签进行处理。其中 <code>bean</code> 标签的解析为核心，它由 <code>processBeanDefinition(Element ele, BeanDefinitionParserDelegate delegate)</code> 方法实现。</p><p><code>processBeanDefinition(Element ele, BeanDefinitionParserDelegate delegate)</code> 方法，开始进入解析核心工作，分为三步：</p><ol><li>解析默认标签的<strong>默认</strong>标签：<code>BeanDefinitionParserDelegate.parseBeanDefinitionElement(Element ele, ...)</code> 方法。该方法会依次解析 <code>bean</code> 标签的属性、各个子元素，解析完成后返回一个 GenericBeanDefinition 实例对象。</li><li>解析默认标签下的<strong>自定义</strong>标签：<code>BeanDefinitionParserDelegate.decorateBeanDefinitionIfRequired(Element ele, BeanDefinitionHolder definitionHolder)</code> 方法。</li><li>注册解析的 BeanDefinition：<code>BeanDefinitionReaderUtils#registerBeanDefinition(BeanDefinitionHolder definitionHolder, BeanDefinitionRegistry registry)</code> 方法。</li></ol>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>bean标签的解析准备</title>
    <link href="/2021/02/24/%E5%BC%80%E5%90%AF%E8%A7%A3%E6%9E%90%E8%BF%9B%E7%A8%8B/"/>
    <url>/2021/02/24/%E5%BC%80%E5%90%AF%E8%A7%A3%E6%9E%90%E8%BF%9B%E7%A8%8B/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">bean标签的解析准备</font></div><p><code>bean</code>标签解析过程：</p><h4 id="processBeanDefinition-方法"><a href="#processBeanDefinition-方法" class="headerlink" title="processBeanDefinition()方法"></a><code>processBeanDefinition()</code>方法</h4><p><code>DefaultBeanDefinitionDocumentReader.processBeanDefinition()</code>：</p><pre><code class="hljs reasonml">protected void process<span class="hljs-constructor">BeanDefinition(Element <span class="hljs-params">ele</span>, BeanDefinitionParserDelegate <span class="hljs-params">delegate</span>)</span> &#123;   <span class="hljs-comment">// 进行 bean 元素解析。</span>   <span class="hljs-comment">//如之前所说BeanDefinitionParserDelegate负责解析`BeanDifinition`对象，定义了解析XML Element的各种方法</span>   <span class="hljs-comment">// &lt;1&gt; 如果解析成功，则返回 BeanDefinitionHolder 对象。而 BeanDefinitionHolder 为 name 和 alias（数组） 的 BeanDefinition 对象</span>   <span class="hljs-comment">// 如果解析失败，则返回 null 。错误由 ProblemReporter 处理（方法注释说明了）。</span>   BeanDefinitionHolder bdHolder = delegate.parse<span class="hljs-constructor">BeanDefinitionElement(<span class="hljs-params">ele</span>)</span>;   <span class="hljs-keyword">if</span> (bdHolder != null) &#123;      <span class="hljs-comment">// &lt;2&gt; 进行自定义标签处理</span>      bdHolder = delegate.decorate<span class="hljs-constructor">BeanDefinitionIfRequired(<span class="hljs-params">ele</span>, <span class="hljs-params">bdHolder</span>)</span>;      <span class="hljs-keyword">try</span> &#123;         <span class="hljs-comment">// Register the final decorated instance.</span>         <span class="hljs-comment">// &lt;3&gt; 进行 BeanDefinition 的注册</span>         <span class="hljs-module-access"><span class="hljs-module"><span class="hljs-identifier">BeanDefinitionReaderUtils</span>.</span></span>register<span class="hljs-constructor">BeanDefinition(<span class="hljs-params">bdHolder</span>, <span class="hljs-params">getReaderContext</span>()</span>.get<span class="hljs-constructor">Registry()</span>);      &#125;      catch (BeanDefinitionStoreException ex) &#123;         get<span class="hljs-constructor">ReaderContext()</span>.error(<span class="hljs-string">&quot;Failed to register bean definition with name &#x27;&quot;</span> +               bdHolder.get<span class="hljs-constructor">BeanName()</span> + <span class="hljs-string">&quot;&#x27;&quot;</span>, ele, ex);      &#125;      <span class="hljs-comment">// Send registration event.</span>      <span class="hljs-comment">// &lt;4&gt; 发出响应事件，通知相关的监听器，已完成该 Bean 标签的解析。</span>      get<span class="hljs-constructor">ReaderContext()</span>.fire<span class="hljs-constructor">ComponentRegistered(<span class="hljs-params">new</span> BeanComponentDefinition(<span class="hljs-params">bdHolder</span>)</span>);   &#125;&#125;</code></pre><h4 id="parseBeanDefinitionElement-方法"><a href="#parseBeanDefinitionElement-方法" class="headerlink" title="parseBeanDefinitionElement()方法"></a><code>parseBeanDefinitionElement()</code>方法</h4><p>bean元素解析。</p><pre><code class="hljs java"><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> BeanDefinitionHolder <span class="hljs-title">parseBeanDefinitionElement</span><span class="hljs-params">(Element ele, <span class="hljs-meta">@Nullable</span> BeanDefinition containingBean)</span> </span>&#123;    String id = ele.getAttribute(ID_ATTRIBUTE);<span class="hljs-comment">//id</span>    String nameAttr = ele.getAttribute(NAME_ATTRIBUTE);<span class="hljs-comment">//name</span>    <span class="hljs-comment">//别名集合</span>    List&lt;String&gt; aliases = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();    <span class="hljs-keyword">if</span> (StringUtils.hasLength(nameAttr)) &#123;        String[] nameArr = StringUtils.tokenizeToStringArray(nameAttr, MULTI_VALUE_ATTRIBUTE_DELIMITERS);        aliases.addAll(Arrays.asList(nameArr));    &#125;    <span class="hljs-comment">//beanName优先使用id</span>    String beanName = id;    <span class="hljs-keyword">if</span> (!StringUtils.hasText(beanName) &amp;&amp; !aliases.isEmpty()) &#123;        <span class="hljs-comment">//其次使用aliases的第一个并移出aliases集合</span>        beanName = aliases.remove(<span class="hljs-number">0</span>);        <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;            logger.trace(<span class="hljs-string">&quot;No XML &#x27;id&#x27; specified - using &#x27;&quot;</span> + beanName +                         <span class="hljs-string">&quot;&#x27; as bean name and &quot;</span> + aliases + <span class="hljs-string">&quot; as aliases&quot;</span>);        &#125;    &#125;    <span class="hljs-keyword">if</span> (containingBean == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// 检查 beanName 的唯一性</span>        checkNameUniqueness(beanName, aliases, ele);    &#125;    <span class="hljs-comment">// 解析属性，构造 AbstractBeanDefinition 对象</span>    AbstractBeanDefinition beanDefinition = parseBeanDefinitionElement(ele, beanName, containingBean);    <span class="hljs-keyword">if</span> (beanDefinition != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// beanName ，再次，使用 beanName 生成规则</span>        <span class="hljs-keyword">if</span> (!StringUtils.hasText(beanName)) &#123;            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-keyword">if</span> (containingBean != <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-comment">// 生成唯一的 beanName</span>                    beanName = BeanDefinitionReaderUtils.generateBeanName(                        beanDefinition, <span class="hljs-keyword">this</span>.readerContext.getRegistry(), <span class="hljs-keyword">true</span>);                &#125;                <span class="hljs-keyword">else</span> &#123;                    <span class="hljs-comment">// 生成唯一的 beanName</span>                    beanName = <span class="hljs-keyword">this</span>.readerContext.generateBeanName(beanDefinition);                    String beanClassName = beanDefinition.getBeanClassName();                    <span class="hljs-keyword">if</span> (beanClassName != <span class="hljs-keyword">null</span> &amp;&amp;                        beanName.startsWith(beanClassName) &amp;&amp; beanName.length() &gt; beanClassName.length() &amp;&amp;                        !<span class="hljs-keyword">this</span>.readerContext.getRegistry().isBeanNameInUse(beanClassName)) &#123;                        aliases.add(beanClassName);                    &#125;                &#125;                <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                    logger.trace(<span class="hljs-string">&quot;Neither XML &#x27;id&#x27; nor &#x27;name&#x27; specified - &quot;</span> +                                 <span class="hljs-string">&quot;using generated bean name [&quot;</span> + beanName + <span class="hljs-string">&quot;]&quot;</span>);                &#125;            &#125;            <span class="hljs-keyword">catch</span> (Exception ex) &#123;                error(ex.getMessage(), ele);                <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;            &#125;        &#125;        <span class="hljs-comment">// &lt;5&gt; 创建 BeanDefinitionHolder 对象</span>        String[] aliasesArray = StringUtils.toStringArray(aliases);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> BeanDefinitionHolder(beanDefinition, beanName, aliasesArray);    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre><p>此处还未真正意义开始bean解析，只是解析前的准备工作，解析id、name属性，确定aliases集合，检测beanName是否唯一。</p><pre><code class="hljs java"><span class="hljs-comment">//保存已经使用过的beanname</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Set&lt;String&gt; usedNames = <span class="hljs-keyword">new</span> HashSet&lt;&gt;();<span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">checkNameUniqueness</span><span class="hljs-params">(String beanName, List&lt;String&gt; aliases, Element beanElement)</span> </span>&#123;    String foundName = <span class="hljs-keyword">null</span>;    <span class="hljs-comment">// 寻找是否 beanName 已经使用</span>    <span class="hljs-keyword">if</span> (StringUtils.hasText(beanName) &amp;&amp; <span class="hljs-keyword">this</span>.usedNames.contains(beanName)) &#123;        foundName = beanName;    &#125;    <span class="hljs-keyword">if</span> (foundName == <span class="hljs-keyword">null</span>) &#123;        foundName = CollectionUtils.findFirstMatch(<span class="hljs-keyword">this</span>.usedNames, aliases);    &#125;    <span class="hljs-comment">// 若已使用，使用 problemReporter 提示错误</span>    <span class="hljs-keyword">if</span> (foundName != <span class="hljs-keyword">null</span>) &#123;        error(<span class="hljs-string">&quot;Bean name &#x27;&quot;</span> + foundName + <span class="hljs-string">&quot;&#x27; is already used in this &lt;beans&gt; element&quot;</span>, beanElement);    &#125;    <span class="hljs-comment">// 添加到 usedNames 集合</span>    <span class="hljs-keyword">this</span>.usedNames.add(beanName);    <span class="hljs-keyword">this</span>.usedNames.addAll(aliases);&#125;</code></pre><p><strong><code>beanName命名规则</code></strong></p><ol><li><p><strong>id不为空，beanName = id；</strong></p></li><li><p><strong>id为空，aliases不为空，beanName为aliases的第一个元素；</strong></p></li><li><p><strong>如果id、aliases都为空，使用默认规则命名：</strong></p><p>spring通过核心接口<code>BeanNameGenerator</code>来生成beanName，目前只有两个具体实现类：<code>AnnotationBeanNameGenerator</code>（注解）和<code>DefaultBeanNameGenerator</code>（XML文件）。</p><p><strong>XML方式生成规则：<code>类名＋&#39;#&#39;+数字</code>，</strong></p><p><strong>注解方式生成规则：取短类名，即不包含路径，如果短类名长度大于１，且第一个第二个字母大写，则直接使用短类名，</strong>例</p><pre><code class="hljs java">com.jievhaha.TEst则beanName=TEst</code></pre><p><strong>其他情况短类名首字母小写。</strong></p></li></ol><h5 id="parseBeanDefinitionElement-方法-1"><a href="#parseBeanDefinitionElement-方法-1" class="headerlink" title="parseBeanDefinitionElement()方法"></a><code>parseBeanDefinitionElement()</code>方法</h5><p>解析bean标签的所有属性和子元素，并创建返回<code>AbstractBeanDefinition</code>。</p><pre><code class="hljs java"><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> AbstractBeanDefinition <span class="hljs-title">parseBeanDefinitionElement</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">    Element ele, String beanName, <span class="hljs-meta">@Nullable</span> BeanDefinition containingBean)</span> </span>&#123;    <span class="hljs-keyword">this</span>.parseState.push(<span class="hljs-keyword">new</span> BeanEntry(beanName));    <span class="hljs-comment">// 解析 class 属性</span>    String className = <span class="hljs-keyword">null</span>;    <span class="hljs-keyword">if</span> (ele.hasAttribute(CLASS_ATTRIBUTE)) &#123;        className = ele.getAttribute(CLASS_ATTRIBUTE).trim();    &#125;    <span class="hljs-comment">// 解析 parent 属性</span>    String parent = <span class="hljs-keyword">null</span>;    <span class="hljs-keyword">if</span> (ele.hasAttribute(PARENT_ATTRIBUTE)) &#123;        parent = ele.getAttribute(PARENT_ATTRIBUTE);    &#125;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// 创建 AbstractBeanDefinition 实例</span>        AbstractBeanDefinition bd = createBeanDefinition(className, parent);        <span class="hljs-comment">// 解析默认 bean 的各种属性</span>        parseBeanDefinitionAttributes(ele, beanName, containingBean, bd);        <span class="hljs-comment">// 提取 description</span>        bd.setDescription(DomUtils.getChildElementValueByTagName(ele, DESCRIPTION_ELEMENT));        <span class="hljs-comment">// 下面的一堆是解析 &lt;bean&gt;......&lt;/bean&gt; 内部的子元素，</span>        <span class="hljs-comment">// 解析出来以后的信息都放到 bd 的属性中</span>        <span class="hljs-comment">// 解析元数据 &lt;meta /&gt;</span>        parseMetaElements(ele, bd);        <span class="hljs-comment">// 解析 lookup-method 属性 &lt;lookup-method /&gt;</span>        parseLookupOverrideSubElements(ele, bd.getMethodOverrides());        <span class="hljs-comment">// 解析 replaced-method 属性 &lt;replaced-method /&gt;</span>        parseReplacedMethodSubElements(ele, bd.getMethodOverrides());        <span class="hljs-comment">// 解析构造函数参数 &lt;constructor-arg /&gt;</span>        parseConstructorArgElements(ele, bd);        <span class="hljs-comment">// 解析 property 子元素 &lt;property /&gt;</span>        parsePropertyElements(ele, bd);        <span class="hljs-comment">// 解析 qualifier 子元素 &lt;qualifier /&gt;</span>        parseQualifierElements(ele, bd);        bd.setResource(<span class="hljs-keyword">this</span>.readerContext.getResource());        bd.setSource(extractSource(ele));        <span class="hljs-keyword">return</span> bd;    &#125;    <span class="hljs-keyword">catch</span> (ClassNotFoundException ex) &#123;        error(<span class="hljs-string">&quot;Bean class [&quot;</span> + className + <span class="hljs-string">&quot;] not found&quot;</span>, ele, ex);    &#125;    <span class="hljs-keyword">catch</span> (NoClassDefFoundError err) &#123;        error(<span class="hljs-string">&quot;Class that bean class [&quot;</span> + className + <span class="hljs-string">&quot;] depends on not found&quot;</span>, ele, err);    &#125;    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        error(<span class="hljs-string">&quot;Unexpected failure during bean definition parsing&quot;</span>, ele, ex);    &#125;    <span class="hljs-keyword">finally</span> &#123;        <span class="hljs-keyword">this</span>.parseState.pop();    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre><h5 id="createBeanDefinition-方法"><a href="#createBeanDefinition-方法" class="headerlink" title="createBeanDefinition()方法"></a><code>createBeanDefinition()</code>方法</h5><p>创建 AbstractBeanDefinition 对象。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> AbstractBeanDefinition <span class="hljs-title">createBeanDefinition</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String className, <span class="hljs-meta">@Nullable</span> String parentName)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> ClassNotFoundException </span>&#123;    <span class="hljs-keyword">return</span> BeanDefinitionReaderUtils.createBeanDefinition(        parentName, className, <span class="hljs-keyword">this</span>.readerContext.getBeanClassLoader());&#125;</code></pre>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>解析import标签</title>
    <link href="/2021/02/24/%E8%A7%A3%E6%9E%90import%E6%A0%87%E7%AD%BE/"/>
    <url>/2021/02/24/%E8%A7%A3%E6%9E%90import%E6%A0%87%E7%AD%BE/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">解析import标签</font></div><p>在注册<code>BeanDefinition</code>中知道解析Bean有两种方式：</p><ul><li>默认解析方式<code>parseDefaultElement()</code></li><li>自定义解析方式<code>parseCustomElement()</code></li></ul><p>默认解析过程：</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String ALIAS_ATTRIBUTE = <span class="hljs-string">&quot;alias&quot;</span>;<span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String IMPORT_ELEMENT = <span class="hljs-string">&quot;import&quot;</span>;<span class="hljs-comment">//&quot;bean&quot;</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String BEAN_ELEMENT = BeanDefinitionParserDelegate.BEAN_ELEMENT;<span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String NESTED_BEANS_ELEMENT = <span class="hljs-string">&quot;beans&quot;</span>;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseDefaultElement</span><span class="hljs-params">(Element ele, BeanDefinitionParserDelegate delegate)</span> </span>&#123;    <span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, IMPORT_ELEMENT)) &#123;        importBeanDefinitionResource(ele);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, ALIAS_ELEMENT)) &#123;        processAliasRegistration(ele);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, BEAN_ELEMENT)) &#123;        processBeanDefinition(ele, delegate);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, NESTED_BEANS_ELEMENT)) &#123;        <span class="hljs-comment">// recurse</span>        doRegisterBeanDefinitions(ele);    &#125;&#125;</code></pre><h4 id="import示例"><a href="#import示例" class="headerlink" title="import示例"></a>import示例</h4><pre><code class="hljs java">&lt;?xml version=<span class="hljs-string">&quot;1.0&quot;</span> encoding=<span class="hljs-string">&quot;UTF-8&quot;</span>?&gt;&lt;beans xmlns=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans&quot;</span>       xmlns:xsi=<span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span>       xsi:schemaLocation=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans</span><span class="hljs-string">       http://www.springframework.org/schema/beans/spring-beans.xsd&quot;</span>&gt;&lt;context:property-placeholder location=<span class="hljs-string">&quot;classpath:application.properties&quot;</span>/&gt;    &lt;<span class="hljs-keyword">import</span> resource=<span class="hljs-string">&quot;spring-$&#123;suffix1&#125;.xml&quot;</span>/&gt;    &lt;<span class="hljs-keyword">import</span> resource=<span class="hljs-string">&quot;spring-$&#123;suffix2&#125;.xml&quot;</span>/&gt;&lt;/beans&gt;        <span class="hljs-comment">//application.properties内容</span>suffix1 = name1suffix2 = name2</code></pre><p>一个<code>applicationContext.xml</code>配置文件中可以使用<code>import</code>标签导入其他模块的配置文件。维护成本较低。</p><h4 id="importBeanDefinitionResource-方法"><a href="#importBeanDefinitionResource-方法" class="headerlink" title="importBeanDefinitionResource()方法"></a>importBeanDefinitionResource()方法</h4><p><code>DefaultBeanDefinitionDocumentReader.importBeanDefinitionResource()</code>，如方法注释：解析import标签，并将给定资源中的bean加载到bean工厂中。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">importBeanDefinitionResource</span><span class="hljs-params">(Element ele)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 获取 resource 的属性值</span>    String location = ele.getAttribute(RESOURCE_ATTRIBUTE);    <span class="hljs-comment">// 为空，直接退出</span>    <span class="hljs-keyword">if</span> (!StringUtils.hasText(location)) &#123;        getReaderContext().error(<span class="hljs-string">&quot;Resource location must not be empty&quot;</span>, ele);        <span class="hljs-keyword">return</span>;    &#125;    <span class="hljs-comment">// &lt;2&gt; 解析系统属性，格式如 ：&quot;$&#123;user.dir&#125;&quot;</span>    location = getReaderContext().getEnvironment().resolveRequiredPlaceholders(location);    <span class="hljs-comment">// 实际 Resource 集合，即 import 的地址，有哪些 Resource 资源</span>    Set&lt;Resource&gt; actualResources = <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;(<span class="hljs-number">4</span>);    <span class="hljs-comment">//location是绝对路径还是相对路径</span>    <span class="hljs-keyword">boolean</span> absoluteLocation = <span class="hljs-keyword">false</span>;    <span class="hljs-keyword">try</span> &#123;        absoluteLocation = ResourcePatternUtils.isUrl(location) || ResourceUtils.toURI(location).isAbsolute();    &#125;    <span class="hljs-keyword">catch</span> (URISyntaxException ex) &#123;        <span class="hljs-comment">//相对位置不能转换为URI除非有一个默认的前缀&quot;classpat*:&quot;</span>    &#125;    <span class="hljs-comment">// Absolute or relative?</span>    <span class="hljs-keyword">if</span> (absoluteLocation) &#123;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">// 添加配置文件地址的 Resource 到 actualResources 中，并加载相应的 BeanDefinition</span>            <span class="hljs-keyword">int</span> importCount = getReaderContext().getReader().loadBeanDefinitions(location, actualResources);            <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                logger.trace(<span class="hljs-string">&quot;Imported &quot;</span> + importCount + <span class="hljs-string">&quot; bean definitions from URL location [&quot;</span> + location + <span class="hljs-string">&quot;]&quot;</span>);            &#125;        &#125;        <span class="hljs-keyword">catch</span> (BeanDefinitionStoreException ex) &#123;            getReaderContext().error(                <span class="hljs-string">&quot;Failed to import bean definitions from URL location [&quot;</span> + location + <span class="hljs-string">&quot;]&quot;</span>, ele, ex);        &#125;    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// No URL -&gt; considering resource location as relative to the current file.</span>        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-keyword">int</span> importCount;            <span class="hljs-comment">// 创建相对地址的 Resource</span>            Resource relativeResource = getReaderContext().getResource().createRelative(location);            <span class="hljs-keyword">if</span> (relativeResource.exists()) &#123;                <span class="hljs-comment">// 加载 relativeResource 中的 BeanDefinition</span>                importCount = getReaderContext().getReader().loadBeanDefinitions(relativeResource);                <span class="hljs-comment">// 添加到 actualResources 中</span>                actualResources.add(relativeResource);            &#125;            <span class="hljs-keyword">else</span> &#123;                <span class="hljs-comment">// 获得根路径地址</span>                String baseLocation = getReaderContext().getResource().getURL().toString();                <span class="hljs-comment">// 添加配置文件地址的 Resource 到 actualResources 中，并加载相应的 BeanDefinition</span>                <span class="hljs-comment">//和上边绝对路径的添加方式一样，不过多了一步获取绝对路径</span>                importCount = getReaderContext().getReader().loadBeanDefinitions(                    StringUtils.applyRelativePath(baseLocation, location), actualResources);            &#125;            <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                logger.trace(<span class="hljs-string">&quot;Imported &quot;</span> + importCount + <span class="hljs-string">&quot; bean definitions from relative location [&quot;</span> + location + <span class="hljs-string">&quot;]&quot;</span>);            &#125;        &#125;        <span class="hljs-keyword">catch</span> (IOException ex) &#123;            getReaderContext().error(<span class="hljs-string">&quot;Failed to resolve current resource location&quot;</span>, ele, ex);        &#125;        <span class="hljs-keyword">catch</span> (BeanDefinitionStoreException ex) &#123;            getReaderContext().error(                <span class="hljs-string">&quot;Failed to import bean definitions from relative location [&quot;</span> + location + <span class="hljs-string">&quot;]&quot;</span>, ele, ex);        &#125;    &#125;    <span class="hljs-comment">// &lt;6&gt; 解析成功后，进行监听器激活处理</span>    Resource[] actResArray = actualResources.toArray(<span class="hljs-keyword">new</span> Resource[<span class="hljs-number">0</span>]);    getReaderContext().fireImportProcessed(location, actResArray, extractSource(ele));&#125;</code></pre><h5 id="判断路径"><a href="#判断路径" class="headerlink" title="判断路径"></a>判断路径</h5><p>判断<code>location</code>是相对路径还是绝对路径：</p><pre><code class="hljs java">absoluteLocation = ResourcePatternUtils.isUrl(location) || ResourceUtils.toURI(location).isAbsolute();</code></pre><h6 id="ResourcePatternUtils-isUrl-location"><a href="#ResourcePatternUtils-isUrl-location" class="headerlink" title="ResourcePatternUtils.isUrl(location)"></a>ResourcePatternUtils.isUrl(location)</h6><p>以 <code>classpath*:</code> 或者 <code>classpath:</code> 开头的或者通过<code>resourceLocation</code>创建的<code>java.net.URL</code>为绝对路径。</p><p>方法具体内容：</p><pre><code class="hljs java"><span class="hljs-comment">//ResourcePatternUtils.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isUrl</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String resourceLocation)</span> </span>&#123;    <span class="hljs-keyword">return</span> (resourceLocation != <span class="hljs-keyword">null</span> &amp;&amp;    (            <span class="hljs-comment">//&quot;classpath*:&quot;开头，绝对路径</span>        resourceLocation.startsWith(ResourcePatternResolver.CLASSPATH_ALL_URL_PREFIX)   ||            <span class="hljs-comment">//</span>        ResourceUtils.isUrl(resourceLocation)    )    );&#125;<span class="hljs-comment">//ResourceUtils.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isUrl</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String resourceLocation)</span> </span>&#123;    <span class="hljs-keyword">if</span> (resourceLocation == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;    <span class="hljs-comment">//&quot;classpath:&quot;开头，绝对路径</span>    <span class="hljs-keyword">if</span> (resourceLocation.startsWith(CLASSPATH_URL_PREFIX)) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">//通过`resourceLocation`创建的&quot;java.net.URL&quot;，绝对路径</span>        <span class="hljs-keyword">new</span> URL(resourceLocation);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-keyword">catch</span> (MalformedURLException ex) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;&#125;<span class="hljs-comment">//ResourceUtils.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> URI <span class="hljs-title">toURI</span><span class="hljs-params">(String location)</span> <span class="hljs-keyword">throws</span> URISyntaxException </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> URI(StringUtils.replace(location, <span class="hljs-string">&quot; &quot;</span>, <span class="hljs-string">&quot;%20&quot;</span>));&#125;<span class="hljs-comment">//URI.java</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isAbsolute</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">return</span> scheme != <span class="hljs-keyword">null</span>;&#125;</code></pre><h5 id="处理绝对路径"><a href="#处理绝对路径" class="headerlink" title="处理绝对路径"></a>处理绝对路径</h5><p>如果是绝对路径，执行<code>int importCount = getReaderContext().getReader().loadBeanDefinitions(location, actualResources);</code>，该方法定义在<code>AbstractBeanDefinitionReader</code>中。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">loadBeanDefinitions</span><span class="hljs-params">(String location, <span class="hljs-meta">@Nullable</span> Set&lt;Resource&gt; actualResources)</span> <span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;    <span class="hljs-comment">// 获得 ResourceLoader 对象</span>    ResourceLoader resourceLoader = getResourceLoader();    <span class="hljs-keyword">if</span> (resourceLoader == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(            <span class="hljs-string">&quot;Cannot load bean definitions from location [&quot;</span> + location + <span class="hljs-string">&quot;]: no ResourceLoader available&quot;</span>);    &#125;    <span class="hljs-keyword">if</span> (resourceLoader <span class="hljs-keyword">instanceof</span> ResourcePatternResolver) &#123;        <span class="hljs-comment">//比如之前的PathMatchingResourcePatternResolver</span>        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">// 获得 Resource 数组，因为 Pattern 模式匹配下，可能有多个 Resource 。</span>            Resource[] resources = ((ResourcePatternResolver)resourceLoader).getResources(location);            <span class="hljs-comment">// 加载 BeanDefinition</span>            <span class="hljs-keyword">int</span> count = loadBeanDefinitions(resources);            <span class="hljs-comment">// 添加到 actualResources 中</span>            <span class="hljs-keyword">if</span> (actualResources != <span class="hljs-keyword">null</span>) &#123;                Collections.addAll(actualResources, resources);            &#125;            <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                logger.trace(<span class="hljs-string">&quot;Loaded &quot;</span> + count + <span class="hljs-string">&quot; bean definitions from location pattern [&quot;</span> + location + <span class="hljs-string">&quot;]&quot;</span>);            &#125;            <span class="hljs-keyword">return</span> count;        &#125;        <span class="hljs-keyword">catch</span> (IOException ex) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(                <span class="hljs-string">&quot;Could not resolve bean definition resource pattern [&quot;</span> + location + <span class="hljs-string">&quot;]&quot;</span>, ex);        &#125;    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// Can only load single resources by absolute URL.</span>        <span class="hljs-comment">//只能通过绝对URL加载单个资源。</span>        <span class="hljs-comment">// 获得 Resource 对象</span>        Resource resource = resourceLoader.getResource(location);        <span class="hljs-comment">// 加载 BeanDefinition</span>        <span class="hljs-keyword">int</span> count = loadBeanDefinitions(resource);        <span class="hljs-comment">// 添加到 actualResources 中</span>        <span class="hljs-keyword">if</span> (actualResources != <span class="hljs-keyword">null</span>) &#123;            actualResources.add(resource);        &#125;        <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;            logger.trace(<span class="hljs-string">&quot;Loaded &quot;</span> + count + <span class="hljs-string">&quot; bean definitions from location [&quot;</span> + location + <span class="hljs-string">&quot;]&quot;</span>);        &#125;        <span class="hljs-keyword">return</span> count;    &#125;&#125;</code></pre><ul><li>首先，获取 ResourceLoader 对象。</li><li>然后，根据不同的 ResourceLoader 执行不同的逻辑，主要是可能存在多个 Resource 。</li><li>最终，都会回归到 <code>XmlBeanDefinitionReader#loadBeanDefinitions(Resource... resources)</code> 方法，所以这是一个递归的过程。</li><li>另外，获得到的 Resource 的对象或数组，都会添加到 <code>actualResources</code> 中。</li></ul><h5 id="处理相对路径"><a href="#处理相对路径" class="headerlink" title="处理相对路径"></a>处理相对路径</h5><p>如果 <code>location</code> 是相对路径，则会根据相应的 Resource 计算出相应的相对路径的 Resource 对象 ，然后：</p><ul><li>若该 Resource 存在，则调用 <code>XmlBeanDefinitionReader#loadBeanDefinitions()</code> 方法，进行 BeanDefinition 加载。</li><li>否则，构造一个绝对 <code>location</code>( 即 <code>StringUtils.applyRelativePath(baseLocation, location)</code> 处的代码)，并调用 <code>#loadBeanDefinitions(String location, Set actualResources)</code> 方法，<strong>与绝对路径过程一样</strong>。</li></ul><h4 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h4><p>解析<code>import</code>的过程：<strong>获取 source 属性值，得到正确的资源路径，然后调用 <code>XmlBeanDefinitionReader#loadBeanDefinitions(Resource... resources)</code> 方法，进行递归的 BeanDefinition 加载</strong>。</p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>注册BeanDefinitions</title>
    <link href="/2021/02/24/%E6%B3%A8%E5%86%8CBeanDefinitions/"/>
    <url>/2021/02/24/%E6%B3%A8%E5%86%8CBeanDefinitions/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">注册BeanDefinitions</font></div><p>通过<code>XmlBeanDefinitionReader.doLoadDocument()</code>获取了<code>XML Documents</code>对象后，会使用该Document对象和Resource资源对象通过<code>XmlBeanDefinitionReader.registerBeanDefinitions()</code>开始注册<code>BeanDefinitions</code>。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">registerBeanDefinitions</span><span class="hljs-params">(Document doc, Resource resource)</span> <span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 创建 BeanDefinitionDocumentReader 对象</span>    BeanDefinitionDocumentReader documentReader = createBeanDefinitionDocumentReader();    <span class="hljs-comment">// &lt;2&gt; 获取已注册的 BeanDefinition 数量</span>    <span class="hljs-keyword">int</span> countBefore = getRegistry().getBeanDefinitionCount();    <span class="hljs-comment">// &lt;3&gt; 创建 XmlReaderContext 对象，参数createReaderContext(resource)</span>    <span class="hljs-comment">// &lt;4&gt; 注册 BeanDefinition</span>    documentReader.registerBeanDefinitions(doc, createReaderContext(resource));    <span class="hljs-comment">// 计算新注册的 BeanDefinition 数量</span>    <span class="hljs-keyword">return</span> getRegistry().getBeanDefinitionCount() - countBefore;&#125;</code></pre><h4 id="createBeanDefinitionDocumentReader-方法"><a href="#createBeanDefinitionDocumentReader-方法" class="headerlink" title="createBeanDefinitionDocumentReader()方法"></a><code>createBeanDefinitionDocumentReader()</code>方法</h4><p><code>createBeanDefinitionDocumentReader()</code>实例化<code>BeanDefinitionDocumentReader</code>对象，用来从XML文件中读取bean定义，默认实例化指定的<code>documentReaderClass</code>，即<code>DefaultBeanDefinitionDocumentReader.class</code>。</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> Class&lt;? extends BeanDefinitionDocumentReader&gt; documentReaderClass =DefaultBeanDefinitionDocumentReader.class;<span class="hljs-function"><span class="hljs-keyword">protected</span> BeanDefinitionDocumentReader <span class="hljs-title">createBeanDefinitionDocumentReader</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">return</span> BeanUtils.instantiateClass(<span class="hljs-keyword">this</span>.documentReaderClass);&#125;</code></pre><h4 id="registerBeanDefinitions-方法"><a href="#registerBeanDefinitions-方法" class="headerlink" title="registerBeanDefinitions()方法"></a><code>registerBeanDefinitions()</code>方法</h4><p><code>BeanDefinitionDocumentReader.registerBeanDefinitions(Document doc, XmlReaderContext readerContext)</code> 方法，注册 BeanDefinition ，在接口 BeanDefinitionDocumentReader 中定义。</p><p>机翻方法注释：<strong>从给定的DOM文档中读取bean定义，并在给定的阅读器上下文中向注册表注册它们。</strong></p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">BeanDefinitionDocumentReader</span> </span>&#123;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">registerBeanDefinitions</span><span class="hljs-params">(Document doc, XmlReaderContext readerContext)</span> <span class="hljs-keyword">throws</span> BeanDefinitionStoreException</span>;&#125;</code></pre><h5 id="DefaultBeanDefinitionDocumentReader"><a href="#DefaultBeanDefinitionDocumentReader" class="headerlink" title="DefaultBeanDefinitionDocumentReader"></a>DefaultBeanDefinitionDocumentReader</h5><p>该类是<code>BeanDefinitionDocumentReader</code>的实现类，且是唯一一个。</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerBeanDefinitions</span><span class="hljs-params">(Document doc, XmlReaderContext readerContext)</span> </span>&#123;    <span class="hljs-keyword">this</span>.readerContext = readerContext;<span class="hljs-comment">//获取XML Document Root Element并执行注册</span>    doRegisterBeanDefinitions(doc.getDocumentElement());&#125;<span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">doRegisterBeanDefinitions</span><span class="hljs-params">(Element root)</span> </span>&#123;    <span class="hljs-comment">// 保存原有的 BeanDefinitionParserDelegate 对象</span>    BeanDefinitionParserDelegate parent = <span class="hljs-keyword">this</span>.delegate;    <span class="hljs-comment">// &lt;1&gt; 创建 BeanDefinitionParserDelegate 对象，并进行设置到 delegate</span>    <span class="hljs-keyword">this</span>.delegate = createDelegate(getReaderContext(), root, parent);    <span class="hljs-comment">// &lt;2&gt; 给定的ＵＲＬ是否包含默认命名空间检查 &lt;beans /&gt; 根标签的命名空间是否为空，或者是 http://www.springframework.org/schema/beans</span>    <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.delegate.isDefaultNamespace(root)) &#123;        <span class="hljs-comment">// &lt;2.1&gt; 处理 profile 属性。&lt;bean profile=&quot;&quot;&gt;&lt;/bean&gt;，比如之前区分Test,Dev...等环境需要定义好几个application-xxx.xml文件，现在使用profile即可。</span>        String profileSpec = root.getAttribute(PROFILE_ATTRIBUTE);        <span class="hljs-keyword">if</span> (StringUtils.hasText(profileSpec)) &#123;            <span class="hljs-comment">// &lt;2.2&gt; 使用分隔符切分，可能有多个 profile 。</span>            String[] specifiedProfiles = StringUtils.tokenizeToStringArray(                profileSpec, BeanDefinitionParserDelegate.MULTI_VALUE_ATTRIBUTE_DELIMITERS);            <span class="hljs-comment">// &lt;2.3&gt; 如果所有 profile 都无效，则不进行注册</span>            <span class="hljs-keyword">if</span> (!getReaderContext().getEnvironment().acceptsProfiles(specifiedProfiles)) &#123;                <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                    logger.debug(<span class="hljs-string">&quot;Skipped XML bean definition file due to specified profiles [&quot;</span> + profileSpec +                                 <span class="hljs-string">&quot;] not matching: &quot;</span> + getReaderContext().getResource());                &#125;                <span class="hljs-keyword">return</span>;            &#125;        &#125;    &#125;    <span class="hljs-comment">// &lt;3&gt; 解析前处理，目前是个空方法，需要子类去实现。</span>    preProcessXml(root);    <span class="hljs-comment">// &lt;4&gt; 解析</span>    parseBeanDefinitions(root, <span class="hljs-keyword">this</span>.delegate);    <span class="hljs-comment">// &lt;5&gt; 解析后处理，目前是个空方法，需要子类去实现。</span>    postProcessXml(root);    <span class="hljs-comment">// 设置 delegate 为开头保存的　BeanDefinitionParserDelegate 对象</span>    <span class="hljs-keyword">this</span>.delegate = parent;&#125;</code></pre><h6 id="createDelegate-方法"><a href="#createDelegate-方法" class="headerlink" title="createDelegate()方法"></a><code>createDelegate()</code>方法</h6><p><code>BeanDefinitionParserDelegate</code>负责解析<code>BeanDifinition</code>对象，定义了解析XML Element的各种方法，如下获取<code>BeanDefinitionParserDelegate</code>：</p><pre><code class="hljs java"><span class="hljs-function">createDelegateprotected BeanDefinitionParserDelegate <span class="hljs-title">createDelegate</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">    XmlReaderContext readerContext, Element root, <span class="hljs-meta">@Nullable</span> BeanDefinitionParserDelegate parentDelegate)</span> </span>&#123;    <span class="hljs-comment">// 创建 BeanDefinitionParserDelegate 对象</span>    BeanDefinitionParserDelegate delegate = <span class="hljs-keyword">new</span> BeanDefinitionParserDelegate(readerContext);    <span class="hljs-comment">// 初始化默认</span>    delegate.initDefaults(root, parentDelegate);    <span class="hljs-keyword">return</span> delegate;&#125;</code></pre><h6 id="parseBeanDefinitions-方法"><a href="#parseBeanDefinitions-方法" class="headerlink" title="parseBeanDefinitions()方法"></a><code>parseBeanDefinitions()</code>方法</h6><p>对给定的<code>Element</code>进行解析：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseBeanDefinitions</span><span class="hljs-params">(Element root, BeanDefinitionParserDelegate delegate)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 根节点是否使用默认命名空间，</span>    <span class="hljs-keyword">if</span> (delegate.isDefaultNamespace(root)) &#123;        <span class="hljs-comment">// 遍历子节点</span>        NodeList nl = root.getChildNodes();        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nl.getLength(); i++) &#123;            Node node = nl.item(i);            <span class="hljs-keyword">if</span> (node <span class="hljs-keyword">instanceof</span> Element) &#123;                Element ele = (Element) node;                <span class="hljs-comment">//该节点是否使用默认命名空间，执行默认解析。</span>                <span class="hljs-keyword">if</span> (delegate.isDefaultNamespace(ele)) &#123;                    parseDefaultElement(ele, delegate);                &#125;                <span class="hljs-comment">//如果不是默认命名空间，执行自定义解析。</span>                <span class="hljs-keyword">else</span> &#123;                    delegate.parseCustomElement(ele);                &#125;            &#125;        &#125;    &#125;    <span class="hljs-comment">//&lt;2&gt;如果不是默认命名空间，执行自定义解析。</span>    <span class="hljs-keyword">else</span> &#123;        delegate.parseCustomElement(root);    &#125;&#125;</code></pre><ul><li>Spring有两种Bean声明方式：<ol><li>配置文件式声明，例：<code>&lt;bean id=&quot;lizi&quot; class=&quot;com.jievhaha.lizi&quot;/&gt;</code>，使用&lt;1&gt;处解析</li><li>自定义注解方式：<code>&lt;tx:anntation-driven&gt;</code>，执行&lt;2&gt;处解析</li></ol></li></ul><p>子节点或者根节点使用默认命名空间，执行默认解析，如下：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parseDefaultElement</span><span class="hljs-params">(Element ele, BeanDefinitionParserDelegate delegate)</span> </span>&#123;    <span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, IMPORT_ELEMENT)) &#123;        importBeanDefinitionResource(ele);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, ALIAS_ELEMENT)) &#123;        processAliasRegistration(ele);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, BEAN_ELEMENT)) &#123;        processBeanDefinition(ele, delegate);    &#125;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (delegate.nodeNameEquals(ele, NESTED_BEANS_ELEMENT)) &#123;        <span class="hljs-comment">// recurse</span>        doRegisterBeanDefinitions(ele);    &#125;&#125;</code></pre><h4 id="createReaderContext-方法"><a href="#createReaderContext-方法" class="headerlink" title="createReaderContext()方法"></a><code>createReaderContext()</code>方法</h4><p>文章开头，注册BeanDefinition对象时需要上送<code>XmlReaderContext</code>对象：</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> ProblemReporter problemReporter = <span class="hljs-keyword">new</span> FailFastProblemReporter();<span class="hljs-keyword">private</span> ReaderEventListener eventListener = <span class="hljs-keyword">new</span> EmptyReaderEventListener();<span class="hljs-keyword">private</span> SourceExtractor sourceExtractor = <span class="hljs-keyword">new</span> NullSourceExtractor();<span class="hljs-function"><span class="hljs-keyword">public</span> XmlReaderContext <span class="hljs-title">createReaderContext</span><span class="hljs-params">(Resource resource)</span> </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> XmlReaderContext(resource, <span class="hljs-keyword">this</span>.problemReporter, <span class="hljs-keyword">this</span>.eventListener,                                <span class="hljs-keyword">this</span>.sourceExtractor, <span class="hljs-keyword">this</span>, getNamespaceHandlerResolver());&#125;</code></pre><h4 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h4><p><code>XmlBeanDefinitionReader.doLoadBeanDefinitions</code>解析过程时序图：</p><p><img src="/images/spring_IOC/%E6%B3%A8%E5%86%8CBeanDefinition%E6%97%B6%E5%BA%8F%E5%9B%BE.jpg" alt="注册BeanDefinition时序图"></p><p>红框是解析过程。</p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>获取Document对象</title>
    <link href="/2021/02/24/%E8%8E%B7%E5%8F%96Document%E5%AF%B9%E8%B1%A1/"/>
    <url>/2021/02/24/%E8%8E%B7%E5%8F%96Document%E5%AF%B9%E8%B1%A1/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">获取Document对象</font></div><p>在 <code>XmlBeanDefinitionReader#doLoadDocument(InputSource inputSource, Resource resource)</code> 方法，中做了两件事情：</p><ul><li>调用 <code>#getValidationModeForResource(Resource resource)</code> 方法，获取指定资源（xml）的<strong>验证模式</strong>。</li><li>调用 <code>DocumentLoader#loadDocument(InputSource inputSource, EntityResolver entityResolver, ErrorHandler errorHandler, int validationMode, boolean namespaceAware)</code> 方法，获取 XML Document 实例。</li></ul><h4 id="DocumentLoader"><a href="#DocumentLoader" class="headerlink" title="DocumentLoader"></a>DocumentLoader</h4><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">DocumentLoader</span> </span>&#123;<span class="hljs-comment">/**</span><span class="hljs-comment"> * <span class="hljs-doctag">@param</span> inputSource 要加载的文档来源</span><span class="hljs-comment"> * <span class="hljs-doctag">@param</span> entityResolver 解析文件的解析器</span><span class="hljs-comment"> * <span class="hljs-doctag">@param</span> errorHandler 处理加载Document对象过程中发生的错误</span><span class="hljs-comment"> * <span class="hljs-doctag">@param</span> validationMode 验证模式</span><span class="hljs-comment"> * <span class="hljs-doctag">@param</span> namespaceAware 如果要提供对 XML 名称空间的支持，则需要值为 true 。</span><span class="hljs-comment"> */</span><span class="hljs-function">Document <span class="hljs-title">loadDocument</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">InputSource inputSource, EntityResolver entityResolver,</span></span><span class="hljs-function"><span class="hljs-params">ErrorHandler errorHandler, <span class="hljs-keyword">int</span> validationMode, <span class="hljs-keyword">boolean</span> namespaceAware)</span></span><span class="hljs-function"><span class="hljs-keyword">throws</span> Exception</span>;&#125;</code></pre><h5 id="DefaultDocumentLoader"><a href="#DefaultDocumentLoader" class="headerlink" title="DefaultDocumentLoader"></a>DefaultDocumentLoader</h5><p>该类是DocumentLoader的默认实现类。</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Document <span class="hljs-title">loadDocument</span><span class="hljs-params">(InputSource inputSource, EntityResolver entityResolver,</span></span><span class="hljs-function"><span class="hljs-params"> ErrorHandler errorHandler, <span class="hljs-keyword">int</span> validationMode, <span class="hljs-keyword">boolean</span> namespaceAware)</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 创建 DocumentBuilderFactory</span>    DocumentBuilderFactory factory = createDocumentBuilderFactory(validationMode, namespaceAware);    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;        logger.trace(<span class="hljs-string">&quot;Using JAXP provider [&quot;</span> + factory.getClass().getName() + <span class="hljs-string">&quot;]&quot;</span>);    &#125;    <span class="hljs-comment">// &lt;2&gt; 创建 DocumentBuilder</span>    DocumentBuilder builder = createDocumentBuilder(factory, entityResolver, errorHandler);    <span class="hljs-comment">// &lt;3&gt; 解析 XML InputSource 返回 Document 对象</span>    <span class="hljs-keyword">return</span> builder.parse(inputSource);&#125;</code></pre><p>第一步：调用<code>createDocumentBuilderFactory()</code>创建<code>DocumentBuilderFactory</code>对象。</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String SCHEMA_LANGUAGE_ATTRIBUTE = <span class="hljs-string">&quot;http://java.sun.com/xml/jaxp/properties/schemaLanguage&quot;</span>;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String XSD_SCHEMA_LANGUAGE = <span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema&quot;</span>;<span class="hljs-function"><span class="hljs-keyword">protected</span> DocumentBuilderFactory <span class="hljs-title">createDocumentBuilderFactory</span><span class="hljs-params">(<span class="hljs-keyword">int</span> validationMode, <span class="hljs-keyword">boolean</span> namespaceAware)</span> <span class="hljs-keyword">throws</span> ParserConfigurationException </span>&#123;    DocumentBuilderFactory factory = DocumentBuilderFactory.newInstance();    <span class="hljs-comment">// 设置命名空间支持</span>    factory.setNamespaceAware(namespaceAware);    <span class="hljs-keyword">if</span> (validationMode != XmlValidationModeDetector.VALIDATION_NONE) &#123;        <span class="hljs-comment">// 开启校验</span>        factory.setValidating(<span class="hljs-keyword">true</span>);        <span class="hljs-comment">// XSD 模式下，设置 factory 的属性</span>        <span class="hljs-keyword">if</span> (validationMode == XmlValidationModeDetector.VALIDATION_XSD) &#123;            <span class="hljs-comment">// XSD 模式下，强制设置命名空间支持</span>            factory.setNamespaceAware(<span class="hljs-keyword">true</span>);            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">// 设置 SCHEMA_LANGUAGE_ATTRIBUTE</span>                factory.setAttribute(SCHEMA_LANGUAGE_ATTRIBUTE, XSD_SCHEMA_LANGUAGE);            &#125;            <span class="hljs-keyword">catch</span> (IllegalArgumentException ex) &#123;                ParserConfigurationException pcex = <span class="hljs-keyword">new</span> ParserConfigurationException(                    <span class="hljs-string">&quot;Unable to validate using XSD: Your JAXP provider [&quot;</span> + factory +                    <span class="hljs-string">&quot;] does not support XML Schema. Are you running on Java 1.4 with Apache Crimson? &quot;</span> +                    <span class="hljs-string">&quot;Upgrade to Apache Xerces (or Java 1.5) for full XSD support.&quot;</span>);                pcex.initCause(ex);                <span class="hljs-keyword">throw</span> pcex;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> factory;&#125;</code></pre><p>第二步：调用<code>createDocumentBuilder()</code>创建<code>DocumentBuilder</code>对象。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> DocumentBuilder <span class="hljs-title">createDocumentBuilder</span><span class="hljs-params">(DocumentBuilderFactory factory,<span class="hljs-meta">@Nullable</span> EntityResolver entityResolver, <span class="hljs-meta">@Nullable</span> ErrorHandler errorHandler)</span><span class="hljs-keyword">throws</span> ParserConfigurationException </span>&#123;    <span class="hljs-comment">// 创建 DocumentBuilder 对象</span>    DocumentBuilder docBuilder = factory.newDocumentBuilder();    <span class="hljs-keyword">if</span> (entityResolver != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// &lt;x&gt; 设置 EntityResolver 属性</span>        docBuilder.setEntityResolver(entityResolver);    &#125;    <span class="hljs-keyword">if</span> (errorHandler != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// 设置 ErrorHandler 属性</span>        docBuilder.setErrorHandler(errorHandler);    &#125;    <span class="hljs-keyword">return</span> docBuilder;&#125;</code></pre><p>设置 DocumentBuilder 的 <strong>EntityResolver</strong> 属性。详解在**<em>EntityResolver**</em>段。</p><p>第三步：调用<code>DocumentBuilder#parse(InputSource)</code> 方法，解析 InputSource ，返回 Document 对象。</p><h4 id="EntityResolver"><a href="#EntityResolver" class="headerlink" title="EntityResolver"></a>EntityResolver</h4><p>在<code>XmlBeanDefinitionReader.doLoadDocument()</code>中通过 <code>DocumentLoader.loadDocument()</code> 方法来获取 Document 对象时，有一个方法参数 <code>entityResolver</code> 。该参数是通过 <code>XmlBeanDefinitionReader.getEntityResolver()</code> 方法来获取的。<strong>该类在于如何获取验证文件，从而验证用户写的 XML 是否通过验证。</strong></p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> EntityResolver <span class="hljs-title">getEntityResolver</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.entityResolver == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// Determine default EntityResolver to use.</span>        ResourceLoader resourceLoader = getResourceLoader();        <span class="hljs-keyword">if</span> (resourceLoader != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">//如果 ResourceLoader不为null，则根据指定的ResourceLoader创建一个ResourceEntityResolver对象。</span>            <span class="hljs-keyword">this</span>.entityResolver = <span class="hljs-keyword">new</span> ResourceEntityResolver(resourceLoader);        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">//如果 ResourceLoader为null，则创建一个DelegatingEntityResolver对象。该Resolver委托给默认的BeansDtdResolver和PluggableSchemaResolver。</span>            <span class="hljs-keyword">this</span>.entityResolver = <span class="hljs-keyword">new</span> DelegatingEntityResolver(getBeanClassLoader());        &#125;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.entityResolver;&#125;</code></pre><h5 id="EntityResoler子类"><a href="#EntityResoler子类" class="headerlink" title="EntityResoler子类"></a>EntityResoler子类</h5><p>上边获取EntityResolver的方法共涉及到４个EntityResolver的子类。</p><h6 id="ResourceEntityResolver"><a href="#ResourceEntityResolver" class="headerlink" title="ResourceEntityResolver"></a>ResourceEntityResolver</h6><p><code>EntityResolverorg.springframework.beans.factory.xml.ResourceEntityResolver</code>继承自<code>DelegatingEntityResolver</code>，通过 ResourceLoader 来解析实体的引用。</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ResourceLoader resourceLoader;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ResourceEntityResolver</span><span class="hljs-params">(ResourceLoader resourceLoader)</span> </span>&#123;    <span class="hljs-keyword">super</span>(resourceLoader.getClassLoader());<span class="hljs-comment">//下边紧接就是父类的构造器</span>    <span class="hljs-keyword">this</span>.resourceLoader = resourceLoader;&#125;</code></pre><h6 id="DelegatingEntityResolver"><a href="#DelegatingEntityResolver" class="headerlink" title="DelegatingEntityResolver"></a>DelegatingEntityResolver</h6><p><code>org.springframework.beans.factory.xml.DelegatingEntityResolver</code>实现了<code>EntityResolver</code>接口，分别代理了<code>BeansDtdResolver</code>和<code>PluggableSchemaResolver</code>。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String DTD_SUFFIX = <span class="hljs-string">&quot;.dtd&quot;</span>;<span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String XSD_SUFFIX = <span class="hljs-string">&quot;.xsd&quot;</span>;<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> EntityResolver dtdResolver;<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> EntityResolver schemaResolver;<span class="hljs-comment">// 默认</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">DelegatingEntityResolver</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> ClassLoader classLoader)</span> </span>&#123;    <span class="hljs-keyword">this</span>.dtdResolver = <span class="hljs-keyword">new</span> BeansDtdResolver();    <span class="hljs-keyword">this</span>.schemaResolver = <span class="hljs-keyword">new</span> PluggableSchemaResolver(classLoader);&#125;<span class="hljs-comment">// 自定义</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">DelegatingEntityResolver</span><span class="hljs-params">(EntityResolver dtdResolver, EntityResolver schemaResolver)</span> </span>&#123;    Assert.notNull(dtdResolver, <span class="hljs-string">&quot;&#x27;dtdResolver&#x27; is required&quot;</span>);    Assert.notNull(schemaResolver, <span class="hljs-string">&quot;&#x27;schemaResolver&#x27; is required&quot;</span>);    <span class="hljs-keyword">this</span>.dtdResolver = dtdResolver;    <span class="hljs-keyword">this</span>.schemaResolver = schemaResolver;&#125;</code></pre><h6 id="BeansDtdResolver"><a href="#BeansDtdResolver" class="headerlink" title="BeansDtdResolver"></a>BeansDtdResolver</h6><p><code>org.springframework.beans.factory.xml.BeansDtdResolver</code>实现了<code>EntityResolver</code>接口，Spring-Bean.dtd解码器，用来从<strong>classpath</strong>或者<strong>jar文件</strong>中加载 dtd ，源码注释有。</p><pre><code class="hljs java"><span class="hljs-comment">//&quot;/org/springframework/beans/factory/xml/spring-beans.dtd&quot;</span><span class="hljs-comment">//&quot;https://www.springframework.org/dtd/spring-beans-2.0.dtd&quot;</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String DTD_EXTENSION = <span class="hljs-string">&quot;.dtd&quot;</span>;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String DTD_NAME = <span class="hljs-string">&quot;spring-beans&quot;</span>;</code></pre><h6 id="PluggableSchemaResolver"><a href="#PluggableSchemaResolver" class="headerlink" title="PluggableSchemaResolver"></a>PluggableSchemaResolver</h6><p><code>org.springframework.beans.factory.xml.PluggableSchemaResolver</code>实现了<code>EntityResolver</code>接口，读取 classpath 下的所有 <code>META-INF/spring.schemas</code> 成一个 namespaceURI 与 Schema 文件地址的 map 。</p><h5 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h5><p>EntityResolver 的作用是项目本身就可以提供一个如何寻找 DTD 声明的方法，即由程序来实现寻找 DTD 声明的过程。比如我们将 DTD 文件放到项目中某处，在实现时直接将此文档读取并返回给 SAX 即可。这样就避免了通过网络来寻找相应的声明。－－《Spring 源码深度解析》</p><p><code>org.xml.sax.EntityResolver</code> 接口：</p><pre><code class="hljs java"><span class="hljs-comment">/* </span><span class="hljs-comment"> *@param 被引用的外部实体的公共标识符，如果没有提供，则返回 null 。.</span><span class="hljs-comment"> *@param 被引用的外部实体的系统标识符。</span><span class="hljs-comment">*/</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> InputSource <span class="hljs-title">resolveEntity</span> <span class="hljs-params">(String publicId,String systemId)</span><span class="hljs-keyword">throws</span> SAXException, IOException</span>;</code></pre><p>这两个参数的实际内容和具体的验证模式的如下：</p><ul><li><p>XSD验证模式：</p><ul><li>publicId ：null</li><li>systemId：<a href="http://www.springframework.org/schema/beans/spring-beans.xsd">http://www.springframework.org/schema/beans/spring-beans.xsd</a></li></ul></li><li><p>DTD验证模式：</p><ul><li>publicId：-//SPRING//DTD BEAN 2.0//EN</li><li>systemId：<a href="http://www.springframework.org/dtd/spring-beans.dtd">http://www.springframework.org/dtd/spring-beans.dtd</a></li></ul></li></ul><h5 id="ResourceEntityResolver-1"><a href="#ResourceEntityResolver-1" class="headerlink" title="ResourceEntityResolver"></a>ResourceEntityResolver</h5><p>ResourceEntityResolver的解析过程：</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> InputSource <span class="hljs-title">resolveEntity</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String publicId, <span class="hljs-meta">@Nullable</span> String systemId)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> SAXException, IOException </span>&#123;    <span class="hljs-comment">// 调用父类的方法，进行解析</span>    InputSource source = <span class="hljs-keyword">super</span>.resolveEntity(publicId, systemId);    <span class="hljs-comment">// 解析失败，resourceLoader 进行解析</span>    <span class="hljs-keyword">if</span> (source == <span class="hljs-keyword">null</span> &amp;&amp; systemId != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// 获得 resourcePath ，即 Resource 资源地址</span>        String resourcePath = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">try</span> &#123;            String decodedSystemId = URLDecoder.decode(systemId, <span class="hljs-string">&quot;UTF-8&quot;</span>);<span class="hljs-comment">// 使用 UTF-8 ，解码 systemId</span>            String givenUrl = <span class="hljs-keyword">new</span> URL(decodedSystemId).toString();<span class="hljs-comment">// 转换成 URL 字符串</span>            <span class="hljs-comment">// 解析文件资源的相对路径（相对于系统根路径）</span>            String systemRootUrl = <span class="hljs-keyword">new</span> File(<span class="hljs-string">&quot;&quot;</span>).toURI().toURL().toString();            <span class="hljs-comment">// Try relative to resource base if currently in system root.</span>            <span class="hljs-keyword">if</span> (givenUrl.startsWith(systemRootUrl)) &#123;                resourcePath = givenUrl.substring(systemRootUrl.length());            &#125;        &#125;        <span class="hljs-keyword">catch</span> (Exception ex) &#123;            <span class="hljs-comment">// Typically a MalformedURLException or AccessControlException.</span>            <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                logger.debug(<span class="hljs-string">&quot;Could not resolve XML entity [&quot;</span> + systemId + <span class="hljs-string">&quot;] against system root URL&quot;</span>, ex);            &#125;            <span class="hljs-comment">// No URL (or no resolvable URL) -&gt; try relative to resource base.</span>            resourcePath = systemId;        &#125;        <span class="hljs-keyword">if</span> (resourcePath != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                logger.trace(<span class="hljs-string">&quot;Trying to locate XML entity [&quot;</span> + systemId + <span class="hljs-string">&quot;] as resource [&quot;</span> + resourcePath + <span class="hljs-string">&quot;]&quot;</span>);            &#125;            <span class="hljs-comment">// 获得 Resource 资源</span>            Resource resource = <span class="hljs-keyword">this</span>.resourceLoader.getResource(resourcePath);            <span class="hljs-comment">// 创建 InputSource 对象</span>            source = <span class="hljs-keyword">new</span> InputSource(resource.getInputStream());            <span class="hljs-comment">// 设置 publicId 和 systemId 属性</span>            source.setPublicId(publicId);            source.setSystemId(systemId);            <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                logger.debug(<span class="hljs-string">&quot;Found XML entity [&quot;</span> + systemId + <span class="hljs-string">&quot;]: &quot;</span> + resource);            &#125;        &#125;        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (systemId.endsWith(DTD_SUFFIX) || systemId.endsWith(XSD_SUFFIX)) &#123;            <span class="hljs-comment">//通过https进行外部dtdxsd查找</span>            String url = systemId;            <span class="hljs-keyword">if</span> (url.startsWith(<span class="hljs-string">&quot;http:&quot;</span>)) &#123;                url = <span class="hljs-string">&quot;https:&quot;</span> + url.substring(<span class="hljs-number">5</span>);            &#125;            <span class="hljs-keyword">try</span> &#123;                source = <span class="hljs-keyword">new</span> InputSource(<span class="hljs-keyword">new</span> URL(url).openStream());                source.setPublicId(publicId);                source.setSystemId(systemId);            &#125;            <span class="hljs-keyword">catch</span> (IOException ex) &#123;                <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                    logger.debug(<span class="hljs-string">&quot;Could not resolve XML entity [&quot;</span> + systemId + <span class="hljs-string">&quot;] through URL [&quot;</span> + url + <span class="hljs-string">&quot;]&quot;</span>, ex);                &#125;                <span class="hljs-comment">//退回到解析器的默认行为</span>                source = <span class="hljs-keyword">null</span>;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> source;&#125;</code></pre><ul><li>首先，调用<strong>父类</strong>的方法，进行解析。</li><li>如果失败，使用 <code>resourceLoader</code> ，尝试读取 <code>systemId</code> 对应的 Resource 资源。</li></ul><h5 id="DelegatingEntityResolver-1"><a href="#DelegatingEntityResolver-1" class="headerlink" title="DelegatingEntityResolver"></a>DelegatingEntityResolver</h5><p><code>DelegatingEntityResolver</code>的解析过程：</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> InputSource <span class="hljs-title">resolveEntity</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String publicId, <span class="hljs-meta">@Nullable</span> String systemId)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> SAXException, IOException </span>&#123;    <span class="hljs-keyword">if</span> (systemId != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// DTD 模式，使用BeansDtdResolver进行解析</span>        <span class="hljs-keyword">if</span> (systemId.endsWith(DTD_SUFFIX)) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.dtdResolver.resolveEntity(publicId, systemId);        &#125;        <span class="hljs-comment">// XSD 模式，使用PluggableSchemaResolver进行解析</span>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (systemId.endsWith(XSD_SUFFIX)) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.schemaResolver.resolveEntity(publicId, systemId);        &#125;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre><h5 id="BeansDtdResolver-1"><a href="#BeansDtdResolver-1" class="headerlink" title="BeansDtdResolver"></a>BeansDtdResolver</h5><p><code>BeansDtdResolver</code>的解析过程：</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> InputSource <span class="hljs-title">resolveEntity</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String publicId, <span class="hljs-meta">@Nullable</span> String systemId)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;        logger.trace(<span class="hljs-string">&quot;Trying to resolve XML entity with public ID [&quot;</span> + publicId +                     <span class="hljs-string">&quot;] and system ID [&quot;</span> + systemId + <span class="hljs-string">&quot;]&quot;</span>);    &#125;    <span class="hljs-comment">// 以 .dtd 结尾</span>    <span class="hljs-keyword">if</span> (systemId != <span class="hljs-keyword">null</span> &amp;&amp; systemId.endsWith(DTD_EXTENSION)) &#123;        <span class="hljs-comment">// 获取最后一个 / 的位置</span>        <span class="hljs-keyword">int</span> lastPathSeparator = systemId.lastIndexOf(<span class="hljs-string">&#x27;/&#x27;</span>);        <span class="hljs-comment">// 获取 spring-beans 的位置</span>        <span class="hljs-keyword">int</span> dtdNameStart = systemId.indexOf(DTD_NAME, lastPathSeparator);        <span class="hljs-keyword">if</span> (dtdNameStart != -<span class="hljs-number">1</span>) &#123;            String dtdFile = DTD_NAME + DTD_EXTENSION;            <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                logger.trace(<span class="hljs-string">&quot;Trying to locate [&quot;</span> + dtdFile + <span class="hljs-string">&quot;] in Spring jar on classpath&quot;</span>);            &#125;            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">// 创建 ClassPathResource 对象</span>                Resource resource = <span class="hljs-keyword">new</span> ClassPathResource(dtdFile, getClass());                <span class="hljs-comment">// 创建 InputSource 对象，并设置 publicId、systemId 属性</span>                InputSource source = <span class="hljs-keyword">new</span> InputSource(resource.getInputStream());                source.setPublicId(publicId);                source.setSystemId(systemId);                <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                    logger.trace(<span class="hljs-string">&quot;Found beans DTD [&quot;</span> + systemId + <span class="hljs-string">&quot;] in classpath: &quot;</span> + dtdFile);                &#125;                <span class="hljs-keyword">return</span> source;            &#125;            <span class="hljs-keyword">catch</span> (FileNotFoundException ex) &#123;                <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                    logger.debug(<span class="hljs-string">&quot;Could not resolve beans DTD [&quot;</span> + systemId + <span class="hljs-string">&quot;]: not found in classpath&quot;</span>, ex);                &#125;            &#125;        &#125;    &#125;    <span class="hljs-comment">// 使用默认行为，从网络上下载</span>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre><h5 id="PluggableSchemaResolver-1"><a href="#PluggableSchemaResolver-1" class="headerlink" title="PluggableSchemaResolver"></a>PluggableSchemaResolver</h5><p><code>PluggableSchemaResolver</code>解析过程：</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> InputSource <span class="hljs-title">resolveEntity</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String publicId, <span class="hljs-meta">@Nullable</span> String systemId)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;        logger.trace(<span class="hljs-string">&quot;Trying to resolve XML entity with public id [&quot;</span> + publicId +                     <span class="hljs-string">&quot;] and system id [&quot;</span> + systemId + <span class="hljs-string">&quot;]&quot;</span>);    &#125;    <span class="hljs-keyword">if</span> (systemId != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// 获得 Resource 所在位置</span>        String resourceLocation = getSchemaMappings().get(systemId);        <span class="hljs-keyword">if</span> (resourceLocation == <span class="hljs-keyword">null</span> &amp;&amp; systemId.startsWith(<span class="hljs-string">&quot;https:&quot;</span>)) &#123;            <span class="hljs-comment">// Retrieve canonical http schema mapping even for https declaration</span>            resourceLocation = getSchemaMappings().get(<span class="hljs-string">&quot;http:&quot;</span> + systemId.substring(<span class="hljs-number">6</span>));        &#125;        <span class="hljs-keyword">if</span> (resourceLocation != <span class="hljs-keyword">null</span>) &#123;            Resource resource = <span class="hljs-keyword">new</span> ClassPathResource(resourceLocation, <span class="hljs-keyword">this</span>.classLoader);            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">// 创建 InputSource 对象，并设置 publicId、systemId 属性</span>                InputSource source = <span class="hljs-keyword">new</span> InputSource(resource.getInputStream());                source.setPublicId(publicId);                source.setSystemId(systemId);                <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                    logger.trace(<span class="hljs-string">&quot;Found XML schema [&quot;</span> + systemId + <span class="hljs-string">&quot;] in classpath: &quot;</span> + resourceLocation);                &#125;                <span class="hljs-keyword">return</span> source;            &#125;            <span class="hljs-keyword">catch</span> (FileNotFoundException ex) &#123;                <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;                    logger.debug(<span class="hljs-string">&quot;Could not find XML schema [&quot;</span> + systemId + <span class="hljs-string">&quot;]: &quot;</span> + resource, ex);                &#125;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre><p><code>getSchemaMappings()</code></p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> Map&lt;String, String&gt; <span class="hljs-title">getSchemaMappings</span><span class="hljs-params">()</span> </span>&#123;    Map&lt;String, String&gt; schemaMappings = <span class="hljs-keyword">this</span>.schemaMappings;    <span class="hljs-comment">// 双重检查锁，实现 schemaMappings 单例</span>    <span class="hljs-keyword">if</span> (schemaMappings == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>) &#123;            schemaMappings = <span class="hljs-keyword">this</span>.schemaMappings;            <span class="hljs-keyword">if</span> (schemaMappings == <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                    logger.trace(<span class="hljs-string">&quot;Loading schema mappings from [&quot;</span> + <span class="hljs-keyword">this</span>.schemaMappingsLocation + <span class="hljs-string">&quot;]&quot;</span>);                &#125;                <span class="hljs-keyword">try</span> &#123;                    <span class="hljs-comment">// 以 Properties 的方式，读取 schemaMappingsLocation</span>                    Properties mappings =                        PropertiesLoaderUtils.loadAllProperties(<span class="hljs-keyword">this</span>.schemaMappingsLocation, <span class="hljs-keyword">this</span>.classLoader);                    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                        logger.trace(<span class="hljs-string">&quot;Loaded schema mappings: &quot;</span> + mappings);                    &#125;                    <span class="hljs-comment">// 将 mappings 初始化到 schemaMappings 中</span>                    schemaMappings = <span class="hljs-keyword">new</span> ConcurrentHashMap&lt;&gt;(mappings.size());                    CollectionUtils.mergePropertiesIntoMap(mappings, schemaMappings);                    <span class="hljs-keyword">this</span>.schemaMappings = schemaMappings;                &#125;                <span class="hljs-keyword">catch</span> (IOException ex) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(                        <span class="hljs-string">&quot;Unable to load schema mappings from location [&quot;</span> + <span class="hljs-keyword">this</span>.schemaMappingsLocation + <span class="hljs-string">&quot;]&quot;</span>, ex);                &#125;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> schemaMappings;&#125;</code></pre><p>例：部分映射</p><pre><code class="hljs java"><span class="hljs-string">&quot;http://www.springframework.org/schema/context/spring-context-3.2.xsd&quot;</span>-&gt;<span class="hljs-string">&quot;org/springframework/context/config/spring-context.xsd&quot;</span><span class="hljs-string">&quot;http://www.springframework.org/schema/cache/spring-cache-4.3.xsd&quot;</span>-&gt;<span class="hljs-string">&quot;org/springframework/cache/config/spring-cache.xsd&quot;</span></code></pre><h5 id="自定义EntityResolver"><a href="#自定义EntityResolver" class="headerlink" title="自定义EntityResolver"></a>自定义EntityResolver</h5><p>如果 SAX 应用程序需要实现自定义处理外部实体，则必须实现EntityResolver接口，并使用 <code>#setEntityResolver(EntityResolver entityResolver)</code> 方法，向 SAX 驱动器注册一个 EntityResolver 实例。</p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>获取验证模型</title>
    <link href="/2021/02/24/%E8%8E%B7%E5%8F%96%E9%AA%8C%E8%AF%81%E6%A8%A1%E5%9E%8B/"/>
    <url>/2021/02/24/%E8%8E%B7%E5%8F%96%E9%AA%8C%E8%AF%81%E6%A8%A1%E5%9E%8B/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">获取验证模型</font></div><p>接《加载BeanDefinition》文。</p><h4 id="getValidationModeForResource"><a href="#getValidationModeForResource" class="headerlink" title="getValidationModeForResource"></a>getValidationModeForResource</h4><pre><code class="hljs java"><span class="hljs-comment">//禁用验证模式，值为0</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> VALIDATION_NONE = XmlValidationModeDetector.VALIDATION_NONE;<span class="hljs-comment">//自动获取验证模式，值为1</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> VALIDATION_AUTO = XmlValidationModeDetector.VALIDATION_AUTO;<span class="hljs-comment">//DTD 验证模式，值为2</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> VALIDATION_DTD = XmlValidationModeDetector.VALIDATION_DTD;<span class="hljs-comment">//XSD 验证模式，值为3</span><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> VALIDATION_XSD = XmlValidationModeDetector.VALIDATION_XSD;<span class="hljs-comment">//验证模式。默认为自动模式。</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> validationMode = VALIDATION_AUTO;<span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">int</span> <span class="hljs-title">getValidationModeForResource</span><span class="hljs-params">(Resource resource)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 获取指定的验证模式</span>    <span class="hljs-keyword">int</span> validationModeToUse = getValidationMode();    <span class="hljs-comment">// 如果手动指定，则直接返回</span>    <span class="hljs-keyword">if</span> (validationModeToUse != VALIDATION_AUTO) &#123;        <span class="hljs-keyword">return</span> validationModeToUse;    &#125;    <span class="hljs-comment">// 自动获取验证模式</span>    <span class="hljs-keyword">int</span> detectedMode = detectValidationMode(resource);    <span class="hljs-keyword">if</span> (detectedMode != VALIDATION_AUTO) &#123;        <span class="hljs-keyword">return</span> detectedMode;    &#125;    <span class="hljs-comment">// 否则，使用 VALIDATION_XSD</span>    <span class="hljs-keyword">return</span> VALIDATION_XSD;&#125;</code></pre><ul><li><code>&lt;1&gt;</code> 处，调用 <code>getValidationMode()</code> 方法，获取指定的验证模式( <code>validationMode</code> )。如果有手动指定，则直接返回。</li></ul><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setValidationMode</span><span class="hljs-params">(<span class="hljs-keyword">int</span> validationMode)</span> </span>&#123;    <span class="hljs-keyword">this</span>.validationMode = validationMode;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">getValidationMode</span><span class="hljs-params">()</span> </span><span class="hljs-function">    <span class="hljs-comment">//如上段，该值默认为VALIDATION_AUTO</span></span><span class="hljs-function">    return <span class="hljs-keyword">this</span>.validationMode</span>;&#125;</code></pre><ul><li><code>&lt;2&gt;</code> 处，调用 <code>detectValidationMode(Resource resource)</code> 方法，自动获取验证模式。</li></ul><pre><code class="hljs java"><span class="hljs-comment">//下段介绍XmlValidationModeDetector</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> XmlValidationModeDetector validationModeDetector = <span class="hljs-keyword">new</span> XmlValidationModeDetector();<span class="hljs-comment">//检测对&#123;@link Resource&#125;标识的XML文件执行哪种验证。如果文件具有&#123;@code DOCTYPE&#125;定义，则使用DTD验证，否则采用XSD验证。在接下来的方法中可以看到。</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">int</span> <span class="hljs-title">detectValidationMode</span><span class="hljs-params">(Resource resource)</span> </span>&#123;    <span class="hljs-comment">// 不可读，抛出 BeanDefinitionStoreException 异常</span>    <span class="hljs-keyword">if</span> (resource.isOpen()) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(            <span class="hljs-string">&quot;Passed-in Resource [&quot;</span> + resource + <span class="hljs-string">&quot;] contains an open stream: &quot;</span> +            <span class="hljs-string">&quot;cannot determine validation mode automatically. Either pass in a Resource &quot;</span> +            <span class="hljs-string">&quot;that is able to create fresh streams, or explicitly specify the validationMode &quot;</span> +            <span class="hljs-string">&quot;on your XmlBeanDefinitionReader instance.&quot;</span>);    &#125;    InputStream inputStream;    <span class="hljs-keyword">try</span> &#123;        inputStream = resource.getInputStream();    &#125;    <span class="hljs-keyword">catch</span> (IOException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(            <span class="hljs-string">&quot;Unable to determine validation mode for [&quot;</span> + resource + <span class="hljs-string">&quot;]: cannot open InputStream. &quot;</span> +            <span class="hljs-string">&quot;Did you attempt to load directly from a SAX InputSource without specifying the &quot;</span> +            <span class="hljs-string">&quot;validationMode on your XmlBeanDefinitionReader instance?&quot;</span>, ex);    &#125;    <span class="hljs-comment">// &lt;x&gt; 获取相应的验证模式</span>    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">//XmlValidationModeDetector上边已经定义</span>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.validationModeDetector.detectValidationMode(inputStream);    &#125;    <span class="hljs-keyword">catch</span> (IOException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(<span class="hljs-string">&quot;Unable to determine validation mode for [&quot;</span> +                                               resource + <span class="hljs-string">&quot;]: an error occurred whilst reading from the InputStream.&quot;</span>, ex);    &#125;&#125;</code></pre><h4 id="XmlValidationModeDetector"><a href="#XmlValidationModeDetector" class="headerlink" title="XmlValidationModeDetector"></a>XmlValidationModeDetector</h4><p><code>org.springframework.util.xml.XmlValidationModeDetector</code> ，XML 验证模式探测器。</p><p>主要看上段return this.validationModeDetector.detectValidationMode(inputStream);</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">detectValidationMode</span><span class="hljs-params">(InputStream inputStream)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;    <span class="hljs-keyword">try</span> (BufferedReader reader = <span class="hljs-keyword">new</span> BufferedReader(<span class="hljs-keyword">new</span> InputStreamReader(inputStream))) &#123;        <span class="hljs-comment">// 是否为 DTD 校验模式。默认为，非 DTD 模式，即 XSD 模式</span>        <span class="hljs-keyword">boolean</span> isDtdValidated = <span class="hljs-keyword">false</span>;        String content;        <span class="hljs-comment">// &lt;0&gt; 循环，逐行读取 XML 文件的内容</span>        <span class="hljs-keyword">while</span> ((content = reader.readLine()) != <span class="hljs-keyword">null</span>) &#123;            content = consumeCommentTokens(content);            <span class="hljs-comment">// 如果是注释，或者没内容，跳过</span>            <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.inComment || !StringUtils.hasText(content)) &#123;                <span class="hljs-keyword">continue</span>;            &#125;            <span class="hljs-comment">// &lt;1&gt; 包含 DOCTYPE 为 DTD 模式</span>            <span class="hljs-comment">//如下</span>            <span class="hljs-keyword">if</span> (hasDoctype(content)) &#123;                isDtdValidated = <span class="hljs-keyword">true</span>;                <span class="hljs-keyword">break</span>;            &#125;            <span class="hljs-comment">// &lt;2&gt;hasOpeningTag方法会校验，如果这一行有&lt;，并且&lt;后面跟着的是字母，则返回 true 。</span>            <span class="hljs-comment">//如下</span>            <span class="hljs-keyword">if</span> (hasOpeningTag(content)) &#123;                <span class="hljs-keyword">break</span>;            &#125;        &#125;        <span class="hljs-keyword">return</span> (isDtdValidated ? VALIDATION_DTD : VALIDATION_XSD);    &#125;    <span class="hljs-comment">//如果发生 CharConversionException 异常，则为 VALIDATION_AUTO 模式。</span>    <span class="hljs-keyword">catch</span> (CharConversionException ex) &#123;        <span class="hljs-keyword">return</span> VALIDATION_AUTO;    &#125;&#125;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String DOCTYPE = <span class="hljs-string">&quot;DOCTYPE&quot;</span>;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">hasDoctype</span><span class="hljs-params">(String content)</span> </span>&#123;    <span class="hljs-keyword">return</span> content.contains(DOCTYPE);&#125;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">hasOpeningTag</span><span class="hljs-params">(String content)</span> </span>&#123;    <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.inComment) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;    <span class="hljs-keyword">int</span> openTagIndex = content.indexOf(<span class="hljs-string">&#x27;&lt;&#x27;</span>);    <span class="hljs-keyword">return</span> (openTagIndex &gt; -<span class="hljs-number">1</span> &amp;&amp; (content.length() &gt; openTagIndex + <span class="hljs-number">1</span>) &amp;&amp;            Character.isLetter(content.charAt(openTagIndex + <span class="hljs-number">1</span>)));&#125;</code></pre>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>加载BeanDefinition</title>
    <link href="/2021/02/24/%E5%8A%A0%E8%BD%BDBeanDefinition/"/>
    <url>/2021/02/24/%E5%8A%A0%E8%BD%BDBeanDefinition/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">加载BeanDefinition</font></div><pre><code class="hljs reasonml"><span class="hljs-comment">//获取资源</span>ClassPathResource resource = <span class="hljs-keyword">new</span> <span class="hljs-constructor">ClassPathResource(<span class="hljs-string">&quot;bean.xml&quot;</span>)</span>;<span class="hljs-comment">//获取 BeanFactory</span>DefaultListableBeanFactory factory = <span class="hljs-keyword">new</span> <span class="hljs-constructor">DefaultListableBeanFactory()</span>;<span class="hljs-comment">//根据新建的 BeanFactory 创建一个 BeanDefinitionReader 对象，该 Reader 对象为资源的解析器</span>XmlBeanDefinitionReader reader = <span class="hljs-keyword">new</span> <span class="hljs-constructor">XmlBeanDefinitionReader(<span class="hljs-params">factory</span>)</span>;<span class="hljs-comment">//装载资源</span>reader.load<span class="hljs-constructor">BeanDefinitions(<span class="hljs-params">resource</span>)</span>;</code></pre><p>这段代码是 Spring 中编程式使用 IoC 容器，IoC 容器的使用过程如下：</p><p>资源定位、装载、注册。</p><p><strong>资源定位：</strong>IOC容器定位资源上一片《Spring统一资源加载策略》。</p><p><strong>装载：</strong>装载就是 BeanDefinition 的载入，<em>BeanDefinitionReader 读取、解析 Resource 资源，也就是将用户定义的 Bean 表示成 IoC 容器的内部数据结构：BeanDefinition</em> 。</p><ul><li>在 IoC 容器内部维护着一个 BeanDefinition Map 的数据结构。</li><li>在配置文件中每一个<bean>都对应着一个 BeanDefinition 对象。</li></ul><p><strong>注册：</strong>向 IoC 容器注册在第二步解析好的 BeanDefinition，其实就是将BeanDefinition注入到一个HashMap容器中，这个过程是通过 BeanDefinitionRegistry 接口来实现的，IoC 容器就是通过这个 HashMap 来维护这些 BeanDefinition 的。</p><ul><li>在这里需要注意的一点是这个过程并没有完成依赖注入（Bean 创建），Bean 创建是发生在应用第一次调用 <code>getBean(...)</code> 方法，向容器索要 Bean 时。</li><li>当然我们可以通过设置预处理，即对某个 Bean 设置 <code>lazyinit = false</code> 属性，那么这个 Bean 的依赖注入就会在容器初始化的时候完成。</li></ul><h4 id="loadBeanDefinitions"><a href="#loadBeanDefinitions" class="headerlink" title="loadBeanDefinitions"></a>loadBeanDefinitions</h4><p>装载，通过reader.loadBeanDefinitions(resource);</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">loadBeanDefinitions</span><span class="hljs-params">(Resource resource)</span> <span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;    <span class="hljs-keyword">return</span> loadBeanDefinitions(<span class="hljs-keyword">new</span> EncodedResource(resource));&#125;</code></pre><ul><li><p>从指定的 xml 文件加载 BeanDefinition ，这里会先对 Resource 资源封装成 <code>org.springframework.core.io.support.EncodedResource</code> 对象。这里为什么需要将 Resource 封装成 EncodedResource 呢？主要是为了对 Resource 进行编码，保证内容读取的正确性。</p></li><li><p>然后，再调用 <code>#loadBeanDefinitions(EncodedResource encodedResource)</code> 方法，执行真正的逻辑实现。</p></li></ul><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ThreadLocal&lt;Set&lt;EncodedResource&gt;&gt; resourcesCurrentlyBeingLoaded =<span class="hljs-keyword">new</span> NamedThreadLocal&lt;Set&lt;EncodedResource&gt;&gt;(<span class="hljs-string">&quot;XML bean definition resources currently being loaded&quot;</span>)&#123;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">protected</span> Set&lt;EncodedResource&gt; <span class="hljs-title">initialValue</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> HashSet&lt;&gt;(<span class="hljs-number">4</span>);&#125;&#125;;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">loadBeanDefinitions</span><span class="hljs-params">(EncodedResource encodedResource)</span> <span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;    Assert.notNull(encodedResource, <span class="hljs-string">&quot;EncodedResource must not be null&quot;</span>);    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;        logger.trace(<span class="hljs-string">&quot;Loading XML bean definitions from &quot;</span> + encodedResource);    &#125;    <span class="hljs-comment">// &lt;1&gt; 获取已经加载过的资源</span>    Set&lt;EncodedResource&gt; currentResources = <span class="hljs-keyword">this</span>.resourcesCurrentlyBeingLoaded.get();    <span class="hljs-keyword">if</span> (!currentResources.add(encodedResource)) &#123;<span class="hljs-comment">// 将当前资源加入记录中。如果已存在，抛出异常</span>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(            <span class="hljs-string">&quot;Detected cyclic loading of &quot;</span> + encodedResource + <span class="hljs-string">&quot; - check your import definitions!&quot;</span>);    &#125;    <span class="hljs-comment">// &lt;2&gt; 从 EncodedResource 获取封装的 Resource ，并从 Resource 中获取其中的 InputStream</span>    <span class="hljs-keyword">try</span> (InputStream inputStream = encodedResource.getResource().getInputStream()) &#123;        InputSource inputSource = <span class="hljs-keyword">new</span> InputSource(inputStream);        <span class="hljs-keyword">if</span> (encodedResource.getEncoding() != <span class="hljs-keyword">null</span>) &#123;            inputSource.setEncoding(encodedResource.getEncoding());        &#125;        <span class="hljs-comment">// 核心逻辑部分，执行加载 BeanDefinition</span>        <span class="hljs-keyword">return</span> doLoadBeanDefinitions(inputSource, encodedResource.getResource());    &#125;    <span class="hljs-keyword">catch</span> (IOException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(            <span class="hljs-string">&quot;IOException parsing XML document from &quot;</span> + encodedResource.getResource(), ex);    &#125;    <span class="hljs-keyword">finally</span> &#123;        <span class="hljs-comment">// 从缓存中剔除该资源 &lt;3&gt;</span>        currentResources.remove(encodedResource);        <span class="hljs-keyword">if</span> (currentResources.isEmpty()) &#123;            <span class="hljs-keyword">this</span>.resourcesCurrentlyBeingLoaded.remove();        &#125;    &#125;&#125;</code></pre><p><code>&lt;1&gt;</code> 处，通过 <code>resourcesCurrentlyBeingLoaded.get()</code> 代码，来获取已经加载过的资源，然后将 <code>encodedResource</code> 加入其中，如果 <code>resourcesCurrentlyBeingLoaded</code> 中已经存在该资源，则抛出 BeanDefinitionStoreException 异常。</p><ul><li>为什么需要这么做呢？答案在 <code>&quot;Detected cyclic loading&quot;</code> ，避免一个 EncodedResource 在加载时，还没加载完成，又加载自身，从而导致<strong>死循环</strong>。</li><li>也因此，在 <code>&lt;3&gt;</code> 处，当一个 EncodedResource 加载完成后，需要从缓存中剔除。</li></ul><p><code>&lt;2&gt;</code> 处理，从 <code>encodedResource</code> 获取封装的 Resource 资源，并从 Resource 中获取相应的 InputStream ，然后将 InputStream 封装为 InputSource ，最后调用 <code>#doLoadBeanDefinitions(InputSource inputSource, Resource resource)</code> 方法，执行加载 Bean Definition 的真正逻辑。</p><h4 id="doLoadBeanDefinitions"><a href="#doLoadBeanDefinitions" class="headerlink" title="doLoadBeanDefinitions"></a>doLoadBeanDefinitions</h4><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">int</span> <span class="hljs-title">doLoadBeanDefinitions</span><span class="hljs-params">(InputSource inputSource, Resource resource)</span></span><span class="hljs-function"><span class="hljs-keyword">throws</span> BeanDefinitionStoreException </span>&#123;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// &lt;1&gt; 获取 XML Document 实例</span>        Document doc = doLoadDocument(inputSource, resource);        <span class="hljs-comment">// &lt;2&gt; 根据 Document 实例，注册 Bean 信息</span>        <span class="hljs-keyword">int</span> count = registerBeanDefinitions(doc, resource);        <span class="hljs-keyword">if</span> (logger.isDebugEnabled()) &#123;            logger.debug(<span class="hljs-string">&quot;Loaded &quot;</span> + count + <span class="hljs-string">&quot; bean definitions from &quot;</span> + resource);        &#125;        <span class="hljs-keyword">return</span> count;    &#125;    <span class="hljs-keyword">catch</span> (BeanDefinitionStoreException ex) &#123;        <span class="hljs-keyword">throw</span> ex;    &#125;    <span class="hljs-keyword">catch</span> (SAXParseException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> XmlBeanDefinitionStoreException(resource.getDescription(),                                                  <span class="hljs-string">&quot;Line &quot;</span> + ex.getLineNumber() + <span class="hljs-string">&quot; in XML document from &quot;</span> + resource + <span class="hljs-string">&quot; is invalid&quot;</span>, ex);    &#125;    <span class="hljs-keyword">catch</span> (SAXException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> XmlBeanDefinitionStoreException(resource.getDescription(),                                                  <span class="hljs-string">&quot;XML document from &quot;</span> + resource + <span class="hljs-string">&quot; is invalid&quot;</span>, ex);    &#125;    <span class="hljs-keyword">catch</span> (ParserConfigurationException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(resource.getDescription(),                                               <span class="hljs-string">&quot;Parser configuration exception parsing XML from &quot;</span> + resource, ex);    &#125;    <span class="hljs-keyword">catch</span> (IOException ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(resource.getDescription(),                                               <span class="hljs-string">&quot;IOException parsing XML document from &quot;</span> + resource, ex);    &#125;    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BeanDefinitionStoreException(resource.getDescription(),                                               <span class="hljs-string">&quot;Unexpected exception parsing XML document from &quot;</span> + resource, ex);    &#125;&#125;</code></pre><ul><li>在 <code>&lt;1&gt;</code> 处，调用 <code>#doLoadDocument(InputSource inputSource, Resource resource)</code> 方法，根据 xml 文件，获取 Document 实例。</li><li>在 <code>&lt;2&gt;</code> 处，调用 <code>#registerBeanDefinitions(Document doc, Resource resource)</code> 方法，根据获取的 Document 实例，注册 Bean 信息。</li></ul><h5 id="doLoadDocument"><a href="#doLoadDocument" class="headerlink" title="doLoadDocument"></a>doLoadDocument</h5><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> Document <span class="hljs-title">doLoadDocument</span><span class="hljs-params">(InputSource inputSource, Resource resource)</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.documentLoader.loadDocument(inputSource, getEntityResolver(), <span class="hljs-keyword">this</span>.errorHandler,getValidationModeForResource(resource), isNamespaceAware());&#125;</code></pre><p>调用 <code>#getValidationModeForResource(Resource resource)</code> 方法，获取指定资源（xml）的<strong>验证模式</strong>。**<em>看下一篇**</em></p><p>调用 <code>DocumentLoader#loadDocument(InputSource inputSource, EntityResolver entityResolver, ErrorHandler errorHandler, int validationMode, boolean namespaceAware)</code> 方法，获取 XML Document 实例。</p><h5 id="registerBeanDefinitions"><a href="#registerBeanDefinitions" class="headerlink" title="registerBeanDefinitions"></a>registerBeanDefinitions</h5><p><strong><em>看注册BeanDefinitions</em></strong></p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>spring统一资源加载策略</title>
    <link href="/2021/02/24/%E7%BB%9F%E4%B8%80%E8%B5%84%E6%BA%90%E5%8A%A0%E8%BD%BD%E7%AD%96%E7%95%A5/"/>
    <url>/2021/02/24/%E7%BB%9F%E4%B8%80%E8%B5%84%E6%BA%90%E5%8A%A0%E8%BD%BD%E7%AD%96%E7%95%A5/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">spring统一资源加载策略</font></div><blockquote><table><tr><td bgcolor="#6495ED"><font color="#FFF">org.springframework.core.io.Resource 为 Spring 框架所有资源的抽象和访问接口，它继承 org.springframework.core.io.InputStreamSource接口。作为所有资源的统一抽象，Source 定义了一些通用的方法，由子类 AbstractResource 提供统一的默认实现。</font></td></tr></table></blockquote><h4 id="统一资源：Resource"><a href="#统一资源：Resource" class="headerlink" title="统一资源：Resource"></a>统一资源：Resource</h4><p><code>org.springframework.core.io.Resource</code>为Spring所有资源的抽象和访问接口，继承自<code>org.springframework.core.io.InputStreamSource</code>，<code>Resource</code>定义的统一方法由其子类<code>AbstractResource</code>提供默认实现。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Resource</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">InputStreamSource</span> </span>&#123;    <span class="hljs-comment">//资源是否存在</span>    <span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">exists</span><span class="hljs-params">()</span></span>;    <span class="hljs-comment">//资源是否可读</span>    <span class="hljs-function"><span class="hljs-keyword">default</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isReadable</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-comment">//资源所代表的句柄是否被一个stream打开了</span>    <span class="hljs-function"><span class="hljs-keyword">default</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isOpen</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;    <span class="hljs-comment">//是否为 File</span>    <span class="hljs-function"><span class="hljs-keyword">default</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isFile</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;    <span class="hljs-comment">//返回资源的URL的句柄</span>    <span class="hljs-function">URL <span class="hljs-title">getURL</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException</span>;    <span class="hljs-comment">//返回资源的URI的句柄</span>    <span class="hljs-function">URI <span class="hljs-title">getURI</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException</span>;    <span class="hljs-comment">//返回资源的File的句柄</span>    <span class="hljs-function">File <span class="hljs-title">getFile</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException</span>;    <span class="hljs-comment">//返回 ReadableByteChannel</span>    <span class="hljs-function"><span class="hljs-keyword">default</span> ReadableByteChannel <span class="hljs-title">readableChannel</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;        <span class="hljs-keyword">return</span> Channels.newChannel(getInputStream());    &#125;    <span class="hljs-comment">//资源内容的长度</span>    <span class="hljs-function"><span class="hljs-keyword">long</span> <span class="hljs-title">contentLength</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException</span>;    <span class="hljs-comment">//资源最后的修改时间</span>    <span class="hljs-function"><span class="hljs-keyword">long</span> <span class="hljs-title">lastModified</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException</span>;    <span class="hljs-comment">//根据资源的相对路径创建新资源</span>    <span class="hljs-function">Resource <span class="hljs-title">createRelative</span><span class="hljs-params">(String relativePath)</span> <span class="hljs-keyword">throws</span> IOException</span>;    <span class="hljs-comment">//资源的文件名</span>    <span class="hljs-meta">@Nullable</span>    <span class="hljs-function">String <span class="hljs-title">getFilename</span><span class="hljs-params">()</span></span>;    <span class="hljs-comment">//资源的描述</span>    <span class="hljs-function">String <span class="hljs-title">getDescription</span><span class="hljs-params">()</span></span>;&#125;</code></pre><p><img src="/images/spring_IOC/Resource.png" alt="Resource"></p><ol><li>FileSystemResource：对 <code>java.io.File</code> 类型资源的封装，只要是跟 File 打交道的，基本上与 FileSystemResource 也可以打交道。支持文件和 URL 的形式，实现 WritableResource 接口。</li><li>ByteArrayResource：对字节数组提供的数据的封装。如果通过 InputStream 形式访问该类型的资源，该实现会根据字节数组的数据构造一个相应的 ByteArrayInputStream。</li><li>UrlResource：对 <code>java.net.URL</code>类型资源的封装。内部委派 URL 进行具体的资源操作。</li><li>ClassPathResource：class path 类型资源的实现。使用给定的 ClassLoader 或者给定的 Class 来加载资源。</li><li>InputStreamResource：将给定的 InputStream 作为一种资源的 Resource 的实现类。</li></ol><p><code>AbstractResource </code>为 Resource 接口的默认实现，它实现了 Resource 接口的大部分的公共实现。</p><p>如果想要实现自定义的 Resource，记住不要实现 Resource 接口，而应该继承 AbstractResource 抽象类，然后根据当前的具体资源特性覆盖相应的方法即可。</p><h4 id="统一资源定位：ResourceLoader"><a href="#统一资源定位：ResourceLoader" class="headerlink" title="统一资源定位：ResourceLoader"></a>统一资源定位：ResourceLoader</h4><h5 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h5><p><code>org.springframework.core.io.ResourceLoader</code> 为 Spring 资源加载的统一抽象，具体的资源加载则由相应的实现类来完成，所以我们可以将 ResourceLoader 称作为统一资源定位器。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">ResourceLoader</span> </span>&#123;    <span class="hljs-comment">//只定义了两个方法</span>    String CLASSPATH_URL_PREFIX = ResourceUtils.CLASSPATH_URL_PREFIX;<span class="hljs-comment">//ResourceUtils.CLASSPATH_URL_PREFIX=&quot;classpath:&quot;</span>    <span class="hljs-function">Resource <span class="hljs-title">getResource</span><span class="hljs-params">(String var1)</span></span>;    <span class="hljs-function">ClassLoader <span class="hljs-title">getClassLoader</span><span class="hljs-params">()</span></span>;&#125;</code></pre><p><code>getResource</code>根据所提供资源的路径 location 返回 Resource 实例，但是它不确保该 Resource 一定存在，需要调用 <code>Resource.exist()</code>方法判断。该方法的主要实现是在其子类 DefaultResourceLoader 中实现，支持以下模式的资源加载：</p><ol><li>URL位置资源，如”file:C:/test.dat”。</li><li>ClassPath位置资源，如”classpath:test.dat”。</li><li>相对路径资源，如”WEB-INF/test.dat”，此时返回的Resource实例根据实现不同而不同。</li></ol><p><code>getClassLoader</code> 返回 ClassLoader 实例，对于想要获取 ResourceLoader 使用的 ClassLoader 用户来说，可以直接调用该方法来获取。</p><p><img src="/images/spring_IOC/ResourceLoader.png" alt="ResourceLoader"></p><h5 id="DefaultResourceLoader"><a href="#DefaultResourceLoader" class="headerlink" title="DefaultResourceLoader"></a>DefaultResourceLoader</h5><p><code>org.springframework.core.io.DefaultResourceLoader</code> 是 ResourceLoader 的默认实现。</p><h6 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h6><pre><code class="hljs java"><span class="hljs-comment">/*</span><span class="hljs-comment">* 在访问资源时，使用当前线程的，一般 Thread.currentThread().getContextClassLoader()</span><span class="hljs-comment">*/</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">DefaultResourceLoader</span><span class="hljs-params">()</span> </span>&#123;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">DefaultResourceLoader</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> ClassLoader classLoader)</span> </span>&#123;    <span class="hljs-keyword">this</span>.classLoader = classLoader;&#125;</code></pre><p>当然，后续也可以使用<code>setClassLoader</code>进行设置。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setClassLoader</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> ClassLoader classLoader)</span> </span>&#123;    <span class="hljs-keyword">this</span>.classLoader = classLoader;&#125;</code></pre><h6 id="getResource-方法"><a href="#getResource-方法" class="headerlink" title="getResource()方法"></a>getResource()方法</h6><p>ResourceLoader 中最核心的方法为<code>public Resource getResource(String location)</code>，<code>DefaultResourceLoader</code>提供了具体实现，他的两个子类并未覆盖此方法，所以ResourceLoader的资源加载策略就封装在 DefaultResourceLoader 中。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> Resource <span class="hljs-title">getResource</span><span class="hljs-params">(String location)</span> </span>&#123;    Assert.notNull(location, <span class="hljs-string">&quot;Location must not be null&quot;</span>);    <span class="hljs-comment">// 首先，通过 ProtocolResolver 来加载资源</span>    <span class="hljs-keyword">for</span> (ProtocolResolver protocolResolver : getProtocolResolvers()) &#123;        Resource resource = protocolResolver.resolve(location, <span class="hljs-keyword">this</span>);        <span class="hljs-keyword">if</span> (resource != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> resource;        &#125;    &#125;    <span class="hljs-comment">//以 / 开头，返回 ClassPathContextResource 类型的资源</span>    <span class="hljs-keyword">if</span> (location.startsWith(<span class="hljs-string">&quot;/&quot;</span>)) &#123;        <span class="hljs-keyword">return</span> getResourceByPath(location);    &#125;    <span class="hljs-comment">//以 classpath: 开头，返回 ClassPathResource 类型的资源</span>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (location.startsWith(CLASSPATH_URL_PREFIX)) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> ClassPathResource(location.substring(CLASSPATH_URL_PREFIX.length()), getClassLoader());    &#125;    <span class="hljs-comment">//根据是否为文件 URL ，是则返回 FileUrlResource 类型的资源，否则返回 UrlResource 类型的资源</span>    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">// Try to parse the location as a URL...</span>            URL url = <span class="hljs-keyword">new</span> URL(location);            <span class="hljs-keyword">return</span> (ResourceUtils.isFileURL(url) ? <span class="hljs-keyword">new</span> FileUrlResource(url) : <span class="hljs-keyword">new</span> UrlResource(url));        &#125;        <span class="hljs-comment">//格式错误异常</span>        <span class="hljs-keyword">catch</span> (MalformedURLException ex) &#123;            <span class="hljs-comment">// 返回 ClassPathContextResource 类型的资源</span>            <span class="hljs-comment">// No URL -&gt; resolve as resource path.</span>            <span class="hljs-keyword">return</span> getResourceByPath(location);        &#125;    &#125;&#125;</code></pre><ul><li><p>首先，通过 ProtocolResolver 来加载资源，成功返回 <strong>Resource</strong> 。</p></li><li><p>否则，若 <code>location</code> 以 <code>&quot;/&quot;</code> 开头，则调用 <code>#getResourceByPath()</code> 方法，构造 <strong>ClassPathContextResource</strong> 类型资源并返回。代码如下：</p><pre><code class="hljs routeros">protected<span class="hljs-built_in"> Resource </span>getResourceByPath(String path) &#123;return new ClassPathContextResource(path, getClassLoader());&#125;</code></pre></li><li><p>再否则，若 <code>location</code> 以 <code>&quot;classpath:&quot;</code> 开头，则构造 <strong>ClassPathResource</strong> 类型资源并返回。在构造该资源时，通过 <code>#getClassLoader()</code> 获取当前的 ClassLoader。</p></li><li><p>再否则，构造 URL ，尝试通过它进行资源定位，若没有抛出 <strong>MalformedURLException</strong> 异常，则判断是否为 FileURL , 如果是则构造 <strong>FileUrlResource</strong> 类型的资源，否则构造 <strong>UrlResource</strong> 类型的资源。</p></li><li><p>最后，若在加载过程中抛出 MalformedURLException 异常，则委派 <code>#getResourceByPath()</code> 方法，实现资源定位加载。该过程和第二步 <code>location</code> 以 <code>&quot;/&quot;</code> 开头相同。</p></li></ul><p><strong>其子类可以通过重写getResourceByPath(String path)来返回和自身相关的资源类型</strong>。</p><h6 id="ProtocolResolver"><a href="#ProtocolResolver" class="headerlink" title="ProtocolResolver"></a>ProtocolResolver</h6><p><code>org.springframework.core.io.ProtocolResolver</code>（是个函数式接口），用户自定义协议资源解决策略，如上介绍Resource所说：如果想要实现自定义的 Resource，应该继承 AbstractResource 抽象类，但是有了 ProtocolResolver 后，不需要直接继承 DefaultResourceLoader，改为实现 ProtocolResolver 接口也可以实现自定义的 ResourceLoader。</p><pre><code class="hljs java"><span class="hljs-meta">@FunctionalInterface</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">ProtocolResolver</span> </span>&#123;    <span class="hljs-comment">//需要用户自定义实现</span><span class="hljs-meta">@Nullable</span><span class="hljs-function">Resource <span class="hljs-title">resolve</span><span class="hljs-params">(String location, ResourceLoader resourceLoader)</span></span>;&#125;</code></pre><p>自定义的resolve添加到spring中：</p><pre><code class="hljs java"><span class="hljs-comment">//在此资源加载器中注册给定的解析器，从而允许处理其他协议，它也可以覆盖任何默认规则。</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">addProtocolResolver</span><span class="hljs-params">(ProtocolResolver resolver)</span> </span>&#123;    Assert.notNull(resolver, <span class="hljs-string">&quot;ProtocolResolver must not be null&quot;</span>);    <span class="hljs-keyword">this</span>.protocolResolvers.add(resolver);&#125;</code></pre><h5 id="FileSystemResourceLoader"><a href="#FileSystemResourceLoader" class="headerlink" title="FileSystemResourceLoader"></a>FileSystemResourceLoader</h5><p><code>org.springframework.core.io.FileSystemResourceLoader</code>重写了<code>getResourceByPath</code>，使之从文件系统加载资源并以 FileSystemResource 类型返回：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> Resource <span class="hljs-title">getResourceByPath</span><span class="hljs-params">(String path)</span> </span>&#123;    <span class="hljs-keyword">if</span> (path.startsWith(<span class="hljs-string">&quot;/&quot;</span>)) &#123;        path = path.substring(<span class="hljs-number">1</span>);    &#125;    <span class="hljs-comment">//创建 FileSystemContextResource 类型的资源，FileSystemContextResource为内部类。</span>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> FileSystemContextResource(path);&#125;</code></pre><h6 id="FileSystemContextResource"><a href="#FileSystemContextResource" class="headerlink" title="FileSystemContextResource"></a>FileSystemContextResource</h6><p>为 FileSystemResourceLoader 的内部类，它继承 FileSystemResource 类，实现 ContextResource 接口。</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FileSystemContextResource</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">FileSystemResource</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">ContextResource</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">FileSystemContextResource</span><span class="hljs-params">(String path)</span> </span>&#123;        <span class="hljs-keyword">super</span>(path);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getPathWithinContext</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> getPath();    &#125;&#125;</code></pre><h5 id="ClassRelativeResourceLoader"><a href="#ClassRelativeResourceLoader" class="headerlink" title="ClassRelativeResourceLoader"></a>ClassRelativeResourceLoader</h5><p><code>org.springframework.core.io.ClassRelativeResourceLoader</code>是DefaultResourceLoader的另一个子类，和<code>FileSystemResourceLoader</code>一样，都是重新写了getResourceByPath()方法，然后有个内部类<code>ClassRelativeContextResource</code>。</p><p>ClassRelativeResourceLoader扩展的功能是可以根据给定的class所在包或者所在包的子包下加载资源。</p><h5 id="ResourcePatternResolver"><a href="#ResourcePatternResolver" class="headerlink" title="ResourcePatternResolver"></a>ResourcePatternResolver</h5><p>ResourceLoader 的 <code>Resource getResource(String location)</code> 方法，每次只能根据 location 返回<strong>一个</strong> Resource 。当需要加载多个资源时，我们除了多次调用 <code>getResource(String location)</code> 方法外，别无他法。<code>org.springframework.core.io.support.ResourcePatternResolver</code> 是 ResourceLoader 的扩展，它支持根据指定的资源路径匹配模式每次返回<strong>多个</strong> Resource 实例。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">ResourcePatternResolver</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ResourceLoader</span> </span>&#123;    <span class="hljs-comment">//注：默认是&quot;classpath:&quot;;</span>String CLASSPATH_ALL_URL_PREFIX = <span class="hljs-string">&quot;classpath*:&quot;</span>;Resource[] getResources(String locationPattern) <span class="hljs-keyword">throws</span> IOException;&#125;</code></pre><p><code>ResourcePatternResolver</code>在<code>ResourceLoader</code>的基础上增加了<code>getResources</code>方法：</p><pre><code class="hljs java"><span class="hljs-comment">//注：父类是getResource</span>Resource[] getResources(String locationPattern) <span class="hljs-keyword">throws</span> IOException;</code></pre><h5 id="PathMatchingResourcePatternResolver"><a href="#PathMatchingResourcePatternResolver" class="headerlink" title="PathMatchingResourcePatternResolver"></a>PathMatchingResourcePatternResolver</h5><p>是<code>ResourcePatternResolver</code>的子类，在父类的基础上额外还支持 Ant 风格的路径匹配模式（类似于 <code>&quot;/*.xml&quot;</code>）。</p><h6 id="构造函数-1"><a href="#构造函数-1" class="headerlink" title="构造函数"></a>构造函数</h6><p>提供了三个构造函数</p><pre><code class="hljs java"><span class="hljs-comment">//内置的 ResourceLoader 资源定位器</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ResourceLoader resourceLoader;<span class="hljs-comment">//Ant 路径匹配器，默认为 AntPathMatcher 对象，用于支持 Ant 类型的路径匹配。</span><span class="hljs-keyword">private</span> PathMatcher pathMatcher = <span class="hljs-keyword">new</span> AntPathMatcher();<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">PathMatchingResourcePatternResolver</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">this</span>.resourceLoader = <span class="hljs-keyword">new</span> DefaultResourceLoader();&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">PathMatchingResourcePatternResolver</span><span class="hljs-params">(ResourceLoader resourceLoader)</span> </span>&#123;    Assert.notNull(resourceLoader, <span class="hljs-string">&quot;ResourceLoader must not be null&quot;</span>);    <span class="hljs-keyword">this</span>.resourceLoader = resourceLoader;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">PathMatchingResourcePatternResolver</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> ClassLoader classLoader)</span> </span>&#123;    <span class="hljs-keyword">this</span>.resourceLoader = <span class="hljs-keyword">new</span> DefaultResourceLoader(classLoader);&#125;</code></pre><h6 id="getResource"><a href="#getResource" class="headerlink" title="getResource"></a>getResource</h6><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> Resource <span class="hljs-title">getResource</span><span class="hljs-params">(String location)</span> </span>&#123;    <span class="hljs-keyword">return</span> getResourceLoader().getResource(location);&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> ResourceLoader <span class="hljs-title">getResourceLoader</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.resourceLoader;&#125;</code></pre><p>该方法，直接委托给相应的 ResourceLoader 来实现。如果我们在实例化的 PathMatchingResourcePatternResolver 的时候，如果未指定 ResourceLoader 参数的情况下，那么在加载资源时，其实就是 DefaultResourceLoader 的过程。</p><h6 id="getResources"><a href="#getResources" class="headerlink" title="getResources"></a>getResources</h6><pre><code class="hljs java"><span class="hljs-keyword">public</span> Resource[] getResources(String locationPattern) <span class="hljs-keyword">throws</span> IOException &#123;    Assert.notNull(locationPattern, <span class="hljs-string">&quot;Location pattern must not be null&quot;</span>);    <span class="hljs-comment">// 以 &quot;classpath*:&quot; 开头</span>    <span class="hljs-keyword">if</span> (locationPattern.startsWith(CLASSPATH_ALL_URL_PREFIX)) &#123;        <span class="hljs-comment">// 路径包含通配符</span>        <span class="hljs-comment">// a class path resource (multiple resources for same name possible)</span>        <span class="hljs-keyword">if</span> (getPathMatcher().isPattern(locationPattern.substring(CLASSPATH_ALL_URL_PREFIX.length()))) &#123;            <span class="hljs-comment">// a class path resource pattern</span>            <span class="hljs-keyword">return</span> findPathMatchingResources(locationPattern);        &#125;        <span class="hljs-comment">// 路径不包含通配符</span>        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// all class path resources with the given name</span>            <span class="hljs-keyword">return</span> findAllClassPathResources(locationPattern.substring(CLASSPATH_ALL_URL_PREFIX.length()));        &#125;    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// Generally only look for a pattern after a prefix here,</span>        <span class="hljs-comment">// and on Tomcat only after the &quot;*/&quot; separator for its &quot;war:&quot; protocol.</span>        <span class="hljs-keyword">int</span> prefixEnd = (locationPattern.startsWith(<span class="hljs-string">&quot;war:&quot;</span>) ? locationPattern.indexOf(<span class="hljs-string">&quot;*/&quot;</span>) + <span class="hljs-number">1</span> :                         locationPattern.indexOf(<span class="hljs-string">&#x27;:&#x27;</span>) + <span class="hljs-number">1</span>);        <span class="hljs-comment">// 路径包含通配符</span>        <span class="hljs-keyword">if</span> (getPathMatcher().isPattern(locationPattern.substring(prefixEnd))) &#123;            <span class="hljs-comment">// a file pattern</span>            <span class="hljs-keyword">return</span> findPathMatchingResources(locationPattern);        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// a single resource with the given name</span>            <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> Resource[] &#123;getResourceLoader().getResource(locationPattern)&#125;;        &#125;    &#125;&#125;</code></pre><ul><li><strong>非</strong> <code>&quot;classpath*:&quot;</code> 开头，且路径<strong>不包含</strong>通配符，直接委托给相应的 ResourceLoader 来实现。</li><li>其他情况，调用 <code>#findAllClassPathResources(...)</code>、或 <code>#findPathMatchingResources(...)</code> 方法，返回多个 Resource 。</li></ul><h6 id="findAllClassPathResources"><a href="#findAllClassPathResources" class="headerlink" title="findAllClassPathResources"></a>findAllClassPathResources</h6><p>当 <code>locationPattern</code> 以 <code>&quot;classpath*:&quot;</code> 开头但是不包含通配符，则调用 <code>findAllClassPathResources(...)</code> 方法加载资源。该方法返回 classes 路径下和所有 jar 包中的所有相匹配的资源。</p><pre><code class="hljs java"><span class="hljs-keyword">protected</span> Resource[] findAllClassPathResources(String location) <span class="hljs-keyword">throws</span> IOException &#123;    String path = location;    <span class="hljs-keyword">if</span> (path.startsWith(<span class="hljs-string">&quot;/&quot;</span>)) &#123;        path = path.substring(<span class="hljs-number">1</span>);    &#125;    <span class="hljs-comment">// 真正执行加载所有 classpath 资源</span>    Set&lt;Resource&gt; result = doFindAllClassPathResources(path);    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;        logger.trace(<span class="hljs-string">&quot;Resolved classpath location [&quot;</span> + location + <span class="hljs-string">&quot;] to resources &quot;</span> + result);    &#125;    <span class="hljs-comment">//转换成 Resource 数组返回</span>    <span class="hljs-keyword">return</span> result.toArray(<span class="hljs-keyword">new</span> Resource[<span class="hljs-number">0</span>]);&#125;</code></pre><p>真正执行加载的是在 <code>doFindAllClassPathResources(...)</code> 方法:</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> Set&lt;Resource&gt; <span class="hljs-title">doFindAllClassPathResources</span><span class="hljs-params">(String path)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;    Set&lt;Resource&gt; result = <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;(<span class="hljs-number">16</span>);    ClassLoader cl = getClassLoader();    <span class="hljs-comment">// &lt;1&gt; 根据 ClassLoader 加载路径下的所有资源</span>    Enumeration&lt;URL&gt; resourceUrls = (cl != <span class="hljs-keyword">null</span> ? cl.getResources(path) : ClassLoader.getSystemResources(path));    <span class="hljs-keyword">while</span> (resourceUrls.hasMoreElements()) &#123;        <span class="hljs-comment">//&lt;2&gt; 将 URL 转换成 UrlResource</span>        URL url = resourceUrls.nextElement();        result.add(convertClassLoaderURL(url));    &#125;    <span class="hljs-comment">// &lt;3&gt; 加载路径下得所有 jar 包</span>    <span class="hljs-keyword">if</span> (!StringUtils.hasLength(path)) &#123;        <span class="hljs-comment">// The above result is likely to be incomplete, i.e. only containing file system references.</span>        <span class="hljs-comment">// We need to have pointers to each of the jar files on the classpath as well...</span>        addAllClassLoaderJarRoots(cl, result);    &#125;    <span class="hljs-keyword">return</span> result;&#125;</code></pre><p>&lt;1&gt;处根据 ClassLoader 加载路径下的所有资源。在加载资源过程时，如果在构造 PathMatchingResourcePatternResolver 实例的时候如果传入了 ClassLoader，则调用该 ClassLoader 的 <code>getResources()</code> 方法，否则调用 <code>ClassLoader.getSystemResources(path)</code> 方法。另外，<code>ClassLoader.getResources()</code> 方法</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> Enumeration&lt;URL&gt; <span class="hljs-title">getResources</span><span class="hljs-params">(String name)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;    Objects.requireNonNull(name);    <span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span>    Enumeration&lt;URL&gt;[] tmp = (Enumeration&lt;URL&gt;[]) <span class="hljs-keyword">new</span> Enumeration&lt;?&gt;[<span class="hljs-number">2</span>];    <span class="hljs-keyword">if</span> (parent != <span class="hljs-keyword">null</span>) &#123;        tmp[<span class="hljs-number">0</span>] = parent.getResources(name);    &#125; <span class="hljs-keyword">else</span> &#123;        tmp[<span class="hljs-number">0</span>] = BootLoader.findResources(name);    &#125;    tmp[<span class="hljs-number">1</span>] = findResources(name);    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> CompoundEnumeration&lt;&gt;(tmp);&#125;</code></pre><p>如果当前父类加载器不为 <code>null</code> ，则通过父类向上迭代获取资源，否则调用<code>BootLoader.findResources(name);</code></p><p><code>&lt;2&gt;</code> 处，遍历 URL 集合，调用 <code>convertClassLoaderURL(URL url)</code> 方法，将 URL 转换成 UrlResource 对象。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> Resource <span class="hljs-title">convertClassLoaderURL</span><span class="hljs-params">(URL url)</span> </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> UrlResource(url);&#125;</code></pre><p><code>&lt;3&gt;</code> 处，若 <code>path</code> 为空（<code>“”</code>）时，则调用 <code>addAllClassLoaderJarRoots(...)</code>方法。该方法主要是加载路径下得所有 jar 包。</p><p>由上可以看出，<code>#findAllClassPathResources(...)</code> 方法，其实就是利用 ClassLoader 来加载指定路径下的资源，不论它是在 class 路径下还是在 jar 包中。如果我们传入的路径为空或者 <code>/</code>，则会调用 <code>addAllClassLoaderJarRoots(...)</code> 方法，加载所有的 jar 包。</p><h6 id="findPathMatchingResources"><a href="#findPathMatchingResources" class="headerlink" title="findPathMatchingResources"></a>findPathMatchingResources</h6><p>当 <code>locationPattern</code> 中包含了<strong>通配符</strong>，则调用该方法进行资源加载。</p><pre><code class="hljs java"><span class="hljs-keyword">protected</span> Resource[] findPathMatchingResources(String locationPattern) <span class="hljs-keyword">throws</span> IOException &#123;    <span class="hljs-comment">// 确定根路径、子路径</span>    String rootDirPath = determineRootDir(locationPattern);    String subPattern = locationPattern.substring(rootDirPath.length());    <span class="hljs-comment">// 获取根据路径下的资源</span>    Resource[] rootDirResources = getResources(rootDirPath);    <span class="hljs-comment">// 遍历，迭代</span>    Set&lt;Resource&gt; result = <span class="hljs-keyword">new</span> LinkedHashSet&lt;&gt;(<span class="hljs-number">16</span>);    <span class="hljs-keyword">for</span> (Resource rootDirResource : rootDirResources) &#123;        rootDirResource = resolveRootDirResource(rootDirResource);        URL rootDirUrl = rootDirResource.getURL();        <span class="hljs-comment">// bundle 资源类型</span>        <span class="hljs-keyword">if</span> (equinoxResolveMethod != <span class="hljs-keyword">null</span> &amp;&amp; rootDirUrl.getProtocol().startsWith(<span class="hljs-string">&quot;bundle&quot;</span>)) &#123;            URL resolvedUrl = (URL) ReflectionUtils.invokeMethod(equinoxResolveMethod, <span class="hljs-keyword">null</span>, rootDirUrl);            <span class="hljs-keyword">if</span> (resolvedUrl != <span class="hljs-keyword">null</span>) &#123;                rootDirUrl = resolvedUrl;            &#125;            rootDirResource = <span class="hljs-keyword">new</span> UrlResource(rootDirUrl);        &#125;        <span class="hljs-comment">// vfs 资源类型</span>        <span class="hljs-keyword">if</span> (rootDirUrl.getProtocol().startsWith(ResourceUtils.URL_PROTOCOL_VFS)) &#123;            result.addAll(VfsResourceMatchingDelegate.findMatchingResources(rootDirUrl, subPattern, getPathMatcher()));        &#125;        <span class="hljs-comment">// jar 资源类型</span>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (ResourceUtils.isJarURL(rootDirUrl) || isJarResource(rootDirResource)) &#123;            result.addAll(doFindPathMatchingJarResources(rootDirResource, rootDirUrl, subPattern));        &#125;        <span class="hljs-comment">// 其它资源类型</span>        <span class="hljs-keyword">else</span> &#123;            result.addAll(doFindPathMatchingFileResources(rootDirResource, subPattern));        &#125;    &#125;    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;        logger.trace(<span class="hljs-string">&quot;Resolved location pattern [&quot;</span> + locationPattern + <span class="hljs-string">&quot;] to resources &quot;</span> + result);    &#125;    <span class="hljs-comment">// 转换成 Resource 数组返回</span>    <span class="hljs-keyword">return</span> result.toArray(<span class="hljs-keyword">new</span> Resource[<span class="hljs-number">0</span>]);&#125;</code></pre><ol><li>确定目录，获取该目录下得所有资源。</li><li>在所获得的所有资源后，进行迭代匹配获取我们想要的资源。</li></ol><p>在这个方法里面，我们要关注两个方法，一个是 <code>#determineRootDir(String location)</code> 方法，一个是 <code>doFindPathMatchingXXXResources(...)</code> 等方法。</p><p><code>determineRootDir(String location)</code> 方法，主要是用于确定根路径。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> String <span class="hljs-title">determineRootDir</span><span class="hljs-params">(String location)</span> </span>&#123;    <span class="hljs-comment">// 找到冒号的后一位</span>    <span class="hljs-keyword">int</span> prefixEnd = location.indexOf(<span class="hljs-string">&#x27;:&#x27;</span>) + <span class="hljs-number">1</span>;    <span class="hljs-comment">// 根目录结束位置</span>    <span class="hljs-keyword">int</span> rootDirEnd = location.length();    <span class="hljs-comment">// 在从冒号开始到最后的字符串中，循环判断是否包含通配符，如果包含，则截断最后一个由”/”分割的部分。</span>    <span class="hljs-comment">// 例如：在我们路径中，就是最后的ap?-context.xml这一段。再循环判断剩下的部分，直到剩下的路径中都不包含通配符。</span>    <span class="hljs-keyword">while</span> (rootDirEnd &gt; prefixEnd &amp;&amp; getPathMatcher().isPattern(location.substring(prefixEnd, rootDirEnd))) &#123;        rootDirEnd = location.lastIndexOf(<span class="hljs-string">&#x27;/&#x27;</span>, rootDirEnd - <span class="hljs-number">2</span>) + <span class="hljs-number">1</span>;    &#125;    <span class="hljs-comment">// 如果查找完成后，rootDirEnd = 0 了，则将之前赋值的 prefixEnd 的值赋给 rootDirEnd ，也就是冒号的后一位</span>    <span class="hljs-keyword">if</span> (rootDirEnd == <span class="hljs-number">0</span>) &#123;        rootDirEnd = prefixEnd;    &#125;    <span class="hljs-comment">// 截取根目录</span>    <span class="hljs-keyword">return</span> location.substring(<span class="hljs-number">0</span>, rootDirEnd);&#125;</code></pre><p>例：</p><table><thead><tr><th>原路径</th><th>确定根路径</th></tr></thead><tbody><tr><td>classpath*:test/cc*/spring-*.xml</td><td>classpath*:test/</td></tr><tr><td>classpath*:test/aa/spring-*.xml</td><td>classpath*:test/aa/</td></tr></tbody></table><h5 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h5><p>Spring资源加载过程如下：</p><ul><li>Spring 提供了 Resource 和 ResourceLoader 来统一抽象整个资源及其定位。使得资源与资源的定位有了一个更加清晰的界限，并且提供了合适的 Default 类，使得自定义实现更加方便和清晰。</li><li>AbstractResource 为 Resource 的默认抽象实现，它对 Resource 接口做了一个统一的实现，子类继承该类后只需要覆盖相应的方法即可，同时对于自定义的 Resource 我们也是继承该类。</li><li>DefaultResourceLoader 同样也是 ResourceLoader 的默认实现，在自定 ResourceLoader 的时候我们除了可以继承该类外还可以实现 ProtocolResolver 接口来实现自定资源加载协议。</li><li>DefaultResourceLoader 每次只能返回单一的资源，所以 Spring 针对这个提供了另外一个接口 ResourcePatternResolver ，该接口提供了根据指定的 locationPattern 返回多个资源的策略。其子类 PathMatchingResourcePatternResolver 是一个集大成者的 ResourceLoader ，因为它即实现了 <code>Resource getResource(String location)</code> 方法，也实现了 <code>Resource[] getResources(String locationPattern)</code> 方法。 </li></ul>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>IOC概要</title>
    <link href="/2021/02/24/IOC%E7%90%86%E8%AE%BA/"/>
    <url>/2021/02/24/IOC%E7%90%86%E8%AE%BA/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">IOC概要</font></div><blockquote><table><tr><td bgcolor="#6495ED"><font color="#FFF">IOC ，就是由 Spring IOC 容器来负责对象的生命周期和对象之间的关系.</font></td></tr></table></blockquote><p>控制反转，是一种设计思想，依赖注入<code>DI</code>是一种具体实现。IOC意味着将设计好的对象交给容器控制，由容器帮我们查找及注入依赖对象（所以对象和对象之间是松散耦合的），对象只是被动的接受依赖对象，即为反转。</p><p>在传统的开发模式下，我们都是采用直接 new 一个对象的方式来创建对象，也就是说依赖的对象直接由我们自己控制，但是有了 IoC 容器后，则直接由 IoC 容器来控制。</p><p>没有 IoC 的时候我们都是在自己对象中主动去创建被依赖的对象，这是正转。但是有了 IoC 后，所依赖的对象直接由 IoC 容器创建后注入到被注入的对象中，依赖的对象由原来的主动获取变成被动接受，所以是反转。</p><h4 id="IOC注入方式"><a href="#IOC注入方式" class="headerlink" title="IOC注入方式"></a>IOC注入方式</h4><ol><li>构造器注入</li><li>setter注入</li><li>接口注入</li></ol><h4 id="重要组件"><a href="#重要组件" class="headerlink" title="重要组件"></a>重要组件</h4><p>主要有五个体系：<code>Resource</code>、<code>BeanFactory</code>、<code>BeanDefinition</code>、<code>BeanDefinitionReader</code>、<code>ApplicationContext</code>。</p><h5 id="Resource"><a href="#Resource" class="headerlink" title="Resource"></a>Resource</h5><p>对资源的抽象，它的每一个实现类都代表了一种资源的访问策略。</p><p><img src="/images/spring_IOC/Resource.png" alt="Resource"></p><p>有了资源，就应该有资源加载，<code>ResourceLoader</code>就是进行统一资源加载。</p><p><img src="/images/spring_IOC/ResourceLoader.png" alt="ResourceLoader"></p><h5 id="BeanFactory"><a href="#BeanFactory" class="headerlink" title="BeanFactory"></a>BeanFactory</h5><p>是bean容器，<code>BeanDefinition</code> 是它的基本结构，它内部维护着一个 <code>BeanDefinition map </code>，并可根据 <code>BeanDefinition</code> 的描述进行 bean 的创建和管理。</p><p><img src="/images/spring_IOC/BeanFactory.png" alt="BeanFactory"></p><p><code>BeanFacoty</code> 有三个直接子类 <code>ListableBeanFactory</code>、<code>HierarchicalBeanFactory</code> 和 <code>AutowireCapableBeanFactory</code>，<code>DefaultListableBeanFactory</code> 为最终默认实现，它实现了所有接口。</p><h5 id="BeanDefinition"><a href="#BeanDefinition" class="headerlink" title="BeanDefinition"></a>BeanDefinition</h5><p>用来描述Spring中Bean对象。</p><p><img src="/images/spring_IOC/BeanDefinition.png" alt="BeanDefinition"></p><h5 id="BeanDefinitionReader"><a href="#BeanDefinitionReader" class="headerlink" title="BeanDefinitionReader"></a>BeanDefinitionReader</h5><p>读取Spring配置文件内容，并将其转换成IOC容器内部的<code>BeanDefinition</code>。</p><p><img src="/images/spring_IOC/BeanDefinitionReader.png" alt="BeanDefinitionReader"></p><h5 id="ApplicationContext"><a href="#ApplicationContext" class="headerlink" title="ApplicationContext"></a>ApplicationContext</h5><p>Spring容器，也叫**<em>应用上下文**</em>，继承自BeanFactory，二者主要区别：</p><ol><li>继承 <code>MessageSource</code>，提供国际化的标准访问策略。</li><li>继承 <code>ApplicationEventPublisher </code>，提供强大的事件机制。</li><li>扩展 <code>ResourceLoader</code>，可以用来加载多个 Resource，可以灵活访问不同的资源。</li><li>对 Web 应用的支持。</li></ol>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>あすか栏目</title>
    <link href="/2021/01/27/%E3%81%82%E3%81%99%E3%81%8B%E6%A0%8F%E7%9B%AE/"/>
    <url>/2021/01/27/%E3%81%82%E3%81%99%E3%81%8B%E6%A0%8F%E7%9B%AE/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">飛鳥栏目</font></div><h4 id="句子"><a href="#句子" class="headerlink" title="句子"></a>句子</h4><blockquote><p>わしのお釣りは？</p><p>我的找零呢？（映像研1，鸟）</p></blockquote><p>汉字假名：</p><ul><li>お釣り：おつり</li></ul><p>知识点：</p><ul><li><strong>「お釣り」</strong>是<strong>「找的零钱」</strong>的意思。</li><li><strong>「お金」</strong>是<strong>「钱」</strong>的意思。–おかね</li></ul><hr><blockquote><p>分かりません。不知道</p><p>分かるか。没理解</p></blockquote><p>汉字假名：</p><ul><li>分：わ</li></ul><hr><blockquote><p>ホントですか？</p><p>真的吗？</p></blockquote><p>知识点：</p><ul><li>「本当に」和「本当」要区分。</li></ul><hr><blockquote><p>どうしよう。</p><p>怎么办。</p></blockquote><hr><blockquote><p>お誕生日おめでとう。</p><p>生日快乐。</p></blockquote><p>汉字假名：</p><ul><li>誕生日：たんじょうび</li></ul><p>知识点：</p><ul><li>加「お」是比较郑重的说法。</li><li>「おめでとう」是「恭喜」的意思。</li></ul><hr><blockquote><p>楽「たの」しい：时间上是持续的，快乐不是短暂的。</p><p>嬉「うれ」しい：时间上是瞬间的，某刻的快乐。</p></blockquote><hr><blockquote><p>もういいよ。</p><p>真是够了。（小鸟情人节被三杀）</p></blockquote><blockquote><p>何「なん」だオマエ。</p><p>你怎么回事啊？（莲加不会接设乐的梗）</p></blockquote><blockquote><p>どうしよう。</p><p>怎么办？</p></blockquote><blockquote><p>かにうまい？</p><p>螃蟹好吃？</p><p>かにゅうまえに。</p><p>加入之前。</p><p>工事中这两个容易搞混</p></blockquote><h4 id="单词"><a href="#单词" class="headerlink" title="单词"></a>单词</h4><table><thead><tr><th>单词</th><th>词义</th></tr></thead><tbody><tr><td>自信満々「じしんまんまん」</td><td>自信满满，充满信心。</td></tr><tr><td>自信「じしん」</td><td></td></tr><tr><td>せーの</td><td>一般用在口语，大意1、2…开始。但和1、2…没一点关系（可以想象中文里比如抬一个很重的东西，大家：1、2…起）。</td></tr><tr><td>五時間「ごじかん」</td><td>五小时。</td></tr><tr><td>五時「ごじ」</td><td>五点。</td></tr><tr><td>財布「さいふ」</td><td>钱包。</td></tr><tr><td>おかゆ</td><td>粥。</td></tr><tr><td>プレゼント</td><td>礼物（present）。</td></tr><tr><td>漢字「かんじ」</td><td></td></tr><tr><td>大丈夫「だいじょうぶ」</td><td>不用担心；不需要；没什么大事。</td></tr><tr><td>怪我「けが」</td><td>身体上受伤。</td></tr><tr><td>勉強「べんきょう」</td><td>学习。</td></tr><tr><td>手紙「てがみ」</td><td>信。</td></tr><tr><td>経理「けいり」</td><td>做会计工作的人。</td></tr><tr><td>たこ焼き「たこやき」</td><td>章鱼烧。</td></tr><tr><td>お菓子「おかし」</td><td>零食。</td></tr><tr><td>好き「すき」</td><td>喜欢。</td></tr><tr><td>大好き「だいすき」</td><td>超喜欢；最喜欢。</td></tr><tr><td>嫌い「きらい」・キライ</td><td>讨厌，不喜欢。</td></tr><tr><td>怖い「こわい」</td><td>害怕；可怕的。</td></tr><tr><td>チョコレート</td><td>巧克力（chocolate）。</td></tr><tr><td>プレゼント</td><td>礼物（present）。</td></tr><tr><td>お嬢「じょう」様「さま」・お嬢</td><td>大小姐。</td></tr><tr><td>遅「おそ」いよ</td><td>太慢了。</td></tr><tr><td>もっと早「はや」く</td><td>快一点。</td></tr><tr><td>嫁「よめ」</td><td>老婆。</td></tr><tr><td>暑「あつ」い</td><td>好烫。</td></tr><tr><td>たぶん</td><td>大概。</td></tr></tbody></table><h4 id="同类型"><a href="#同类型" class="headerlink" title="同类型"></a>同类型</h4><h5 id="三餐"><a href="#三餐" class="headerlink" title="三餐"></a>三餐</h5><table><thead><tr><th></th><th>早饭</th><th>午饭</th><th>晚饭</th></tr></thead><tbody><tr><td>ご飯（ごはん）</td><td>朝ご飯（あさごはん）</td><td>昼ご飯（ひるごはん）</td><td>晩ご飯（ばんごはん）</td></tr><tr><td>食（しょく）</td><td>朝食（ちょうしょく）</td><td>昼食（ちゅうしょく）</td><td>夕食（ゆうしょく）</td></tr><tr><td>飯（めし）</td><td>朝飯（あさめし）</td><td>昼飯（ひるめし）</td><td>晩飯（ばんめし）</td></tr></tbody></table><h5 id="星期（曜日）"><a href="#星期（曜日）" class="headerlink" title="星期（曜日）"></a>星期（曜日）</h5><p>〜曜日（ようび）</p><table><thead><tr><th align="center">星期日（日）</th><th align="center">星期一（月）</th><th align="center">星期二（火）</th><th align="center">星期三（水）</th><th align="center">星期四（木）</th><th align="center">星期五（金）</th><th align="center">星期六（土）</th></tr></thead><tbody><tr><td align="center">にちようび「日曜日」</td><td align="center">げつようび「月曜日」</td><td align="center">かようび「火曜日」</td><td align="center">すいようび「水曜日」</td><td align="center">もくようび「木曜日」</td><td align="center">きんようび「金曜日」</td><td align="center">どようび「土曜日」</td></tr></tbody></table><h5 id="方位（ほうこう）"><a href="#方位（ほうこう）" class="headerlink" title="方位（ほうこう）"></a>方位（ほうこう）</h5><table><thead><tr><th align="center"><strong>上</strong></th><th align="center"><strong>下</strong></th><th align="center"><strong>里</strong></th><th align="center"><strong>外</strong></th><th align="center"><strong>前</strong></th><th align="center"><strong>后</strong></th></tr></thead><tbody><tr><td align="center">うえ「上」</td><td align="center">した「下」</td><td align="center">なか「中」</td><td align="center">そと「外」</td><td align="center">まえ「前」</td><td align="center">うしろ「後ろ」</td></tr><tr><td align="center"><strong>左</strong></td><td align="center"><strong>右</strong></td><td align="center"><strong>旁边</strong></td><td align="center"><strong>旁边</strong></td><td align="center"></td><td align="center"></td></tr><tr><td align="center">ひだり「左」</td><td align="center">みぎ「右」</td><td align="center">ちかく「近く」</td><td align="center">となり「隣」</td><td align="center"></td><td align="center"></td></tr></tbody></table><h5 id="四季（しき）"><a href="#四季（しき）" class="headerlink" title="四季（しき）"></a>四季（しき）</h5><p>日语对应的汉字和假名</p><table><thead><tr><th>春</th><th>夏</th><th>秋</th><th>冬</th></tr></thead><tbody><tr><td>はる</td><td>なつ</td><td>あき</td><td>ふゆ</td></tr></tbody></table>]]></content>
    
    
    <categories>
      
      <category>日语</category>
      
      <category>课外</category>
      
      <category>综艺影视</category>
      
    </categories>
    
    
    <tags>
      
      <tag>日语</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>志村せんせい</title>
    <link href="/2021/01/27/%E5%BF%97%E6%9D%91%E3%81%9B%E3%82%93%E3%81%9B%E3%81%84/"/>
    <url>/2021/01/27/%E5%BF%97%E6%9D%91%E3%81%9B%E3%82%93%E3%81%9B%E3%81%84/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">日常语句积累</font></div><h4 id="mini形容词"><a href="#mini形容词" class="headerlink" title="mini形容词"></a>mini形容词</h4><hr><h5 id="忙しい：忙碌的"><a href="#忙しい：忙碌的" class="headerlink" title="忙しい：忙碌的"></a>忙しい：忙碌的</h5><blockquote><p>忙しくて、目が回りそう…</p><p>忙到晕头转向。</p></blockquote><p>汉字假名：</p><ul><li>忙：いそが        目：め</li></ul><p>知识点：</p><ul><li>接续句子的时候使用<strong>「くて」</strong>来连接。</li><li><strong>「回りそう」</strong>：快要…、差一点…。</li></ul><hr><h5 id="危ない：危险"><a href="#危ない：危险" class="headerlink" title="危ない：危险"></a>危ない：危险</h5><blockquote><p>危ないからコンセントに触らないで！</p><p>不要碰插座，因为很危险。</p></blockquote><p>汉字假名：</p><ul><li>危：あぶ　　触：さわ</li></ul><p>知识点：</p><ul><li><strong>「から」</strong>：因为。</li><li><strong>「コンセント」</strong>：插座。</li><li><strong>「触る」</strong>：碰。</li><li><strong>「ない形（否定形）」</strong>就是<strong>「触らないで」</strong>。</li><li><strong>「ない形（否定形）」</strong>后面加个<strong>「で」</strong>就是<strong>「请不要…」</strong>的意思。</li><li>要想更礼貌，末尾增加<strong>「ください」</strong>。</li></ul><hr><h5 id="美味しい：美味的"><a href="#美味しい：美味的" class="headerlink" title="美味しい：美味的"></a>美味しい：美味的</h5><blockquote><p>やっぱりチーズケーキが一番美味しい。</p><p>果然还是起司蛋糕最好吃。</p></blockquote><p>汉字假名：</p><ul><li>一番：いちばん　　美味：おい</li></ul><p>知识点：</p><ul><li><strong>「やっぱり」</strong>：果然还是…</li><li><strong>「一番」</strong>是一个副词，放在形容词前面，是<strong>「最」</strong>的意思。</li></ul><hr><h5 id="上手：擅长的"><a href="#上手：擅长的" class="headerlink" title="上手：擅长的"></a>上手：擅长的</h5><blockquote><p>歌がすごく上手ですね。</p><p>真的很会唱歌耶。</p></blockquote><p>汉字假名：</p><ul><li>歌：うた　　上手：じょうず</li></ul><p>知识点：</p><ul><li><p><strong>「歌が」</strong>：<strong>「唱歌，歌」</strong>的意思。</p></li><li><p><strong>「すごく」</strong>是一个副词，可以用来修饰形容词，是<strong>「非常，很…」</strong>的意思，类似的还有：<strong>「とても」</strong>，<strong>「すごい」</strong>。</p></li></ul><hr><h5 id="白い：白色的"><a href="#白い：白色的" class="headerlink" title="白い：白色的"></a>白い：白色的</h5><blockquote><p>黒い牛からも白い牛乳が出るのかな？</p><p>黑色的牛也可以挤出白色的牛奶吗？</p></blockquote><p>汉字假名：</p><ul><li>黒：くろ　　牛：うし　　白：しろ　　牛乳：きゅうにゅう　　出：で</li></ul><p>知识点：</p><ul><li><strong>「から」</strong>是<strong>「从…」</strong>的意思。</li><li><strong>「も」</strong>是一个助词，<strong>「也」</strong>的意思。</li><li><strong>「かな」</strong>是<strong>「想象或猜测」</strong>的语气，<strong>「な」</strong>就是自言自语的语气。</li></ul><hr><h5 id="広い：宽广的"><a href="#広い：宽广的" class="headerlink" title="広い：宽广的"></a>広い：宽广的</h5><blockquote><p>新しい部屋は広いんだ。</p><p>新房间好宽敞。</p></blockquote><p>汉字假名：</p><ul><li>新：あたら　　広：ひろ</li></ul><p>知识点：</p><ul><li><strong>「い」</strong>形容词后可以直接跟名词。</li><li>句末接<strong>「です」</strong>也可以，<strong>「んだ」</strong>是一种口语的说法，对别人说明或者解释的语气。</li></ul><hr><h5 id="不味い：难吃的"><a href="#不味い：难吃的" class="headerlink" title="不味い：难吃的"></a>不味い：难吃的</h5><blockquote><p>うわ、これ不味い。口语化</p><p>うわ、これは不味いです。比较正式</p><p>哇，这个好难吃啊。</p></blockquote><p>汉字假名：</p><ul><li>不味：まず</li></ul><p>知识点：</p><ul><li>很惊讶，吓到的时候，可以使用<strong>「うわ」</strong>。</li><li>感觉「不味い」太直接，可以委婉说法<strong>「口に合わない」</strong>。</li><li>「口に合わない」假名<strong>「くちにあわない」</strong>，不合口味，不喜欢这个味道。</li><li>稍微委婉可以说「美味しくない」假名<strong>「おいしくない」</strong>，不好吃。</li></ul><hr><h5 id="安い：便宜的"><a href="#安い：便宜的" class="headerlink" title="安い：便宜的"></a>安い：便宜的</h5><blockquote><p>このバッグ、安いけど可愛いね。</p><p>这个包包虽然有点便宜，但很可爱。</p></blockquote><p>汉字假名：</p><ul><li>安：やす　可愛：かわい</li></ul><p>知识点：</p><ul><li><strong>「けど」</strong>在两个形容词之间，是个逆接接续词，是<strong>「但」</strong>的意思。和<strong>「ですが」</strong>一样，不过前者偏向口语化，多用于平辈之间。</li></ul><h4 id="其他用语"><a href="#其他用语" class="headerlink" title="其他用语"></a>其他用语</h4><hr><h5 id="表达感谢"><a href="#表达感谢" class="headerlink" title="表达感谢"></a>表达感谢</h5><blockquote><p>ありがとうございます。正在进行时…例：别人正在帮你提行李。</p><p>ありがとうございました。过去式…例：别人帮你把行李放到了该放的地方。</p><p>本当にありがとうございます。更多的感谢—-本当に：真</p><p>「ありがとうね」、「どうもね」轻微的感谢</p></blockquote><hr><h5 id="总之…"><a href="#总之…" class="headerlink" title="总之…"></a>总之…</h5><blockquote><p>兎に角可愛い。</p><p>总之，非常可爱。</p></blockquote><p>汉字假名：</p><ul><li>兎に角：とにかく　　可愛：かわい</li></ul><p>知识点：</p><ul><li><strong>「兎に角」</strong>是<strong>「总之，无论如何」</strong>的意思。</li><li><strong>「兎に角」</strong>可以类似汉语随意组合，例：寂「さび」しい、美味しい、煩「うるさ」い</li></ul>]]></content>
    
    
    <categories>
      
      <category>日语</category>
      
      <category>课外</category>
      
      <category>日本老师</category>
      
    </categories>
    
    
    <tags>
      
      <tag>日语</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>词形变化</title>
    <link href="/2021/01/26/%E8%AF%8D%E5%BD%A2/"/>
    <url>/2021/01/26/%E8%AF%8D%E5%BD%A2/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">词形变化</font></div><h4 id="动词"><a href="#动词" class="headerlink" title="动词"></a>动词</h4><p>动词的<strong>基本型</strong>词尾以<strong>「ウ」</strong>段结尾，又称原形、字典形。</p><h5 id="动词分类"><a href="#动词分类" class="headerlink" title="动词分类"></a>动词分类</h5><table><thead><tr><th>一类动词</th><th>二类动词</th><th>三类动词</th></tr></thead><tbody><tr><td>动1</td><td>动2</td><td>动3</td></tr><tr><td>五段动词</td><td>一段动词</td><td>サ变动词、か变动词</td></tr></tbody></table><h6 id="三类动词"><a href="#三类动词" class="headerlink" title="三类动词"></a>三类动词</h6><p>サ变动词：只有一个<strong>「来る」</strong>，假名くる。</p><p>か变动词：<strong>「する」</strong>和<strong>以「する」结尾的具有动作性的名词</strong>。</p><h6 id="二类动词"><a href="#二类动词" class="headerlink" title="二类动词"></a>二类动词</h6><p>以「る」结尾，「る」前的假名在<strong>「い」段</strong>和<strong>「え」段</strong>。</p><p>上一段动词：「る」前的假名在<strong>「い」段</strong>。例：起「お」きる、見「み」る</p><p>下一段动词：「る」前的假名在<strong>「え」段</strong>。例：食「た」べる、寝「ね」る</p><h6 id="一类动词"><a href="#一类动词" class="headerlink" title="一类动词"></a>一类动词</h6><p>以<strong>「ウ」</strong>结尾：う・く・ぐ・す・つ・ぬ・ぶ・む・る。</p><p>以「る」结尾的有二类动词和一类动词，区分方法：</p><ol><li>词尾不为「る」，一定是一类动词。</li><li>词尾为「る」时，（除三类动词外）看「る」前假名在哪段：<ul><li>不在「い・え」段为一类动词。</li><li>在「い・え」段，大部分为二类动词，二三十个特例是一类动词。</li></ul></li></ol><p>以「る」结尾的一类动词：</p><table><thead><tr><th>切「き」る</th><th>入「はい」る</th><th>いる</th><th>知「し」る</th><th>滑「すべ」る</th></tr></thead><tbody><tr><td>帰「かえ」る</td><td></td><td></td><td></td><td></td></tr></tbody></table><h5 id="动词变形"><a href="#动词变形" class="headerlink" title="动词变形"></a>动词变形</h5><p>基本型变<strong>「ます」</strong>形。</p><h6 id="三类动词-1"><a href="#三类动词-1" class="headerlink" title="三类动词"></a>三类动词</h6><p>サ变动词：<strong>「来(く)る」</strong>——&gt;<strong>「来(き)ます」</strong>。</p><p>か变动词：<strong>「する」</strong>——&gt;<strong>「します」</strong>。</p><h6 id="二类动词-1"><a href="#二类动词-1" class="headerlink" title="二类动词"></a>二类动词</h6><p>去掉<strong>「る」</strong>加<strong>「ます」</strong>，例：起「お」きる——&gt;起きます。</p><h6 id="一类动词-1"><a href="#一类动词-1" class="headerlink" title="一类动词"></a>一类动词</h6><p>词尾<strong>「ウ」</strong>段变为<strong>同行的「い」段加「ます」</strong>，例：終「お」わる——&gt;終わります。</p><h6 id="「ます」作用"><a href="#「ます」作用" class="headerlink" title="「ます」作用"></a>「ます」作用</h6><ol><li>表尊敬的助动词。</li><li>与<strong>「です」</strong>构成句子的礼貌体。</li><li>在句中做谓语，只能用在结句。</li></ol><h5 id="动词时态"><a href="#动词时态" class="headerlink" title="动词时态"></a>动词时态</h5><p>动词时态分为非过去式（现在・将来）和过去式。</p><p>动词<strong>「ます」</strong>形去掉<strong>「ます」</strong>剩下的部分就是<strong>动词连用形</strong>。</p><table><thead><tr><th></th><th>肯定</th><th>否定</th></tr></thead><tbody><tr><td>非过去式</td><td>动词<strong>「ます」</strong>形</td><td>动词连用形 +<strong>「ません」</strong></td></tr><tr><td>过去式</td><td>动词连用形 +<strong>「ました」</strong></td><td>动词连用形 +<strong>「ませんでした」</strong></td></tr></tbody></table>]]></content>
    
    
    <categories>
      
      <category>日语</category>
      
      <category>新标日</category>
      
      <category>上册</category>
      
      <category>单词</category>
      
    </categories>
    
    
    <tags>
      
      <tag>日语</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>理财投资扫盲</title>
    <link href="/2021/01/16/%E7%90%86%E8%B4%A2%E6%8A%95%E8%B5%84%E6%89%AB%E7%9B%B2/"/>
    <url>/2021/01/16/%E7%90%86%E8%B4%A2%E6%8A%95%E8%B5%84%E6%89%AB%E7%9B%B2/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">理财投资扫盲</font></div><h4 id="基金类型"><a href="#基金类型" class="headerlink" title="基金类型"></a>基金类型</h4><h5 id="债券型基金"><a href="#债券型基金" class="headerlink" title="债券型基金"></a>债券型基金</h5><p>债券型基金是指<strong>80%以上</strong>的资产投资于债券的基金。</p><p>债券主要分为政府债券，金融债券，企业债以及可转债，风险依次增大。</p><p>债券型基金，波动率小，稳健，适合防守，稳固后方。</p><p>混合型基金，风格可变，可攻可受，适合当中场，掌握中军。</p><p>股票型、指数型、QDII型，风格激进，适合当前锋，冲锋陷阵。</p><p>1） 货币型基金：主要投资银行货币间市场，风险极低。</p><p>2） 债权型基金：主要投资债权，里面有个分支叫偏债基金可以投资不超过20%的股票。风险偏低</p><p>3） 混合型基金：混合型基金同时投资于股票、债券和货币市场等工具，但投资比例灵活，几乎没有限制。风险和预期收益相对适中。</p><p>4） 股票型基金：80%以上的资产要投资于股票，风险较大</p><p>5） 指数型基金：全部投资股票，也可以理解为一种特殊的股票型基金。</p><p>工具：<strong>天天基金网</strong>，可查看基金的<strong>资产配置</strong>（股票、债券、现金），<strong>投资风格</strong>，都在最下边，基金经理介绍上边。</p><h5 id="股票分类"><a href="#股票分类" class="headerlink" title="股票分类"></a>股票分类</h5><p>１.<strong>根据公司市值的大小，可以把股票分成大盘股/小盘股。</strong></p><p>大盘股风险低，收益也较低，小盘股风险高，收益也较高。</p><p>例：总市值小于200亿定义为小盘股，200-500亿为中盘股，500亿以上是大盘股。</p><p>大盘股的典型代表就是熟知的贵州茅台，总市值2万多亿。</p><p>小盘股大多都在深交所上市，比较典型的有曙光股份，北纬科技等。</p><p>２.<strong>根据成长性，可以把股票分为价值股和成长股</strong>。</p><p>价值股一般属于传统行业，市值较高，赢收<strong>稳定</strong>，比如说可口可乐，五粮液；</p><p>成长股一般是新兴行业，比如人工智能，互联网，同时具备较大的发展<strong>潜力</strong>。</p><h5 id="业绩表现"><a href="#业绩表现" class="headerlink" title="业绩表现"></a>业绩表现</h5><p>主要投资大盘股的基金会跟沪深300指数做对比，主要投资债券的基金会跟中债综合指数做对比。</p><p>基金一段时期的最大跌幅叫做最大回撤，这个数字越低说明该选手的防守能力越强。</p><h4 id="如何选择基金"><a href="#如何选择基金" class="headerlink" title="如何选择基金"></a>如何选择基金</h4><p><strong>１．选择基金类型</strong></p><p><strong>２．查询过往业绩</strong></p><p>​    a、 在同类基金里，选出最近6个月业绩前30名的基金；</p><p>​    b、 在同类基金里，选出最近1年业绩前30名的基金。</p><p>​    c、 在同类基金里，选出最近3年业绩前30名的基金。</p><p>​    c、 在同类基金里，选出最近5年业绩前30名的基金。</p><p>​    e、 在同类基金里，选出成立以来业绩前30名的基金。</p><p><strong>３．选择基金公司</strong></p><p>​    前十名基金公司：1）天弘、2）易方达、3）博时、4）南方、5）华夏、6）汇添富、7）广发、8）工银瑞信、    9）嘉实、10）鹏华。</p><p>​    也有很多小而美的基金公司，例：富国擅长量化投资，交银施罗德擅长投科技领域、成长企业，兴全擅长价值    投资等。</p><p><strong>４．选择基金经理</strong></p><p>​    现任基金经理至少要管理这支基金三年以上，</p><p><strong>５．选择投资风格</strong></p><p>​    大盘股和小盘股、价值股和成长股。</p><p><strong>６．选择三大细节</strong></p><p>​    a、成立时间，最好是选成立5年以上。要观察基金穿越一个牛熊周期的表现。</p><p>​    b、管理规模，30以内太小，100亿以上太大。</p><p>​    c、回撤率，回撤率的意思是从高点买入，在到达最低点时，下跌了多少。最大回撤不要超过30%。在基金净值    走势图里，可以查到回撤率。</p><h5 id="如何选择保守型基金"><a href="#如何选择保守型基金" class="headerlink" title="如何选择保守型基金"></a>如何选择保守型基金</h5><p>1.<strong>选择收益率高的货币基金</strong></p><p>货币型基金<strong>近乎于无风险投资</strong>，因此<strong>收益率的高低</strong>是衡量货币基金的核心。</p><p>２.<strong>选择基金规模中等的基金</strong></p><p>在经济学中有一个叫做“<strong>规模经济</strong>”的概念。意思是说生产的产品越多，平摊到每一个产品的费用就越低。但随着<strong>基金份额过大</strong>，就有可能出现<strong>“规模不经济</strong>”效应。当规模过大时，无形中拉高了成本。</p><p>选择的货币基金份额最好保持在<strong>200亿-1000亿</strong>之间，这样基金经理既有筹码去获得更高的利率，也不至于投资风格太过保守。</p><h5 id="如何选择债券型基金"><a href="#如何选择债券型基金" class="headerlink" title="如何选择债券型基金"></a>如何选择债券型基金</h5><p>1.<strong>债券持仓</strong>：政府债券，金融债券，企业债券，风险和收益依次增大。</p><p>2.<strong>杠杆比例</strong>：有个概念，加杠杆，杠杆比例越高，风险和收益也越高。</p><p>3.<strong>收益率</strong></p><p>4.<strong>回撤率</strong>：回撤率过高的债基已经失去了防守型资产的特性，不建议选择，不要挑选最大回撤<strong>超过10%以上</strong>的债基。</p><h4 id="保守型基金投资时机"><a href="#保守型基金投资时机" class="headerlink" title="保守型基金投资时机"></a>保守型基金投资时机</h4><p>1.<strong>宏观经济数据</strong>：无论是货币型基金还是债券型基金，主要收益都和市场利率相关，当利率上升时，货币型和债券型基金的收益会下降。</p><p>2.<strong>股市表现</strong></p><h4 id="定投"><a href="#定投" class="headerlink" title="定投"></a>定投</h4><h5 id="定投原理"><a href="#定投原理" class="headerlink" title="定投原理"></a>定投原理</h5><p><strong>假如每期投入1000元</strong>，</p><p><strong>买入时</strong>，每期定投金额/该期定投价格 = 该期定投份额，</p><p>每期定投份额累加即为<strong>总份额</strong>，</p><p>1000 * 投入期数 / 总份额 = 平均成本，</p><p><strong>卖出时</strong>，(该期定投价格 - 平均成本) * 总份额 = 收益。</p><h5 id="定投思想"><a href="#定投思想" class="headerlink" title="定投思想"></a>定投思想</h5><p>被动型投资和主动型投资。</p><h6 id="指数基金"><a href="#指数基金" class="headerlink" title="指数基金"></a>指数基金</h6><p>被动投资，简单理解就是投资特定的指数基金，跟着市场走。指数基金就是以特定指数的成分股（沪深300，标普500）为投资标的，投资此类指数基金，就能<strong>复制该指数的表现</strong>。</p><p>Ａ股指数介绍</p><p><strong>1.上证50指数</strong>：从<strong>上海证券交易所</strong>的股票里挑选50支市值最大，流动性好的股票，组成一个组合。例：贵州茅台，伊利股份。**<em>注：道琼指数类似上证50。**</em></p><p><strong>2.沪深300指数</strong>：沪深300指数更加全面一些，相比于上证50，加入了深交所的股票，从<strong>深交所</strong>和<strong>上交所</strong>挑选出市值最大，流动性最好的300支股票。</p><p><strong>3.中证500指数</strong>：将沪深300的公司排除，在剩下的公司当中选择总市值前500的指数组成。例：光线传媒，森马服饰。</p><p><strong>4.创业板指数</strong>：由创业板中最优秀的100家企业组成。创业板是以中小型企业为主，因此主要是成长型企业，容易暴涨暴跌。**<em>注：纳斯达克类似于创业板和科创板。**</em></p><p><strong>5.科创50指数</strong>：由科创板中市值最大的50支股票组成。不稳定性较高。</p><h6 id="指数增强型基金"><a href="#指数增强型基金" class="headerlink" title="指数增强型基金"></a>指数增强型基金</h6><p>基金经理可以通过自己的判断（<strong>主观选股</strong>）或者计算机的计算结果（<strong>量化选股</strong>）来调整指数中各个成分股的比例。</p><p>例：中证500指数包含了500个中型公司，增强型的基金经理可以通过他的判断，多买一些优等公司，少买甚至不买劣等公司。他可能只买了300家公司。</p><h6 id="主动型基金"><a href="#主动型基金" class="headerlink" title="主动型基金"></a>主动型基金</h6><p>完全跟指数没关系，基金经理完全根据自己的判断择时选股，投资主动型基金就等于投资基金经理。</p><p><strong>资产配置，股票持仓，投资风格以及业绩表现</strong>是需要关注的重点。</p><h5 id="如何定投"><a href="#如何定投" class="headerlink" title="如何定投"></a>如何定投</h5><p>1.股市长期走势是向上</p><p>2.长期坚持，中国股市，一个牛熊市周期大概在5-7年。</p><p>3.最基本的要点是要选择<strong>波动率大</strong>的基金（摊薄成本，涨幅）。</p><p><strong>债权投资，货币投资</strong>（固收类投资）为主的基金<strong>波动率比较小</strong>，<strong>股票型，偏股混合型基金适用于定投；债券型，货币型基金不适合定投</strong>。</p><p>1）<strong>不要选择被动型指数基金</strong>！ </p><p>2）<strong>不要选择债券型基金</strong>，要选择波动率大的基金。</p><p>3）选择<strong>主动管理型基金来搭配指数增强型基金</strong>。</p><h4 id="基金组合"><a href="#基金组合" class="headerlink" title="基金组合"></a>基金组合</h4><h5 id="防守、中场、进攻如何选择"><a href="#防守、中场、进攻如何选择" class="headerlink" title="防守、中场、进攻如何选择"></a>防守、中场、进攻如何选择</h5><p><strong>１、防守型基金</strong></p><p>主要选债权型基金，偏债和纯债都是可以的。</p><p><strong>２、中场型基金</strong></p><p>主要是一些价值型投资的混合&amp;股票基金。</p><p>这些基金主要投资一些大盘股、价值股。比如：茅台、中国平安、格力电器这样的公司。另外大盘的一些指数基金，也适合做中场。例：沪深300，上证50指数。</p><p><strong>３、进攻型基金</strong></p><p>创业板指数、科创50指数、中证500指数，就适合做进攻，科技、5G、医疗、新能源这些产业的基金，都适合做进攻型基金。</p><h5 id="如何搭配"><a href="#如何搭配" class="headerlink" title="如何搭配"></a>如何搭配</h5><p>根据个人能力和市场情况：</p><p>防守：中场：进攻</p><p><strong>保守型投资者</strong>：5：3：2，5年下来，平均年化回报，可能在0%-10%之间</p><p><strong>稳健型投资者</strong>：3：4：3，5年下来，平均年化回报，可能在-5%-15%之间，可能会亏损。</p><p><strong>进取型投资者</strong>：2：4：4，5年下来，平均年化回报，可能在-10%-20%之间。</p><h4 id="打新基金"><a href="#打新基金" class="headerlink" title="打新基金"></a>打新基金</h4><p><strong>注意：</strong>具体还是没明白是什么…</p><p>对于全职打新基金还是要注意<strong>资金规模，建议不要超过10亿元，5亿元以下最好</strong>。</p><h4 id="宽基和窄基"><a href="#宽基和窄基" class="headerlink" title="宽基和窄基"></a>宽基和窄基</h4><p>宽基：</p><p>1）含10个或更多个股票； </p><p>2）单个成份股权重不超过30％；</p><p>3）权重最大的5个股票累计权重不超过指数的60％；</p><p>4）成份股平均日交易额超过5000万美元；</p><p>5）包含行业种类要多，要至少有两个一级行业以上，要至少有三个二级行业以上。</p><p>例：汇添富消费行业混合(000083)</p><p>窄基：</p><p>一般持有某种特定行业的基金。</p><p>例：工银前沿医疗，全部持在医疗行业。招商中证白酒指数，全部持在白酒这个二级行业。</p><p>长期效益来看，宽基完胜，短期效益可能会有几只窄基表现比较好，特别是一年以内。</p><h4 id="实战方面看一个基金"><a href="#实战方面看一个基金" class="headerlink" title="实战方面看一个基金"></a>实战方面看一个基金</h4><p>以下以**兴全合润混合(163406)**为例。</p><h5 id="基金公司"><a href="#基金公司" class="headerlink" title="基金公司"></a>基金公司</h5><p>资金规模</p><p><img src="/images/licai/rumen/%E8%B5%84%E9%87%91%E8%A7%84%E6%A8%A1.png" alt="资金规模"></p><p>资金规模变动</p><p><img src="/images/licai/rumen/%E8%B5%84%E9%87%91%E8%A7%84%E6%A8%A1%E5%8F%98%E5%8A%A8.png" alt="资金规模变动"></p><h5 id="大致看基金信息"><a href="#大致看基金信息" class="headerlink" title="大致看基金信息"></a>大致看基金信息</h5><p><img src="/images/licai/rumen/%E8%B5%84%E9%87%91%E5%A4%A7%E8%87%B4%E4%BF%A1%E6%81%AF.png" alt="资金大致信息"></p><ol><li><p>名称看不出基金投资的行业。</p></li><li><p>成立时间，成立各期间的收益率。</p></li><li><p>资金管理规模：超过100亿，规模大，比较信赖，但是灵活度不够高，之前选择基金有说明。</p></li><li><p>基金类型：该基金混合型，配置灵活，中高风险，收益也适中。具体都是股票、债券、现金等的什么组合，可查看资产配置。</p><p><img src="/images/licai/rumen/%E8%B5%84%E4%BA%A7%E9%85%8D%E7%BD%AE.png" alt="资产配置"></p><p><img src="/images/licai/rumen/%E8%B5%84%E4%BA%A7%E9%85%8D%E7%BD%AE%E6%98%8E%E7%BB%86.png" alt="资产配置明细"></p></li><li><p>看持仓，该基金投资行业比较分散，但都是龙头企业，宽基。</p><p><img src="/images/licai/rumen/%E6%8C%81%E4%BB%93%E6%AF%94.png" alt="持仓比"></p></li><li><p>近几个月，仅今年收益。</p></li></ol><h5 id="基金过往表现"><a href="#基金过往表现" class="headerlink" title="基金过往表现"></a>基金过往表现</h5><p>天天基金网产品信息查看涨幅更多信息。</p><p><img src="/images/licai/rumen/%E8%BF%87%E5%BE%80%E4%B8%9A%E7%BB%A9.png" alt="过往业绩"></p><p>还有重点，查看<strong>最大回撤率</strong>。超过30%有些偏高，之前也有介绍。具体还没有找到地方查看，比如30%回撤率，假如买在了最高点，这个基金最大会让你亏损30%。</p><h5 id="基金经理"><a href="#基金经理" class="headerlink" title="基金经理"></a>基金经理</h5><p>最近一任经理最好超过3年，还要查看任职回报。</p><p><img src="/images/licai/rumen/%E5%9F%BA%E9%87%91%E7%BB%8F%E7%90%86%E4%BF%A1%E6%81%AF.png" alt="基金经理信息"></p><p>还有该经理的投资风格。风格之前也有说明，该基金比较稳重。</p><p><img src="/images/licai/rumen/%E6%8A%95%E8%B5%84%E9%A3%8E%E6%A0%BC.png" alt="投资风格"></p>]]></content>
    
    
    <categories>
      
      <category>理财</category>
      
      <category>基础</category>
      
    </categories>
    
    
    <tags>
      
      <tag>理财</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>第一单元对话</title>
    <link href="/2021/01/16/%E6%96%B0%E6%A0%87%E6%97%A5%E7%AC%AC%E4%B8%80%E5%8D%95%E5%85%83%E5%AF%B9%E8%AF%9D/"/>
    <url>/2021/01/16/%E6%96%B0%E6%A0%87%E6%97%A5%E7%AC%AC%E4%B8%80%E5%8D%95%E5%85%83%E5%AF%B9%E8%AF%9D/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">第一单元对话</font></div><h4 id="这是什么呢"><a href="#这是什么呢" class="headerlink" title="这是什么呢"></a>这是什么呢</h4><p><strong>问</strong>：これは何ですか。</p><p><strong>答</strong>：それは貯金箱でしよ。</p><p><strong>知识点</strong>：</p><ul><li><p>貯<strong>「ちょ」</strong>金<strong>「きん」</strong>箱<strong>「ばこ」</strong>：储蓄罐</p></li><li><p>よ：类似与”哟“，那是储蓄罐哟…</p></li></ul><h4 id="询问价钱"><a href="#询问价钱" class="headerlink" title="询问价钱"></a>询问价钱</h4><p><strong>问</strong>：これはおいくらですか。</p><p><strong>答</strong>：こちらは3,000円・こちらは5,000円です。</p><p><strong>知识点</strong>：</p><ul><li>円<strong>「えん」</strong>，同”圆“的意思。</li><li>いくら多少钱，注意同いくつ多少岁区分。</li></ul><h4 id="询问东西在哪"><a href="#询问东西在哪" class="headerlink" title="询问东西在哪"></a>询问东西在哪</h4><p><strong>问</strong>：絵はがきはどこにありますか。</p><p><strong>答</strong>：あちらです。レジの横です。</p><p><strong>知识点</strong>：</p><ul><li><p>用到了<strong>「…は…にありますか」</strong><del>在</del>的句型。</p></li><li><p>絵はがき：明信片。</p></li><li><p>レジ：收银台。</p></li><li><p>横<strong>「よこ」</strong>：旁边。</p></li></ul><h4 id="选好要买的东西，付钱"><a href="#选好要买的东西，付钱" class="headerlink" title="选好要买的东西，付钱"></a>选好要买的东西，付钱</h4><p><strong>问</strong>：これをください。</p><p><strong>答</strong>：ありがとうございます。850円です。</p><p><strong>知识点</strong>：</p><ul><li>これをください。请给我这个，购买东西向售货员要某个东西时的句型。</li></ul><h4 id="别人介绍朋友给你，如何寒暄"><a href="#别人介绍朋友给你，如何寒暄" class="headerlink" title="别人介绍朋友给你，如何寒暄"></a>别人介绍朋友给你，如何寒暄</h4><p><strong>A</strong>：こちらはわたしの友人の王です。</p><p><strong>王</strong>：王です。どうぞよろしくお願いします。</p><p><strong>铃木</strong>：鈴木です。はじめまして。どうぞお願いします。</p><p><strong>知识点</strong>：</p><ul><li><p>…の…の…同位语</p></li><li><p>友人<strong>「よこ」</strong>：朋友。</p></li><li><p>王<strong>「おう」</strong>、願<strong>「ねが」</strong>、鈴木<strong>「すずき」</strong></p></li></ul><h4 id="询问对方的兴趣爱好"><a href="#询问对方的兴趣爱好" class="headerlink" title="询问对方的兴趣爱好"></a>询问对方的兴趣爱好</h4><p><strong>问</strong>：ご趣味は何ですか。</p><p><strong>答</strong>：趣味はテニスです。</p><p><strong>知识点</strong>：</p><ul><li>趣<strong>「しゅ」</strong>味<strong>「み」</strong>：爱好。</li><li>テニス：网球。</li><li>読<strong>「どく」</strong>書<strong>「しょ」</strong>：读书。</li><li>野<strong>「や」</strong>球<strong>「きゅう」</strong>：棒球</li></ul><h4 id="询问对方职业"><a href="#询问对方职业" class="headerlink" title="询问对方职业"></a>询问对方职业</h4><p><strong>问</strong>：失礼ですが・お仕事は何ですか。</p><p><strong>答</strong>：中国語の教師です。</p><p><strong>知识点</strong>：</p><ul><li>失<strong>「しつ」</strong>礼<strong>「れい」</strong></li><li>仕<strong>「し」</strong>事<strong>「ごと」</strong>：职业</li><li>中国語<strong>「ちゅうごくご」</strong>             教<strong>「きょう」</strong>師<strong>「し」</strong></li></ul><h4 id="被问及出生地"><a href="#被问及出生地" class="headerlink" title="被问及出生地"></a>被问及出生地</h4><p><strong>问</strong>：ご出身はどちらですか。</p><p><strong>答</strong>：大連です。</p><p><strong>知识点</strong>：</p><ul><li><p>出身<strong>「しゅっしん」</strong>：职业</p></li><li><p>大<strong>「だい」</strong>連<strong>「れん」</strong></p></li></ul>]]></content>
    
    
    <categories>
      
      <category>日语</category>
      
      <category>新标日</category>
      
      <category>上册</category>
      
      <category>对话</category>
      
    </categories>
    
    
    <tags>
      
      <tag>日语</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>第一单元语法</title>
    <link href="/2021/01/16/%E6%96%B0%E6%A0%87%E6%97%A5%E7%AC%AC%E4%B8%80%E5%8D%95%E5%85%83%E8%AF%AD%E6%B3%95/"/>
    <url>/2021/01/16/%E6%96%B0%E6%A0%87%E6%97%A5%E7%AC%AC%E4%B8%80%E5%8D%95%E5%85%83%E8%AF%AD%E6%B3%95/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">第一单元语法</font></div><h4 id="はです【肯定的判断句】"><a href="#はです【肯定的判断句】" class="headerlink" title="はです【肯定的判断句】"></a><del>は</del>です【肯定的判断句】</h4><ul><li>【是】おたしは李です。我是小李。</li><li>【在】李さんはトイレです。小李在厕所。</li></ul><p><strong>注</strong>：说自己的时候不需要加「さん」。</p><hr><h4 id="はではありません【否定的判断句】"><a href="#はではありません【否定的判断句】" class="headerlink" title="はではありません【否定的判断句】"></a><del>は</del>ではありません【否定的判断句】</h4><p>森さんは課長ではありません（或者じゃありません）。</p><p>課長「かちょう」</p><hr><h4 id="はですか【疑问句】"><a href="#はですか【疑问句】" class="headerlink" title="はですか【疑问句】"></a><del>は</del>ですか【疑问句】</h4><p>疑问句后边加个<strong>「か」</strong>，读升调。</p><ul><li><p>李さんは中国人ですか。</p><p>・・・はい、「李さんは」中国人です。括号里可省略。</p><p>・・・はい、そうです。</p></li><li><p>李さんは日本人ですか。</p><p>・・・いいえ、「李さんは」日本人ではありません。</p><p>・・・いいえ、ちがいます。「中国人です。」最后纠正可有可无。</p></li></ul><hr><h4 id="の【所属】"><a href="#の【所属】" class="headerlink" title="の【所属】"></a><del>の</del>【所属】</h4><p>基本上翻译过来都是“的”的意思。</p><p>これはわたしのかぎ「钥匙」です。</p><p>あれは家族「かぞく」の写真「しゃしん」です。</p><hr><h4 id="こ-そ-あ-ど"><a href="#こ-そ-あ-ど" class="headerlink" title="こ/そ/あ/ど"></a>こ/そ/あ/ど</h4><table><thead><tr><th align="center"></th><th align="center">こ</th><th align="center">そ</th><th align="center">あ</th><th align="center">ど</th></tr></thead><tbody><tr><td align="center">物</td><td align="center">これ</td><td align="center">それ</td><td align="center">あれ</td><td align="center">どれ</td></tr><tr><td align="center">连体词</td><td align="center">この</td><td align="center">その</td><td align="center">あの</td><td align="center">どの</td></tr><tr><td align="center">场所</td><td align="center">ここ</td><td align="center">そこ</td><td align="center">あそこ</td><td align="center">どこ</td></tr><tr><td align="center">场所（郑重）方向</td><td align="center">こちら</td><td align="center">そちら</td><td align="center">あちら</td><td align="center">どちら</td></tr></tbody></table><p>注：连体词不能单独使用，后需要接名词。基本上都是「こ」「そ」相对。</p><ul><li><p>【方向】こちらへどうぞ。</p></li><li><p>【地点】ここ・そこ・あそこは学校「がっこう」です。</p><p>・・・郑重说法：こちら・そちら・あちらは学校「がっこう」です。</p></li><li><p>【敬称】そちらは田中「たなか」さんです。</p></li></ul><hr><h4 id="も【也】"><a href="#も【也】" class="headerlink" title="も【也】"></a><del>も</del>【也】</h4><p>わたしは中国人です。あなたも中国人ですか。</p><p>わたしは＝＝私は</p><hr><h4 id="はですか、ですか。【选择疑问句】"><a href="#はですか、ですか。【选择疑问句】" class="headerlink" title="はですか、ですか。【选择疑问句】"></a><del>は</del>ですか、ですか。【选择疑问句】</h4><p>今日「きょう」は水曜日ですか、木曜日ですか。</p><p>・・・「今日は」木曜日です。</p><hr><h4 id="にがあります-います【存在句】"><a href="#にがあります-います【存在句】" class="headerlink" title="にがあります/います【存在句】"></a><del>に</del>があります/います【存在句】</h4><p>表示存在，在<del>有</del>。</p><p><strong>「に」</strong>格助词，表示存在的场所，<strong>「が」</strong>主格助词，表示存在的事物、人、动物，做主语。</p><ul><li>【无生命】部屋に机があります。</li><li>【有生命】部屋に猫がいます。</li></ul><hr><h4 id="はにあります-います【所在句】"><a href="#はにあります-います【所在句】" class="headerlink" title="はにあります/います【所在句】"></a><del>は</del>にあります/います【所在句】</h4><ul><li>【无生命】いすは部屋にあります。</li><li>【有生命】子供は公園にいます。</li></ul><hr><h4 id="疑问句"><a href="#疑问句" class="headerlink" title="疑问句"></a>疑问句</h4><ul><li><p>不含疑问词的疑问句。</p><p>本はいすの上にありますか。</p><p>・・・はい、あります。</p><p>・・・いいえ、ありません。</p></li><li><p>含疑问词的疑问句</p><p>【何】对什么东西进行提问。</p><p>テレビの上に何がありますか。</p><p>【だれ】对人进行提问。</p><p>会議室にだれがいますか。</p><p>【どこ】对地点进行提问。</p><p>スミスさんの家はどこにありますか。</p></li></ul><h4 id="と【并列】"><a href="#と【并列】" class="headerlink" title="と【并列】"></a><del>と</del>【并列】</h4><p>時計と眼鏡。</p><h4 id="疑问词-も-否定【全面否定句】"><a href="#疑问词-も-否定【全面否定句】" class="headerlink" title="疑问词 +  も + 否定【全面否定句】"></a>疑问词 +  も + 否定【全面否定句】</h4><p>教室にだれもいません。</p><p>冷蔵庫に何もありません。 </p>]]></content>
    
    
    <categories>
      
      <category>日语</category>
      
      <category>新标日</category>
      
      <category>上册</category>
      
      <category>语法</category>
      
    </categories>
    
    
    <tags>
      
      <tag>日语</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>五十音</title>
    <link href="/2021/01/16/%E4%BA%94%E5%8D%81%E9%9F%B3/"/>
    <url>/2021/01/16/%E4%BA%94%E5%8D%81%E9%9F%B3/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">五十音</font></div><h4 id="五十音记忆"><a href="#五十音记忆" class="headerlink" title="五十音记忆"></a>五十音记忆</h4><h5 id="联想记忆"><a href="#联想记忆" class="headerlink" title="联想记忆"></a>联想记忆</h5><p><code>い</code>理服人<code>イ</code>。</p><p>烟囱<code>ウ</code>放烟造成雾<code>u</code>霾，导致人弯腰咳嗽<code>う</code>。</p><p>爱<code>e</code>上了元<code>え</code>首的工<code>工</code>作。</p><p>大力<code>力</code>水手握拳<code>ka</code>、<code>か</code>响。</p><p>在夕阳下<code>ku</code>、<code>く</code>了好久，直到太阳落了山<code>ク</code>–和狠人、狼人一个梗。</p><p>浪客<code>ke</code>剑心举着刀<code>ケ</code>，是名真正的武士<code>け</code>。</p><p><code>こ</code>和<code>コ</code>是朝着不同方向张开的大口<code>ko</code>。</p><p><code>さ</code>、<code>サ</code>、<code>sa</code>想到了<code>ki</code>，但是<code>き</code>不知道怎么联想。</p><p>弯曲的<code>し</code>溪<code>shi</code>流溢满了水<code>シ</code>。</p><p>长着肚腩的人<code>す）</code>做起了俯卧撑<code>ス</code>，没有丝<code>su</code>毫懈怠。</p><p><code>そ</code>、<code>ソ</code>、<code>so</code>求指点。</p><p>他<code>ta</code>的晚年好比午后的太<code>た</code>阳—夕<code>タ</code>阳。</p><p>千<code>チ</code>年古<code>ち</code>器<code>chi</code>，或者5.7。</p><p>刺<code>tsu</code>猬慢慢蜷缩<code>ツ</code>，直到看不见腿<code>つ</code>—先藏了两只腿。</p><p>他太<code>te</code>喜欢荡单杠<code>て</code>和双杠<code>テ</code>了。</p><p>独角兽<code>と</code>从网上淘<code>to</code>了一根萝卜<code>ト</code>。</p><p>一个人拿<code>na</code>着剑<code>ナ</code>，骑着马<code>な</code>—右半部分一直看着像窝在地上的马，点是马头。</p><p>一个人用筷子<code>に </code>、<code>ニ</code>吃到了泥<code>ni</code>。</p><p><code>ぬ</code>、<code>nu</code>隶又<code>ヌ</code>失去了自由。</p><p>权<code>ね</code>利让我对这种事视<code>ネ</code>而不见，很无奈<code>ne</code>。</p><p><code>の</code>和<code>ノ</code>都是<code>no</code>，禁止的意思—-有种一个圈一个斜杠，例如禁止停车的感觉。</p><p>卓别林拄着拐杖<code>は</code>，走路外八<code>ハ</code>，让人哈<code>ha</code>大笑。</p><p>他拿着刀<code>ヒ</code>切肉，端着酒碗<code>ひ</code>，很<code>hi</code>是惬意。</p><p>他呼呼huhu–<code>fufu</code>地跑过去<code>ふ</code>抢猪耳朵<code>フ</code>吃—-自己想象一只猪头。</p><p><code>へ</code>和<code>ヘ</code>都是黑<code>he</code>色的胡子—-哈哈哈毛利小五郎。</p><p>不管什么样的马路<code>ほ</code>边都种了好<code>ho</code>多树木<code>ホ</code>—-笔直的路弯曲的路十字路丁字路。</p><p>少了一只腿<code>マ</code>的蚂<code>ma</code>蚁<code>ま</code>—-圆圈很像蚂蚁屁股。</p><p>端午有甜米<code>mi</code>粽和带有好多木桨<code>ミ</code>的龙舟<code>み</code>—-姑且看成船。</p><p>坐在地上的和服姑娘<code>む</code>在纺织，她的旁边站了头母<code>mu</code>牛<code>ム</code>。</p><p>标着记号<code>メ</code>的绵羊<code>め</code>咩咩<code>me</code>叫—-上边有羊角的感觉，左边羊脸，小羊肖恩，养牧场经常看到牲畜有标记。</p><p><code>も</code>像长满了毛<code>モ</code>的猫<code>mo</code>——猫的尾巴很灵活。</p><p>亚<code>ya</code>军也<code>や ヤ</code>行—注意和<code>se せ セ</code>区分。</p><p>第2<code>ユ</code>有点中<code>ゆ </code>庸<code>yu</code>—像2有点勉强，倒是像礼帽，庸谐音。</p><p>音乐<code>yo</code>、<code>よ</code>和梳子<code>ヨ</code>是歌妓必备—–音谱里的小蝌蚪。</p><p>眯着眼睛大声说<code>ラ</code>辣<code>ra</code>椒5<code>ら</code>元。</p><p>分离<code>ri</code>的人背对背离开<code>り リ</code>。</p><p>你是个歹<code>る</code>徒儿<code>ル</code>，6陆<code>ru</code>—-儿化音，哈哈哈。</p><p>我从机<code>れ</code>构<code>レ</code>回来<code>re</code>。</p><p>老<code>ro</code>3<code>ろ</code>的口<code>ロ</code>才真好。</p><p>挖<code>wa</code>土机<code>わ</code>、<code>ワ</code>—-很像挖土机的机械手臂。</p><p>我<code>wo</code>跨栏<code>を</code>就像张开翅膀在飞<code>ヲ</code>—-上边一个小人下边栏杆，火影里的中二跑法手臂向后。</p><p><code>n</code>嗯，大脑想象中：<code>ん</code>是普通的椅子，<code>ン</code>是电影院或者礼堂的折叠椅。</p><h5 id="成语记忆（涵盖浊音）"><a href="#成语记忆（涵盖浊音）" class="headerlink" title="成语记忆（涵盖浊音）"></a>成语记忆（涵盖浊音）</h5><table><thead><tr><th></th><th>あ段</th><th>い段</th><th>う段</th><th>え段</th><th>お段</th></tr></thead><tbody><tr><td>あ行</td><td>吴下あ蒙</td><td>掉い轻心</td><td>う合之众</td><td>え声叹气</td><td>おお待哺</td></tr><tr><td>か行</td><td>雀巢か啡</td><td>白手き家</td><td>痛く流涕</td><td>遍地け花</td><td>病从こ入</td></tr><tr><td>さ行</td><td>赶尽さ绝</td><td>一贫如し</td><td>一座す庙</td><td>せ翁失马</td><td>そ人墨客</td></tr><tr><td>た行</td><td>客死た乡</td><td>出ち制胜</td><td>理屈つ穷</td><td>不识て举</td><td>と塔天王</td></tr><tr><td>な行</td><td>摇旗な喊</td><td>我喜欢に</td><td>沦为ぬ隶</td><td>无可ね何</td><td>一の千金</td></tr><tr><td>は行</td><td>点头は腰</td><td>ひひ哈哈</td><td>出水ふ蓉</td><td>人山人へ</td><td>死去ほ来</td></tr><tr><td>ま行</td><td>婆婆まま</td><td>神み莫测</td><td>衣食父む</td><td>爱情め卖</td><td>不も之地</td></tr><tr><td>や行</td><td>咿や学语</td><td></td><td>尊老爱ゆ</td><td></td><td>无关紧よ</td></tr><tr><td>ら行</td><td>摧枯ら朽</td><td>见り忘义</td><td>看片る管</td><td>别れ无恙</td><td>滴水不ろ</td></tr><tr><td>わ行</td><td>开わ掘机</td><td></td><td></td><td></td><td>笑を江湖</td></tr></tbody></table><h4 id="音节声调"><a href="#音节声调" class="headerlink" title="音节声调"></a>音节声调</h4><h5 id="声调"><a href="#声调" class="headerlink" title="声调"></a>声调</h5><p>①型：单词不论是几个音节（最少是1个音节），从高音入手，第二拍下降，一直到读到最后<br>拍节也不上升。<br>②型：单词不论是几个音节（最少是2个音节），从低音入手，第二拍升高，第三拍下降读到<br>最后拍节也不上升。<br>③型：单词不论是几个音节（最少是3个音节），从低音入手，第二拍升高，第三拍保持，第<br>四拍下降，读到最后拍节也不上升。<br>④型：单词不论是几个音节（最少是4个音节），从低音入手，第二拍升高，保持到第四拍，<br>第五拍下降，读到最后拍节也不上升。<br>⑤型：单词不论是几个音节（最少是5个音节），从低音入手，第二拍升高，保持到第五拍，<br>第六拍下降，读到最后拍节也不上升。<br>⑥型：单词不论是几个音节（最少是6个音节），从低音入手，第二拍升高，保持到第六拍</p><h5 id="音节"><a href="#音节" class="headerlink" title="音节"></a>音节</h5><p>片假名用于名词、外来语、拟声拟态词。</p><p>送气音和不送气音。</p><p><code>か</code>行、<code>た</code>行、<code>ぱ</code>行：词头，送气词，正常读；词中或者词尾，不送气词。</p><h6 id="浊音：右上两点"><a href="#浊音：右上两点" class="headerlink" title="浊音：右上两点"></a><strong>浊音：</strong>右上两点</h6><p><code>が</code>行也有两种读法：词头－－正常读音，词中或者词尾－－正常读音或者鼻浊音（播音员、女性、老年人多用）。</p><p><code>k</code>－－<code>g</code>、<code>s</code>－－<code>z</code>、<code>t</code>－－<code>d</code>、<code>h</code>－－<code>b</code></p><h6 id="半浊音：右上○"><a href="#半浊音：右上○" class="headerlink" title="半浊音：右上○"></a><strong>半浊音：</strong>右上○</h6><p><code>h</code>－－<code>p</code></p><table><thead><tr><th></th><th>あ段</th><th>い段</th><th>う段</th><th>え段</th><th>お段</th></tr></thead><tbody><tr><td>が行</td><td>尴が无比</td><td>ぎ祀祖先</td><td>粉身碎ぐ</td><td>农田灌げ</td><td>奔走相ご</td></tr><tr><td>ざ行</td><td>安营ざ寨</td><td>劫富じ贫</td><td>汰ず洗衣</td><td>天ぜ人祸</td><td>自ぞ自受</td></tr><tr><td>だ行</td><td>博だ精深</td><td>偷ぢ摸狗</td><td>雄づ英发</td><td>で若木鸡</td><td>ど转星移</td></tr><tr><td>ば行</td><td>ば格牙路</td><td>***び的</td><td>真ぶ要脸</td><td>青天べ日</td><td>厚积ぼ发</td></tr><tr><td>ぱ行</td><td>噼里ぱ啦</td><td>鸡毛蒜ぴ</td><td>飞蛾ぷ火</td><td>ぺ案而起</td><td>牢不可ぽ</td></tr></tbody></table><p><code>水ざ</code>行和<code>だ</code>行中都有<code>ji</code>和<code>zu</code>。</p><p><code>ざ</code>行的<code>じ</code>和和<code>ず</code> ，使用频率高，打字直接使用<code>ji</code>和<code>zu</code>。</p><p><code>だ</code>行的<code>ぢ</code>和<code>づ</code>，使用频率低，打字使用<code>di</code>和<code>du</code>。</p><h6 id="拗音"><a href="#拗音" class="headerlink" title="拗音"></a>拗音</h6><p><code>拗音</code>是模仿中国古汉字的发音。</p><p><code>い</code>段（除<code>い</code>、<code>ぢ</code>外）＋　<code>や</code>、<code>ゆ</code>、<code>よ</code>（右下角小一号，类似于整个字的一部分）。</p><p><strong>注：</strong>是<u>段</u>不是<del>行</del>。</p><p>き、し、ち、に、ひ、み、り　　　ぎ、じ、び、ぴ</p><p><code>合拗音</code>是模仿西方外来语的发音，专门用于标记外来语，故只有片假名。</p><h5 id="特殊音节"><a href="#特殊音节" class="headerlink" title="特殊音节"></a>特殊音节</h5><h6 id="拨音"><a href="#拨音" class="headerlink" title="拨音"></a>拨音</h6><p><code>拨音</code>受其后音节影响。</p><p><strong>注：</strong>都是<u>段</u>不是<del>行</del>。</p><table><thead><tr><th>表记方式</th><th>发音</th></tr></thead><tbody><tr><td>あ段　＋　ん</td><td>ang</td></tr><tr><td>い段　＋　ん</td><td>in</td></tr><tr><td>う段　＋　ん</td><td>un</td></tr><tr><td>え段　＋　ん</td><td>en</td></tr><tr><td>お段　＋　ん</td><td>ong</td></tr></tbody></table><h6 id="促音"><a href="#促音" class="headerlink" title="促音"></a>促音</h6><p><strong>促音</strong>（日语：促音／そくおん <em>sokuon</em>）是在日语中用来表示停顿的符号，写法为っ、片假名ッ。</p><p>促音`分为摩擦促音和阻塞促音，促音不可单独发音，占一个音拍，小一号（片假名和平假名）。</p><p>促音っ只出现在か行、さ行、た行、ぱ(半浊音)行前面。</p><p>阻塞促音：在促音っ后面连接か行（か、き、く、け、こ）的时候，以 k 的发音方法顿挫一拍（只做口型不出声）。</p><p>摩擦促音：促音位于<code>さ</code>行之前。</p><p><strong>注：</strong>都是<u>行</u>不是<del>段</del>。</p><p>在电脑打字时，若要单独输入促音“っ”，可输入xtsu、xtu、ltsu或ltu。</p><h6 id="长音"><a href="#长音" class="headerlink" title="长音"></a>长音</h6><p>例：<code>いい</code>发一个音，但是拉长，yi……，不是yiyi。</p><p><strong>注：</strong>都是<u>段</u>不是<del>行</del>。</p><table><thead><tr><th></th><th>表记方式</th></tr></thead><tbody><tr><td>平假名</td><td>あ段　＋　あ</td></tr><tr><td></td><td>い段　＋　い</td></tr><tr><td></td><td>う段　＋　う</td></tr><tr><td></td><td>え段　＋　う（多数）</td></tr><tr><td></td><td>え段　＋　え（少数）</td></tr><tr><td></td><td>お段　＋　う（多数）</td></tr><tr><td></td><td>お段　＋　お（少数）</td></tr><tr><td>片假名</td><td>一</td></tr></tbody></table><h6 id="拗长音"><a href="#拗长音" class="headerlink" title="拗长音"></a>拗长音</h6><p>在<code>拗音</code>后边加上<code>い</code>、<code>う</code>即可成为拗长音。外来语后加上<code>一</code>。</p><table><thead><tr><th></th><th>表记方式</th></tr></thead><tbody><tr><td>平假名</td><td>[い段　＋　や]　＋　い</td></tr><tr><td></td><td>[い段　＋　ゆ]　＋　う</td></tr><tr><td></td><td>[い段　＋　よ]　＋　う</td></tr><tr><td>片假名</td><td>一</td></tr></tbody></table><h6 id="外来语专用音节"><a href="#外来语专用音节" class="headerlink" title="外来语专用音节"></a>外来语专用音节</h6><table><thead><tr><th>ツァ　<strong>tsa</strong></th><th>ファ　<strong>fa</strong></th><th></th><th></th><th></th><th></th></tr></thead><tbody><tr><td>ウィ　<strong>wi</strong></td><td>ティ　<strong>ti</strong></td><td>フィ　<strong>fi</strong></td><td>ディ　<strong>di</strong></td><td></td><td></td></tr><tr><td>トゥ　<strong>tu</strong></td><td>ドゥ　<strong>du</strong></td><td>デュ　<strong>dyu</strong></td><td></td><td></td><td></td></tr><tr><td>ウェ　<strong>we</strong></td><td>シェ　<strong>she</strong></td><td>チェ　<strong>che</strong></td><td>ツェ　<strong>tse</strong></td><td>フェ　<strong>fe</strong></td><td>ジェ　<strong>je</strong></td></tr><tr><td>ウォ　<strong>wo</strong></td><td>ツォ　<strong>tso</strong></td><td>フォ　<strong>fo</strong></td><td></td><td></td><td></td></tr></tbody></table><h4 id="五十音日语单词"><a href="#五十音日语单词" class="headerlink" title="五十音日语单词　　　　　　　　　　"></a>五十音日语单词　　　　　　　　　　</h4><table><thead><tr><th>日语</th><th>中文</th><th>日语</th><th>中文</th></tr></thead><tbody><tr><td>あい</td><td>爱</td><td>え</td><td>绘画</td></tr><tr><td>あおい</td><td>蓝色</td><td>うお</td><td>鱼</td></tr><tr><td>いか</td><td>乌贼</td><td>えき</td><td>车站</td></tr><tr><td>いけ</td><td>池塘</td><td>かく</td><td>书写</td></tr><tr><td>かお</td><td>脸</td><td>あき</td><td>秋天</td></tr><tr><td>すし</td><td>寿司</td><td>いす</td><td>椅子</td></tr><tr><td>さけ</td><td>酒</td><td>せかい</td><td>世界</td></tr><tr><td>あさ</td><td>早上</td><td>おかし</td><td>点心</td></tr><tr><td>はな</td><td>花</td><td>ひたい</td><td>额头</td></tr><tr><td>へた</td><td>不擅长</td><td>はし</td><td>桥</td></tr><tr><td>ほし</td><td>星星</td><td>ふさい</td><td>夫妻</td></tr><tr><td>まち</td><td>街道</td><td>あめ</td><td>雨</td></tr><tr><td>かいもの</td><td>购物</td><td>かみ</td><td>头发</td></tr><tr><td>やおや</td><td>果蔬店</td><td>おかゆ</td><td>粥</td></tr><tr><td>よなか</td><td>半夜</td><td>やすい（休）</td><td>休息</td></tr><tr><td>さくら（桜）</td><td>樱花</td><td>くすり（薬）</td><td>药</td></tr><tr><td>ホテル（hotel）</td><td>宾馆（外来语使用片假名）</td><td>ロシア（Russia）</td><td>俄罗斯（外来语）</td></tr><tr><td>かれし</td><td>男朋友</td><td>はしる</td><td>奔跑</td></tr><tr><td>にわ</td><td>院子</td><td>わらう</td><td>笑</td></tr><tr><td>お休み（なさい——敬语）</td><td>晚安</td><td>またね</td><td>再见</td></tr><tr><td>また明日（あした）</td><td>明天见</td><td>おねちよ</td><td>尿床</td></tr><tr><td></td><td></td><td></td><td></td></tr></tbody></table>]]></content>
    
    
    <categories>
      
      <category>日语</category>
      
      <category>新标日</category>
      
      <category>上册</category>
      
      <category>单词</category>
      
    </categories>
    
    
    <tags>
      
      <tag>日语</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>解析bean标签子元素二</title>
    <link href="/2021/01/09/%E8%A7%A3%E6%9E%90%E8%87%AA%E5%AE%9A%E4%B9%89%E6%A0%87%E7%AD%BE%E4%BA%8C/"/>
    <url>/2021/01/09/%E8%A7%A3%E6%9E%90%E8%87%AA%E5%AE%9A%E4%B9%89%E6%A0%87%E7%AD%BE%E4%BA%8C/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">解析bean标签子元素二</font></div><h4 id="自定义标签的使用"><a href="#自定义标签的使用" class="headerlink" title="自定义标签的使用"></a>自定义标签的使用</h4><p>自定义标签的使用大致分为以下步骤：</p><ol><li>定义一个自定义标签组件，就是一个普通的<code>Java Bean</code>。</li><li>定义一个<code>xsd</code>文件，用来描述自定义标签的属性内容。</li><li>创建一个实现了 <code>org.springframework.beans.factory.xml.AbstractSingleBeanDefinitionParser</code> 接口的<code>Parser</code>，用来解析<code>xsd</code>文件中的自定义标签内容。</li><li>创建一个继承了 <code>org.springframework.beans.factory.xml.NamespaceHandlerSupport</code> 抽象类的<code>NameSpaceHandler</code>，用于将自定义标签组件注册到 Spring 容器。</li><li>编写 <code>spring.handlers</code> 和 <code>spring.schemas</code> 文件，放在<code>resource/META-INF</code>目录下。</li></ol><p>文件结构图：</p><p><img src="/images/spring_IOC/%E8%87%AA%E5%AE%9A%E4%B9%89%E6%A0%87%E7%AD%BE%E7%9B%AE%E5%BD%95%E7%BB%93%E6%9E%84%E5%9B%BE.png" alt="自定义标签目录结构图"></p><h5 id="自定义标签组件"><a href="#自定义标签组件" class="headerlink" title="自定义标签组件"></a>自定义标签组件</h5><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Hello</span> </span>&#123;<span class="hljs-keyword">private</span> String id;<span class="hljs-keyword">private</span> Integer age;<span class="hljs-keyword">private</span> String name;<span class="hljs-keyword">private</span> String address;<span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getId</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> id;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setId</span><span class="hljs-params">(String id)</span> </span>&#123;<span class="hljs-keyword">this</span>.id = id;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> Integer <span class="hljs-title">getAge</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> age;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setAge</span><span class="hljs-params">(Integer age)</span> </span>&#123;<span class="hljs-keyword">this</span>.age = age;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getName</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> name;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setName</span><span class="hljs-params">(String name)</span> </span>&#123;<span class="hljs-keyword">this</span>.name = name;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getAddress</span><span class="hljs-params">()</span> </span>&#123;<span class="hljs-keyword">return</span> address;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setAddress</span><span class="hljs-params">(String address)</span> </span>&#123;<span class="hljs-keyword">this</span>.address = address;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Hello</span><span class="hljs-params">()</span> </span>&#123;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">sayHello</span><span class="hljs-params">()</span> </span>&#123;System.out.println(<span class="hljs-keyword">this</span>.name + <span class="hljs-string">&quot;今年&quot;</span> + <span class="hljs-keyword">this</span>.age + <span class="hljs-string">&quot;岁，&quot;</span> + <span class="hljs-string">&quot;住在&quot;</span> + <span class="hljs-keyword">this</span>.address);&#125;&#125;</code></pre><h5 id="定义xsd文件"><a href="#定义xsd文件" class="headerlink" title="定义xsd文件"></a>定义<code>xsd</code>文件</h5><p>该文件用来对自定义标签组件进行描述，还定义了<code>xmlns=&quot;http://www.jievhaha.com/schema/hello&quot; </code>和<code>targetNamespace=&quot;http://www.jievhaha.com/schema/hello&quot;</code></p><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">xsd:schema</span> <span class="hljs-attr">xmlns:xsd</span>=<span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema&quot;</span> <span class="hljs-attr">xmlns</span>=<span class="hljs-string">&quot;http://www.jievhaha.com/schema/hello&quot;</span> <span class="hljs-attr">targetNamespace</span>=<span class="hljs-string">&quot;http://www.jievhaha.com/schema/hello&quot;</span> <span class="hljs-attr">elementFormDefault</span>=<span class="hljs-string">&quot;qualified&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">xsd:element</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;hello&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">xsd:complexType</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">xsd:attribute</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;id&quot;</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;xsd:string&quot;</span>/&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">xsd:attribute</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;age&quot;</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;xsd:string&quot;</span> /&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">xsd:attribute</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;name&quot;</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;xsd:string&quot;</span> /&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">xsd:attribute</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;address&quot;</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;xsd:string&quot;</span> /&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">xsd:complexType</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">xsd:element</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">xsd:schema</span>&gt;</span></code></pre><h5 id="定义Parser类"><a href="#定义Parser类" class="headerlink" title="定义Parser类"></a>定义<code>Parser</code>类</h5><p>该类继承了<code>AbstractSingleBeanDefinitionParser</code>。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HelloDefinitionParser</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractSingleBeanDefinitionParser</span> </span>&#123;<span class="hljs-meta">@Override</span><span class="hljs-keyword">protected</span> Class&lt;?&gt; getBeanClass(Element element) &#123;<span class="hljs-keyword">return</span> Hello.class;&#125;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">doParse</span><span class="hljs-params">(Element element, BeanDefinitionBuilder builder)</span> </span>&#123;String id = element.getAttribute(<span class="hljs-string">&quot;id&quot;</span>);String age = element.getAttribute(<span class="hljs-string">&quot;age&quot;</span>);String name = element.getAttribute(<span class="hljs-string">&quot;name&quot;</span>);String address = element.getAttribute(<span class="hljs-string">&quot;address&quot;</span>);<span class="hljs-keyword">if</span>(StringUtils.hasLength(<span class="hljs-string">&quot;id&quot;</span>))&#123;builder.addPropertyValue(<span class="hljs-string">&quot;id&quot;</span>, id);&#125;<span class="hljs-keyword">if</span>(StringUtils.hasLength(age))&#123;builder.addPropertyValue(<span class="hljs-string">&quot;age&quot;</span>, age);&#125;<span class="hljs-keyword">if</span>(StringUtils.hasText(name))&#123;builder.addPropertyValue(<span class="hljs-string">&quot;name&quot;</span>, name);&#125;<span class="hljs-keyword">if</span>(StringUtils.hasLength(address))&#123;builder.addPropertyValue(<span class="hljs-string">&quot;address&quot;</span>, address);&#125;&#125;&#125;</code></pre><h5 id="定义NameSpaceHandler类"><a href="#定义NameSpaceHandler类" class="headerlink" title="定义NameSpaceHandler类"></a>定义<code>NameSpaceHandler</code>类</h5><p>该类继承了<code>NamespaceHandlerSupport</code>。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HelloNameSpaceHandler</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">NamespaceHandlerSupport</span> </span>&#123;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">init</span><span class="hljs-params">()</span> </span>&#123;registerBeanDefinitionParser(<span class="hljs-string">&quot;hello&quot;</span>,<span class="hljs-keyword">new</span> HelloDefinitionParser());&#125;&#125;</code></pre><h5 id="定义spring-handlers和spring-schemas文件"><a href="#定义spring-handlers和spring-schemas文件" class="headerlink" title="定义spring.handlers和spring.schemas文件"></a>定义<code>spring.handlers</code>和<code>spring.schemas</code>文件</h5><pre><code class="hljs xml">#spring.handlershttp\://www.jievhaha.com/schema/hello=com.jievhaha.HelloNameSpaceHandler#spring.schemashttp\://www.jievhaha.com/schema/hello.xsd=hello.xsd</code></pre><h5 id="配置spring配置文件并测试"><a href="#配置spring配置文件并测试" class="headerlink" title="配置spring配置文件并测试"></a>配置spring配置文件并测试</h5><p>spring.xml</p><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">beans</span> <span class="hljs-attr">xmlns</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans&quot;</span></span><span class="hljs-tag">   <span class="hljs-attr">xmlns:xsi</span>=<span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span><span class="hljs-tag">   <span class="hljs-attr">xmlns:mytag</span>=<span class="hljs-string">&quot;http://www.jievhaha.com/schema/hello&quot;</span></span><span class="hljs-tag">   <span class="hljs-attr">xsi:schemaLocation</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd</span></span><span class="hljs-tag"><span class="hljs-string">       http://www.jievhaha.com/schema/hello http://www.jievhaha.com/schema/hello.xsd&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">mytag:hello</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;hello&quot;</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;jievhaha&quot;</span> <span class="hljs-attr">age</span>=<span class="hljs-string">&quot;27&quot;</span> <span class="hljs-attr">address</span>=<span class="hljs-string">&quot;山西&quot;</span>/&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">beans</span>&gt;</span></code></pre><p>MyApplication.java</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MyApplication</span> </span>&#123;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;ApplicationContext applicationContext = <span class="hljs-keyword">new</span> ClassPathXmlApplicationContext(<span class="hljs-string">&quot;hello.xml&quot;</span>);Hello hello = (Hello) applicationContext.getBean(<span class="hljs-string">&quot;hello&quot;</span>);hello.sayHello();<span class="hljs-comment">//jievhaha今年27岁，住在山西</span>&#125;&#125;</code></pre><p><img src="/images/spring_IOC/%E8%87%AA%E5%AE%9A%E4%B9%89%E6%A0%87%E7%AD%BE%E8%BF%90%E8%A1%8C%E7%BB%93%E6%9E%9C.png" alt="自定义标签运行结果"></p><h4 id="解析自定义标签"><a href="#解析自定义标签" class="headerlink" title="解析自定义标签"></a>解析自定义标签</h4><h5 id="parseCustomElement-方法"><a href="#parseCustomElement-方法" class="headerlink" title="parseCustomElement()方法"></a><code>parseCustomElement()</code>方法</h5><p>applicationContext.xml文件开始读取的入口方法：<code>DefaultBeanDefinitionDocumentReader.parseBeanDefinitions()</code>方法，该方法负责标签的解析工作，根据命名空间的不同进行不同标签的解析。其中，<strong>自定义标签</strong>由<code>BeanDefinitionParserDelegate</code>的 <code>parseCustomElement(Element ele, BeanDefinition containingBd)</code> 方法来实现。</p><pre><code class="hljs java"><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> BeanDefinition <span class="hljs-title">parseCustomElement</span><span class="hljs-params">(Element ele)</span> </span>&#123;    <span class="hljs-keyword">return</span> parseCustomElement(ele, <span class="hljs-keyword">null</span>);&#125;<span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> BeanDefinition <span class="hljs-title">parseCustomElement</span><span class="hljs-params">(Element ele, <span class="hljs-meta">@Nullable</span> BeanDefinition containingBd)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 获取 namespaceUri</span>    String namespaceUri = getNamespaceURI(ele);    <span class="hljs-keyword">if</span> (namespaceUri == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;    <span class="hljs-comment">// &lt;2&gt; 根据 namespaceUri 获取相应的 Handler</span>    NamespaceHandler handler = <span class="hljs-keyword">this</span>.readerContext.getNamespaceHandlerResolver().resolve(namespaceUri);    <span class="hljs-keyword">if</span> (handler == <span class="hljs-keyword">null</span>) &#123;        error(<span class="hljs-string">&quot;Unable to locate Spring NamespaceHandler for XML schema namespace [&quot;</span> + namespaceUri + <span class="hljs-string">&quot;]&quot;</span>, ele);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;    <span class="hljs-comment">// 调用自定义的 Handler 处理</span>    <span class="hljs-keyword">return</span> handler.parse(ele, <span class="hljs-keyword">new</span> ParserContext(<span class="hljs-keyword">this</span>.readerContext, <span class="hljs-keyword">this</span>, containingBd));&#125;</code></pre><h6 id="getNamespaceURI-方法"><a href="#getNamespaceURI-方法" class="headerlink" title="getNamespaceURI()方法"></a><code>getNamespaceURI()</code>方法</h6><p>先获取了NamespaceURI：</p><pre><code class="hljs java"><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getNamespaceURI</span><span class="hljs-params">(Node node)</span> </span>&#123;    <span class="hljs-keyword">return</span> node.getNamespaceURI();&#125;</code></pre><h6 id="getNamespaceHandlerResolver-方法"><a href="#getNamespaceHandlerResolver-方法" class="headerlink" title="getNamespaceHandlerResolver()方法"></a><code>getNamespaceHandlerResolver()</code>方法</h6><p>调用 <code>XmlReaderContext.getNamespaceHandlerResolver()</code> 方法，获得命名空间的解析器。</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> NamespaceHandlerResolver namespaceHandlerResolver;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> NamespaceHandlerResolver <span class="hljs-title">getNamespaceHandlerResolver</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.namespaceHandlerResolver;&#125;</code></pre><p><code>NamespaceHandlerResolver</code>的初始化：</p><p>在《注册BeanDefinitions》中提到注册BeanDefinitions时：</p><ul><li>首先，是通过<code>XmlBeanDefinitionReader</code>的<code>createBeanDefinitionDocumentReader()</code>方法，获取 Document解析器<code>BeanDefinitionDocumentReader</code>实例。</li><li>然后，调用<code>BeanDefinitionDocumentReader</code>实例的<code>registerBeanDefinitions(Document doc, XmlReaderContext readerContext)</code>方法，进行注册。而该方法需要提供两个参数，一个是<code>Document</code>实例 <code>doc</code>，一个是<code>XmlReaderContext</code>实例 <code>readerContext</code> 。</li></ul><p><code>readerContext</code> 实例对象由<code>XmlBeanDefinitionReader</code>的<code>createReaderContext(Resource resource)</code>方法创建。<code>namespaceHandlerResolver</code> 实例对象就是在这个时候初始化的：</p><pre><code class="hljs java"><span class="hljs-comment">// XmlBeanDefinitionReader.java</span><span class="hljs-comment">//该方法最后一个参数为`NamespaceHandlerResolver`对象，通过`getNamespaceHandlerResolver()`方法获取</span><span class="hljs-function"><span class="hljs-keyword">public</span> XmlReaderContext <span class="hljs-title">createReaderContext</span><span class="hljs-params">(Resource resource)</span> </span>&#123;<span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> XmlReaderContext(resource, <span class="hljs-keyword">this</span>.problemReporter, <span class="hljs-keyword">this</span>.eventListener,<span class="hljs-keyword">this</span>.sourceExtractor, <span class="hljs-keyword">this</span>, getNamespaceHandlerResolver());&#125;<span class="hljs-comment">//获取`NamespaceHandlerResolver`对象</span><span class="hljs-function"><span class="hljs-keyword">public</span> NamespaceHandlerResolver <span class="hljs-title">getNamespaceHandlerResolver</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.namespaceHandlerResolver == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">this</span>.namespaceHandlerResolver = createDefaultNamespaceHandlerResolver();    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.namespaceHandlerResolver;&#125;<span class="hljs-comment">//可以看出`NamespaceHandlerResolver`对象类型默认为DefaultNamespaceHandlerResolver</span><span class="hljs-function"><span class="hljs-keyword">protected</span> NamespaceHandlerResolver <span class="hljs-title">createDefaultNamespaceHandlerResolver</span><span class="hljs-params">()</span> </span>&#123;    ClassLoader cl = (getResourceLoader() != <span class="hljs-keyword">null</span> ? getResourceLoader().getClassLoader() : getBeanClassLoader());    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> DefaultNamespaceHandlerResolver(cl);&#125;</code></pre><h5 id="resolve-方法"><a href="#resolve-方法" class="headerlink" title="resolve()方法"></a><code>resolve()</code>方法</h5><p>由上可以看出<code>this.readerContext.getNamespaceHandlerResolver().resolve(namespaceUri)</code>最终调用的就是<code>DefaultNamespaceHandlerResolver</code>类的<code>resolve()</code>方法。</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> NamespaceHandler <span class="hljs-title">resolve</span><span class="hljs-params">(String namespaceUri)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 获取所有已经配置的 Handler 映射</span>    Map&lt;String, Object&gt; handlerMappings = getHandlerMappings();    <span class="hljs-comment">// &lt;2&gt; 根据 namespaceUri 获取 handler 的信息</span>    Object handlerOrClassName = handlerMappings.get(namespaceUri);    <span class="hljs-keyword">if</span> (handlerOrClassName == <span class="hljs-keyword">null</span>) &#123;<span class="hljs-comment">//不存在返回null</span>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;    <span class="hljs-comment">//已经初始化过了直接强制类型转换</span>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (handlerOrClassName <span class="hljs-keyword">instanceof</span> NamespaceHandler) &#123;        <span class="hljs-keyword">return</span> (NamespaceHandler) handlerOrClassName;    &#125;    <span class="hljs-comment">//未初始化</span>    <span class="hljs-keyword">else</span> &#123;        String className = (String) handlerOrClassName;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">// &lt;2&gt; 根据 namespaceUri 获取 handler 的信息</span>            Class&lt;?&gt; handlerClass = ClassUtils.forName(className, <span class="hljs-keyword">this</span>.classLoader);            <span class="hljs-keyword">if</span> (!NamespaceHandler.class.isAssignableFrom(handlerClass)) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> FatalBeanException(<span class="hljs-string">&quot;Class [&quot;</span> + className + <span class="hljs-string">&quot;] for namespace [&quot;</span> + namespaceUri +                                             <span class="hljs-string">&quot;] does not implement the [&quot;</span> + NamespaceHandler.class.getName() + <span class="hljs-string">&quot;] interface&quot;</span>);            &#125;            NamespaceHandler namespaceHandler = (NamespaceHandler) BeanUtils.instantiateClass(handlerClass);            <span class="hljs-comment">// 初始化 NamespaceHandler 对象</span>            namespaceHandler.init();            <span class="hljs-comment">// 添加到缓存</span>            handlerMappings.put(namespaceUri, namespaceHandler);            <span class="hljs-keyword">return</span> namespaceHandler;        &#125;        <span class="hljs-keyword">catch</span> (ClassNotFoundException ex) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> FatalBeanException(<span class="hljs-string">&quot;Could not find NamespaceHandler class [&quot;</span> + className +                                         <span class="hljs-string">&quot;] for namespace [&quot;</span> + namespaceUri + <span class="hljs-string">&quot;]&quot;</span>, ex);        &#125;        <span class="hljs-keyword">catch</span> (LinkageError err) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> FatalBeanException(<span class="hljs-string">&quot;Unresolvable class definition for NamespaceHandler class [&quot;</span> +                                         className + <span class="hljs-string">&quot;] for namespace [&quot;</span> + namespaceUri + <span class="hljs-string">&quot;]&quot;</span>, err);        &#125;    &#125;&#125;</code></pre><p>获取所有已经配置的 Handler 映射，通过<code>getHandlerMappings()</code>方法</p><pre><code class="hljs java"><span class="hljs-comment">/**</span><span class="hljs-comment">* Resource location to search for.</span><span class="hljs-comment">* NamespaceHandler 映射配置文件地址</span><span class="hljs-comment">*/</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> String handlerMappingsLocation;<span class="hljs-comment">/**</span><span class="hljs-comment">* Stores the mappings from namespace URI to NamespaceHandler class name / instance.</span><span class="hljs-comment">* NamespaceHandler 映射。</span><span class="hljs-comment">*</span><span class="hljs-comment">* key：命名空间</span><span class="hljs-comment">* value：分成两种情况：</span><span class="hljs-comment">* 1）未初始化时，对应的 NamespaceHandler 的类路径；</span><span class="hljs-comment">* 2）已初始化，对应的 NamespaceHandler 对象</span><span class="hljs-comment">*/</span><span class="hljs-meta">@Nullable</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> Map&lt;String, Object&gt; handlerMappings;<span class="hljs-function"><span class="hljs-keyword">private</span> Map&lt;String, Object&gt; <span class="hljs-title">getHandlerMappings</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-comment">//使用了延迟加载，双重检查锁</span>    Map&lt;String, Object&gt; handlerMappings = <span class="hljs-keyword">this</span>.handlerMappings;    <span class="hljs-keyword">if</span> (handlerMappings == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">synchronized</span> (<span class="hljs-keyword">this</span>) &#123;            handlerMappings = <span class="hljs-keyword">this</span>.handlerMappings;            <span class="hljs-keyword">if</span> (handlerMappings == <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                    logger.trace(<span class="hljs-string">&quot;Loading NamespaceHandler mappings from [&quot;</span> + <span class="hljs-keyword">this</span>.handlerMappingsLocation + <span class="hljs-string">&quot;]&quot;</span>);                &#125;                <span class="hljs-keyword">try</span> &#123;                    <span class="hljs-comment">// 读取 handlerMappingsLocation</span>                    Properties mappings =                        PropertiesLoaderUtils.loadAllProperties(<span class="hljs-keyword">this</span>.handlerMappingsLocation, <span class="hljs-keyword">this</span>.classLoader);                    <span class="hljs-keyword">if</span> (logger.isTraceEnabled()) &#123;                        logger.trace(<span class="hljs-string">&quot;Loaded NamespaceHandler mappings: &quot;</span> + mappings);                    &#125;                    <span class="hljs-comment">// 初始化到 handlerMappings 中</span>                    handlerMappings = <span class="hljs-keyword">new</span> ConcurrentHashMap&lt;&gt;(mappings.size());                    CollectionUtils.mergePropertiesIntoMap(mappings, handlerMappings);                    <span class="hljs-keyword">this</span>.handlerMappings = handlerMappings;                &#125;                <span class="hljs-keyword">catch</span> (IOException ex) &#123;                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(                        <span class="hljs-string">&quot;Unable to load NamespaceHandler mappings from location [&quot;</span> + <span class="hljs-keyword">this</span>.handlerMappingsLocation + <span class="hljs-string">&quot;]&quot;</span>, ex);                &#125;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> handlerMappings;&#125;</code></pre><p>执行完<code>getHandlerMappings()</code>获取了<code>Handler</code>映射后，之后还会执行<code>NamespaceHandler.init()</code>方法，该方法主要是将自定义标签解析器进行注册，调用的就是我们上边实例中的<code>HelloNameSpaceHandler.init()</code>方法，内部其实还是调用了父类<code>NamespaceHandlerSupport</code>的<code>registerBeanDefinitionParser()</code>来注册指定元素的<code>BeanDefinitionParser</code>解析器，其实就是将映射关系放在一个 Map 结构的 parsers 对象中。。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HelloNameSpaceHandler</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">NamespaceHandlerSupport</span> </span>&#123;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">init</span><span class="hljs-params">()</span> </span>&#123;registerBeanDefinitionParser(<span class="hljs-string">&quot;hello&quot;</span>,<span class="hljs-keyword">new</span> HelloDefinitionParser());&#125;&#125;</code></pre><p><code>NamespaceHandlerSupport</code>的<code>registerBeanDefinitionParser()</code>：</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, BeanDefinitionDecorator&gt; decorators = <span class="hljs-keyword">new</span> HashMap&lt;&gt;();<span class="hljs-comment">/**</span><span class="hljs-comment">* key：元素名，不是命名空间名称</span><span class="hljs-comment">* value：对应 BeanDefinitionParser 的解析器</span><span class="hljs-comment">*/</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">registerBeanDefinitionDecorator</span><span class="hljs-params">(String elementName, BeanDefinitionDecorator dec)</span> </span>&#123;    <span class="hljs-keyword">this</span>.decorators.put(elementName, dec);&#125;</code></pre><h5 id="parse-方法"><a href="#parse-方法" class="headerlink" title="parse()方法"></a><code>parse()</code>方法</h5><p>执行完<code>this.readerContext.getNamespaceHandlerResolver().resolve(namespaceUri);</code>获取了相应的<code>Handler</code>后，然后调用<code>parse()</code>方法执行自定义标签的解析。返回的<code>NamespaceHandlerHandler</code>是我们自己定义的<code>HelloNameSpaceHandler</code>，我们定义的继承了<code>NamespaceHandlerSupport</code>，故其实调用的是<code>NamespaceHandlerSupport.parse()</code>：</p><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> BeanDefinition <span class="hljs-title">parse</span><span class="hljs-params">(Element element, ParserContext parserContext)</span> </span>&#123;    BeanDefinitionParser parser = findParserForElement(element, parserContext);    <span class="hljs-keyword">return</span> (parser != <span class="hljs-keyword">null</span> ? parser.parse(element, parserContext) : <span class="hljs-keyword">null</span>);&#125;</code></pre><ol><li>调用 <code>findParserForElement(Element element, ParserContext parserContext)</code>方法，获取对应的 <code>BeanDefinitionParser</code>实例。实际上，其实就是获取在<code>NamespaceHandlerSupport</code>的 <code>registerBeanDefinitionParser()</code> 方法里面注册的实例对象，注册是在我们执行<code>init()</code>的时候。</li></ol><pre><code class="hljs java"><span class="hljs-comment">//NamespaceHandlerSupport.java</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">private</span> BeanDefinitionParser <span class="hljs-title">findParserForElement</span><span class="hljs-params">(Element element, ParserContext parserContext)</span> </span>&#123;    <span class="hljs-comment">// 获得元素名</span>    String localName = parserContext.getDelegate().getLocalName(element);    <span class="hljs-comment">// 获得 BeanDefinitionParser 对象</span>    BeanDefinitionParser parser = <span class="hljs-keyword">this</span>.parsers.get(localName);    <span class="hljs-keyword">if</span> (parser == <span class="hljs-keyword">null</span>) &#123;        parserContext.getReaderContext().fatal(            <span class="hljs-string">&quot;Cannot locate BeanDefinitionParser for element [&quot;</span> + localName + <span class="hljs-string">&quot;]&quot;</span>, element);    &#125;    <span class="hljs-keyword">return</span> parser;&#125;</code></pre><ol start="2"><li>最后执行<code>parse()</code>，得到的<code>BeanDefinitionParser</code>其实就是我们自己定义的<code>HelloDefinitionParser</code>继承了<code>AbstractSingleBeanDefinitionParser</code>，而<code>AbstractSingleBeanDefinitionParser</code>又继承了<code>AbstractBeanDefinitionParser</code>，所以最终执行的是该类的<code>parse()</code>方法。</li></ol><pre><code class="hljs java"><span class="hljs-comment">//`AbstractBeanDefinitionParser.java`</span><span class="hljs-meta">@Override</span><span class="hljs-meta">@Nullable</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> BeanDefinition <span class="hljs-title">parse</span><span class="hljs-params">(Element element, ParserContext parserContext)</span> </span>&#123;    <span class="hljs-comment">// &lt;1&gt; 内部解析，返回 AbstractBeanDefinition 对象</span>    AbstractBeanDefinition definition = parseInternal(element, parserContext);    <span class="hljs-keyword">if</span> (definition != <span class="hljs-keyword">null</span> &amp;&amp; !parserContext.isNested()) &#123;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">// 解析 id 属性</span>            String id = resolveId(element, definition, parserContext);            <span class="hljs-keyword">if</span> (!StringUtils.hasText(id)) &#123;                parserContext.getReaderContext().error(                    <span class="hljs-string">&quot;Id is required for element &#x27;&quot;</span> + parserContext.getDelegate().getLocalName(element)                    + <span class="hljs-string">&quot;&#x27; when used as a top-level tag&quot;</span>, element);            &#125;            <span class="hljs-comment">// 解析 aliases 属性</span>            String[] aliases = <span class="hljs-keyword">null</span>;            <span class="hljs-keyword">if</span> (shouldParseNameAsAliases()) &#123;                String name = element.getAttribute(NAME_ATTRIBUTE);                <span class="hljs-keyword">if</span> (StringUtils.hasLength(name)) &#123;                    aliases = StringUtils.trimArrayElements(StringUtils.commaDelimitedListToStringArray(name));                &#125;            &#125;            <span class="hljs-comment">// 创建 BeanDefinitionHolder 对象</span>            BeanDefinitionHolder holder = <span class="hljs-keyword">new</span> BeanDefinitionHolder(definition, id, aliases);            <span class="hljs-comment">// 注册 BeanDefinition</span>            registerBeanDefinition(holder, parserContext.getRegistry());            <span class="hljs-comment">// 触发事件</span>            <span class="hljs-keyword">if</span> (shouldFireEvents()) &#123;                BeanComponentDefinition componentDefinition = <span class="hljs-keyword">new</span> BeanComponentDefinition(holder);                postProcessComponentDefinition(componentDefinition);                parserContext.registerComponent(componentDefinition);            &#125;        &#125;        <span class="hljs-keyword">catch</span> (BeanDefinitionStoreException ex) &#123;            String msg = ex.getMessage();            parserContext.getReaderContext().error((msg != <span class="hljs-keyword">null</span> ? msg : ex.toString()), element);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;    &#125;    <span class="hljs-keyword">return</span> definition;&#125;</code></pre><p>核心在 <code>parseInternal(Element element, ParserContext parserContext)</code>方法。为什么这么说？因为该方法返回的是<code>AbstractBeanDefinition</code>对象。从前面默认标签的解析过程来看，我们就可以判断该方法就是将标签解析为<code>AbstractBeanDefinition</code>，且后续代码都是将 <code>AbstractBeanDefinition</code>转换为 <code>BeanDefinitionHolder</code>对象。所以真正的解析工作都交由<code>parseInternal(Element element, ParserContext parserContext)</code>方法来实现。</p><pre><code class="hljs java"><span class="hljs-comment">// AbstractSingleBeanDefinitionParser.java</span><span class="hljs-comment">//解析元素为AbstractBeanDefinition对象</span><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> AbstractBeanDefinition <span class="hljs-title">parseInternal</span><span class="hljs-params">(Element element, ParserContext parserContext)</span> </span>&#123;    <span class="hljs-comment">// 创建 BeanDefinitionBuilder 对象</span>    BeanDefinitionBuilder builder = BeanDefinitionBuilder.genericBeanDefinition();    <span class="hljs-comment">// 获取父类元素</span>    String parentName = getParentName(element);    <span class="hljs-keyword">if</span> (parentName != <span class="hljs-keyword">null</span>) &#123;        builder.getRawBeanDefinition().setParentName(parentName);    &#125;    <span class="hljs-comment">// 获取自定义标签中的 class，这个时候会去调用自定义解析中的 getBeanClass()</span>    Class&lt;?&gt; beanClass = getBeanClass(element);    <span class="hljs-keyword">if</span> (beanClass != <span class="hljs-keyword">null</span>) &#123;        builder.getRawBeanDefinition().setBeanClass(beanClass);    &#125;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// beanClass 为 null，意味着子类并没有重写 getBeanClass() 方法，则尝试去判断是否重写了 getBeanClassName()</span>        String beanClassName = getBeanClassName(element);        <span class="hljs-keyword">if</span> (beanClassName != <span class="hljs-keyword">null</span>) &#123;            builder.getRawBeanDefinition().setBeanClassName(beanClassName);        &#125;    &#125;    <span class="hljs-comment">// 设置 source 属性</span>    builder.getRawBeanDefinition().setSource(parserContext.extractSource(element));    <span class="hljs-comment">// 设置 scope 属性</span>    BeanDefinition containingBd = parserContext.getContainingBeanDefinition();    <span class="hljs-keyword">if</span> (containingBd != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">// Inner bean definition must receive same scope as containing bean.</span>        builder.setScope(containingBd.getScope());    &#125;    <span class="hljs-comment">// 设置 lazy-init 属性</span>    <span class="hljs-keyword">if</span> (parserContext.isDefaultLazyInit()) &#123;        <span class="hljs-comment">// Default-lazy-init applies to custom bean definitions as well.</span>        builder.setLazyInit(<span class="hljs-keyword">true</span>);    &#125;    <span class="hljs-comment">// 调用子类的 doParse() 进行解析</span>    doParse(element, parserContext, builder);    <span class="hljs-keyword">return</span> builder.getBeanDefinition();&#125;</code></pre><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>自定义标签解析的大致流程：</p><ul><li>首先，会加载 <code>spring.handlers</code> 文件，将其中内容进行一个解析，形成 <code>&lt;namespaceUri, 类路径&gt;</code> 这样的一个映射。</li><li>然后，根据获取的 <code>namespaceUri</code> 就可以得到相应的类路径，对其进行初始化等到相应的 NamespaceHandler 对象。</li><li>之后，调用该 NamespaceHandler 的 <code>parse(...)</code> 方法，在该方法中根据标签的 <code>localName</code> 得到相应的 BeanDefinitionParser 实例对象。</li><li>最后，调用该 BeanDefinitionParser 的 <code>parse(...)</code> 方法。该方法定义在 AbstractBeanDefinitionParser 抽象类中，核心逻辑封装在其 <code>parseInternal(...)</code> 方法中，该方法返回一个 AbstractBeanDefinition 实例对象，其主要是在 AbstractSingleBeanDefinitionParser 中实现。对于自定义的 Parser 类，其需要实现 <code>getBeanClass()</code> 或者 <code>getBeanClassName()</code> 任一方法，和 <code>doParse(...)</code> 方法。</li></ul><p>流程图：</p><p><img src="/images/spring_IOC/%E8%A7%A3%E6%9E%90%E8%87%AA%E5%AE%9A%E4%B9%89%E6%A0%87%E7%AD%BE%E6%B5%81%E7%A8%8B%E5%9B%BE.png" alt="解析自定义标签流程图"></p>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>spring</category>
      
      <category>IOC</category>
      
    </categories>
    
    
    <tags>
      
      <tag>spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Tomcat源码运行</title>
    <link href="/2020/11/09/idea%E5%AF%BC%E5%85%A5tomcat%E6%BA%90%E7%A0%81%E5%B9%B6%E5%90%AF%E5%8A%A8/"/>
    <url>/2020/11/09/idea%E5%AF%BC%E5%85%A5tomcat%E6%BA%90%E7%A0%81%E5%B9%B6%E5%90%AF%E5%8A%A8/</url>
    
    <content type="html"><![CDATA[<div align="center"><font color="#000" size="5rem">Idea导入Tomcat源码并运行</font></div><ul><li><h4 id="下载Tomcat源码并解压"><a href="#下载Tomcat源码并解压" class="headerlink" title="下载Tomcat源码并解压"></a>下载Tomcat源码并解压</h4><p>下载地址：<a href="http://mirror.bit.edu.cn/apache/tomcat/tomcat-8/v8.5.59/src/apache-tomcat-8.5.59-src.zip">http://mirror.bit.edu.cn/apache/tomcat/tomcat-8/v8.5.59/src/apache-tomcat-8.5.59-src.zip</a></p><p>浏览器打开，或者终端直接执行</p><pre><code class="hljs shell">wget http://mirror.bit.edu.cn/apache/tomcat/tomcat-8/v8.5.59/src/apache-tomcat-8.5.59-src.zip</code></pre><p>解压到想要存放的目录：</p><p>/home/jievhaha/Documents/workspace/ideaworkspace/apacheTomcat</p><p>解压得到：</p><p><img src="/images/tomcat/tomcat%E8%A7%A3%E5%8E%8B.png" alt="tomcat解压内容"></p></li><li><h4 id="Idea导入源码并配置"><a href="#Idea导入源码并配置" class="headerlink" title="Idea导入源码并配置"></a>Idea导入源码并配置</h4><ol><li><p><font style="color:rgb(69, 182, 247)">在源码根目录创建pom.xml（如上边截图）并添加如下内容：</font></p><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span><span class="hljs-comment">&lt;!--suppress ALL --&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">project</span> <span class="hljs-attr">xmlns</span>=<span class="hljs-string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span></span><span class="hljs-tag">         <span class="hljs-attr">xmlns:xsi</span>=<span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span><span class="hljs-tag">         <span class="hljs-attr">xsi:schemaLocation</span>=<span class="hljs-string">&quot;http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">modelVersion</span>&gt;</span>4.0.0<span class="hljs-tag">&lt;/<span class="hljs-name">modelVersion</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.apache.tomcat<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>Tomcat8.5<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">name</span>&gt;</span>Tomcat8.5 src<span class="hljs-tag">&lt;/<span class="hljs-name">name</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>8.5<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">build</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">finalName</span>&gt;</span>Tomcat8.5<span class="hljs-tag">&lt;/<span class="hljs-name">finalName</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">sourceDirectory</span>&gt;</span>java<span class="hljs-tag">&lt;/<span class="hljs-name">sourceDirectory</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">resources</span>&gt;</span>            <span class="hljs-tag">&lt;<span class="hljs-name">resource</span>&gt;</span>                <span class="hljs-tag">&lt;<span class="hljs-name">directory</span>&gt;</span>java<span class="hljs-tag">&lt;/<span class="hljs-name">directory</span>&gt;</span>            <span class="hljs-tag">&lt;/<span class="hljs-name">resource</span>&gt;</span>        <span class="hljs-tag">&lt;/<span class="hljs-name">resources</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">plugins</span>&gt;</span>            <span class="hljs-tag">&lt;<span class="hljs-name">plugin</span>&gt;</span>                <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.apache.maven.plugins<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span>                <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>maven-compiler-plugin<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span>                <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>3.5<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span>                <span class="hljs-tag">&lt;<span class="hljs-name">configuration</span>&gt;</span>                    <span class="hljs-tag">&lt;<span class="hljs-name">encoding</span>&gt;</span>UTF-8<span class="hljs-tag">&lt;/<span class="hljs-name">encoding</span>&gt;</span>                    <span class="hljs-tag">&lt;<span class="hljs-name">source</span>&gt;</span>1.7<span class="hljs-tag">&lt;/<span class="hljs-name">source</span>&gt;</span>                    <span class="hljs-tag">&lt;<span class="hljs-name">target</span>&gt;</span>1.7<span class="hljs-tag">&lt;/<span class="hljs-name">target</span>&gt;</span>                <span class="hljs-tag">&lt;/<span class="hljs-name">configuration</span>&gt;</span>            <span class="hljs-tag">&lt;/<span class="hljs-name">plugin</span>&gt;</span>        <span class="hljs-tag">&lt;/<span class="hljs-name">plugins</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">build</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">dependencies</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>ant<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>ant<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>1.7.0<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>ant<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>ant-apache-log4j<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>1.6.5<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>ant<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>ant-commons-logging<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>1.6.5<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>wsdl4j<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>wsdl4j<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>1.6.2<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span>            <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>javax.xml.rpc<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span>            <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>javax.xml.rpc-api<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span>            <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>1.1<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span>        <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span>            <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.eclipse.jdt.core.compiler<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span>            <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>ecj<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span>            <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>4.5.1<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span>        <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">dependencies</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">properties</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">project.build.sourceEncoding</span>&gt;</span>UTF-8<span class="hljs-tag">&lt;/<span class="hljs-name">project.build.sourceEncoding</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">properties</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">project</span>&gt;</span></code></pre></li><li><p><font style="color:rgb(69, 182, 247)">Idea导入项目并配置参数：</font></p><ul><li>导入项目后Run –&gt; Edit Configurations</li></ul><p><img src="/images/tomcat/EditConfig.png" alt="EditConfig"></p><ul><li><p>点击”+“，添加Application</p><p><img src="/images/tomcat/EditConfigParam.png" alt="EditConfigParam"></p><p>Name随意，然后配置Configuration –&gt; Main class，添加如下内容。</p><pre><code class="hljs shell">org.apache.catalina.startup.Bootstrap</code></pre><p>配置VM options，添加如下内容（路径自己看着改，没有的文件夹可以自己创建，不影响）。</p><pre><code class="hljs shell">  -Dcatalina.home=/home/jievhaha/Documents/workspace/ideaworkspace/apacheTomcat-Dcatalina.base=/home/jievhaha/Documents/workspace/ideaworkspace/apacheTomcat  -Djava.endorsed.dirs=/home/jievhaha/Documents/workspace/ideaworkspace/apacheTomcat/endorsed-Djava.io.tmpdir=/home/jievhaha/Documents/workspace/ideaworkspace/apacheTomcattemp  -Djava.util.logging.manager=org.apache.juli.ClassLoaderLogManager-Djava.util.logging.config.file=/home/jievhaha/Documents/workspace/ideaworkspace/apacheTomcat/conf/logging.properties  -Djava.protocol.handler.pkgs=org.apache.catalina.webresources-Djdk.tls.ephemeralDHKeySize=2048</code></pre></li></ul></li><li><p><font style="color:rgb(69, 182, 247)">打开mvn compile编译项目：</font></p><p><img src="/images/tomcat/mvn%E7%BC%96%E8%AF%91.png" alt="mvn编译"></p></li><li><p><font style="color:rgb(69, 182, 247)">启动项目，访问localhost:8080</font></p><p><img src="/images/tomcat/startWeb.png" alt="startWeb"></p><p><img src="/images/tomcat/startsuccess.png" alt="startsuccess"></p></li></ol></li><li><h4 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h4><ol><li><p><font style="color:rgb(69, 182, 247)">启动Tomcat，Idea控制台乱码问题</font></p><p>具体原因记得是某个配置文件idea打开就是乱码，之后需要重新查看</p><p>修改tomcat源代码，在代码中获取到了值之后手动重新编码解码，添加如下内容:</p><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;    value = <span class="hljs-keyword">new</span> String(value.getBytes(<span class="hljs-string">&quot;ISO-8859-1&quot;</span>), <span class="hljs-string">&quot;UTF-8&quot;</span>);&#125;<span class="hljs-keyword">catch</span>(Exception e)&#123;    e.printStackTrace();&#125;</code></pre><p><strong>第一处</strong>：<kbd>org.apache.tomcat.util.res.StringManager</kbd>类中的<kbd>getString(final String key, final Object... args)</kbd>方法。</p><p><img src="/images/tomcat/StringManagerGetString.png" alt="StringManagerGetString"></p><p><strong>第二处</strong>：<kbd>org.apache.jasper.compiler.Localizer</kbd>类的<kbd>getMessage(String errCode)</kbd>方法。</p><p><img src="/images/tomcat/LocalizerGetMessage.png" alt="LocalizerGetMessage"></p></li><li><p><font style="color:rgb(69, 182, 247)">启动失败，打开localhost:8080报错，提示：无法为JSP编译类</font></p><p>解决办法：<kbd>org.apache.catalina.startup.ContextConfig.java</kbd>文件中<kbd>protected synchronized void configureStart() {}</kbd>中<kbd>webConfig();</kbd>下添加</p><pre><code class="hljs java">context.addServletContainerInitializer(<span class="hljs-keyword">new</span> JasperInitializer(), <span class="hljs-keyword">null</span>);</code></pre><p><img src="/images/tomcat/ContextConfig.png" alt="ContextConfig"></p></li></ol></li></ul>]]></content>
    
    
    <categories>
      
      <category>java</category>
      
      <category>tomcat</category>
      
    </categories>
    
    
    <tags>
      
      <tag>tomcat</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
